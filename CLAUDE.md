# CLAUDE.md — 경매 매도가 추정 프로젝트 규칙

이 문서는 `molit-project`에서 Claude Code가 매 세션 자동으로 로드하는 분석 규칙이다.
사용자가 물건 정보를 말하거나 API 조회 결과(XML/텍스트)를 붙여넣으면, 아래 규칙을
그대로 적용해서 매도가를 계산한다. 별도 스크립트를 몰라도 이 문서만으로 수동 계산이
가능해야 한다 (스크립트는 `scripts/estimate_price.py`에 동일 로직이 구현되어 있다).

## 1. 프로젝트 개요

- 목적: 경매 물건(연립다세대/빌라)의 예상 매도가를 국토교통부 실거래가 데이터 기반으로 추정.
- 빌라/다세대는 한 건물에 3~4세대뿐인 경우가 흔해 "동일건물" 비교만으로는 표본이
  거의 항상 부족하다. 그래서 실제 중개업소·투자자들처럼 물건 주소를 좌표로 바꿔
  **실제 반경(기본 400m)** 안의 유사면적·유사층 매물을 비교 대상으로 삼는다 (5절).
- 기본 흐름:
  1. 사용자가 "OO빌라 매도가 알아봐줘, 사건번호 OOOO, 주소 OOOO, 전용면적 OO㎡, N층"이라고 요청
  2. **정보 확인**: 층, 준공년도(연식), 방 개수, 욕실 개수 중 하나라도 빠져 있으면
     바로 진행하지 말고 사용자에게 먼저 되물어서 확인한다. 층·연식은 몰라도 계산은
     되지만(7절 — 모르면 중립 가중치로 처리), 정확도에 영향을 주므로 매번 명시적으로
     확인한다. 방 개수·욕실 개수는 18절 구조 보정에 필요하다. 사용자가 "몰라"/"넘어가"
     라고 답하면 그 항목은 생략하고 진행한다 — 억지로 캐묻지 않는다.
  3. Claude가 2절 규칙에 따라 조회에 필요한 LAWD_CD/DEAL_YMD와 실행 커맨드를 안내
  4. 사용자가 로컬에서 직접 실행하고 결과를 `data/raw/`에 저장
  5. `scripts/estimate_price.py`가 대상 주소를 좌표로 바꾸고(카카오 로컬 API), 5~8절
     규칙에 따라 반경 안 비교거래를 골라 매도가를 계산해서 8절 포맷으로 보여줌

## 2. API 조회 커맨드 생성 규칙

- 사용자가 준 주소로 법정동코드(LAWD_CD) 앞 5자리를 확인한다. **기억에 의존해 추측하지
  말고 `data/lawd_codes.md` 표를 먼저 확인한다.** 표에 없는 지역이면 사용자에게
  시/군/구 단위로 재확인하거나 행정표준코드관리시스템(code.go.kr)에서 확인해달라고
  요청하고, 확인된 코드는 표에 새로 추가해둔다.
- DEAL_YMD(계약년월, YYYYMM)는 기본적으로 4절의 연도 필터(기준연도 이후)를 만족하도록
  최근 달부터 역순으로 필요한 만큼 지정한다. 반경 안 비교거래가 부족하면 조회 기간을
  넓히거나(`--year-min`을 낮추거나) 반경(`--radius`)을 넓히도록 안내한다.
- 실행 커맨드는 그대로 복사해서 쓸 수 있는 형태로 제시한다. `molit_rhtrade_api.py`/
  `molit_rhrent_api.py` 둘 다 DEAL_YMD를 여러 개 이어서 한 번에 받을 수 있고, 각
  달을 `data/raw/<LAWD_CD>/<YYYYMM>.xml`(전월세는 `data/raw_rent/<LAWD_CD>/<YYYYMM>.xml`)
  형태로 자동 저장한다 — 사용자가 따로 복사/저장할 필요 없다. 예:
  ```
  MOLIT_SERVICE_KEY="발급받은_인증키" python scripts/molit_rhtrade_api.py 11530 202508 202509 202510
  ```
- ⚠️ API는 샌드박스 네트워크 허용 목록에 없어 Claude가 직접 호출할 수 없다. 반드시
  사용자가 로컬/서버에서 실행하도록 안내하고, Claude는 커맨드 생성과 결과 해석만 담당한다.

## 3. 데이터 입력 방식

- 사용자가 붙여넣는 XML/텍스트를 그대로 파싱 대상으로 삼는다.
- 응답에 `<response>...</response>` 블록이 여러 개 이어붙어 있어도 관대하게 처리한다
  (정규식으로 `<response>...</response>` 단위를 각각 추출해서 파싱).
- `data/raw/`에 저장된 `.xml`/`.txt` 파일이 있으면 함께 합쳐서 계산 대상에 포함한다.

## 4. 데이터 정제 규칙

- 중복 제거 키: `umdNm, mhouseNm, jibun, dealYear, dealMonth, dealDay, dealAmount, excluUseAr, floor`
  (완전히 동일한 조합이면 같은 거래로 보고 하나만 남긴다)
- `cdealType`이 "해제"인 거래는 계산에서 제외한다 (계약 해제 건).
- 연도 필터: 기본적으로 (실행 시점 기준) 작년 이후 계약만 사용한다. 사용자가 다른
  기준연도를 지정하면 그 값을 따른다.

## 5. 비교거래 선정 규칙 (반경 기반)

빌라/다세대는 한 건물에 3~4세대뿐인 경우가 흔해 "동일건물" 비교로는 표본이 거의
항상 부족하다. 그래서 동일건물 여부가 아니라 **실제 거리**를 기준으로 비교거래를
고른다 (`scripts/estimate_price.py`의 `find_comparables`).

- 대상 물건의 지번 주소를 카카오 로컬 API로 좌표(위도/경도)로 변환한다
  (`scripts/geocode.py`). 변환에 실패하면(주소 오류, 키 미설정 등) 계산을 중단하고
  사용자에게 원인을 안내한다.
- 각 실거래 행도 같은 방식으로 좌표를 구한다 — `umdNm`+`jibun`에
  `data/lawd_codes.md`로 역조회한 **시도+시/군/구**를 붙여 지번 주소를 복원한다
  (`scripts/lawd_lookup.py`). `data/lawd_codes.md`는 시도 열이 있는 3열 표라
  서울특별시와 경기도 둘 다 지원한다 — 국토부 API 자체는 전국이 대상이니, 표에
  없는 시/도가 나오면 2절/10절 규칙대로 코드를 확인해서 표에 새로 추가하면 된다.
- API 호출을 아끼기 위해, 대상 주소에 포함된 구와 다른 구의 거래는 지오코딩하지
  않고 건너뛴다 (400m 반경이 구 경계를 넘는 경우는 사실상 없다고 보는 근사다).
- 전용면적이 대상 물건과 **±15%**(기본값) 이내인 거래만 남긴다 (반경 안이라도
  면적 차이가 크면 제외한다 — 다른 평형은 층당가가 달라 직접 비교가 어렵다).
  이 허용범위는 CLI `--area-tolerance`(%, 기본 15)나 웹 폼 "상세 옵션"의
  "면적 허용범위(%)"로 사용자가 직접 넓히거나 좁힐 수 있다 — 비교거래가
  너무 적거나(반경을 넓혀도 안 나오면) 너무 느슨하다고 느낄 때 조정한다.
- 준공년도(`--build-year`)를 알고 있으면, 비교거래의 `buildYear`가 대상 물건
  준공년도 **±4년**(기본값)을 벗어나면 비교 대상에서 아예 제외한다 (예:
  2010년식이면 2006~2014년식만 비교군에 들어간다). 오래된 건물과 신축을
  섞어서 계산하면 안 되기 때문에 가중치가 아니라 하드 필터로 처리한다.
  `--build-year`를 생략하면 이 필터는 적용하지 않는다. 이 허용범위도 CLI
  `--build-year-tolerance`(년, 기본 4)나 웹 폼 "준공년도 허용범위(년)"로
  조정 가능하다.
- 남은 거래 중 실제 거리(하버사인 공식)가 반경(기본 400m, `--radius`로 조절) 이내인
  것만 비교 대상으로 삼는다.
- **적응형 반경(`find_comparables_adaptive()`) — ⛔ 지금은 꺼져 있다.**
  ⚠️ **사용자 요청으로 껐다.** "400m로 잡았는데 표본 적으면 넓혀지는 거,
  그거 빼고 싶다"는 판단이다 — **지정한 반경이 곧 계산 범위**다.
  - **왜 뺐나**: 반경이 조용히 600·800m로 바뀌면 (ㄱ) 화면에 뜨는 "반경 Nm"와
    사용자가 머릿속에 잡아둔 기준이 어긋나고, (ㄴ) 걸어서 10분이 넘는 **다른
    생활권 거래가 표본에 섞여 들어온다**(거리 점수가 낮아질 뿐 빠지지는
    않는다). 표본이 부족하면 사용자가 직접 `--radius`(웹은 상세 옵션)를
    올리는 쪽이 의도가 분명하다.
  - **지금 상태**: CLI·웹 둘 다 `find_comparables()`를 지정한 반경 그대로
    부른다. "자동으로 넓혔습니다" 안내 문구(CLI 안내 줄, 웹 `expanded_note`)도
    비웠고, 비교거래가 0건일 때의 메시지도 "반경 Nm 안에서 못 찾았으니
    반경/면적 허용범위를 넓혀 보라"로 바꿨다.
  - **남겨둔 것**: 7-2절 시계열 보정과 같은 처리로 `find_comparables_adaptive()`
    함수와 `ADAPTIVE_RADIUS_STEPS_M`/`ADAPTIVE_RADIUS_MIN_COMPARABLES` 상수,
    단위 테스트를 그대로 뒀다 — 되살리려면 호출부 두 군데를 이 함수로 바꾸고
    `radius_expanded` 안내 문구를 다시 붙이면 된다.
  - ⚠️ `scripts/tests/test_estimate_price.py`의 `TestAdaptiveRadiusIsOff`가
    **"함수는 남아 있지만 CLI·웹 어디서도 호출하지 않는다"를 소스 검사로
    고정한다** — 주석·독스트링은 세지 않으므로 되살리는 방법을 문서에 적어
    둬도 오탐하지 않는다.

  아래는 껐을 당시의 동작 설명이다(되살릴 때 참고).
  고정 반경 하나만 보면 동네마다
  표본 편차가 크다(어떤 동네는 400m 안에 15건, 어떤 동네는 2건뿐). 그래서 CLI/웹
  둘 다 이 함수를 통해 먼저 지정한(또는 기본 400m) 반경으로 찾아보고,
  유효 비교거래가 `ADAPTIVE_RADIUS_MIN_COMPARABLES`(기본 7)건보다 적으면
  `ADAPTIVE_RADIUS_STEPS_M`(200/300/400/600/800/1000m) 중 지정 반경보다 큰
  단계로만 순서대로 다시 찾는다 — **지정한 반경보다 좁게는 절대 시도하지
  않는다**(사용자 설정을 최소값으로 존중). 거리가 멀어질수록 `similarity_score()`
  의 거리 점수가 자동으로 낮아지므로 "억지로 먼 거래를 가깝게 취급"하는 문제는
  없다. 반경이 실제로 넓어지면 "지정한 반경(Nm) 안 비교거래가 7건 미만이라
  반경을 Mm로 자동으로 넓혔습니다"라고 항상 알렸다(CLI는 안내 줄, 웹은
  "핵심 비교거래" 헤더 문구에 덧붙임) — 조용히 다른 반경으로 바꾸면 사용자가
  "왜 내가 지정한 반경이랑 결과가 다르지"라고 혼란스러울 수 있어서였다.
  결국 **그 혼란이 안내 문구 하나로 덮이지 않는다는 게 이 기능을 끈 이유**다.

  16절 예상 전세가·23-1절 실측 전환율·26절 역세권 프리미엄·30절 유동성은
  원래부터 각자의 이유로 고정 반경을 썼으므로 이번 변경의 영향을 받지 않는다
  (예: 30절은 "동네 유동성 자체"를 보는 거라 표본을 인위적으로 늘리면 목적과
  안 맞는다) — 이제 8절 매도가 산출까지 포함해 **모든 절이 지정한 반경을
  그대로 쓴다**.
- **거래유형(`dealingGbn`) 다운웨이트.** ✅ `molit_rhtrade_api.py` 상단의 공식
  기술문서로 검증된 필드다. 직거래(가족 간 거래 등 시세를 반영 안 할 수 있는
  거래)는 완전히 제외하지 않고 가중치만 낮춘다(`DEALING_TYPE_WEIGHT` — 중개거래
  1.0, 직거래 0.4) — 빌라는 표본 자체가 워낙 적어서 거래를 통째로 버리면 표본
  부족 문제가 더 심해질 수 있다는 지적을 반영했다. ⚠️ 실제 응답 문자열이 정확히
  "중개거래"/"직거래"인지는 아직 실측 샘플로 확인 못 했다 — 알려진 두 값 외의
  다른 표기(빈 문자열 포함)는 잘못 추측해서 부당하게 낮추는 것보다 안전하게
  중개거래와 동일하게(1.0) 취급한다. 실제 값이 다르면 `DEALING_TYPE_WEIGHT`
  표만 고치면 된다.
  - 실측으로 확인 못 한 필드이므로 값 판정은 완전 일치(`==`)가 아니라
    `normalize_dealing_gbn()`의 **포함 여부**로 한다("직거래"가 들어 있으면
    직거래, "중개"가 들어 있으면 중개거래, 둘 다 아니면 `None`). 완전 일치로
    두면 앞뒤 공백이나 부가 표기("직거래(개인)" 등)가 섞여 오는 순간 모든
    거래가 조용히 "알 수 없음"으로 떨어져 이 다운웨이트도, 8-1-1절
    다이아몬드 표시도 통째로 작동을 멈추는데 **화면으로는 그걸 알아챌
    방법이 없다**(범례에 "◆ 직거래"는 그대로 떠 있으니까). 그 상태를
    화면에서도 드러내는 장치가 8-1-1절 압축 범례다.
- **평당가 이상치 다운웨이트.** 같은 반경·면적대인데 평당가(㎡당가)가 유독
  튀는 거래(특수관계자 거래, 입력 오류 등)가 가중 중앙값을 왜곡하지 않도록,
  비교거래를 다 고른 뒤 ㎡당가 기준 `median ± 3×MAD`(중위절대편차, 스케일
  보정 없이 그대로) 밖인 거래는 가중치를 `PRICE_OUTLIER_WEIGHT`(0.05)로 크게
  낮춘다 (⚠️ 원래 0.1이었는데, 7절 `WEIGHT_REPLICATION_SCALE` 버그를 고치면서
  이 값이 **비로소 실제로 작동하게 된 김에** 사용자 요청대로 한 단계 더
  낮췄다 — 같은 표본에서 이상치 4건이 정상 거래 7건의 1/20 비중으로 들어간다) — **역시 제외가 아니라 다운웨이트**다(표본이 5건 미만이면 이상치
  판단 자체가 무의미해서 건너뛴다, `PRICE_OUTLIER_MIN_SAMPLE`). 화면에는 이상치
  여부를 따로 표시하지 않는다(`_price_outlier` 내부 플래그만 존재) — 굳이
  비교거래 하나하나에 "이거 이상치라 깎았어요"라고 설명하면 5절의 다른 개정
  이력(층 선호순위 등)에서 이미 겪은 "너무 복잡해서 이해하기 어렵다"는 문제가
  재발할 수 있어서, 조용히 가중치에만 반영한다.
- **동일건물 보너스.** GPT 조언 반영 — 좌표가 거의 겹치는(`SAME_BUILDING_
  DISTANCE_M`, 기본 20m 이내) 거래는 "바로 이 건물이 실제로 얼마에 팔렸는지"
  보여주는 가장 직접적인 증거라, 가중치를 `SAME_BUILDING_BONUS`(기본 2.0배,
  GPT가 제안한 1.5~2.5 범위의 중간값 — 검증된 계수는 아니다)만큼 추가로
  높인다. ⚠️ **판별 방식이 51-1절에서 바뀌었다** — 예전엔 좌표 거리(20m)로만
  판정했는데, 빌라 밀집지에서는 **바로 옆 동도 20m 안에 들어와 남의 건물이
  보너스를 받았다.** 지금은 `동 이름 + 지번`으로 판별하고, 대상 물건의 지번을
  모를 때만 좌표 20m로 폴백한다. 지번 표기 흔들림("123-4"/"0123-0004")은
  `normalize_jibun()`이 정수로 맞춰 흡수한다. ⚠️ 평당가 이상치 다운웨이트와 마찬가지로 이 플래그(`_same_building`)
  는 화면에 새로 노출하지 않는다 — 이미 있는 유사도 점수·거리로 판단이
  충분히 드러나는데 또 다른 부가 판단 문구를 얹으면 5절의 "너무 복잡해서
  이해하기 어렵다" 전례가 재발할 수 있어서, 순수 내부 가중치 보정으로만 쓴다.
- **비교거래가 어떤 기준으로 골랐는지는 항상 사용자에게 설명한다** —
  반경·면적 허용범위·준공년도 허용범위 값을 그대로 문장으로 풀어서 "핵심
  비교거래" 바로 위(웹) 또는 목록 헤더(CLI)에 보여준다. 숫자만 던지면
  사용자가 이 표본을 신뢰해도 되는지 판단할 수 없기 때문이다.
- **비교거래 각각의 정보를 항목마다 짧게 보여준다** (`describe_comparable_
  similarity()`) — **면적(㎡+평)·층·준공년도**를 "69.3㎡(21.0평) · 4층 ·
  2012년식"처럼 절대값 그대로 붙인다. 거리는 목록에 이미 따로 표시되므로
  여기 넣지 않는다.
  - ⚠️ **이 절은 두 번 개정됐다.** 처음엔 대상 물건과의 차이(면적 ±N%·층
    ±N·준공 N년 신축/구축)를 보여줬는데, "차이만 보여주면 그 비교거래가
    대상 물건보다 신축인지 구축인지 알 수 없다"는 지적을 받고 방향(신축/
    구축)·층 선호순위(3층>2층>4층>5층>{1층,6층})·탑층 주의·반지하 시세
    문구까지 얹었다가, 그 결과를 실제 화면에서 본 사용자가 "너무 복잡해서
    이해하기 어렵다"고 다시 지적해서 **전부 걷어내고 절대값 세 가지(면적/
    층/준공년도)만 단순하게 보여주는 첫 방식으로 되돌렸다.** 비교거래가
    대상 물건과 얼마나 비슷한지는 같은 행의 "유사도" 점수와 거리로 이미
    드러나므로, 이 텍스트에서 다시 판단을 얹으려 하지 않는다. 층 선호순위·
    탑층 주의·반지하 시세 절반 같은 사용자의 도메인 지식 자체는 유효하고
    나중에 다른 형태(예: 표 옆 배지, 접었다 펼 수 있는 상세정보)로 다시
    넣을 수도 있지만, 지금은 코드에서 완전히 제거된 상태다.
  - 면적→평 환산은 `PYEONG_PER_SQM`(= 1 / 3.3058, `listing_parser.py`의
    평→㎡ 환산과 같은 계수)을 쓴다.
- 층은 필터링(제외)이 아니라 7절의 가중치로만 반영한다 — 대상 물건의 층을
  몰라도(`--floor` 생략) 계산 자체는 그대로 된다.
- **반지하/지하(층 0 이하) 예외 — 양방향이다**: 대상 물건과 비교거래의
  반지하 여부가 **서로 다르면** 가중치를 낮추는 정도가 아니라 **비교 대상에서
  아예 제외**한다. 반지하는 지상층보다 시세가 절반 이하로 형성되는 경우가
  흔해 섞이면 중앙값 자체가 왜곡되기 때문이다. 반지하끼리는 정상 비교한다.
  - ⚠️ **한쪽만 걸려 있던 버그가 있었다(실측 확인).** 예전 조건은
    `row_is_basement and not subject_is_basement`라, 대상이 지상층일 때
    반지하를 빼주기만 하고 **대상이 반지하일 때 지상층 거래가 그대로 섞여
    들어왔다.** 반지하 물건의 매도가가 두 배 가까이 부풀었다(경기 백테스트
    반지하 4건 평균 오차 42.6% · 편향 +32.6% · 최악 +94.8%). 48-4절 참고.
    이 절 원문이 말하는 "반지하끼리는 정상 비교"가 곧 대칭 조건인데 코드가
    절반만 구현돼 있었다 — `TestBasementSymmetry`가 양방향을 고정한다.
- `cdealType`이 "해제"인 거래, 4절의 연도 필터에 못 미치는 거래는 제외한다.
- 결과는 거리가 가까운 순으로 정렬한다.

## 5-1. 탑층 추정 표시

5절 비교거래 설명(`describe_comparable_similarity()`)에서 한 번 시도했다가
"너무 복잡하다"는 지적으로 완전히 걷어낸 "탑층 주의" 문구(위 표 참고)를,
사용자가 나중에 "표 옆 배지 같은 다른 형태로 다시 넣을 수도 있다"고 남겨둔
대로 훨씬 단순한 형태로 다시 들여왔다 — 판단 문장이 아니라 층 번호 옆에
붙는 짧은 태그 하나뿐이다. `scripts/estimate_price.py`의
`estimate_building_top_floors()`/`format_floor_label()`/
`is_estimated_top_floor()`가 구현한다.

- **⚠️ 진짜 건축물대장 총 층수가 아니라 "관측된 최고층" 추정치다.** 7절에서
  "비교거래 수십 건마다 건축물대장을 추가로 조회하면 API 호출량이 급격히
  는다"는 이유로 엘리베이터 정보를 포기한 것과 똑같은 이유로, 비교거래마다
  건축물대장을 조회하지 않는다. 대신 이미 갖고 있는 국토부 실거래 데이터
  (`rows`, 필터링 전 원본 전체)만으로 근사한다 — 같은 지번(umdNm+jibun,
  사실상 같은 건물)으로 묶어서 그 안에서 **지금까지 거래된 기록 중 가장
  높은 층**을 "탑층 추정"으로 본다. 진짜 최고층이 우리가 가진 기간 동안
  한 번도 거래되지 않았다면 놓칠 수 있다는 한계가 있다.
- 같은 건물에서 서로 다른 층이 **2개 이상** 관측돼야만 계산한다 — 거래가
  1건뿐인 건물은 "관측된 최고층"이라는 말 자체가 의미가 없어서(그 1건이
  진짜 최고층인지 알 방법이 없다) 제외한다.
- 반지하/지하(층 0 이하)는 대상에서 제외한다(애초에 "탑층"이라는 개념과
  무관하다).
- CLI는 "핵심 비교거래" 각 줄의 층 표시에 `N층(탑층 추정)`처럼 그대로
  이어붙인다. 웹은 `층`과 `(탑층 추정)`을 줄바꿈해서 두 줄로 나눠 보여준다
  (`result.html`, `c.floor`+`c.is_top_floor`) — 처음엔 CLI와 똑같이 한
  문자열로 합쳐서 넣었더니, "핵심 비교거래" 표의 층 열이 모바일에서 워낙
  좁아 `4층(탑층 추정)`처럼 띄어쓰기 없는 긴 한글 문자열이 글자 단위로
  줄바꿈되어(`4`/`층`/`(탑`/`층`/`추정)`) 읽기 힘들어지는 문제를 실제로
  확인해서, 두 줄로 나눠 각 줄이 짧게 끝나도록 고쳤다.
- 8절 "핵심 비교거래" 목록(CLI/웹)에만 적용된다 — 16절 예상 전세가
  비교거래, 28절 붙여넣은 매물 유사도 순위에는 적용하지 않는다(전세는
  건물 식별 키가 다를 이유가 없어 확장 여지는 있지만 아직 안 했고, 28절
  매물은 MOLIT `jibun` 데이터가 없어 애초에 같은 방식을 못 쓴다).

## 6. 보안 원칙

- `MOLIT_SERVICE_KEY`와 `KAKAO_REST_API_KEY`는 절대 코드에 하드코딩하지 않는다.
  반드시 환경변수(`.env`)로 주입한다.
- `.env` 파일은 git에 커밋하지 않는다(`.gitignore` 처리). 빈 양식만
  **`.env.example`로 커밋**해두고, 쓰는 사람은 `cp .env.example .env` 한 뒤
  값을 채운다.
- ⚠️ **`.env`를 실제로 읽는 코드가 한참 없었다.** 여러 스크립트가 "`.env`에
  KEY를 추가해 주세요"라고 **안내만 하고**, 정작 그 파일을 읽는 코드가 아무
  데도 없어서 파일에 적어놔도 동작하지 않았다(사용자가 백테스트를 돌리려다
  드러났다). `scripts/env_file.py`의 `load_env()`가 이제 각 진입점
  (`backtest.py`·`estimate_price.py`·`molit_*_api.py`)에서 import 직후 한 번
  불린다.
  - 14절/22절의 "외부 라이브러리 최소화" 원칙대로 `python-dotenv`를 쓰지
    않고 표준 라이브러리만으로 파싱한다(`KEY=VALUE` 한 줄씩, `#` 주석,
    값을 감싼 따옴표 제거, `export ` 접두사 허용).
  - ⚠️ **이미 설정된 환경변수는 절대 덮어쓰지 않는다** — 그래야 Render
    대시보드 값이나 `KEY=... python ...` 방식이 항상 파일보다 우선한다.
    `test_env_file.py`가 이걸 고정한다(이게 깨지면 배포 환경에서 엉뚱한 키를
    쓰게 된다).
  - `load_env()`는 **채운 키의 이름만** 돌려준다(값은 안 돌려준다) — 실수로
    로그에 키가 찍히는 걸 막기 위해서다.
  - `.env.example`에 실제 값이 채워진 채 커밋되는 사고를 막는 테스트도 같이
    있다(`TestExampleFileStaysSafe` — 모든 값이 빈 문자열인지 확인).
- 사용자에게 보여주는 커맨드 예시에도 실제 키 값을 그대로 노출하지 않는다 — 항상
  `MOLIT_SERVICE_KEY="발급받은_인증키"`, `KAKAO_REST_API_KEY="발급받은_키"` 형태의
  플레이스홀더로 안내한다.
- ⚠️ **예외: `KAKAO_JS_KEY`(27-1절 카카오맵 미리보기용)는 프론트엔드 노출이
  정상 사용법이다.** 카카오맵 JS SDK는 브라우저에서 직접 스크립트 태그로
  불러써야 하는 구조라 이 키는 애초에 HTML에 그대로 보이도록 설계됐다 —
  대신 카카오 개발자 콘솔에서 이 키를 쓸 도메인을 등록해서 다른 도메인의
  무단 사용을 막는 방식이다. 그래도 "코드에 하드코딩하지 않고 환경변수로
  주입" 원칙은 동일하게 지킨다(값을 git에 커밋하지 않기 위해서다).

## 7. 가중치 및 신뢰도 계산 규칙

⚠️ **이 절은 개정됐다.** 원래는 "연도가중치 × 거리가중치 × 층가중치"를 각각
곱하는 단순한 방식이었는데, 사용자가 두 가지를 요청해서 통째로 바꿨다:
(1) 계약 시점 가중치를 연 단위가 아니라 월 단위로 더 세밀하게, (2) 거리·
면적·층·준공년도를 하나로 합친 0~100점 종합 유사도 점수를 만들고, 고득점
거래를 저득점 거래보다 "훨씬 크게" 반영. `scripts/estimate_price.py`의
`weight_for_recency()`/`similarity_score()`/`SIMILARITY_EMPHASIS_CURVE()`.

**계약 시점 가중치 (`weight_for_recency()`)** — 월 단위로 3단계:
- 최근 3개월 이내 계약: 1.6
- 4~12개월 전 계약: 1.0
- 13개월 이상 전 계약: 0.4

⚠️ 이 배율은 "최근 3개월 50% + 4~12개월 30% + 전년도 20%"라는 사용자 요청의
**상대적 비중**을 참고해서 정한 경험적 값이다 — 실제 반영 비중은 각 구간에
거래가 몇 건 있는지에 따라 달라진다(예: 최근 3개월 거래가 원래 적은 동네면
배율을 아무리 높여도 그만큼 못 나온다). 예전(연 단위)엔 "올해 1월 거래"와
"올해 12월 거래"가 똑같이 취급되는 문제가 있었는데, 특히 가격이 떨어지는
동네에서 "작년 초의 고가 거래"가 최근 거래와 동등하게 반영돼 매도가가
과대평가될 수 있다는 지적을 반영해 월 단위로 세분화했다.

**비교물건 종합 유사도 점수 (`similarity_score()`, 0~100점)** — 거리·면적·
층·준공년도 네 가지를 하나의 점수로 합친다:
- 거리 점수 = `100 × max(0, 1 − 거리/반경)`
- 면적 점수 = `100 × max(0, 1 − |면적차이%| / 면적허용범위%)` (5절 하드
  필터를 이미 통과했으므로 허용범위 안에서만 움직인다)
- 층 점수 = 층 차이 0~1이면 100, 그 뒤로 차이 1당 20점씩 감점, 최저 20점
  (대상 물건 층을 모르면 중립값 60)
- 준공년도 점수 = `100 × max(0, 1 − |연식차이| / 준공년도허용범위)` (마찬가지로
  5절 하드 필터를 이미 통과했으므로 허용범위 안에서만 움직인다. 대상
  물건 준공년도를 모르거나 비교거래에 `buildYear`가 없으면 이 항목을
  빼고 나머지 세 항목에 비중을 비례 배분한다)
- 가중합: **거리 35% · 면적 30% · 층 20% · 준공년도 15%** — 검증된 공식이
  아니라 경험적 배분이다.
- ⚠️ 사용자가 원래 요청한 건 "거리+면적+준공연도+**역거리+층+엘리베이터+
  주차+방/욕실+방향**" 9개 요소였다. 이 중 지금 실제로 넣을 수 있는 건
  **거리·면적·층·준공년도 네 가지뿐**이다:
  - **역거리**: 26절처럼 비교거래마다 카카오 키워드 검색을 추가로 호출해야
    해서 비용 문제로 기본 점수에는 안 넣었다 — 26절과 같은 방식으로 옵션
    화해서 나중에 추가할 수는 있다.
  - **엘리베이터**: 20절 건축물대장 조회로 얻을 수 있는 정보이지만, 대상
    물건 하나가 아니라 비교거래 수십 건마다 추가로 호출해야 해서 API
    호출량이 급격히 늘어난다.
  - **주차·방개수·욕실개수·방향**: 국토부 실거래가(과거 체결 기록)에는
    이 필드 자체가 없다 — 18절에서 방 개수를 "현재 매물" 붙여넣기
    텍스트로만 근사하는 것과 같은 이유다. 과거에 팔린 물건의 방향·주차
    대수는 공개된 어떤 데이터로도 구할 방법이 없다.
- 비교거래마다 `_similarity_score`(0~100)로 저장되고, "핵심 비교거래"
  목록(8절)에 "유사도 N점"으로 함께 표시된다.

**고득점을 "훨씬 크게" 반영하기 (`SIMILARITY_EMPHASIS_CURVE()`)** — 점수를
그대로 곱하지 않고 `(점수/100)³`으로 눌러서 쓴다. 90점은 0.729, 60점은
0.216이 되어 약 3.4배 차이가 나므로, 사용자가 요청한 "90점짜리를 60점짜리
보다 훨씬 크게 반영"을 만족한다. 세제곱(3)은 임의로 고른 경험적 지수이고,
더 강하게/약하게 강조하고 싶으면 `SIMILARITY_EMPHASIS_POWER` 하나만 바꾸면
된다.

**최종 가중치** = 계약 시점 가중치 × 유사도 강조 곡선값 × 거래유형 가중치(5절)
× 평당가 이상치 다운웨이트(해당 시, 5절) × 동일건물 보너스(해당 시, 5절).
- 가중 중앙값 근사: 각 거래 금액을 `_replication_count()`가 정한 개수
  (= `round(가중치 × WEIGHT_REPLICATION_SCALE)`, 최소 1개)만큼 리스트에 복제한 뒤
  중앙값/사분위를 구한다.
  - ⚠️ **버그 이력(실측 확인).** 예전엔 배율 없이 `round(가중치)`만 썼는데,
    실제 가중치는 대부분 **0.05~1.6** 범위(최근성 0.4~1.6 × 유사도 곡선
    0.125~1.0)라 **1.5 미만이 전부 1개로 뭉개졌다** — 18가지 조합을 찍어보니
    2개가 되는 건 "유사도 100점 + 최근 3개월"(1.6) 하나뿐이었다. 즉 유사도
    90점 최근 거래도, 가중치를 1/10로 깎은 평당가 이상치도, 직거래
    다운웨이트(0.4)도, 동일건물 보너스(2.0)도 **계산에는 똑같이 1표씩**
    들어가고 있었다(화면의 점 크기·진하기에만 반영돼서 눈으로는 정상으로
    보였다). 사용자가 "평당가 이상치라고 나오는 것들 반영도가 너무 큰 것
    같다, 내가 생각한 매도가는 1.7억인데 2.3억이 나온다"고 지적해서 발견했다.
    100을 곱한 뒤 반올림하면 각각 117개 / 5개가 되어 배율이 그대로 살아난다.
  - 배율 100은 "가중치 0.01 차이까지 살린다"는 뜻의 해상도 선택이다 —
    비교거래 40건 기준 리스트 길이가 4천 개 정도라 정렬 비용도 무시할 수 있다.
- 시세 신뢰도(0~100점), 100점에서 시작해 아래를 차감:
  - 전체 유효거래가 5건 미만이면 부족한 건수 × 10점
  - 반경 절반 이내(더 가까운) 거래가 2건 미만이면 부족한 건수 × 10점
  - 사분위 스프레드 `(상단가 − 급매가) / 현실적 체결가` × 100, 최대 30점까지 차감
  - 올해 거래 비중이 낮을수록 최대 15점 차감
  - 7-1절 모델 합의도(㎡당가 모델과 총액 모델의 괴리율)가 클수록 최대 15점 차감
  - 최종적으로 10~100 사이로 clamp

⚠️ **버그 이력**: `find_comparables()`가 예전엔 입력받은 `rows`의 dict를
직접 수정했는데, 30절 유동성 점수가 같은 `rows`를 층/준공년도 조건 없이
넓은 반경으로 다시 조회하면서 앞서 8절 계산에 쓰인 `_similarity_score`/
`_weight`/`_distance_m`를 덮어써버리는 버그가 있었다(실측 확인됨 — CLI
테스트에서 완전히 동일한 스펙의 비교거래인데 유사도가 100점이 아니라 91점
으로 나왔다). `find_comparables()`가 이제 입력 dict를 직접 고치지 않고
복사본에 값을 써서 반환하도록 고쳐서 해결했다 — 같은 `rows`를 여러 조건
으로 반복 조회해도 서로 영향을 주지 않는다.

## 7-4. 가중 분위수 — 복제 근사를 걷어냈다

7절이 원래 쓰던 "가중치만큼 값을 복제한 리스트의 중앙값" 방식을
**진짜 가중 분위수**(`weighted_quantile()`)로 바꿨다. 동작은 했지만 두 가지가
나빴다:

1. **가중치를 정수로 양자화한다.** 배율을 살리려고 100을 곱해야 했는데,
   **그 배율을 빠뜨린 탓에 모든 가중치가 1표로 뭉개지는 버그를 실제로 겪었다**
   (7절 버그 이력 — 유사도 90점 거래도, 1/20로 깎은 이상치도, 직거래
   다운웨이트도 계산에는 똑같이 1표씩 들어갔다). 새 방식에는 **그 실수가
   존재할 수 없다.**
2. 비교거래 40건이면 리스트가 4천 개로 불어난다.

**방식**: 값으로 정렬한 뒤 누적가중치에 **중점 규칙**(각 점의 대표 위치 =
누적 − 자기 가중치/2)을 쓰고 점 사이를 선형보간한다. 가중치가 전부 같으면
통상적인 백분위수와 정확히 일치한다(`test_equal_weights_match_ordinary_percentiles`).

**숫자는 거의 안 바뀐다** — 300회 시뮬레이션 실측:

| | 평균 변화 | 평균 절대 변화 | 최대 |
|---|---|---|---|
| p25 | +0.04% | 1.19% | 16.2% |
| median | +0.08% | 0.81% | 7.3% |
| p75 | +0.11% | 1.03% | 14.7% |

체계적인 쏠림이 없다(평균 +0.1% 미만) — 정확도를 바꾸려는 변경이 아니라
**버그가 생길 수 없는 구조로 바꾸는 것**이 목적이다.

### ⚠️ 덤으로 잡은 것 — 8절과 29절이 다른 분포를 쓰고 있었다

바꾸는 김에 드러났다. 두 군데가 어긋나 있었다:

| | 예전 8절 | 예전 29절 |
|---|---|---|
| 분위수 방식 | "하위 절반의 중앙값"(튜키 힌지) | 최근접-순위 백분위수 |
| 모델 | 총액·㎡당가 **50:50 블렌딩**(7-1절) | **총액 모델만** |

그래서 29절 원문이 "60일 목표가(p50)는 현실적 체결가와 **거의** 같음"이라고
적어야 했던 것이다. **한 화면에 나란히 뜨는 두 숫자라 어긋나면 바로 보인다.**

`compute_price_tiers()`에 `subject_area`를 새로 받아 8절과 **같은 함수·같은
블렌딩**을 쓰게 했다 — 이제 `60일 목표가 == 현실적 체결가`가 정확히 성립한다
(`TestTiersShareTheScenarioDistribution`이 고정). `subject_area`를 생략하면
예전처럼 총액 모델만 쓴다(하위호환).

⚠️ `_replication_count()`/`WEIGHT_REPLICATION_SCALE`은 **더 이상 계산에
쓰이지 않지만 함수와 테스트를 남겨 뒀다** — 5절 적응형 반경·7-2절 시계열
보정과 같은 처리다. 위 버그 이력이 이 프로젝트에서 제일 비싼 교훈이라
지워버리면 같은 실수를 다시 한다.

## 7-1. ㎡당가 모델 병렬 계산 (모델 합의도)

GPT 조언 2단계 반영 — 7절까지는 "비교거래 원 체결가(총액)"를 가중 중앙값
내는 방식(**총액 모델**) 하나뿐이었다. 이 절은 독립적인 두 번째 계산
경로(**㎡당가 모델**)를 병렬로 계산해서, 두 모델이 서로 얼마나 일치하는지
(모델 합의도)까지 신뢰도에 반영한다. `scripts/estimate_price.py`의
`_weighted_unit_prices_sorted()`/`compute_scenarios()`가 구현한다.

- **㎡당가 모델**: 비교거래마다 평당가(만원/㎡ = 체결가 ÷ 그 거래의 전용면적)를
  구하고, 그걸 **대상 물건의 전용면적**에 곱해 "이 물건 넓이 기준으로
  환산했다면 얼마였을지" 값으로 바꾼다. 총액 모델과 똑같이 7절 가중치로
  복제한 뒤 중앙값/p25/p75를 구한다.
  - 총액 모델은 5절 면적 허용범위(±15%) 안에서 골라진 거래의 **원 체결가를
    그대로** 쓰는 반면, ㎡당가 모델은 그 안에서도 남아있는 면적 차이를 한 번
    더 정규화한다는 점이 다르다 — 예를 들어 대상 물건이 69㎡인데 비교거래가
    65㎡와 73㎡ 두 건뿐이면, 총액 모델은 두 체결가를 그대로 반영하지만
    ㎡당가 모델은 둘 다 "69㎡였다면"으로 환산해서 비교한다.
- **블렌딩**: 최종 p25/중앙값/p75 = 총액 모델 값 50% + ㎡당가 모델 값 50%
  (단순 평균) — GPT가 제안한 "총액모델 50% + 단가모델 50%"를 그대로 반영한
  값이다. 8절의 보수적 급매가/현실적 체결가/상단 매도가는 전부 이 블렌딩된
  값을 기준으로 계산된다.
- **모델 합의도(`model_divergence_pct`)**: 두 모델의 중앙값이 얼마나
  갈리는지 = `|총액모델 중앙값 − ㎡당가모델 중앙값| / 블렌딩된 중앙값 × 100`.
  두 독립적인 계산 방식이 비슷한 답을 내면(합의도가 높으면=괴리율이 낮으면)
  그 추정치를 더 믿을 수 있다는 뜻이고, 많이 갈리면(예: 비교거래들의 면적
  분포가 대상 물건과 치우쳐 있다거나 표본이 이질적이라는 신호) 신뢰도를
  낮춰야 한다는 뜻이다 — 7절 시세 신뢰도 계산에 최대 15점까지 차감으로
  반영되고, 8%(`MODEL_DIVERGENCE_MENTION_THRESHOLD_PCT`) 이상이면 32절 종합
  판단 문단에도 "모델 간 의견이 다소 엇갈립니다"라고 짧게 언급된다(그 미만은
  우연한 오차로 보고 언급하지 않는다).
- `subject_area`(대상 물건 전용면적)를 주지 않고 `compute_scenarios()`를
  부르면(기존 호출부와의 하위호환) 예전처럼 총액 모델만 쓴다 — 지금은 매매
  (8절)·전세(16절) 계산 양쪽 모두 대상 물건 면적을 항상 넘겨주므로 실제로는
  거의 항상 블렌딩이 적용된다.
- ⚠️ GPT가 원래 제안한 건 "가중중앙값 모델·㎡당가 모델·최근 6개월 모델·
  가장 가까운 5건 모델" 네 가지를 종합한 합의도였는데, 지금은 그중 **두
  모델(총액·㎡당가)만** 구현했다 — 모델을 더 늘릴수록 서로 독립적이지 않은
  파생 지표끼리 비교하게 되어 "합의도"의 의미가 흐려질 위험이 있다고 판단해,
  가장 명확하게 서로 다른 두 계산 방식으로 시작했다. 필요하면 나중에 세
  번째 모델을 추가하고 합의도 계산을 다수결/분산 기반으로 확장할 수 있다.

## 7-2. 시계열 가격보정 — ✅ **68절에서 다시 켰다**

⚠️ **한동안 꺼져 있었다.** "어차피 최근 2년치만 쓰는데 그 안에서 다시 지금
시세로 환산해서 뭐 하냐"는 판단이었고, **오래된 거래를 덜 반영하는 일은 7절
계약 시점 가중치가 이미 하고 있다**는 것이 근거였다.

**그 근거가 68절 실측으로 틀린 것이 드러나 다시 켰다** — 7절 가중치가 켜진
상태에서도 월 0.68% 오르는 동네에서 계산기가 **−5.8% 낮게 부른다.** 줄여주긴
하지만 없애진 못한다. 자세한 측정은 68절 참고.

**지금 상태**: CLI 8절·웹 `/estimate`·백테스트 **매매 경로만** 켠다.
16절 전세·23-1절 전환율은 그대로 꺼져 있다(`monthly_trend_rate` 기본값 None).

GPT 조언 3단계로 들어온 기능이다 — 7절 `weight_for_recency()`는 오래된 거래의
**가중치**만 낮출 뿐, 그 거래의 **가격 자체**는 그대로 쓴다. 이 절은 한 걸음 더
나가서, 15절 가격 추이(이 동네 월별 평당가 흐름)로 추정한 변동률을 이용해 오래된
거래를 "그 추세를 따라 지금까지 왔다면 얼마"로 환산한 값을 계산에 쓴다.
`scripts/estimate_price.py`의 `estimate_monthly_trend_rate()`/
`time_correction_factor()`/`time_correction_note()`/`find_comparables()`의
`monthly_trend_rate` 인자가 구현한다.

- **월평균 변동률 추정**: 15절과 같은 월별 평당가 시계열(`_price_trend_monthly_series()`
  — 두 절이 같은 집계 로직을 공유한다)에 **최소제곱 회귀**를 걸어 월 복리
  변동률을 구한다. 15절과 동일한 최소 표본 기준(3개월 미만이면 계산 안 함)을
  그대로 따르고, 그보다 적으면 `None`을 돌려줘서 호출부가 조용히 시계열 보정을
  건너뛰게 한다. ⚠️ **추정 방식이 68절에서 세 군데 바뀌었다 — 아래 참고.**
- **보정 배율(`time_correction_factor()`)**: 비교거래마다 "몇 개월 전 계약인지"
  (`months_ago`)를 구해 `(1 + 월평균변동률) ^ months_ago`를 곱한다. 다만
  안전장치 두 가지를 뒀다:
  - `months_ago`가 `TIME_CORRECTION_MAX_MONTHS`(12개월)를 넘으면 더 키우지
    않는다 — 오래된 거래일수록 "그 추세가 그렇게 오래 이어졌으리라"는
    가정이 약해지므로 외삽을 제한한다.
  - 최종 배율도 `TIME_CORRECTION_MAX_PCT`(±15%)를 넘지 않게 clamp한다 —
    5절 평당가 이상치 다운웨이트와 같은 "보정은 하되 폭주는 막는다" 원칙.
- **원본 체결가는 그대로 둔다**: 보정된 값은 `_amount_man_adjusted`라는
  **별도 필드**에 저장하고, 원래 `_amount_man`(실제 체결가)은 손대지 않는다
  — "핵심 비교거래" 목록에는 항상 실제 체결가가 그대로 표시된다(7-1절
  ㎡당가 모델과 같은 원칙 — 화면은 사실, 계산에만 보정값을 쓴다).
  `_weighted_amounts_sorted()`/`_weighted_unit_prices_sorted()`(7절/7-1절
  공용)가 `_amount_man_adjusted`가 있으면 그 값을, 없으면 `_amount_man`으로
  자동 폴백하므로, 8절 매도가 계산과 7-1절 ㎡당가 모델 둘 다 시계열 보정을
  자동으로 반영한다. 5절 평당가 이상치 다운웨이트도 보정값이 있으면 그 값
  기준으로 이상치를 판단한다("오래돼서 싼" 거래를 "비정상적으로 싼" 거래로
  오판하지 않기 위해서다). ⚠️ 64절 1층 보정과 **같은 필드를 공유하므로 덮어쓰지
  않고 곱한다** — 한쪽이 조용히 사라지면 안 된다.
- **적용 범위**: CLI는 `--dong`(필수 인자)과 `data/raw`의 전체 기간 데이터로
  추정한다. 웹은 사용자 입력 주소에서 동을 추출(`find_dong_in_address()`)해
  같은 방식으로 적용하는데, ⚠️ 웹은 `get_trade_rows()`가 `year_min` 이후
  데이터만 가져오므로 그 범위 안에서만 추세를 추정한다 — CLI와 완전히 같은
  값이 나오지 않을 수 있다.
  ⚠️ **백테스트는 `prior`(대상 계약일 이전)로만 추정한다** — 전체 `rows`로
  추정하면 대상 계약일 **이후** 가격이 추세에 섞여 48절이 막아둔 데이터
  누출이 뒷문으로 되살아난다.
  16절 예상 전세가·23-1절 실측 전환율에는 **적용하지 않는다** — 이 변동률은
  매매(`dealAmount`) 시세 추세이고, 전세가는 매매가와 다르게 움직일 수 있어
  같은 추세를 전세 계산에 그대로 쓰면 오히려 왜곡된다.
- **보정이 걸리면 반드시 알린다**(`time_correction_note()`) — "이 동네는 가격이
  오르는 추세(월 +0.7%)라, 오래된 실거래는 지금 시세 수준으로 보정(최대 ±15%)해서
  계산했습니다(아래 목록의 금액은 신고된 실제 체결가 그대로입니다)." CLI·웹이
  **같은 함수**를 불러서 문장이 갈라지지 않게 했다(테스트로 고정). 변동률이
  `TIME_CORRECTION_MENTION_RATE`(월 ±0.1%) 미만이면 문구를 안 붙인다.
- ⛔ **8-1-1절 그래프의 연결선·빈 점·범례 항목은 되살리지 않았다** — 껐을 때
  지운 그대로다. `test_price_chart.py`에 "보정값이 있어도 그리지 않는지"
  확인하는 회귀 테스트가 그대로 살아 있다.

## 7-3. 역세권 회귀보정 (조건부, 참고용 what-if 수치)

GPT 조언 3단계 반영 — 원래 제안은 26절 거리-가격 회귀식으로 8절 공식
매도가 값 자체를 보정하는 것이었다. 하지만 26절 회귀는 표본이 최대 15건뿐인
단순 선형회귀이고, 24절 시장동향·26절 자체도 이미 "검증된 보정 공식이
아니므로 8절 계산에 자동 반영하지 않는다"는 원칙을 지켜왔다 — 이 절도 같은
원칙을 따라 **공식 매도가 값 자체는 절대 바꾸지 않고**, 조건이 까다롭게
다 맞을 때만 "만약 역세권 위치를 반영해 보정하면" 이라는 **별도 참고용
what-if 수치**를 하나 더 보여준다. `scripts/estimate_price.py`의
`compute_distance_premium_correction()`이 구현한다.

- `--station-premium`(CLI)/역세권 프리미엄 체크박스(웹)를 켰을 때만
  계산한다 — 26절과 마찬가지로 카카오 키워드 검색 비용 때문에 기본은
  꺼져 있다(대상 물건 자신의 역까지 거리를 한 번 더 조회해야 해서 호출이
  하나 늘지만, 26절이 이미 비교거래마다 호출하는 것에 비하면 미미하다).
- 아래 네 조건을 **모두** 만족해야 계산한다(하나라도 안 맞으면 조용히
  생략 — 8절 계산 자체는 항상 그대로 진행된다):
  - 표본 `STATION_REGRESSION_MIN_SAMPLE`(15건) 이상 — 26절 `max_sample`과
    같은 값이라 사실상 "가까운 15건을 다 구했을 때만" 계산하는 셈이다.
  - 거리 범위(최대−최소)가 `STATION_REGRESSION_MIN_DISTANCE_SPREAD_M`(300m)
    이상 — 표본이 다 비슷한 거리에 몰려 있으면 기울기를 못 믿는다.
  - 설명력 R²가 `STATION_REGRESSION_MIN_R_SQUARED`(0.3) 이상 — 26절 출력의
    경고 임계치(0.2)보다 더 엄격하게 잡았다(참고용 텍스트 하나 보여주는
    것과, 숫자를 하나 더 계산해서 보여주는 것은 신뢰 기준이 달라야 한다).
  - 계수 방향이 상식적(역에서 멀어질수록 평당가가 떨어짐, 즉
    `change_per_100m < 0`) — 역방향(멀수록 비쌈)이면 이상치이거나 다른
    요인이 더 크다는 뜻이라 보정하지 않는다.
- **계산식**: 대상 물건의 실제 역까지 거리(`subject_distance_m`)와 비교거래
  평균 거리(`mean_distance`)의 차이에 회귀 기울기(`change_per_100m`/100,
  만원/㎡ per m)를 곱해 평당가 차이를 구하고, 이를 비교거래 평균 평당가로
  나눠 보정 비율(%)을 얻는다. 대상 물건이 평균보다 역에 가까우면(음수
  차이) 기울기가 음수이므로 곱하면 양수(가격 위로 보정), 멀면 반대다.
- **보정폭도 `STATION_REGRESSION_MAX_CORRECTION_PCT`(±5%)로 clamp**한다 —
  GPT가 제안한 값 그대로. 회귀 하나로 가격을 크게 흔들지 않도록 하는
  마지막 안전장치다.
- **출력은 어디까지나 참고용**: "대상 물건은 지하철역까지 Nm로 비교거래
  평균(Mm)보다 가깝습니다/멉니다 — 이 회귀를 반영해 보정하면 현실적
  체결가는 X.XX억 → Y.YY억(±N.N%)입니다"라고 26절 역세권 프리미엄 출력
  바로 뒤에 한 줄 덧붙이고, 항상 "공식 매도가 값에는 반영되지 않은
  참고용 수치"라는 문구를 함께 단다 — 8절 보수적 급매가/현실적 체결가/
  상단 매도가/AI 기준매도가/권장 최초 호가/8-2절 경매용 매도가는 이 절과
  무관하게 항상 그대로다.

## 8. 매도가 산출 포맷

- **보수적 급매가** = 가중 데이터 하위 절반의 중앙값(p25)
- **현실적 체결가** = 가중 중앙값(median)
- **상단 매도가** = 가중 데이터 상위 절반의 중앙값(p75)
- **AI 기준매도가** = `p25×0.3 + 현실적체결가×0.5 + 상단매도가×0.2`, 10만원 단위 반올림
- **권장 최초 호가** = 상단 매도가 × 1.03, 10만원 단위 반올림

출력은 아래 형식을 그대로 따른다:

```
분석기간: YYYY.01 ~ YYYY.12
유효 비교거래: N건 (반경 400m 이내, 200m 이내 N건 / YYYY년 N건)
시세 신뢰도: N/100

보수적 급매가: X.XX억
현실적 체결가: X.XX억
상단 매도가: X.XX억
AI 기준매도가: X.XX억
권장 최초 호가: X.XX억

핵심 비교거래 (과거 실거래 기준 — 국토교통부에 신고된 실제 체결 기록입니다, 지금 나온 매물 호가가 아닙니다):
가까운 순 — 반경 Nm 안, 전용면적 ±N% 이내인 실거래 중 거리·면적·층·
준공년도 종합 유사도(0~100점, 7절)가 높을수록, 계약월이 최근일수록
가중치를 높게 준 것입니다:
- 단지명 면적㎡, N층(탑층 추정, 해당 시 5-1절), YYYY.MM 계약, X.XX억 — Nm (유사도 N점 · N.N㎡(N.N평) · N층 · YYYY년식)
```

비교거래 목록은 가까운 순으로 최대 8건까지만 보여준다. 목록 헤더의 반경·
면적/준공년도 허용범위 안내와 각 항목의 "(면적/층/준공년도 비교)" 괄호는
5절 규칙에 따른 것이다. ⚠️ **제목을 "핵심 비교거래 (과거 실거래 기준)"으로
명시했다** — 사용자가 "이게 뭐에서 온 데이터인지 처음 보는 사람은 모를
것 같다"고 지적해서, 이게 지금 나온 매물 호가가 아니라 국토교통부에 신고된
과거 실제 체결 기록이라는 걸 제목에서부터 명확히 했다. 웹 버전(8-2절
아래)에서는 여기서 한 걸음 더 나가 설명 문단까지 붙인다.

### 8-1. 가격 분포 그래프 (웹/HTML 리포트 전용)

숫자 다섯 개(p25/median/p75/AI기준/권장호가)만 나열하면 "실제 비교거래들이
어떻게 퍼져 있고 그 안에서 이 값들이 어디쯤인지"가 안 보인다는 지적을 반영해,
개별 비교거래 가격과 8절 산출값을 가로 점그래프(스트립플롯) 하나에 같이
보여준다. `scripts/price_chart.py`의 `render_price_distribution_html()`가
순수 SVG로 그리고(외부 차트 라이브러리 없음), 웹 버전(`webapp/app.py`, 22절)
결과 페이지와 CLI `--html` 리포트(`scripts/report.py`, 14절) 양쪽에서 재사용한다
(텍스트 전용 CLI 출력에는 그래프를 넣을 수 없어 없다).

- **점**: 비교거래 하나당 점 하나. ⚠️ 예전엔 가까운 순 최대 40건만 그리고
  나머지는 통째로 잘라냈는데, 8-1-1절 확장으로 **더 이상 표본을 잘라내지
  않는다** — 가중치 상위 `max_dots`(기본 40)건은 아래 8-1-1절의 모양/테두리/
  클릭상세를 다 갖춘 "강조" 점으로, 그 밖의 나머지는 작고 옅은 배경 점으로
  전부 그린다(안전 상한 `MAX_TOTAL_DOTS`=150건은 넘지 않는다). 가격이 비슷한
  점끼리는 겹치지 않게 위로 쌓는 간단한 비스웜(beeswarm) 배치를 쓴다.
  PC에서는 마우스를 올리면, 모바일에서는 탭하면 단지명·면적·금액·계약월·
  거리·반영도가 커스텀 툴팁으로 뜬다(네이티브 SVG `<title>`은 모바일 탭에서
  잘 안 뜨는 경우가 많아, 아주 작은 바닐라 JS로 직접 구현했다 — 외부
  라이브러리는 여전히 안 쓴다). 다른 곳을 탭/클릭하면 닫힌다.
- **그래프 밑 캡션은 줄 단위 범례로 나눠져 있다.** 예전엔 네 가지 설명(점
  하나=거래 1건, 진하기/크기=유사도, 글자 붙은 점=대표값, 진한 배경=강조
  구간)을 " · "로 이어붙인 한 문장으로 길게 나열했는데, 사용자가 "너무
  길게 주르륵 나열돼 있어 읽기 힘들다"고 지적해서 항목마다 줄을 나누고
  제목(굵게, 12.5px, 항목마다 다른 색 — 검정/`primary`/`HIGHLIGHT_EDGE`)과
  부연설명(연하게, 11px 회색)을 위아래로 배치하는 작은 범례 목록으로
  바꿨다(`render_price_distribution_html()`의 `_legend_row()`). 굵기·크기·
  색을 다르게 줘서 훑어봤을 때 "어떤 게 핵심 문구고 어떤 게 부연설명인지"
  바로 구분되게 하는 게 목적이다.
- **"근거" 설명과 가격 눈금이 추가됐다.** 사용자가 "이 그래프가 매도가를
  추정하는 근거인데 더 자세히 설명해달라"고 요청해서 두 가지를 더했다:
  1. 그래프 맨 위에 "📊 매도가 산출 근거 — 실제 비교거래 분포" 제목과
     "아래 점들이 실제 거래된 가격이고, 그 안에서 매도가 값들이 어디쯤
     위치하는지 보면 이 매도가가 어떤 실거래를 근거로 나온 숫자인지 알 수
     있다"는 설명 문단을 붙였다 — 이 그래프가 왜 매도가의 "근거"인지를
     숫자를 보기 전에 먼저 말로 짚어준다.
  2. 값을 안 눌러봐도 대략적인 가격대가 바로 읽히도록, 그래프 위쪽에
     점선 세로 눈금선 + 숫자(억 단위)를 **최저·중간·최고 3개만** 찍는다
     (`render_price_distribution_html()`, `lo`/`(lo+hi)/2`/`hi`). 마커
     이름표를 SVG에 다 못 넣는 것과 같은 이유로, 눈금을 너무 많이 찍으면
     좁은 가격대에서 겹쳐 안 보이니 3개로 제한했다.
  - 이 변경으로 그래프 기본 높이도 142px → 190px로 키웠다(`height` 기본값)
    — 위 눈금 표시할 공간을 확보하면서 전체적으로 더 여유 있게 보이도록.
    너비는 이미 카드 폭에 맞춰 100%로 늘어나는 구조라 별도로 안 키웠다.
- **점 색·크기 = 유사도**: `find_comparables()`가 이미 계산해둔 가중치
  (`_weight` — 7절의 연도·거리·층 유사도를 곱한 값)를 그대로 재사용해서,
  대상 물건과 조건이 비슷한(가중치가 높은) 비교거래일수록 점을 더
  **진하고 크게**(연한 회색 → 짙은 잉크색 램프, 반지름 4~7.5px) 그린다 —
  숫자 하나로 "이 표본이 얼마나 믿을 만한지"까지 한눈에 보이게 하는
  용도다. 새 계산·필터를 만드는 게 아니라 이미 있는 가중치를 색으로
  옮기는 것뿐이라 8절 산출값과 항상 일치한다. ⚠️ 점의 **위아래 높이는
  값과 무관하다** — 가격이 비슷한 점끼리 겹치지 않으려고 비스웜으로 쌓은
  것일 뿐이라, 캡션에도 항상 "값과는 무관합니다"라고 명시한다(안 그러면
  세로축이 뭔가 의미 있는 척도처럼 보일 수 있다).
- **강조 구간**: 기본적으로 **보수적 급매가~현실적 체결가** 구간을 배경으로
  칠한다(`HIGHLIGHT_FILL = "#ffb020"`, 채도 있는 호박색, `fill-opacity 0.20`
  + 진한 점선 테두리 `HIGHLIGHT_EDGE`) — 처음엔 `primary`색을 8% 옅게
  깔았더니 거의 안 보인다는 피드백을 받아서, 히어로 마커 색과는 아예 다른
  전용 색을 더 진하게 쓰는 걸로 바꿨다. ⚠️ 이건 통계적으로 유도한
  신뢰구간이 아니라, 사용자가 실제 경매 낙찰 후 매도한 사례들을 이 계산기
  산출값과 대조해본 결과 "실제 매도가가 이 구간에서 형성되는 경우가
  많더라"고 직접 확인해준 것을 반영한 것이다 — 그래서 캡션에 항상 그
  출처(사용자 관찰, 참고용)를 명시하고 통계적으로 확정된 값처럼 보이지
  않게 한다. `render_price_distribution_html()`의 `highlight` 인자로 다른
  두 마커 구간으로 바꿀 수도 있다.
- **산출값 마커**: 축 아래로 짧은 색깔 핀(작은 원)만 찍는다 — 이름 6개를
  전부 SVG 텍스트로 쓰면 좁은 가격대에 몰릴 때(흔한 경우) 아무리 줄바꿈을
  늘려도 결국 겹쳐서 안 보인다는 걸 사용자가 실제로 겪어서, "어떤 색이
  무슨 값인지"는 그래프 밖의 화면 카드에 같은 색 점으로 설명하는 방식으로
  바꿨다:
  - `MARKER_COLORS`(이름 → 고정 색, 카테고리 컬러 순서 고정 — blue/orange/
    violet/magenta/aqua)로 보수적 급매가/현실적 체결가/상단 매도가/
    AI 기준매도가/권장 최초 호가 각각의 고정 색을 정해뒀다. `hero_name`
    (기본 "경매용 매도가" — 8-2절, 지금 화면의 대표값)만 `MARKER_COLORS`가
    아니라 `primary` 인자 색(웹은 네이버 그린, CLI 리포트는 인디고)을 쓴다.
  - 이 `MARKER_COLORS`를 그래프를 호출하는 쪽(`webapp/app.py`, CLI
    `scripts/report.py`)에서도 그대로 가져다 써서, 웹의 4칸 가격 그리드
    카드마다 라벨 앞에 같은 색 점을, CLI HTML 리포트의 stat 카드마다 같은
    색 왼쪽 테두리를 넣는다 — 그래프의 색깔 핀과 화면 카드가 항상 같은
    색으로 연결되어 있어서, "이 핀이 무슨 값인지" 바로 알 수 있다.
  - **예외**: `hero_name`(경매용 매도가) 하나만은 핀 밑에 이름을 텍스트로도
    같이 써준다 — 겹칠 다른 라벨이 없어서 안전하고, 이 그래프에서 제일
    중요한 값이라는 걸 그래프만 보고도 바로 알아보게 하기 위함이다.
- 색은 페이지 톤에 맞춘다 — 웹 버전은 기본값(네이버 그린 `#03c75a`), CLI
  HTML 리포트는 그 페이지의 인디고 accent(`#4f46e5`)를 `primary` 인자로
  넘겨서 맞춘다.
- **웹 버전에서는 이 그래프만 쓰는 전용 카드(결과 페이지 전체 폭)에
  넣는다** — 히어로 카드 안, 4칸 가격 그리드 아래에 넣던 것을 밖으로 뺐다.
  ⚠️ 히어로 카드는 29절 가격구간전략과 나란히 놓이는 2단 레이아웃
  (`.hero-row.two-col`)의 왼쪽 칸이라, 그 안에 있으면 데스크톱에서도
  본문 폭의 절반 남짓(≈500px)까지밖에 못 커진다 — 사용자가 "매도가 바로
  밑에 딸려있게 하지 말고 가로로 길게 크게 나오게 해달라"고 지적해서
  히어로 카드 밖, 2단 레이아웃 아래의 독립 카드로 옮겼다(핵심 비교거래
  카드 바로 위). 같은 이유로 CLI HTML 리포트도 "매도가 산출" 카드 안에서
  같은 폭을 쓴다.
- **전용 카드에 맞춰 viewBox 비율도 가로로 길게 바꿨다** — 호출부
  (`webapp/app.py`, `scripts/report.py`)가 `width=880, height=270`을
  넘긴다(기본값 420×300은 함수 시그니처에 그대로 남아있다). 1440px 모니터
  기준 실제 렌더링 1102×338px로, 좁은 칸에 끼워넣던 때보다 훨씬 크고
  시원하게 보인다. 넓은 모니터에서 더 살리려고 `base.html`의 본문 폭도
  1280px 이상에서 1180px로 넓혔다(그 아래는 기존 1000px 그대로).
- ⚠️ **가로로 긴 viewBox는 좁은 화면에서 글자가 뭉개진다** — "모바일
  가독성 확대" 항목에서 설명한 축소 배율 문제가 그대로 재발한다(880 폭
  viewBox를 390px 화면에 넣으면 0.44배). 그래서 `render_price_distribution_
  html()`에 `min_width` 인자를 새로 뒀다: 그 폭(호출부는 720px) 아래로는
  줄어들지 않게 `min-width`를 걸고, SVG를 `overflow-x:auto` 래퍼로 감싸
  좁은 화면에서는 **그래프만 가로 스크롤**되게 한다(표가 넘칠 때
  `.tier-table-wrap`으로 스크롤을 주는 것과 같은 처리). 390px 화면에서
  페이지 자체는 가로 스크롤이 생기지 않는 것까지 Playwright로 확인했다.
  `min_width=0`(기본값)이면 래퍼도 `min-width`도 붙지 않아 기존 동작
  그대로다.

#### 8-1-1. 그래프 확장 (모양·테두리·시계열 보정 연결선·클릭 상세·밀도·필터)

사용자가 "그래프가 설명만 있고 심심하다, 더 자세히 만들 방법이 없을까"라고
물어서 지피티와 상의한 우선순위를 그대로 반영했다. 핵심 원칙은 그대로다 —
**점 하나에 정보를 다 우겨넣지 않는다.** 시각 채널은 딱 세 개(크기·진하기=
반영도, 모양=거래유형, 테두리=특별히 중요한 거래)로 제한하고, 나머지(계약월·
거리·시계열보정 여부 등)는 클릭했을 때만 보여주는 상세 카드로 뺐다 — 5절
`describe_comparable_similarity()`가 "판단을 너무 많이 얹었다가 이해하기
어렵다"고 지적받은 전례를 여기서도 반복하지 않기 위해서다.

- **점 모양 = 거래유형(5절 `dealingGbn` 다운웨이트와 같은 데이터)**: 원(●)
  = 중개거래(또는 정보 없음), 다이아몬드(◆) = 직거래. 직거래는 가족 간
  거래처럼 시세를 반영 안 할 수 있어 계산에서도 가중치를 낮추는데, 그
  판단 근거를 그래프에서도 바로 볼 수 있게 했다.
- **점 테두리 = 특별히 중요한 거래(5절 동일건물 보너스·평당가 이상치
  다운웨이트와 같은 데이터)**: 골드색 굵은 테두리(`SAME_BUILDING_RING_
  COLOR`) = 좌표가 거의 겹치는 동일건물 거래(대상 물건이 실제로 얼마에
  팔렸는지 보여주는 가장 직접적인 증거), 빨간 점선 테두리(`OUTLIER_RING_
  COLOR`) = 평당가 이상치(가중치를 크게 낮춘 거래). 5절에서는 이 두 플래그
  (`_same_building`/`_price_outlier`)를 텍스트 목록에 노출하지 않기로
  했었는데, 그래프는 판단 문장을 새로 얹는 게 아니라 이미 있는 데이터를
  시각 속성 하나(테두리)로 옮기는 것뿐이라 같은 "복잡해서 이해하기 어렵다"
  문제가 재발하지 않는다고 보고 여기서는 노출한다.
- ⛔ **시계열 보정 연결선은 제거했다** — 7-2절 자체가 꺼지면서 같이
  뺐다(연결선·끝의 빈 점·범례 항목·클릭 상세의 "시계열 보정" 줄 전부).
  `test_price_chart.py`에 "`_amount_man_adjusted`가 있어도 아무것도
  그리지 않는지" 확인하는 회귀 테스트를 뒀다.
- **점 클릭 → 상세 카드**: 강조 점을 클릭하면 그래프 바로 아래 카드에
  "단지명 금액 · 반영도 N / 거리·개월전·거래유형·(이상치·시계열보정 있으면
  추가) / → 왜 많이/적게 반영됐는지 한 줄 결론"이 뜬다(`_reflect_sentence()`).
  배경 점(강조 밖으로 밀려난 낮은 가중치 거래)은 클릭 불가능하다 — 정보량을
  늘리되 "이 점이 왜 진하고 큰지" 같은 핵심 질문에 대한 답을 항상 클릭
  한 번으로 찾을 수 있게 하는 게 목적이다.
- **표본을 잘라내지 않고 "강조/배경"으로 나눈다**: 가중치 내림차순 상위
  `max_dots`(기본 40)건만 위 모양·테두리·연결선·클릭상세를 다 갖춘 "강조"
  점으로 그리고, 나머지는 옅고 작은 배경 점(상호작용 없음)으로 전부 그린다
  (안전 상한 `MAX_TOTAL_DOTS`=150건 — 극단적으로 큰 표본 방지용일 뿐 보통은
  전체 표본이 다 그려진다). 캡션에 "비교거래 N건 · 주요 유사거래 M건 강조"
  라고 항상 정직하게 알려준다 — 예전처럼 40건 넘으면 나머지를 그냥 안
  보여주던 것보다, "더 있지만 덜 중요하게 표시했다"는 걸 명확히 하는 쪽이
  낫다고 판단했다.
- **거래 밀집도 히스토그램**: 그래프 맨 아래에 강조/배경 구분 없이 표본
  전체(`rows`)의 가격이 어디에 몰려 있는지 얇은 막대 그래프로 보여준다
  (`_histogram_counts()`, 최대 18구간). 점들만으로는 "대부분 어디에 몰려
  있나"를 한눈에 파악하기 어려워서 추가했다 — 가장 거래가 많은 구간만
  살짝 진한 색(`HIST_BAR_COLOR_STRONG`)으로 표시한다.
- **필터 토글("최근 3개월만 강조"/"200m 이내만 강조")**: 그래프 위 작은
  버튼 두 개로, 강조 점마다 미리 심어둔 `data-months-ago`/`data-distance`
  속성만으로 조건에 안 맞는 강조 점을 옅게(`opacity 0.15`) 만든다 — 데이터를
  다시 안 불러오고 이미 그려진 SVG 요소의 스타일만 바꾸는 순수 프론트엔드
  동작이다. 배경 점은 토글의 영향을 받지 않는다(애초에 상세 정보가 없어서
  필터링할 의미가 적다).
- **압축 범례 + 접이식 상세 설명**: 예전엔 4줄짜리 범례를 항상 펼쳐서
  보여줬는데, 요소가 늘어나면서(모양·테두리·연결선까지) 그대로 두면 캡션이
  너무 길어진다. 그래서 한 줄짜리 압축 범례만 항상 보여주고, "? 그래프
  보는 법"을 눌러야 기존 방식의 자세한 줄 단위 설명(`_legend_row()`)이
  펼쳐진다 — 모바일에서 화면을 덜 차지하게 하려는 목적이 크다.
- ⚠️ **압축 범례는 실제로 그려진 기호만, 건수와 함께 보여준다.** 예전엔
  다섯 기호를 항상 늘어놓았는데, 그 표본에 직거래가 한 건도 없으면
  "◆ 직거래"만 범례에 떠 있고 그래프에는 다이아몬드가 안 보여서
  **"다이아몬드가 왜 사라졌지?"라고 오해하게 된다**(사용자가 실제로 그렇게
  물었다 — 그래프는 정상이었고 그 지역 표본에 직거래가 없었을 뿐이다).
  그래서 지금은:
  - 직거래가 있으면 `◆ 직거래 N`, 없으면 `◆ 직거래 0건`(흐리게)로 **없다는
    사실 자체를 적는다**.
  - 표본 전체에 `_dealing_gbn`이 아예 없으면(국토부 응답에 `dealingGbn`이
    안 왔거나 5절 정규화가 못 알아본 값이면) `◆ 직거래 — 거래유형 정보
    없음`으로 표시한다 — "직거래가 0건"인 것과 "구분할 수가 없는" 것은
    전혀 다른 상태라서 구별해 보여준다.
  - 동일건물·이상치는 해당하는 거래가 있을 때만 건수와 함께 범례에
    넣는다(없으면 아예 안 적는다).
  - 건수는 **강조 점(`emphasis_rows`) 기준**이다 — 배경 점은 애초에 모양·
    테두리·연결선을 그리지 않으므로 범례에 세면 숫자와 화면이 어긋난다.
- 클릭 상세 카드의 "N개월 전" 표시는 `this_year`/`this_month`를 새
  선택 인자로 받아서 계산한다(생략하면 렌더링 시점의 현재 시각으로 근사) —
  7절 `weight_for_recency()`가 실제 계산에 쓰는 기준 시점과는 별개로,
  화면 표시용 근사치일 뿐이라 오차가 있어도 계산 자체에는 영향이 없다.
  웹 버전(`webapp/app.py`)과 CLI HTML 리포트(`scripts/report.py`)
  양쪽 다 실제 분석에 쓴 this_year/this_month를 그대로 넘겨준다.
- ⚠️ 지피티가 원래 제안한 "계약월도 점의 시각 속성으로"는 넣지 않았다 —
  이미 크기·모양·테두리 세 채널을 쓰고 있어서 네 번째를 더하면 오히려
  못 읽게 된다는 지적을 그대로 받아들였다. 대신 "최근 3개월만 강조" 토글로
  대체했다. 거리도 마찬가지로 점 위치가 아니라 "200m 이내만 강조" 토글과
  클릭 상세 카드로만 노출한다.

**시각 스타일 다듬기(참고 목업 반영)** — 사용자가 지피티에게 직접 그려달라고
한 참고 목업(카드형 목업 이미지)을 보내줘서, 그 톤에 맞춰 세부 스타일을
다시 손봤다(기능·조건·게이팅 로직 자체는 위 항목들과 동일하고 그대로다):
- **점 색 램프를 회색조 → 파란 계열로 바꿨다**(`DOT_COLOR_LOW`/`DOT_COLOR_HIGH`
  — 연한 블루그레이 → 짙은 네이비). 목업이 파란 톤이었던 것을 그대로 반영했다.
- **호버/탭 툴팁을 검정 말풍선 한 줄에서 흰색 카드 두 줄로 바꿨다** — 굵은
  제목 줄(단지명·금액)과 연한 회색 부제 줄(거리·개월수·거래유형 등)로 나눠
  목업의 카드형 툴팁과 같은 형태로 만들었다(`.pd-tip-title`/`.pd-tip-sub`,
  `data-tip-title`/`data-tip-sub` 속성). 클릭 시 뜨는 그래프 아래쪽 영구
  상세 카드(`.pd-detail`, "왜 반영됐는지" 결론 문장까지 포함)는 그대로 유지
  — 모바일에서 호버가 없어 지속적으로 볼 수 있는 카드가 여전히 필요해서다.
- **거래 밀집도를 막대그래프 대신 부드러운 밀도 곡선(언덕 모양)으로
  바꿨다**(`_smooth_area_path()` — 인접한 두 점의 중점을 지나는 2차 베지어로
  잇는 가벼운 스무딩 기법, 외부 차트 라이브러리 없이 SVG만으로 구현). 구간
  수도 18 → 28로 늘려 더 매끈하게 보이도록 했다. 목업이 계단식 막대가 아니라
  부드러운 곡선이었던 것을 반영했다.
- Playwright로 실제 크로미움에 렌더링해서 데스크톱·모바일 폭, 호버 카드,
  클릭 상세 카드, 필터 토글까지 스크린샷으로 직접 대조해서 확인했다.

**모바일 가독성 확대(글자·점이 너무 작다는 지적 반영)** — 사용자가 "그래프
글자랑 점이 너무 작아서 잘 안 보인다"고 지적했다. 원인은 SVG `viewBox`
내부 좌표 폭(예전 660)이 실제 카드 렌더링 폭(모바일에서 보통 300~350px대
— `base.html`의 `body` 좌우 패딩 16px + `.card` 좌우 패딩 22px를 빼면
그 정도만 남는다)보다 훨씬 넓어서, 브라우저가 `preserveAspectRatio`
기본값(`xMidYMid meet`)으로 전체를 절반 가까이(≈0.5배) 축소해 그리고
있었던 것 — 안의 폰트 크기·점 크기를 아무리 숫자로 크게 적어놔도 실제
화면에는 그 절반 크기로만 나온다는 뜻이다. 그래서 두 가지를 같이 고쳤다:
1. **`viewBox` 내부 좌표 폭 자체를 줄였다**(기본 `width` 660 → 420,
   `height` 215 → 300). 폭을 줄이면 같은 실제 화면 폭에 대응하는 좌표
   단위 수가 적어지므로, 축소 배율이 1에 더 가까워져서(예: 모바일 320px
   기준 320/660≈0.48배 → 320/420≈0.76배) 안의 요소들이 실제로 더 크게
   보인다 — 폭을 늘리는 게 아니라 **줄이는 쪽이 모바일에서 더 커 보이는
   역설적인 트릭**이다(카드 자체는 어차피 `width="100%"`라 컨테이너
   폭까지만 늘어나고, 그 이상은 못 커진다).
2. 그 위에 점 반지름(`DOT_R_MIN/MAX` 4~7.5 → 5~9)·히트 타깃(`hit_r` 13→15)·
   행 간격(`row_gap` 14→16)·거의 모든 텍스트(눈금 10→13px, 툴팁 제목
   12.5→14px, 범례 11~12.5→12.5~14px, 안내 문구 13.5/11.5→16/13px,
   히어로 라벨 11→13px)를 실제 좌표값 자체도 20~30%씩 키웠다 — 1번만으로도
   체감 크기가 커지지만, 요소 자체도 커야 정말 또렷해진다.
   `axis_y`(120→150)·히스토그램 위치(`hist_y0`, `axis_y+46`→`axis_y+55`)·
   마커 핀 크기도 같이 늘어난 공간에 맞춰 재배치했다.
- Playwright로 모바일 폭(390px)과 데스크톱 폭(800px) 양쪽에 렌더링해서
  글자가 뭉개지지 않고 읽히는지, 클릭 상세 카드·펼친 범례까지 레이아웃이
  안 깨지는지 스크린샷으로 확인했다.
- 이 기본값은 `render_price_distribution_html()`의 `width`/`height`
  인자라 호출부(`webapp/app.py`, `scripts/report.py`)에서 명시적으로
  다른 값을 넘기면 그 값이 우선한다 — 지금은 둘 다 기본값을 그대로 쓴다.
- ⚠️ **바로 다음 수정: 데스크톱에서는 오히려 여백만 남던 문제.** 위 1번
  트릭(viewBox 폭을 줄이는 것)만으로는 안 끝났다 — `<svg>`에 `height`를
  고정 픽셀(예: 300)로 박아두면, `preserveAspectRatio`(기본값 `xMidYMid
  meet`)가 "viewBox 비율을 유지하며 박스 안에 맞추는" 스케일을
  `min(가로스케일, 세로스케일)`로 정하는데, 세로 스케일이 정확히 1.0(높이가
  꼭 맞아떨어지므로)이라서 **가로 스케일도 1.0을 절대 못 넘는다** — 즉
  카드가 viewBox보다 넓어져도(데스크톱, 특히 29절 가격구간전략과 나란히
  놓이는 2단 레이아웃이나 넓은 모니터) 원래 크기(예: 420px) 이상은 절대
  안 커지고 남는 폭만큼 좌우로 빈 여백이 생긴다. 사용자가 "나는 모바일이
  아니라 웹(데스크톱)으로만 본다"고 지적해서 실측 확인했다 — Playwright로
  504/644/968px 카드 폭 세 가지를 렌더링해보니 셋 다 똑같이 420px로만
  그려지고 나머지는 여백이었다. **`height="{height}"` 고정 속성을 CSS
  `style="height:auto"`로 바꿔서 해결했다** — 이제 높이가 실제 렌더링된
  가로폭에 비례해서 정해지므로(`<img>`가 폭만 지정하고 높이를 자동으로
  따라가게 하는 것과 같은 원리) 세로 스케일이라는 상한 자체가 없어진다.
  카드가 넓으면 그래프도 그만큼 커지고(1.0배를 넘어 확대되는 것도
  가능), 좁으면 비례해서 줄어든다 — "컨테이너 폭 = 실제 렌더링 폭"이 항상
  성립한다.

### 8-2. 경매용 매도가

일반 매매 시세를 기준으로 한 현실적 체결가(median)는 "이 동네에서 시간을
들여 협상해서 파는" 일반적인 매매 상황을 전제로 한 값이다. 경매로 낙찰받은
물건은 대개 더 빨리 정리해야 하는 사정(대출 이자, 명도, 자금 회전 등)이
있어 실제로는 일반 매매 시세보다 낮게 형성되는 경향이 있다 — 사용자가
실제 낙찰 후 매도 사례들을 이 계산기 값과 대조해보고 확인해준 내용이다.

- 계산식: **경매용 매도가 = (보수적 급매가 + 현실적 체결가) ÷ 2**, 10만원
  단위 반올림. "보수적 급매가와 현실적 체결가 사이"라는 사용자 관찰을
  가장 단순하고 투명하게 반영한 값 — 통계적으로 유도한 값이 아니라 두
  기준값의 중간값이라는 점을 항상 명확히 한다.
- **웹 버전에서는 이 값이 결과 페이지의 대표(히어로) 숫자다** — 8절의
  다른 값(보수적 급매가/현실적 체결가/상단 매도가/AI 기준매도가/권장
  최초 호가)은 모두 그대로 계산·표시하되, 화면 맨 위 큰 숫자 자리는
  현실적 체결가 대신 경매용 매도가가 차지한다. 8-1절 가격 분포 그래프의
  강조 마커(accent 색)도 이 값에 맞춘다(`hero_name="경매용 매도가"`).
- 출력 포맷 (CLI, 8절 "현실적 체결가" 바로 뒤):
  ```
  보수적 급매가: X.XX억
  현실적 체결가: X.XX억 (일반 매매 기준)
  경매용 매도가: X.XX억 (보수적 급매가~현실적 체결가 중간값 — 경매 낙찰 후 되파는 경우 참고)
  상단 매도가: X.XX억
  AI 기준매도가: X.XX억
  권장 최초 호가: X.XX억
  ```
- 19절 수익성 계산(CLI, `--bid-price` 지정 시)의 매도 시나리오 목록에도
  "경매용 매도가 기준" 한 줄이 추가된다 — 경매 수익성 판단에는 이 값이
  현실적 체결가보다 더 적합한 기준이기 때문이다.

## 9. 스크립트 참고

- `scripts/molit_rhtrade_api.py`: 매매 실거래가 API 직접 호출 (로컬/서버 전용 — 샌드박스에서 실행 불가).
  여러 DEAL_YMD를 한 번에 받아 각각 `data/raw/<LAWD_CD>/<YYYYMM>.xml`로 자동 저장한다.
- `scripts/molit_apt_api.py`: 아파트 매매 실거래가 API 직접 호출 (40절, 로컬/서버 전용).
  `data/raw_apt/<LAWD_CD>/<YYYYMM>.xml`로 저장한다 — 8절 비교거래에 아파트가
  섞이지 않도록 `data/raw`와 폴더를 분리한다.
- `scripts/molit_rhrent_api.py`: 전월세 실거래가 API 직접 호출 (16절, 로컬/서버 전용).
  마찬가지로 `data/raw_rent/<LAWD_CD>/<YYYYMM>.xml`로 자동 저장한다.
- `scripts/geocode.py`: 카카오 로컬 API로 지번 주소 → 좌표 변환, `data/geocode_cache.json`에
  캐시. `nearby_place()`(19절 입지체크, 26절 역세권 프리미엄, 34절 주변
  지형 참고)는 좌표+키워드+반경
  조합을 `data/nearby_place_cache.json`에 캐시한다
- `scripts/lawd_lookup.py`: `data/lawd_codes.md` 표를 읽어 법정동코드 → (시도, 시/군/구) 역조회
- `scripts/price_chart.py`: 8-1절 가격 분포 그래프(SVG 스트립플롯) —
  `scripts/report.py`(CLI HTML 리포트)와 `webapp/app.py`(웹 버전) 양쪽에서
  재사용한다
- `scripts/report.py`: 14절 CLI HTML 리포트(`--html` 옵션) 생성
- `scripts/naver_link.py`: 27절 네이버부동산 지도 바로가기 URL 생성
- `scripts/listing_parser.py`: 28절 붙여넣은 매물 텍스트 파싱·유사도 순위,
  31절 경쟁매물 가격 포지션(`price_rank_among_listings()`), 39절 매도 압력
  (`sale_pressure()`)
- `scripts/broker_lookup.py`: 21절 인근 중개업소 조회 (서울 CSV 지오코딩 / 경기데이터드림 API)
- `scripts/market_index.py`: 24절 시장 동향 참고 지표 — 한국부동산원 연립다세대
  매매수급동향지수(`data/reb_villa_market_index.csv`, 서울은 5대 생활권 세분화된
  `data/reb_villa_market_index_seoul_zone.csv`도 함께) 일회성 스냅샷을 읽고,
  지역간 순위(`rank_region`)·쉬운 말 풀이(`_plain_market_desc`) 계산까지 담당.
  KB부동산 스타일 아파트 매수우위/전세수급지수(`data/kb_market_index.csv`,
  `compute_market_trend`/`yoy_change`) 계산 로직도 파일 안에 남아있지만,
  사용자 요청으로 현재는 화면에 출력하지 않는다
- `scripts/estimate_price.py`: `data/raw/`에 저장된 XML로 1~8절 로직을 그대로 일괄 계산하고,
  `data/raw_rent/`에 전월세 데이터가 있으면 16절 예상 전세가·매매 대비 비교도 이어서 계산.
  `compute_price_tiers()`(29절)·`compute_liquidity()`(30절)·`build_verdict()`
  (32절)도 이 파일에 있다 — 웹 버전(`webapp/app.py`)도 그대로 가져다 쓴다.
  `--dir`은 `data/raw`(지금까지 조회한 모든 지역을 합쳐서)와
  `data/raw/<LAWD_CD>`(특정 지역만) 둘 다 그대로 동작한다(하위 폴더까지 재귀적으로 찾는다).
  ```
  python scripts/estimate_price.py --dir data/raw --rent-dir data/raw_rent \
      --address "서울특별시 강북구 수유동 468-202" --dong 수유동 \
      --area 69.27 --floor 5 --build-year 2012 --radius 400 --year-min 2025
  ```
- `scripts/tests/`: 33절 — 핵심 계산 함수 단위 테스트
  (`python3 -m unittest discover -s scripts/tests -v`로 실행)

## 10. 법정동코드 참고 우선순위

LAWD_CD는 기억에 의존해 추측하지 않는다. `data/lawd_codes.md` 표를 먼저 확인하고,
표에 없으면 사용자에게 재확인을 요청한 뒤 확인된 코드를 표에 추가한다.

## 11. 현재 매물(네이버부동산 등) 비교 규칙

실거래가(3~8절)는 "과거에 실제로 팔린 가격"이고, 이 절은 "지금 시장에 나와 있는
호가"를 참고해서 두 값을 나란히 보여주기 위한 규칙이다. 네이버부동산 등은 공식
API가 없고 자동 수집은 이용약관 위반 소지가 있으므로, 절대 스크래핑을 시도하지
않는다 — 사용자가 직접 사이트에서 반경(예: 400m)을 잡아 검색한 뒤 매물 목록
화면을 복사해서 붙여넣는 방식만 사용한다.

**입력 처리**
- 형식이 사이트마다 다르므로 정해진 스키마 없이 관대하게 읽는다. 매물마다 다음을
  정보를 최대한 추출한다: 단지명/건물명, 거래유형(매매만 사용, 전세·월세는 제외),
  호가(가격), 공급/전용면적(㎡), 층, 사용승인일 또는 연식(있으면), 기타 특징(참고용).
- "3억 5,000" 같은 한국어 혼합 표기는 만원 단위로 환산한다 (3억 5,000 → 35,000만원).
- 여러 매물이 한 덩어리 텍스트로 붙어 있으면 단지명·층·가격이 한 세트가 되도록
  최대한 분리한다. 도저히 분리가 안 되는 항목은 무시하고 몇 건을 제외했는지 알린다.

**비교 기준** (5절과 동일한 원칙 재사용)
- 전용면적 ±15% 이내를 "유사면적"으로 취급한다.
- 사용승인일/연식 정보가 있으면 준공년도 ±3년 이내를 "유사연식"으로 취급한다
  (정보가 없으면 생략하고 동네·면적만으로 비교한다).
- 매매가 아닌 거래(전세/월세)는 비교 대상에서 제외한다.

**출력 포맷** (8절 결과 바로 뒤에 이어서 표시한다)

```
[현재 매물 시세 비교] (네이버부동산 기준, 확인일: YYYY.MM.DD)
비교 매물: N건 (유사면적·유사연식)

최저 호가: X.XX억
중간 호가: X.XX억
최고 호가: X.XX억

매물 목록:
- 단지명 면적㎡, 층, X.XX억 (연식정보)

→ 실거래가 기준 현실적 체결가(X.XX억) 대비 현재 호가 중간값은 X.XX억으로 [더 높음/비슷함/더 낮음]
```

**주의사항**
- 호가는 실제 체결가가 아니라 매도인 희망가이므로 실거래가보다 통상 높게
  형성된다는 점을 항상 언급한다.
- 비교 매물이 5건 미만이면 "비교 매물이 적어 참고용으로만 활용" 문구를 덧붙인다.

## 12. 계절성(월별 거래 패턴) 분석 규칙

가격 추정(3~8절)과 별개로, "언제 내놓아야 잘 팔리는지" 참고 정보를 추가로 제공한다.
`scripts/estimate_price.py`가 매도가 계산 뒤에 자동으로 이 분석도 출력한다.

- 8절의 연도 필터(작년 이후)를 적용하지 않고 `data/raw/`(또는 붙여넣은 XML)에 있는
  **모든 기간**의 데이터를 사용한다. 계절성은 특정 연도의 가격 수준이 아니라
  "몇 월에 거래가 몰리는지" 패턴을 보는 것이기 때문이다.
- `cdealType`이 "해제"인 거래는 제외한다.
- 기준 범위: 먼저 동일 법정동(`umdNm`)으로 제한해서 계산한다. 단, 동 단위 거래가
  24건 미만이거나 2개 연도 미만으로만 존재하면 표본이 부족하므로, 같은 조회 지역
  (구) 전체 데이터로 범위를 넓히고 그 사실을 결과에 표시한다.
- 계산 방법:
  1. 각 거래를 계약월(`dealMonth`, 1~12)로 묶는다.
  2. 각 월의 "연도별 평균 거래건수" = 해당 월의 전체 거래건수 ÷ 그 월이 등장한
     연도 수 (연도마다 데이터 기간이 다를 수 있으므로 이렇게 보정한다).
  3. 12개월 평균 거래건수를 100으로 놓고 각 월을 지수화한다 (예: 130 = 평균보다
     30% 많음).
  4. 지수 110 이상인 달을 "성수기"(최대 3개), 90 이하인 달을 "비수기"(최대 3개)로
     분류한다. 이 기준에 드는 달이 없으면 억지로 채우지 말고 "뚜렷한 성수기/비수기
     없음"이라고 표시한다 (평균과 비슷한 달을 억지로 성수기/비수기로 몰아넣지 않는다).
- 최소 6개월 이상 분포된 데이터가 없으면 "표본 부족으로 계절성 분석 불가"라고
  안내하고 생략한다.
- 결과는 8절 매도가 산출 결과 뒤에 이어서 아래 형식으로 보여준다:

```
[계절성 분석] (OO동 기준, 거래량 지수 — 전체 평균=100)
1월:xx 2월:xx 3월:xx 4월:xx 5월:xx 6월:xx 7월:xx 8월:xx 9월:xx 10월:xx 11월:xx 12월:xx
거래 활발한 달: N월, N월, N월
거래 적은 달: N월, N월, N월

→ 매도 시점을 조정할 수 있다면 위 활발한 달 사이에 내놓는 것이 매수 수요가
  가장 많은 시기입니다. (참고용 통계이며 표본이 적을수록 신뢰도가 낮습니다)
```

## 13. 동네 랭킹(가격 상승·거래 활발 지역) 규칙

특정 물건 비교가 아니라 "지금 어떤 동네가 뜨고 있는지"를 실거래가 데이터만으로
보여준다. `scripts/rank_areas.py`가 이 로직을 구현한다.

- `data/raw/`에 저장된 모든 XML을 동(`umdNm`) 단위로 모은다 — 지금까지 조회해서
  저장해둔 동네만 대상이 되며, 조회 지역이 늘어날수록 랭킹도 넓어진다.
- 데이터 전체에서 가장 최근 계약월을 기준으로 "최근 3개월"과 "그 이전 3개월"
  두 구간을 나눈다 (시스템 날짜가 아니라 실제 데이터 기준).
- `cdealType`이 "해제"인 거래는 제외한다.
- 동별로 두 구간 각각 3건 미만이면 통계적으로 신뢰하기 어려우므로 랭킹에서
  제외한다 (표본 부족).
- **가격 상승률 랭킹**: 동별 최근 3개월 평균 평당가(만원/㎡)를 이전 3개월 평균과
  비교해 상승률(%) 내림차순으로 정렬한다.
- **거래 활발도 랭킹**: 동별 최근 3개월 거래건수를 내림차순으로 정렬한다.
- 사용법: `python scripts/rank_areas.py --dir data/raw --top 10`

## 14. HTML 리포트 옵션

`scripts/estimate_price.py`에 `--html` 옵션을 주면 텍스트 출력과 함께
`reports/`에 예쁘게 정리된 정적 HTML 리포트(외부 라이브러리·인터넷 연결 불필요)를
저장하고 Windows 기본 브라우저로 자동으로 연다. 8절 매도가 산출 결과와
8-1절 가격 분포 그래프, 핵심 비교거래 표, 12절 계절성 막대그래프를 한
화면에 보여준다.
사용법: `python scripts/estimate_price.py --dir data/raw --address "서울특별시 강북구 수유동 468-202" --dong 수유동 --area 69.27 --floor 5 --html`

## 15. 가격 추이(월별 평당가) 분석 규칙

계절성(12절)이 "몇 월에 거래가 몰리는지"라면, 이 절은 "가격 자체가 오르는지
내리는지"를 시간순으로 본다. `scripts/estimate_price.py`가 매도가 계산 뒤에
자동으로 텍스트로도 출력하고, `--html` 옵션을 주면 꺾은선 그래프로도 보여준다.

- 12절과 마찬가지로 8절의 연도 필터를 적용하지 않고 전체 기간 데이터를 쓴다.
- `cdealType`이 "해제"인 거래는 제외한다.
- 기준 범위: 동일 법정동(`umdNm`) 거래가 12건 이상이면 그 동으로 한정하고,
  부족하면 조회 지역 전체로 넓히고 그 사실을 표시한다.
- 계약월(연-월)별로 평당가(만원/㎡ = 거래금액 ÷ 전용면적)의 평균을 구해 시간순으로
  나열한다. 이 점들을 이은 것이 가격 추이다.
- 데이터가 있는 계약월이 3개월 미만이면 "표본 부족으로 생략"한다고 안내한다.
- 텍스트 출력 형식:
  ```
  [가격 추이] (OO동 기준, 첫달 → 마지막달)
  평당가(만원/㎡) X → Y (+Z.Z%)
  ```

## 16. 예상 전세가 및 매매 대비 비교 규칙

매매 실거래가만으로는 "지금 매매가가 비싼 건지 싼 건지", "갭투자하면 얼마가
묶이는지" 판단하기 어려울 때, 같은 물건 기준으로 예상 전세가도 함께 추정해서
매매가와 나란히 비교한다. `scripts/molit_rhrent_api.py`(전월세 조회)로 받은
데이터를 `scripts/estimate_price.py`가 5~8절과 **완전히 동일한 반경 기반
로직**으로 계산해서, 8절 매도가 결과 바로 뒤에 자동으로 이어서 보여준다
(`--rent-dir`, 기본값 `data/raw_rent`).

- ✅ 전월세 API(`RTMSDataSvcRHRent`)는 사용자가 업로드한 공식 기술문서(국토부
  실거래가 정보 오픈API 활용가이드 — 연립다세대 전월세 실거래가 자료)로 스펙을
  검증했다. 매매 API와는 별도로 활용신청이 필요하고, 인증키는 `MOLIT_SERVICE_KEY`를
  재사용한다.
- 조회 결과는 `data/raw_rent/`에 저장한다 (매매 데이터가 있는 `data/raw/`와 분리).
- 월세가 섞인 거래(`monthlyRent` != 0)는 제외하고 순수 전세 거래만 쓴다
  (환산보증금 계산은 하지 않는다).
- 예상 전세가 계산은 대상 물건의 반경(기본 400m)·전용면적(±15%)·준공년도
  (±4년)·층 조건을 매매가 계산 때와 **완전히 동일하게** 적용한다 (5~7절 규칙
  그대로, 금액 필드만 `dealAmount` 대신 `deposit`을 쓴다). 반지하 하드 제외
  규칙도 동일하게 적용된다.
- 전세는 "AI 기준매도가"/"권장 최초 호가" 같은 호가 전략 개념이 없으므로 세
  단계(보수적 급매 전세가=p25, 현실적 전세가=중앙값, 상단 전세가=p75)만
  보여준다. 신뢰도 점수는 8절과 같은 방식으로 별도 계산한다.
- 반경 안에 유사조건 전세 비교거래가 없으면(표본 0건) 그 사실만 안내하고
  생략한다 — 매매가 계산 자체를 막지 않는다.
- 전세가율 = 예상 전세가(현실적 전세가) ÷ 예상 매매가(현실적 체결가) × 100.
  갭(매매가 − 전세가)도 함께 보여준다 — 갭투자 시 실제 묶이는 자금의 근사치다.
- 참고 기준: 80% 이상이면 갭투자 부담이 적은 편, 65~80%면 평균적, 65% 미만이면
  매매가에 거품이 있거나 전세 수요가 약한 편으로 안내한다 (참고용 통계일 뿐 확정
  판단이 아니며, 전세 매물 수가 매매보다 적어 표본이 부족할 수 있음을 항상
  언급한다).
- 출력 포맷 (8절 "핵심 비교거래" 바로 뒤에 이어서 표시한다):
  ```
  [예상 전세가] (매매가와 동일 조건 — 반경 400m, 유사면적·유사층·유사연식)
  유효 비교거래: N건 (반경 400m 이내, 200m 이내 N건 / YYYY년 N건)
  시세 신뢰도: N/100

  보수적 급매 전세가: X.XX억
  현실적 전세가: X.XX억
  상단 전세가: X.XX억

  핵심 전세 비교거래 (가까운 순):
  - 단지명 면적㎡, N층, YYYY.MM 계약, X.XX억 — Nm

  [매매 vs 전세 비교]
  예상 매매가(현실적 체결가): X.XX억
  예상 전세가(현실적 전세가): X.XX억
  전세가율: N%
  갭(매매가 - 전세가, 갭투자 시 필요자금 근사): X.XX억
  → [참고 기준 문구]
  ```

## 17. 면적 구간별(평형대) 비교 규칙

같은 동네라도 소형·중형·대형이 서로 다르게 움직일 수 있어, 13절 동네 랭킹을
한 동 안에서 면적 구간별로 더 세분화해서 본다. `scripts/rank_areas.py`에
`--dong`을 주면 함께 출력된다.

- 면적 구간: 40㎡ 미만(소형), 40~60㎡(중소형), 60~85㎡(중형), 85㎡ 이상(대형).
- 계산 방식은 13절 가격 상승률 랭킹과 동일하다 — 지정한 동 안에서, 최신 계약월
  기준 최근 3개월 대 이전 3개월 평당가를 구간별로 비교한다.
- 구간마다 최근·이전 3개월에 각 3건 미만이면 "데이터 부족"으로 그 구간은 생략한다.
- 사용법: `python scripts/rank_areas.py --dir data/raw --dong 역촌동`

## 18. 방 개수(구조) 보정 규칙

국토부 실거래가 데이터에는 방 개수 필드가 없어 3~8절 계산이 구조 차이를 반영하지
못한다. 이 절은 11절에서 붙여넣은 매물 텍스트를 활용해 "방 개수에 따른 가격 차이
비율"만 추산하고, 그 **비율**을 8절 산출값에 참고용으로 얹는다 (호가 절대값 자체는
믿지 않고, 방 개수 간 상대적 차이만 가져오는 방식이라 비교적 안전하다).

- 11절 입력 처리 규칙을 그대로 따르되, 매물마다 방 개수(예: "3룸", "방3", "방 3개"
  등 표기)도 추가로 추출한다. 표기가 없는 매물은 이 보정 계산에서 제외한다
  (11절의 시세 비교 자체에는 계속 포함해도 된다).
- 방 개수별로 그룹화해서 그룹별 평당가(호가 ÷ 전용면적) 중앙값을 구한다. 그룹당
  매물이 2건 미만이면 그 그룹은 신뢰할 수 없으므로 보정에 쓰지 않는다.
- 비교 가능한 방 개수 그룹이 2개 미만이면(예: 전부 3룸뿐이라 비교 대상이 없음)
  보정을 생략하고 그 이유를 안내한다.
- 구조보정비율 = 대상 물건과 같은 방 개수 그룹의 평당가 중앙값 ÷ 전체 매물(방 개수
  무관) 평당가 중앙값.
- 8절 산출값(보수적 급매가/현실적 체결가/상단 매도가/AI 기준매도가/권장 호가) 각각에
  이 비율을 곱한 "구조보정 참고값"을 **원래 8절 값과 나란히** 보여준다 — 8절 값 자체를
  덮어쓰지 않는다. 실거래 비교군의 실제 방 개수 구성은 알 수 없어서 이 보정은 근사치일
  뿐임을 항상 언급한다.
- 출력 포맷 (8절/11절 결과 뒤에 이어서 표시한다):

```
[방 개수 구조 보정] (네이버부동산 매물 기준, 참고용)
방 개수별 평당가: 2룸 X만원/㎡ (N건) · 3룸 Y만원/㎡ (N건)
대상 물건(N룸) 구조보정비율: ±Z% (전체 평균 대비)

구조보정 참고값 (원래 8절 값 → 보정값):
보수적 급매가: X.XX억 → X.XX억
현실적 체결가: X.XX억 → X.XX억
상단 매도가: X.XX억 → X.XX억
AI 기준매도가: X.XX억 → X.XX억
권장 최초 호가: X.XX억 → X.XX억
```

- 항상 "실거래가와 현재 호가는 서로 다른 데이터라 이 보정값은 참고용일 뿐"이라는
  문구를 덧붙인다.

## 19. 입지 체크 및 수익성 계산 규칙

사용자가 관심물건 메모에서 실제로 항상 확인하는 두 가지 — "역세권/초등학교
거리"와 "낙찰가 대비 얼마 남는지" — 를 자동화한다. `scripts/estimate_price.py`가
지오코딩 직후(입지 체크)와 8절 결과 직후(`--bid-price`를 준 경우만, 수익성 계산)에
출력한다.

**입지 체크** (`scripts/geocode.py`의 `nearby_place`, `--no-location`으로 끌 수 있음)
- 대상 물건 좌표 기준 반경 **1.5km**(`LOCATION_SEARCH_RADIUS_M`) 안에서 카카오
  로컬 키워드 검색으로 가장 가까운 시설을 찾는다.
  ⚠️ 원래 1km였는데 45절 때문에 넓혔다 — "역까지 1.1km"와 "아예 없음"은 전혀
  다른 상태인데 1km에서 자르면 둘 다 "없음"으로 뭉개진다.
- **항목은 10개, 네 갈래로 묶여 있다**(`LOCATION_GROUPS`) — 사용자가 "편의점이나
  병원, 중학교 고등학교 등등 더 자세히 만들 수 있냐"고 물어서 세 개(지하철역·
  초등학교·마트)에서 늘렸다.

  | 갈래 | 항목 |
  |---|---|
  | 🚇 교통 | **지하철역**, 버스정류장 |
  | 🏫 교육 | **초등학교**, 중학교, 고등학교 |
  | 🏪 생활 | 편의점, 대형마트, 공원 |
  | 🏥 의료 | 병원, 약국 |

  **굵은 두 개(`LOCATION_SCORED`)만 45절 환금성 판정에 쓰이고, 나머지 여덟은
  화면 참고용이다.** 사용자와 정한 원칙 — 편의점이 200m냐 400m냐가 빌라 매도
  속도를 가르지는 않는데 다 점수에 넣으면 오히려 변별력이 흐려진다. 화면에도
  점수에 쓰이는 항목에는 ★를 달고 "★ 표시만 판정에 쓰입니다"라고 밝힌다.
  회귀 테스트(`TestLocationKeywords`)로 이 범위를 고정해 뒀다.
- ⚠️ **혐오시설(유흥가·모텔촌·공장)은 일부러 뺐다.** 매도에 제일 크게 영향
  주는 요소인 건 맞지만 키워드 검색으로는 정확히 못 잡는다(34절 산·하천에서
  겪은 것과 같은 한계) — 없는 문제를 만들어내는 쪽이 더 해로워서 36절 임장
  체크로 넘긴다. 나중에 정확한 카테고리 코드나 별도 데이터가 확보되면 그때
  16절 전월세 API처럼 **검증된 방법**으로 붙인다.
- ⚠️ **계산부는 `compute_location_check()`로 분리돼 있다** — CLI 출력
  (`print_location_check()`), 웹 결과 카드, 45절 환금성 항목이 **같은 결과를
  나눠 쓴다**(카카오를 항목마다 여러 번 부르지 않는다). 지금은 CLI·웹 둘 다
  같은 10개 전체를 조회한다(예전엔 웹만 두 개로 줄여 불렀다).
- **호출 비용**: 새 주소 첫 조회는 카카오 키워드 검색 10번이다. `nearby_place()`가
  좌표(소수점 5자리)+키워드+반경으로 `data/nearby_place_cache.json`에 캐시하므로
  **같은 주소를 다시 조회하면 0번**이고, 같은 건물·인접 건물도 좌표가 거의 같아
  캐시가 걸린다. 26절 역세권 프리미엄(비교거래 15건마다 호출)에 비하면 작다.
- ⚠️ **웹 버전에도 생겼다** — 예전엔 CLI 전용이었는데 45절을 붙이면서 결과
  페이지에 "입지 체크" 카드로 같이 넣었다(수익성 계산만 CLI 전용으로 남는다).
  웹은 갈래별 카드 그리드(`.loc-groups`, `base.html`)로 보여주고, **한 갈래에서
  하나도 못 찾으면 그 갈래는 통째로 뺀다**.
- ⚠️ **웹 카드 위치: 41절 환금성·경쟁 진단 바로 뒤다**(맨 아래 "참고 정보"
  섹션이 아니라). 사용자가 "매도가 예상 → 환금성 체크 → 입지 → 동 시세·
  거래량 순으로 시선이 가는 게 맞다"고 해서 올렸다 — 45절 역세권·학세권
  판정이 바로 위에서 나오는데 그 근거가 되는 거리는 페이지 맨 아래에 있으면
  연결이 안 된다(20절 건축물대장을 41절 층·승강기 항목 안으로 옮긴 것과
  같은 이유). 카드 맨 위에도 "위 환금성 진단 역세권·학세권 항목이 어디서
  나온 숫자인지 보여주는 칸"이라고 한 줄 적는다.
- ⚠️ **34절 주변 지형(산·하천)이 이 카드의 다섯 번째 갈래 "🌳 자연"으로
  합쳐졌다** — 사용자가 "두 개를 합쳐도 괜찮을 것 같은데? 교통·교육·생활·
  의료 다음에 자연이라는 파트로"라고 해서다. 둘 다 카카오 로컬 키워드
  검색이라 데이터 성격이 같다. 자세한 건 34절 참고.
  - **검색 반경이 다르다**: 입지 1.5km(`LOCATION_SEARCH_RADIUS_M`) vs 자연
    1km(`TERRAIN_SEARCH_RADIUS_M`). 그래서 "없음" 문구를 갈래가 아니라
    **항목마다** 들고 다닌다(`none_text`) — 한 카드 안에서 "1.5km 안에 없음"과
    "1km 안에 없음"이 섞여 나오는 게 정확하다.
  - 자연 갈래 제목 옆에는 주황색 `실험적` 배지(`.loc-exp`)가 붙고, 카드 아래
    안내에도 "산·강은 전용 장소 분류가 없어 이름으로 걸러낸 결과"라는 34절
    경고가 한 줄 들어간다 — 다른 네 갈래와 신뢰도가 다르다는 걸 같은 카드
    안에서 구분해 보여주기 위해서다.
- 반경 안에 없으면 "1.5km 안에 없음"이라고 표시한다. API 호출 자체가 실패해도
  (키 미설정 등) 전체 계산을 중단하지 않고 그 항목만 "조회 실패"로 표시한다 —
  입지 체크는 참고 정보이지 계산의 필수 조건이 아니다.
- 출력 포맷 (CLI — 갈래마다 한 줄):
  ```
  [입지 체크] (카카오 로컬 기준, 반경 1.5km)
  🚇 교통: 지하철역 OO역 420m · 버스정류장 OO앞 140m
  🏫 교육: 초등학교 OO초 180m · 중학교 OO중 810m · 고등학교 없음
  🏪 생활: 편의점 OO점 95m · 대형마트 OO 620m · 공원 OO공원 380m
  🏥 의료: 병원 OO의원 210m · 약국 OO약국 230m
  ※ 지하철역·초등학교 거리만 환금성 진단 판정에 쓰입니다 — 나머지는 참고 정보이고, 매도가 계산에는 어느 것도 반영되지 않아요.
  ```

**수익성 계산** (`--bid-price`를 준 경우만 동작. ⚠️ **CLI 전용이다** —
사용자 요청으로 웹 버전(22절)에서는 뺐다. 낙찰가 입력 필드도 웹 폼에서
같이 제거했다.)
- 모든 금액은 만원 단위로 다룬다.
- 총 매수비용 = 낙찰가 × (1 + 취득 부대비용률) + 추가비용. 취득 부대비용률
  **기본값은 1.1%**(1주택/무주택 실수요 기준 — 사용자 확인) — 실제
  취득세율은 다주택 여부·규제지역 여부에 따라 최대 13%까지 올라가므로
  **항상 본인 상황에 맞게 `--acquisition-rate`를 조정하라고 안내한다**
  (기본값을 확정적인 사실처럼 말하지 않는다).
- 매도비용 = 매도가 × 매도 중개수수료율 (기본 0.5%).
- 순수익 = 매도가 − 총 매수비용 − 매도비용. 수익률(%) = 순수익 ÷ 총 매수비용 × 100.
- 8절/8-2절의 여섯 가지 매도가 시나리오(보수적 급매가/현실적 체결가/
  경매용 매도가/상단 매도가/AI 기준매도가/권장 최초 호가) 각각에 대해
  순수익·수익률을 따로 보여준다 — 하나의 "정답" 수익률을 제시하지 않고
  범위로 보여준다.
- 명도비·수리비·대출이자 등은 자동으로 알 수 없으므로 `--extra-cost`로 사용자가
  직접 더하게 하고, 대출이자는 반영되지 않는다는 점을 항상 언급한다.
- 출력 포맷 (8절/8-2절 결과 바로 뒤, 계절성/가격추이보다 먼저):
  ```
  [수익성 계산] (낙찰가/입찰예정가 X.XX억 기준)
  총 매수비용: X.XX억 (낙찰가 + 취득 부대비용 N% + 추가비용 X.XX억)

  매도 시나리오별 순수익:
  - 보수적 급매가 기준: 순수익 ±X.XX억 (수익률 ±N.N%)
  - 현실적 체결가 기준: 순수익 ±X.XX억 (수익률 ±N.N%)
  - 경매용 매도가 기준: 순수익 ±X.XX억 (수익률 ±N.N%)
  - 상단 매도가 기준: 순수익 ±X.XX억 (수익률 ±N.N%)
  - AI 기준매도가 기준: 순수익 ±X.XX억 (수익률 ±N.N%)
  - 권장 최초 호가 기준: 순수익 ±X.XX억 (수익률 ±N.N%)
  ```

## 20. 건축물대장 조회 규칙

사용자가 관심물건 메모에서 임장(현장답사)으로 확인하던 "승강기 유무"와
"세대수/사용승인일"을 국토교통부 건축물대장 정보 서비스(건축HUB)로 미리
확인한다. `scripts/building_register.py`가 구현하고, `scripts/estimate_price.py`가
입지 체크 직후 출력한다 (`--no-building-info`로 끌 수 있음).

- ✅ 사용자가 업로드한 공식 기술문서(건축HUB 건축물대장정보 서비스,
  `getBrTitleInfo` 오퍼레이션)로 엔드포인트·요청 파라미터·응답 필드명을
  모두 검증했다. 공공데이터포털에서 매매 API와는 별도로 "건축물대장정보
  서비스" 활용신청이 필요하다. 인증키는 `MOLIT_SERVICE_KEY`를 재사용한다
  (같은 공공데이터포털 provider `1613000`).
- 조회에 필요한 법정동코드(10자리)·지번 본번·부번은 새 주소 검색을 하지
  않고, `scripts/geocode.py`의 `geocode_full()`이 카카오 주소 검색 응답에서
  그대로 뽑아 쓴다(`b_code`/`main_address_no`/`sub_address_no`). 이 값을
  얻지 못하면(지오코딩 실패 등) 건축물대장 조회도 생략한다.
- 조회 실패·결과 없음·키 미설정이어도 전체 계산을 중단하지 않고 그 섹션만
  실패로 표시한다 — 5~8절 매도가 계산의 필수 조건이 아니다.
- 승강기 여부는 승용승강기수(`rideUseElvtCnt`)+비상용승강기수
  (`emgenUseElvtCnt`)의 합이 0보다 크면 "있음"으로 판단한다.
- 위반건축물 여부는 **자동으로 확인할 수 없다** — 검증된 `getBrTitleInfo`
  기술문서에 해당 필드가 아예 없다. 사용자가 "위반건축물 딱지가 있는 경우도
  건축물대장으로 확인해서 주의를 넣어달라"고 요청했지만, 21절 경기데이터드림
  같은 "추정 스펙"조차 만들 근거가 없어 **필드명을 지어내지 않았다**. 대신
  CLI `print_building_info()`와 웹 건물 정보 카드에 "이 조회로는 위반건축물
  여부를 알 수 없으니 정부24 건축물대장 열람으로 직접 확인하라"는 안내를
  한 줄 붙인다 — 조용히 빠뜨리면 사용자가 "확인됐다"고 오해할 수 있어서다.
  실제 응답에 관련 필드가 있는 것이 확인되면 그때 붙인다.
- `print_building_info()`는 조회한 `info` dict를 **반환한다** — 41절 층·승강기
  항목이 승강기 유무(`has_elevator`)와 지상층수(`ground_floors`)를 그대로
  쓰기 때문이다(실패·결과 없음이면 `None`). 웹도 `building` dict에 같은 값을
  담는다.
- ⚠️ **웹에는 "건물 정보" 카드가 더 이상 없다.** 예전엔 결과 페이지 맨 아래
  "참고 정보" 섹션에 승강기·세대수·사용승인일·지상층수를 나열하는 카드가
  따로 있었는데, 사용자가 "건축물대장 정보는 환금성 진단의 층·승강기와
  묶어서 더 자세히 설명해야 한다"고 지적해서 **41절 1번 항목 안으로
  통째로 옮겼다** — "승강기 없음 / 지상 5층"이라는 **사실**과 "그래서 팔기
  불리하다"는 **판정**이 화면상 멀리 떨어져 있으면 연결이 안 되기 때문이다
  (40절 인근 아파트 대비를 진단 항목 안으로 합친 것과 같은 원칙).
  `_building_facts()`가 칩 목록을, `BUILDING_PERMIT_NOTES`가 위반건축물
  안내를 만들고, 둘 다 층·승강기 항목의 `facts`/`notes` 필드로 실린다.
  **CLI는 `[건물 정보]` 섹션을 그대로 둔다** — 위에서 아래로 한 번 읽고
  끝나는 텍스트 출력이라 "멀리 떨어져 있다"는 문제 자체가 없다.
- 출력 포맷 (입지 체크 바로 뒤):
  ```
  [건물 정보] (건축물대장 기준)
  승강기: 있음 (1대) / 없음
  세대수: N세대
  사용승인일: YYYYMMDD
  지상층수: N층
  ```

## 21. 인근 중개업소 조회 규칙

사용자가 임장(현장답사) 때 바로 전화해볼 수 있도록, 대상 물건 근처에서 영업 중인
공인중개사사무소 목록(상호명·중개업자명·전화번호)을 미리 찾아둔다.
`scripts/broker_lookup.py`가 구현하고, `scripts/estimate_price.py`가 입지 체크
바로 뒤에 출력한다 (`--no-brokers`로 끌 수 있음). 시/도별로 데이터 소스와 정확도가
다르다 — 서울/경기 외 지역은 아직 데이터 소스가 없어 조용히 생략한다.

**서울특별시** (`--brokers-csv`, 기본 `data/brokers_seoul.csv`)
- 사용자가 서울 열린데이터광장에서 직접 받은 "공인중개사사무소 정보" 전체
  스냅샷 CSV를 이 경로에 저장해둔다 (자동 다운로드하지 않는다 — 11절/16절과
  같은 원칙으로, 스크래핑·자동 수집은 하지 않는다). 사무소가 개업/폐업하므로
  주기적으로 다시 받아 교체하길 권장한다고 안내한다.
- 대상 물건과 같은 법정동(`법정동명`) + 상태구분이 "영업중"인 사무소만 남긴다.
- 한 동에 영업중 사무소가 너무 많으면(80건 초과, 강남구 역삼동처럼 상업지구인
  경우) 지오코딩 API 호출을 아끼기 위해 상위 80건까지만 지오코딩하고, 생략된
  사실을 안내한다.
- 남은 사무소의 "주소"(이미 완전한 도로명주소)를 `geocode.py`의 `geocode()`로
  좌표 변환해서(5절과 같은 캐시 재사용) 실제 반경(기본 1000m, `--broker-radius`)
  안, 가까운 순으로 정렬한다.

**경기도**
- ⚠️ 경기데이터드림 오픈API(`Rlestatebrkragofc`)를 사용자가 페이지에서 본
  항목 설명만으로 만든 "추정" 스펙이다. 실제 호출 결과가 다르면(필드명, 시/군
  단위 세분화 여부 등) 사용자에게 실제 응답을 보여달라고 해서 고친다. 인증키는
  `GG_DATA_KEY` 환경변수로 주입한다 (MOLIT/카카오 키와는 별개로 openapi.gg.go.kr
  에서 발급받아야 한다).
- 이 API는 개별 사무소의 주소(지번/도로명)를 제공하지 않아 반경 계산이
  **불가능**하다 — 시/군(`SIGUN_NM`)으로 조회한 뒤 법정동명(`LEGALDONG_NM`)이
  대상 물건의 동과 일치하고 상태구분(`STATE_DIV_NM`)에 "영업"이 포함된 사무소만
  남긴다. 이 한계를 항상 출력에 함께 언급한다.
- 성남시 분당구처럼 구가 있는 시는 `SIGUN_NM`이 구 단위가 아닌 시 단위로
  추정되어 "성남시"까지만 잘라서 조회한다 — 실제로 구 단위 조회가 필요하면
  실제 응답을 보고 고친다.

- 출력 포맷 (입지 체크 바로 뒤, 서울/경기 여부에 따라 둘 중 하나):
  ```
  [인근 중개업소] (OO동 기준, 반경 1000m, 영업중만 — 서울 열린데이터광장 CSV)
  - 상호명 (중개업자명) 전화번호 — Nm
  ```
  ```
  [인근 중개업소] (OO동 기준, 영업중만 — 경기데이터드림, ⚠️ 개별 주소 정보가 없어 거리순 정렬 불가)
  - 상호명 (중개업자명) 전화번호
  ```
  목록은 최대 8건까지만 보여준다.

## 22. 웹 버전(다른 사람도 쓸 수 있는 매도가 추정기)

**서비스 이름은 "주영이의 빌라 경매 매도가 계산기"다**(사용자 지정). 화면에
이름이 나가는 곳은 네 군데 — `base.html`의 `<title>`, `index.html` 히어로
`<h1>`, `result.html` 결과 페이지 `<h1>`, `webapp/app.py` 모듈 독스트링.
이름을 또 바꾸게 되면 이 네 군데를 같이 고친다. ⚠️ `render.yaml`의
`name: molit-price-estimator`는 **배포 URL을 정하는 값이라 건드리지
않는다** — 바꾸면 주소(`https://molit-price-estimator.onrender.com`)가
같이 바뀌어 기존 링크가 죽는다.

CLI(`estimate_price.py`)는 사용자 본인이 로컬에서 국토부 API를 직접 실행하고
`data/raw/`에 저장한 뒤 계산하는 방식이지만, 다른 사람들도 주소만 입력하면
바로 쓸 수 있도록 `webapp/`에 Flask 웹 서버를 따로 만들었다. **계산 로직
자체는 Claude/AI를 전혀 호출하지 않는 순수 파이썬**이라, 웹으로 서비스해도
AI 토큰 비용이 들지 않는다 — 국토부·카카오 API 키 관리와 서버 호스팅 비용만
고려하면 된다.

- `webapp/data_source.py`: 사용자가 입력한 주소를 지오코딩해서 얻은
  법정동코드 앞 5자리(LAWD_CD)로 국토부 매매 실거래가를 그때그때 조회한다.
  완료된 달(이번 달 제외)은 `webapp/cache/trade/<LAWD_CD>/<YYYYMM>.json`에
  캐시해서, 같은 구를 다시 조회하는 방문자가 매번 국토부 API를 다시 호출하지
  않게 한다. 이번 달은 계약 신고가 계속 들어오므로(신고기한 30일) 캐시하지
  않고 매번 새로 받는다.
  - ⚡ **달별 조회는 병렬이다**(`_fetch_months()`, `FETCH_WORKERS`=8). 기본
    조회 기간이 21개월이라 순차로 돌리면 빌라+아파트 합쳐 **42번을 줄줄이
    기다리게 되고**(국토부 응답이 한 번에 0.5~2초라 이것만 20~80초), 이게
    "로딩이 너무 느리다"는 지적의 가장 큰 원인이었다. 달끼리는 서로 의존이
    없고 각자 자기 캐시 파일만 쓰므로 그냥 병렬로 던지면 된다 — 0.5초/호출
    기준 실측으로 **10.5초 → 1.5초(약 7배)**.
  - 병렬로 받아도 **결과는 달 순서대로 이어붙인다**(`executor.map`) — 이후
    단계가 어차피 정렬·중복제거를 하지만, 같은 입력에 같은 출력이 나와야
    디버깅이 된다. 회귀 테스트(`scripts/tests/test_data_source.py`)로 고정.
  - ⚠️ 국토부 제한은 **동시 호출 수가 아니라 일일 트래픽 건수**라, 병렬로
    바꿔도 총 호출량은 똑같다(캐시 덕에 오히려 줄어든다).
- `webapp/app.py`: Flask 서버. `/`에서 주소·전용면적·층·준공년도(이 셋은
  **필수 입력** — 하나라도 빠지면 5~7절 유사층/유사연식 가중치·필터가 제대로
  안 걸려서 비교거래가 뒤죽박죽 섞이고 매도가 산출값이 중구난감해진다는
  걸 사용자가 실제로 겪어서, `/estimate`에서 서버 쪽 검증도 폼의
  `required` 속성과 별개로 한 번 더 한다)·반경 등을 입력받아
  `scripts/estimate_price.py`의 `find_comparables()`/`compute_scenarios()`를
  그대로 재사용해서 8절/8-2절 포맷으로 결과를 보여준다. LAWD_CD는 카카오
  지오코딩 응답의 법정동코드(`geocode_full()`의 `b_code`) 앞 5자리를 그대로
  쓴다 — `data/lawd_codes.md` 역조회 없이 바로 구해진다. 지오코딩 직후
  `scripts/building_register.py`의 `get_building_info()`도 호출해서 20절
  건물 정보(승강기·세대수·사용승인일)를 결과 페이지 맨 위에 보여준다 —
  조회 실패해도(키 미설정 등) 매도가 계산은 그대로 진행한다.
- **MVP 범위**: 8절(매도가, 8-1절 가격 분포 그래프·8-2절 경매용 매도가
  포함)·20절(건물정보)·23절(예상 월세 추정, 기본값만 — 23-1절 실측
  전환율은 아직 없음)·24절(시장 동향 참고)·25절(인근 동 비교)·26절
  (역세권 프리미엄, 체크박스로 켜야 함)·27절(네이버부동산 지도 바로가기,
  27-1절 CTA 카드+카카오맵 위치 미리보기 포함)·28절(붙여넣은 매물 유사도
  순위)·29절(가격 구간별 매도 전략)·30절
  (거래량·유동성 점수)·31절(경쟁매물 가격 포지션, 매물을 붙여넣은 경우만)·
  32절(종합 판단 자동 요약)·34절(주변 지형 참고, 실험적)·36절(임장
  체크리스트)·38절(상태별 매도가 3단계 — 35절 배율을 사다리로 펼친 것)·
  39절(매도 압력, 매물을 붙여넣은 경우만)·40절(인근 아파트 대비 비율)·
  41절(환금성·경쟁 진단)·43절(임장 체크)·44절(계절성·가격 추이 그래프)·
  45절(역세권·학세권, 19절 입지 체크 포함)까지 지원한다.
  19절(수익성 계산)은
  사용자 요청으로 웹 버전에서 뺐다(CLI에는 있음). 16절(예상 전세가)·
  21절(중개업소)·23-1절(실측 전월세전환율)은 아직 웹 버전에 없다 —
  필요하면 CLI처럼 하나씩
  추가한다.
- ⚠️ 이 서버는 apis.data.go.kr/dapi.kakao.com에 접속 가능한 환경(본인 PC,
  또는 실제 배포한 서버)에서 실행해야 한다 — Claude Code 샌드박스에서는
  실행 불가. 인증키는 서버 환경변수(`MOLIT_SERVICE_KEY`/`KAKAO_REST_API_KEY`)로
  주입하고, 절대 프론트엔드(HTML/JS)에 노출하지 않는다 — 방문자는 API 키
  없이 주소만 입력하면 된다.
- 여러 사람이 쓰게 되면 카카오 지오코딩 무료 할당량, 국토부 API 일일 트래픽
  한도에 걸릴 수 있다는 점을 사용자에게 항상 안내한다.
- ⛔ **비밀번호 보호는 제거했다(사용자 요청 — "비번 넣어서 들어가는 거 없애
  버리고 싶어").** 지금은 주소를 아는 사람 누구나 바로 쓴다.
  - 예전엔 `SITE_PASSWORD` 환경변수를 설정하면 그 비밀번호를 아는 사람만
    쓸 수 있었다(`/login`에서 확인, 세션 쿠키로 유지). 공개 URL을 낯선
    사람이 발견해서 API 할당량을 소진시키는 것을 막는 용도였다.
  - ⚠️ **환경변수만 지우는 게 아니라 코드에서 뺐다.** 사용자가 "너가 해줘"
    라고 해서 — Render 대시보드는 Claude가 건드릴 수 없으니, 대시보드에
    `SITE_PASSWORD`가 **남아 있어도** 로그인이 안 뜨도록 게이트 자체를
    없앴다. 제거한 것: `_password_required` 데코레이터, `/login` 라우트,
    `SITE_PASSWORD` 상수, `webapp/templates/login.html`, `render.yaml`의
    `SITE_PASSWORD`/`SECRET_KEY` 항목, `app.secret_key`(세션을 아예 안
    쓰게 되어 불필요).
  - **되살리려면** 커밋 `aa236d1`에서 위 조각들을 꺼내 오면 된다.
  - ⚠️ 그래서 지금은 **API 할당량 보호 장치가 없다** — 링크가 퍼져서 국토부
    일일 트래픽이나 카카오 무료 할당량에 걸리면, 비밀번호를 되살리거나
    (추측하기 어려운 URL 파라미터·일일 조회 횟수 제한 같은) 다른 방법을
    새로 넣어야 한다.
**⚡ 느린 구간과 대책 (사용자가 "로딩이 너무 느리다"고 지적해서 정리)**

네트워크를 기다리는 구간이 전부다 — 계산 자체는 순수 파이썬이라 순식간이다.
**서로 의존이 없는 호출은 전부 병렬로 던지는 것**이 이 프로젝트의 기본 방침이다.

| 구간 | 호출 수 | 상태 |
|---|---|---|
| 국토부 실거래 (빌라+아파트) | 21개월 × 2 = 42 | ✅ 병렬 `FETCH_WORKERS`=8 |
| 5절 비교거래 지오코딩 | 최대 `MAX_GEOCODE_CANDIDATES`(200) | ✅ 병렬 `GEOCODE_WORKERS`=8 |
| 19절 입지 체크 | 10 | ✅ 병렬 (실측 4.0초 → 0.8초) |
| 40절 인접 동 지오코딩 | 구당 10~25 | ✅ 병렬 |
| 26절 역세권 프리미엄 | 15 (옵션, 기본 꺼짐) | ✅ 병렬 |

- ⚠️ **워커 수를 더 올리지 않는 이유**: 카카오 로컬은 초당 호출 제한이 있어
  너무 키우면 오히려 실패한다. 8은 경험적으로 잡은 값이고, 실제로 429가
  뜨면 낮춘다.
- ⚠️ **남은 느린 구간은 Render 무료 티어 콜드 스타트다** — 방문자가 없으면
  서버가 잠들고, 다음 접속 때 깨어나느라 첫 요청이 수십 초 걸린다. 이건
  코드로 못 고친다(유료 플랜으로 올리거나, 주기적으로 깨우는 외부 핑을
  걸어야 한다).
- ⚡ **57절에서 두 가지를 더 고쳤다** — 지오코딩 캐시를 메모리에 들고
  있게 해서(예전엔 호출마다 파일 전체를 읽었다, 캐시 3만 건이면 200회에
  4.5초) 그리고 40절 아파트 조회를 빌라 조회와 겹쳐 돌려서(−20%).
- **두 번째 조회부터는 훨씬 빠르다** — 국토부 월별 캐시(`webapp/cache/`)와
  지오코딩 캐시(`data/geocode_cache.json`)가 차 있어서다. 단, 무료 티어는
  재배포·재시작 때 캐시가 날아간다.

- **로딩 화면 단계별 문구**: 조회가 오래 걸릴 때(특히 처음 조회하는
  지역 — 비교거래마다 카카오 지오코딩을 하나씩 호출해서 느림) 스피너만
  덩그러니 있으면 멈춘 것처럼 보인다는 지적을 반영해, `webapp/templates/
  base.html`의 로딩 오버레이가 백엔드가 실제로 처리하는 순서 그대로
  ("주소를 좌표로 변환하는 중" → "실거래가를 조회하는 중" → "비교거래를
  찾는 중" → "매도가를 계산하는 중") 2.2초 간격으로 문구를 바꿔 보여준다
  (`LOADING_STEPS`). 그 이후로는 아래 37절 글자 팁을 계속 순환시켜서,
  로딩이 아주 오래 걸려도(무료 티어 콜드 스타트 등) 화면이 같은 문구만
  반복하거나 비어 보이지 않게 한다. 순수 프론트엔드 타이머일 뿐 실제
  진행률을 서버에서 받아오는 건 아니다 — 체감 대기시간을 줄이는 용도의
  연출이라는 점은 참고.
- ⚠️ **"아이폰 사파리에서 로딩 마크가 안 뜬다" 버그 이력 — 원인은 제출
  타이밍이다.** iOS Safari는 **화면 전환(내비게이션)이 시작되면 떠나는
  페이지를 더 이상 다시 그리지 않는다.** 제출 핸들러 안에서 오버레이를
  켜 봐야 핸들러가 끝나는 즉시 전환이 시작되므로 **새 프레임이 한 번도
  그려지지 않는다** — DOM은 바뀌었는데 화면은 그대로인 것이다. 데스크톱
  크로뮴은 전환 전에 프레임을 한 번 더 그려서 정상으로 보였다.
  - **고친 방법**: 제출을 `e.preventDefault()`로 일단 막고, 오버레이를 켠 뒤
    **`requestAnimationFrame`을 두 번 겹쳐** 실제로 한 프레임이 그려진 것을
    확인하고 나서 `form.submit()`으로 직접 보낸다. rAF가 안 오는 경우
    (백그라운드 탭 등)를 대비해 `setTimeout(send, 400)` 안전망을 같이 두고,
    `sent` 플래그로 중복 전송을 막는다. 오버레이 연출 코드는 `try/catch`로
    감싸서 **연출이 실패해도 전송은 반드시 되게** 했다.
  - ⚠️ `void overlay.offsetHeight`(강제 리플로우)로는 **안 된다** — 그건
    레이아웃만 강제할 뿐 페인트를 강제하지 못한다. 처음에 이걸로 고쳤다고
    생각했다가 아니었다.
  - ⚠️ `form.submit()`은 submit 이벤트를 다시 발생시키지 않고 HTML5 검증도
    건너뛴다 — 하지만 이 핸들러는 **검증을 통과한 뒤에만** 불리므로 문제가
    없다(필수값을 비우고 제출하면 여전히 브라우저가 막는 것까지 테스트로
    확인했다).
  - 오버레이에 `transform: translateZ(0)`/`will-change: opacity`를 줘서
    사파리가 별도 레이어로 합성하게 했다 — 위 rAF와 한 세트로, 떠나기 직전
    한 프레임 안에 확실히 그려지도록 돕는 장치다.
  - **같이 고친 것들**(이 건을 파면서 발견한 별개의 취약점들):
    1. `inset: 0`에 롱핸드 폴백이 없었다 — `inset`은 iOS Safari 14.1부터라
       그보다 낮은 브라우저는 이 선언을 통째로 무시하고, 그러면
       `position: fixed`인데 오프셋이 전부 `auto`라 오버레이가 **문서 맨
       아래**에 그려져 화면에서 사라진다. `top/right/bottom/left: 0`을 먼저
       쓰고 `inset: 0`은 뒤에 남겼다. (⚠️ 이건 **구형 기기용 보험**이지
       이번 신고의 원인은 아니었다 — 신고자는 최신 iOS였다.)
    2. flex `gap`도 같은 버전부터라 간격을 `margin`으로도 한 번 더 준다.
    3. 제출 직전 `activeElement.blur()` — 소프트 키보드가 떠 있으면 iOS가
       `position: fixed` 요소를 화면 밖에 그린다.
    4. 제출 직전 `window.scrollTo(0, 0)`.
    5. `btn.disabled = true`를 다음 틱으로 미룬다 — 제출 이벤트 안에서 바로
       비활성화하면 일부 구형 WebKit이 **폼 전송 자체를 취소한다**.
    6. `pageshow`에서 오버레이를 끈다 — 뒤로가기(BFCache)로 복원되면 멀쩡한
       페이지 위에 스피너만 계속 돌아간다.
  - ⚠️ **rAF만으로는 절반만 고쳐진다 — 그래서 결국 화면을 안 넘기기로 했다.**
    rAF 수정 뒤 사용자가 아이폰에서 "로딩 마크는 뜨는데 팁까지는 안 뜬다"고
    확인해 줬다. 당연한 결과다 — rAF는 **첫 프레임 하나**를 억지로 그려내는
    수단일 뿐이고, 그 뒤 `setInterval`로 문구를 바꿔봐야 이미 전환이
    시작돼서 두 번째 프레임이 영영 안 그려진다. 22절 로딩 단계 문구도 37절
    팁 순환도 모바일에서는 통째로 죽어 있었던 것이다.
    - **해법: 폼을 `fetch`로 직접 POST하고 화면 전환을 아예 안 한다**
      (`submitWithoutLeaving()`). 지금 페이지가 살아 있으니 타이머도 돌고
      페인트도 정상이라 단계 문구·팁이 그대로 순환한다. 응답이 오면
      `document.open()/write()/close()`로 문서를 통째로 갈아끼운다.
    - ⚠️ `innerHTML`이 아니라 `document.write`를 쓰는 이유: 결과 페이지에는
      인라인 `<script>`(8-1절 그래프 툴팁, 27-1절 카카오맵, 28절 북마클릿)가
      있는데 `innerHTML`로 넣으면 **스크립트가 실행되지 않는다**.
    - ⚠️ 본문은 `URLSearchParams`로 만들어 보낸다 — 서버가 받는 형식이 평소
      폼 전송(`application/x-www-form-urlencoded`)과 **완전히 같아서**
      `webapp/app.py`는 한 줄도 안 고쳤다(실측 확인).
    - ⚠️ **URL은 `/`에 그대로 둔다**(`replaceState` 안 함). 새로고침하면
      POST가 다시 날아가는 대신 빈 입력 폼이 떠서 오히려 안전하고, 결과
      페이지의 "← 다시 계산하기"(`href="/"`)도 그대로 동작한다. 결과
      페이지 안의 재제출 폼들은 `action="/estimate"`를 명시하고 있어
      영향받지 않는다.
    - **폴백**: `fetch`/`FormData`/`URLSearchParams`가 없거나 요청이 실패하면
      `form.submit()`으로 평소처럼 보낸다 — 그런 기기에서는 팁이 안 도는
      대신 계산은 정상적으로 된다. 문서를 갈아끼우기 전에 연출 타이머를
      `clearLoadingTimers()`로 정리한다(안 하면 사라진 노드를 붙잡고 계속 돈다).
    - **검증**(아이폰 뷰포트 Chromium): 단계 문구 4개 → 팁으로 순환하는 것,
      실제 결과 페이지 HTML로 교체되고 접이식·인라인 스크립트가 동작하며 JS
      오류가 없는 것, POST가 정확히 1회이고 값이 그대로 도착하는 것, 서버가
      받는 Content-Type이 평소와 같은 것, `fetch`를 지운 브라우저에서 폴백이
      실제로 전송되는 것까지 전부 확인했다.
  - ⚠️ **실기 iOS Safari로는 여전히 확인 못 했다** — 이 환경에는 Chromium만
    있어 WebKit 테스트가 불가능하다. 다만 "전환 시작 후 페인트 안 함"은 iOS
    Safari의 알려진 동작이고, rAF로 미루는 건 그에 대한 표준 해법이다.
    Chromium(아이폰 뷰포트)에서 **오버레이가 뜨는 것·POST가 정확히 한 번
    가는 것·폼 값이 그대로 도착하는 것·내비게이션이 끝까지 가는 것·필수값
    검증이 살아 있는 것**까지는 전부 확인했다.

**로컬 실행**
```
pip install -r requirements.txt
MOLIT_SERVICE_KEY="발급받은_인증키" KAKAO_REST_API_KEY="발급받은_키" python webapp/app.py
```
브라우저에서 `http://localhost:5000` 접속.

**Render.com 무료 배포로 진짜 인터넷 주소(URL) 만들기**
1. [render.com](https://render.com) 가입 (GitHub 계정으로 로그인 가능)
2. 대시보드에서 **New +** → **Web Service** → 이 프로젝트의 GitHub 저장소
   연결, 브랜치 선택
3. 저장소 루트에 있는 `render.yaml`을 Render가 자동으로 읽어서 빌드/실행
   커맨드(`pip install -r requirements.txt` / `gunicorn --chdir webapp
   --bind 0.0.0.0:$PORT --timeout 120 app:app`)를 채워준다 — 못 읽으면 이
   두 값을 직접 입력한다. `--timeout 120`이 꼭 필요하다 — 처음 조회하는
   지역은 비교거래마다 카카오 지오코딩을 하나씩 호출해서 30초 넘게 걸릴
   수 있는데, gunicorn 기본 타임아웃(30초)보다 짧으면 요청이 중간에 끊겨
   500 에러가 난다. Render 대시보드에서 "New Web Service"로 수동 설정한
   경우 render.yaml이 자동 반영되지 않으니, Settings에서 Start Command를
   직접 이 값으로 바꿔야 한다.
4. **Environment** 탭에서 환경변수 입력: `MOLIT_SERVICE_KEY`,
   `KAKAO_REST_API_KEY`, (선택, 27-1절 카카오맵 미리보기용) `KAKAO_JS_KEY`,
   (선택, 50-1절 정비구역 확인용) `VWORLD_API_KEY`
   — 절대 코드나 render.yaml에 값을 직접 적지 않는다, Render 대시보드에만
   입력한다. (`SITE_PASSWORD`/`SECRET_KEY`는 비밀번호 제거로 더 이상 안
   쓴다 — 대시보드에 남아 있어도 무시되니 지워도 되고 둬도 된다.)
5. 배포되면 `https://molit-price-estimator.onrender.com` 같은 주소가
   생긴다 — 이걸로 어디서나(다른 사람 휴대폰에서도) 접속 가능하다.
   ⚠️ `KAKAO_JS_KEY`를 쓰려면 카카오 개발자 콘솔에서 이 배포 주소를
   "Web 플랫폼" 도메인으로 등록해야 지도가 뜬다(27-1절) — 등록 전에는
   지도 카드가 조용히 생략될 뿐 나머지 기능은 정상 동작한다.
6. ⚠️ 무료 티어는 일정 시간 방문자가 없으면 서버가 잠들고, 다음 접속 때
   다시 깨어나느라 첫 요청이 몇 초~수십 초 느릴 수 있다(콜드 스타트). 또한
   캐시 파일(`webapp/cache/`)도 재배포·재시작 시 초기화될 수 있다 — 계산
   결과 자체는 그대로 정확하고, 속도만 영향받는다.

### 22-1. 탱크옥션 붙여넣기로 입력 폼 자동 채우기

사용자가 경매 물건을 찾을 때 탱크옥션 같은 경매정보 사이트를 자주 쓰는데,
거기서 확인한 주소·전용면적·층·준공년도를 매번 손으로 옮겨 적어야 하는
번거로움이 있었다. 그래서 입력 폼(`webapp/templates/index.html`) 맨 위에
"탱크옥션에서 복사한 내용 붙여넣기" 접이식 섹션을 추가해서, 물건 상세
페이지에서 Ctrl+A(전체 선택) → Ctrl+C(복사)한 내용을 텍스트 영역에 붙여넣고
"자동 채우기"를 누르면 아래 지번 주소·전용면적·층·준공년도 네 칸을 정규식으로
채워준다.

- **완전히 클라이언트 사이드(순수 바닐라 JS)로 동작한다** — 28절 매물
  유사도 순위(`listing_parser.py`, 서버 파이썬 정규식)와 달리 서버 왕복이
  없다. 계산 시작 전 "폼 채우기" 단계라서 굳이 서버까지 갔다 올 필요가
  없고, 22절 원칙(웹 버전은 AI 호출 없는 순수 로직)과 28절의 클립보드
  자동 붙여넣기·북마클릿(순수 프론트엔드 JS)과 같은 성격이다.
- **스크래핑이 아니다** — 11절/28절과 같은 원칙. 사용자가 직접 복사해서
  붙여넣은 텍스트만 정규식으로 읽고, 자동으로 탱크옥션 사이트에 접속하지
  않는다. "📋 클립보드에서 붙여넣기" 버튼(28절과 동일한 패턴,
  `navigator.clipboard.readText()`)도 함께 둬서 수동 Ctrl+V 없이 바로
  붙여넣을 수 있게 했다.
- **추출 규칙** (모두 정규식 여러 개를 순서대로 시도하고, 첫 매치를 쓴다):
  - **지번 주소**: 실제 탱크옥션(실사용자가 붙여넣은 사건 2025타경52099 실측
    확인)은 지번 주소를 **라벨 없는 줄**로 먼저 쓰고, 바로 뒤에
    `(도로명주소:서울 OO구 OO로39길 21)`처럼 **괄호로 도로명주소를 병기**한다.
    그래서 먼저 `(도로명주소:...)` 괄호 구간을 통째로 제거한 뒤에 "소재지
    (지번)"/"소재지(등기)"/"소재지"/"지번 주소"/"주소" 라벨 뒤 텍스트를
    찾고, 라벨을 못 찾으면(실측 사례처럼 라벨이 아예 없는 경우가 흔하다)
    "OO시/도 + OO시/군/구 + OO동/읍/면 + 번지" 패턴의 텍스트를 통째로
    찾는다. 건물명·층·호수 등 번지 뒤에 붙는 글자는 잘라내고 번지까지만
    남긴다(지오코딩엔 지번까지만 있으면 충분하고, 건물명이 섞이면 오히려
    인식률이 떨어질 수 있어서다). ⚠️ **처음 버전엔 이 괄호 제거가 없어서,
    "도로명주소"라는 글자 안에 "주소"가 들어있는 바람에 "주소" 라벨
    패턴이 도로명 쪽을 잘못 잡아채는 실제 버그가 있었다** — 실사용자가
    붙여넣은 결과 화면에서 지번 대신 도로명 주소가 채워지는 걸로 발견해서
    고쳤다.
  - **전용면적**: "전용면적" 라벨을 먼저 찾고, 없으면(실측 확인 — 탱크옥션은
    "전용면적"이 아니라 **"건물면적"**으로만 표기하는 경우가 있었다)
    "건물면적" 라벨 뒤 숫자+㎡ 패턴으로 폴백한다(둘 다 대지권면적·대지면적과
    헷갈리지 않게 라벨을 반드시 요구한다).
  - **층**: 실측 확인된 실제 표기는 "총" 없이 **"N층 중 M층"**(예: "5층 중
    4층")이라 이 패턴을 최우선으로 시도한다. 그 뒤로 "소재층"·"층별 위치"
    라벨, "M층/N층" 표기를 순서대로 시도한다. ⚠️ **탱크옥션 페이지에는
    "층수(지하/지상)	0층 / 5층"처럼 건물 전체 층수(지상/지하 합계, 물건이
    위치한 층이 아니다)를 보여주는 별도 줄이 있는데, 처음 버전은 이 줄의
    "0층 / 5층"을 "M층/N층" 패턴으로 잘못 매치해서 층을 0으로 잘못
    채우는 실제 버그가 있었다** — 그래서 이 "층수(지하/지상)" 줄부터
    먼저 통째로 제거한 뒤에 나머지 패턴을 시도하도록 고쳤다.
  - **준공년도**: "사용승인일"/"사용승인일자"/"보존등기일" 등(뒤에 한글이
    더 붙는 변형도 `[가-힣]*`로 느슨하게 받는다) 라벨 뒤 날짜에서 연도
    4자리만 뽑는다("사용승인일:2014-11-12"처럼 라벨과 콜론이 붙어 있는
    실측 표기도 정상 인식된다).
- ⚠️ **"추정 스펙"에서 출발해 실측 1건으로 검증된 상태다** — 21절
  경기데이터드림·27절 네이버 딥링크와 같이, 탱크옥션은 로그인/유료 구간이
  있는 사이트라 처음엔 실제 페이지 구조를 확인할 방법이 없어 일반적인 경매
  물건 정보 표기 관행을 근거로 여러 후보 패턴을 추측으로 만들었다. 실제
  사용자가 이걸 써보고 지번 대신 도로명 주소가 채워지고 전용면적·층이
  인식되지 않는 문제를 신고해줘서, 실제로 붙여넣은 원문 텍스트(사건
  2025타경52099)를 받아 위 네 가지 규칙을 실측 형식에 맞게 고쳤다. **다만
  이건 사건 1건으로 검증된 것**이라, 다른 물건 유형(예: 오피스텔·상가·
  단독주택)이나 다른 레이아웃에서는 여전히 라벨 표기가 다를 수 있다 —
  인식이 안 되는 사례가 또 나오면 그 원문을 받아서 계속 고친다.
- **채워진 칸은 노란 배경으로 잠깐 반짝였다 사라진다**(`highlightFilled()`)
  — 자동으로 값이 바뀐 걸 사용자가 알아채고 확인하게 하려는 용도다. 항목별로
  못 찾은 게 있으면 상태 메시지에 "OO은(는) 자동으로 못 찾았어요, 직접
  입력해 주세요"라고 알려준다 — 잘못된 값을 억지로 채우지 않고, 못 찾으면
  그 칸은 그대로 비워두거나 사용자가 이미 입력해둔 값을 건드리지 않는다.
  값을 채워도 폼이 자동 제출되지는 않는다 — 사용자가 직접 확인하고
  [매도가 계산하기]를 눌러야 한다.
- CLI에는 없다 — CLI는 애초에 대화창에서 Claude가 직접 텍스트를 읽고
  처리할 수 있어서 이런 자동 채우기가 필요 없다.

## 23. 예상 월세 추정 규칙 (전월세전환율 역산)

전세보증보험 가입한도 제한으로 순수 전세보다 반전세·월세가 늘어나는 추세라,
현업에서는 월세 시세를 전세가가 아니라 **8절에서 산출한 예상 매도가(현실적
체결가)에 전월세전환율을 적용해 역산**하는 경우가 많다 (현업 공인중개사 확인
사항 — 16절의 전세가 비교와는 별개로, 매도가 기준으로 월세를 추정하는
용도다). `scripts/estimate_price.py`가 8절 매도가 산출 직후, `--monthly-deposit`를
준 경우에만 계산한다 (기본은 꺼져 있음 — 물건마다 보증금 편차가 커서 기본값을
못박지 않는다).

- 계산식: 예상 월세(만원) = (현실적 체결가(만원) − 월세보증금(만원)) × 연 전환율(%) ÷ 100 ÷ 12
- `--monthly-deposit`(만원): 월세 계약의 보증금. 사용자가 인근 실제 매물 시세
  (네이버부동산 등)를 참고해서 넣는 걸 권장한다. 순수 월세(보증금 없음)면 0.
- `--conversion-rate`(연 %, 선택): 전월세전환율. **생략하면 23-1절 실측치를
  우선 쓰고, 그것도 없으면 6.0(참고용 기본값)으로 폴백한다.** 사용자가 직접
  값을 주면(CLI 인자/웹 폼 입력) 그 값을 그대로 쓴다 — 법정 상한(기준금리+2%p,
  주택임대차보호법 시행령 기준)과 실제 시장 관행이 다를 수 있어 실측치든
  기본값이든 **참고값일 뿐 확정치가 아니다**.
- 실거래 데이터가 아니라 8절 산출 매도가에 전환율을 곱해 역산한 추정치이므로,
  항상 "실제 월세 매물과 비교해서 확인하라"는 문구를 덧붙인다.
- 출력 포맷 (8절 "핵심 비교거래" 바로 앞, 16절 예상 전세가보다 먼저):
  ```
  [예상 월세 추정] (전월세전환율 역산, 예상 매도가 기준)
  기준 매도가(현실적 체결가): X.XX억
  가정: 월세보증금 N만원, 연 전환율 N% (실측치, 반경 안 월세 거래 N건 기준 / 사용자 지정 / 기본값, ...)
  예상 월세: N만원/월
  (사용자가 직접 전환율을 지정했고 실측치와 차이가 있으면) 참고: 반경 안 실제 월세 거래 N건으로 역산한
  이 지역 실측 전환율은 N%입니다 (예상 전세보증금 N만원 기준).
  ⚠️ 실거래 데이터가 아닌 추정치입니다 — 인근 실제 월세 매물과 비교해서 조정하세요.
  ```

### 23-1. 실측 전월세전환율 (반경 안 실제 월세 거래로 역산)

16절은 전세가 계산을 위해 월세가 섞인 거래(`monthlyRent != 0`)를 버리는데,
그 버려지는 거래야말로 "보증금 대비 월세가 실제로 얼마인지"를 보여주는
자료다. `scripts/estimate_price.py`의 `estimate_conversion_rate()`가 이걸
활용해서, 6.0이라는 임의의 가정값 대신 **이 동네 실제 전환율**을 계산한다.

- `filter_wolse()`로 월세 낀 거래만 남기고(16절 `filter_pure_jeonse()`와 반대),
  16절과 동일한 `find_comparables()` 반경/면적/연식/층 조건으로 대상 물건
  기준까지 좁힌다(`amount_field="monthlyRent"`).
- 전환율(연 %) = 월세×12 ÷ (예상 전세보증금 − 월세보증금) × 100. "예상
  전세보증금"은 16절과 똑같은 조건으로 구한 순수 전세 비교거래의 현실적
  전세가(median)를 그대로 재사용한다 — 매매가 계산 때와 같은 반경 안에서
  "이 정도면 전세로 나갔을 보증금"을 기준선 삼는 것이다.
- 계산된 전환율이 0~30% 범위 밖이면(입력 오류 등으로 인한 이상치) 그 건은
  버린다. 반경 안에 전세 비교거래가 0건이거나, 월세 비교거래가 0건이거나,
  남은 전환율이 하나도 없으면 실측 실패로 보고 None을 돌려준다 — 이 경우
  호출부는 조용히 기본값(6.0%)으로 폴백한다(에러 아님).
- 여러 건이 잡히면 중앙값을 쓴다.
- CLI에서는 `--conversion-rate`를 생략하면 자동으로 이 실측치를 시도한다.
  ⚠️ **웹 버전(22절)에는 아직 없다** — 전월세 실거래 데이터를 CLI처럼
  `--rent-dir`로 미리 받아둔 파일에서 읽는 방식이라, 웹 버전이 아직
  16절(예상 전세가) 자체를 지원하지 않는 것과 같은 이유로 실측 전환율도
  지원하지 못한다. 16절을 웹에 추가할 때 같이 추가하면 된다.

## 24. 시장 동향 참고 지표 규칙 (매매수급동향지수)

"지금 이 지역 시장이 매수자 우위인지 매도자 우위인지"를 참고만 하는 절이다.
**한국부동산원 연립다세대 매매수급동향지수**(`data/reb_villa_market_index.csv`
— 이 프로젝트가 다루는 연립다세대/빌라 자체 지표라 직접적인 참고치다)를
`scripts/market_index.py`가 읽고, `scripts/estimate_price.py`가 20절 건물
정보 직후 출력한다 (`--no-market-trend`로 끌 수 있음). 8절 매도가 계산에는
자동으로 반영(가감)하지 않는다 — 검증된 보정 공식이 없어서, 판단은 사용자
몫으로 남긴다.

⚠️ **KB부동산 스타일 아파트 매수우위지수/전세수급지수**(`data/kb_market_index.csv`)도
한때 두 번째 카드로 같이 보여줬으나, 사용자가 "빌라 계산기에 아파트
지표는 안 맞다"며 화면에서 빼달라고 요청해서 출력에서 제외했다. 계산
로직(`compute_market_trend`/`load_market_index` 등)은 `market_index.py`에
그대로 남아있어 필요하면 다시 켤 수 있다 — 다만 지금은 `print_market_trend()`/
`webapp/app.py` 어디서도 호출하지 않는다.

**지수 숫자만 던지면 초보자는 이해하기 어렵다** — 그래서 아래 세 가지를
항상 함께 보여준다 (`market_index._plain_market_desc`/`rank_region`/
`format_ranking_peers`/`latest_value_for`):
1. **쉬운 말 풀이**: "매도자 우위 — 상승 압력" 같은 용어만 쓰지 않고,
   "사려는 사람이 더 많아 집주인에게 유리하고, 가격이 오르기 쉬운
   분위기예요" 식으로 한 문장 더 풀어서 보여준다(`_plain_market_desc`).
   기준선(100) 대비 몇 포인트 높/낮은지도 숫자로 같이 보여준다.
2. **다른 지역과의 수치 비교**: 같은 시점 기준으로 동료 지역(서울이면
   5개 생활권끼리, 시/도 단위면 17개 시/도끼리)을 순위 매겨서 상위
   5개 + 검색한 지역의 순위를 보여준다(`rank_region`/`format_ranking_peers`).
   전국 평균과의 차이(p)도 함께 보여준다.
3. **시간에 따른 비교**: 최근 3개월 vs 그 이전 3개월 추세를 보여준다. 이
   지표는 2025-11부터 모은 지 얼마 안 돼(10개월치) 작년 대비(YoY) 계산이
   아직 불가능하므로, 대신 최초 집계 월 대비 변화(`since_start`)로 참고하게
   하고 "작년 대비는 아직 계산 불가"라고 명시한다.

- 사용자가 data.go.kr에서 찾은 한국부동산원 R-ONE "전국주택가격동향조사
  월별동향 연립다세대 매매수급동향" 자료를 한 번 받아둔 스냅샷
  (`data/reb_villa_market_index.csv` — `date,region,index` 3열, `date`는
  `YYYY-MM` 월별, 시/도 단위, 2025-11~스냅샷 시점까지). data.go.kr에도
  이 자료는 "오픈API"가 아니라 "파일데이터"(수동 다운로드) 형태로만
  제공된다 — KB 데이터와 마찬가지로 자동 갱신 불가.
- 지역 매칭은 `market_index.region_from_address(address, VILLA_SIDO_ALIAS)`를
  쓴다 — 이 CSV는 "경기도"를 "경기"로만 표기해서(나머지 시/도 표기는
  `SIDO_ALIAS`와 동일), `VILLA_SIDO_ALIAS = {**SIDO_ALIAS, "경기도": "경기"}`
  로 그 한 글자 차이만 보정한다.
- **서울은 구 단위까지 더 세분화된다.** 원본 R-ONE 자료에는 시/도 전체
  평균 외에도 서울을 5대 생활권(2030 서울생활권계획 기준 — 도심권/동북권/
  서북권/서남권/동남권)으로 나눈 하위 데이터가 이미 포함되어 있어, 이를
  `data/reb_villa_market_index_seoul_zone.csv`(`date,region,index` — `region`이
  권역명)로 따로 뽑아뒀다. `market_index.SEOUL_GU_TO_ZONE`(서울 25개 구 →
  5개 권역 매핑, 표준 생활권 구분)로 대상 주소의 구를 권역에 매칭해서
  (`seoul_zone_from_address()`), 서울 주소면 시/도 전체("서울") 대신 그
  구가 속한 권역 수치를 우선 보여준다 — 시/도 단위보다 한 단계 더
  구체적인 참고치다. 구를 못 찾으면(매핑에 없는 표기 등) 시/도("서울")로
  자동 폴백한다.
  ⚠️ 경기(7권역)·부산(3권역)도 원본에 권역 데이터가 있지만, 권역을 구성하는
  정확한 시/군/구 목록을 신뢰할 수 있게 확인하지 못해 아직 추가하지
  않았다 — 시/도 단위로만 표시한다. 확인되면 서울과 같은 방식으로 추가한다.
- **매매수급동향지수** 해석은 KB 매수우위지수와 같은 관례(0~200, 기준선
  100): 100 초과 = 수요 > 공급 응답 비중이 높음 → 매도자 우위(상승 압력),
  100 미만 = 매수자 우위(하락 압력).
- **추세**: 월별 데이터이므로 13절 동네 랭킹과 같은 원칙을 주 단위가 아니라
  **최근 3개월 평균 vs 이전 3개월 평균**으로 적용한다(`compute_villa_market_trend`,
  `recent_months=3`) — 권역 데이터와 시/도 데이터 모두 같은 함수를 재사용한다
  (둘 다 `date,region,index` 스키마라 `region`에 권역명 또는 시/도명만
  다르게 넣으면 된다). 6개월치 데이터가 없으면 추세 계산 없이 생략한다.
- 출력 포맷 (20절 건물 정보 바로 뒤, 첫 번째 카드 — 서울 구 매칭 시/시·도
  단위 시 문구만 다름):
  ```
  [시장 동향 참고 - 연립다세대] (서울 OO권 또는 OO, 한국부동산원 매매수급동향지수, YYYY-MM 기준 스냅샷)
  매매수급동향지수: N.N (기준선 100 대비 ±N.N)
  → [쉬운 말 풀이 — 예: 사려는 사람이 더 많아 집주인에게 유리하고, 가격이 오르기 쉬운 분위기예요.]
  최근 3개월 추세: 상승/하락/보합 중 (±N.Np) (YYYY-MM 최초 집계 N.N → 지금 N.N, ±N.Np)
  서울 5개 생활권 비교 (YYYY-MM 기준):
    1위 OO권 N.N [← 검색하신 지역]
    ... (상위 5개, 검색한 지역이 5위 밖이면 그 지역 순위도 따로 한 줄 더)
  전국 평균(N.N) 대비: ±N.Np (전국보다 강세/약세/비슷)
  ⚠️ 서울 내 5대 생활권 단위 참고용입니다 (개별 구/동 신호 아님) / 시/도 단위 참고용입니다 (구/동 단위 신호 아님). 매도가 계산에 자동 반영되지 않습니다.
     이 지표는 2025년 11월부터 모은 스냅샷이라 '작년 대비' 계산은 아직 할 수 없습니다 —
     대신 최초 집계(YYYY-MM) 대비 변화로 참고하세요. 최신 수치는 한국부동산원 R-ONE에서 확인하세요.
  ```

⚠️ **일회성 스냅샷이다** — 자동 갱신되지 않는다. 출력할 때마다 스냅샷
날짜를 항상 같이 보여줘서 "이 수치가 언제 기준인지" 명확히 한다. 최신
데이터가 필요하면 한국부동산원 R-ONE에서 새 파일을 받아 같은 경로·형식으로
덮어써야 한다고 안내한다.

웹 버전(22절)에도 동일하게(쉬운 말 풀이·지역간 비교·최초 집계 대비 변화
포함) 있다 — 대상 물건 주소만으로 자동 표시되고, 별도 입력 필드는 없다
(표에 등록된 시/도면 자동, 아니면 카드 자체를 조용히 생략).

## 25. 인근 동 비교 규칙 (거래 활발도·가격 상승률)

24절이 "시/도·권역 단위의 시장 분위기"라면, 이 절은 "같은 구 안에서 이
동네가 다른 동네보다 거래가 활발한지, 가격이 더 오르는지"를 실거래가
데이터로 직접 비교한다 (예: 잠실동을 검색했을 때 옆 삼전동과 비교해서
거래가 활발한 편인지). 13절 동네 랭킹과 계산 로직은 동일하지만(최근 3개월
vs 이전 3개월), 13절이 "지금까지 조회한 모든 지역"을 대상으로 한 전국
랭킹인 것과 달리, 이 절은 **대상 물건이 속한 구 하나로 좁혀서** 그 구
안의 동끼리만 비교한다는 점이 다르다. `scripts/rank_areas.py`의
`build_dong_stats`/`rank_by_volume`/`rank_by_price_change`를 그대로
재사용하고(`find_dong_rank`로 대상 동의 순위를 찾음),
`scripts/estimate_price.py`가 15절 가격 추이 바로 뒤에 출력한다
(`--no-dong-compare`로 끌 수 있음).

- 대상 물건이 속한 구로 rows를 미리 좁힌다 — CLI는 `lawd_lookup.gu_name()`으로
  각 거래의 `sggCd`가 대상 주소의 구(`find_gu_in_address()`)와 같은 것만
  남기고, 웹 버전은 애초에 `get_trade_rows(lawd_cd, ...)`가 그 구(LAWD_CD)
  하나만 조회해오므로 별도 필터가 필요 없다.
- 대상 동 이름은 CLI는 `--dong` 인자를 그대로 쓰고, 웹 버전은 폼에 별도
  입력란이 없어서 사용자가 입력한 지번 주소 텍스트에서 정규식으로
  추출한다(`lawd_lookup.find_dong_in_address()` — "OO동" 뒤에 공백+숫자
  (지번)가 오는 패턴만 인정해서 오탐을 줄인다).
- **거래 활발도**: 최근 3개월 거래건수 기준으로 같은 구 안 동들을 순위
  매긴다. 동마다 최근 3개월 3건 이상이어야 순위에 들어간다(13절과 동일한
  표본 기준, `MIN_SAMPLE=3`). 대상 동이 이 기준에 못 미치면 "순위에서
  빠졌다"고 명시한다 — 거래가 없다는 뜻이 아니라 표본이 적어 통계적으로
  못 믿는다는 뜻임을 분명히 한다.
- **가격 상승률**: 최근 3개월 평균 평당가(만원/㎡)를 이전 3개월과 비교한
  변동률(%) 기준으로 순위 매긴다. 최근·이전 3개월에 각 3건 이상이어야
  순위에 들어간다.
- 둘 다 상위 5개 동을 보여주고, 대상 동이 5위 밖이면 그 동의 순위도 한
  줄 더 따로 보여준다(표를 다 나열하지 않고도 "내 동네가 몇 위인지" 알 수
  있게).
- ⚠️ "지금까지 조회해서 가지고 있는 데이터" 기준이다 — 13절과 마찬가지로,
  실제로 그 동에 거래가 없다는 뜻이 아니라 아직 그 기간/지역을 조회하지
  않았을 수 있다는 점을 항상 언급한다.
- 출력 포맷 (15절 가격 추이 바로 뒤):
  ```
  [인근 동 비교] (OO구 내 다른 동네와 비교, 최근 거래월 YYYY.MM 기준)
  거래 활발도 (최근 3개월 거래건수):
    1위 OO동 N건
    ...
    (검색한 동이 5위 밖이면) N위 OO동 N건 ← 검색하신 동 (M개 동 중)
  가격 상승률 (최근 3개월 평균 평당가, 이전 3개월 대비):
    1위 OO동 ±N.N%
    ...
  ※ 지금까지 조회해서 가지고 있는 데이터 기준입니다 — 실제로 거래가 없다는 뜻이 아니라
    아직 조회하지 않은 기간/지역일 수 있습니다.
  ```
- 웹 버전(22절)에도 동일하게 있다 — 별도 입력 필드 없이 주소에서 동을
  자동 추출한다. 동을 못 찾았거나(주소에 "OO동" 패턴이 없음) 비교할 만한
  동이 하나도 없으면 카드 자체를 생략한다.

## 26. 역세권 프리미엄 참고 규칙 (거리-가격 실측)

19절 입지체크는 "대상 물건에서 지하철역까지 420m"처럼 **사실 하나**만
보여준다. 이 절은 한 걸음 더 나가서 "그 거리가 실제로 가격에 얼마나
영향을 주는지"를 이 동네 실거래로 직접 계산한다 — 5절에서 반경 안
비교거래를 고를 때 카카오로 이미 좌표를 구해두고 버리던 데이터를
재활용하는 것이다. `scripts/estimate_price.py`의 `compute_distance_premium()`이
계산하고, `print_distance_premium()`이 8절 "핵심 비교거래" 바로 뒤에
출력한다.

- **비용 문제로 기본은 꺼져 있다**: 비교거래 하나마다 카카오 키워드 검색을
  추가로 호출해야 한다(지오코딩과 달리 결과가 좌표+키워드 조합마다 달라
  캐시 재사용이 제한적). CLI는 `--station-premium` 플래그를 줘야 계산하고,
  웹 버전은 폼에 체크박스("역세권 프리미엄도 계산하기")를 켜야 계산한다.
  이미 거리순으로 정렬된 비교거래 중 **가까운 상위 15건까지만** 써서 호출
  횟수를 묶는다(`max_sample=15`).
- `geocode.nearby_place()`를 재사용한다 — 다만 이 절 도입으로 반복 호출이
  잦아져서, 좌표를 소수점 5자리(≈1m)로 반올림한 뒤 키워드·반경까지 묶어
  `data/nearby_place_cache.json`에 캐시하도록 바꿨다(기존엔 캐시가 없었다).
  같은 동네를 여러 번 조회하면 갈수록 API 호출이 줄어든다.
- 계산 방법: 비교거래마다 (지하철역까지 거리, 평당가(만원/㎡)) 쌍을 모아서
  단순 선형회귀(최소제곱법)로 "거리 100m당 평당가 변화"와 설명력(R²)을
  구한다. 결정계수 R²가 1에 가까울수록 "거리만으로 가격 차이가 잘
  설명된다"는 뜻이고, 낮으면 층·연식·개별 단지 차이 등 다른 요인의 영향이
  크다는 뜻이므로 그 경고를 같이 보여준다.
- 유효 쌍이 5건 미만이거나(카카오가 반경 안에서 지하철역을 못 찾은 경우
  포함), 거리 값이 전부 같으면(분산이 0이라 기울기를 구할 수 없음)
  "표본 부족으로 생략"한다.
- 키워드는 기본 "지하철역"이지만 함수 자체는 임의의 키워드(초등학교, 마트
  등)를 받을 수 있게 만들어뒀다 — 지금은 지하철역만 CLI/웹에 연결했다.
- 출력 포맷 (8절 "핵심 비교거래" 바로 뒤):
  ```
  [역세권 프리미엄 참고] (반경 안 비교거래 N건, 지하철역까지 거리 기준 — Nm~Nm 분포)
  지하철역에서 100m 멀어질 때마다 평당가 N.N만원/㎡ (N.N%) 하락/상승 경향
  → 이 지역 비교거래는 지하철역에 가까울수록 비싸지는/오히려 싸지는 경향을 보입니다
    (설명력 R²=N.NN, 1에 가까울수록 거리만으로 가격 차이가 잘 설명됨).
  (R²<0.2면) ⚠️ 설명력(R²)이 낮아 거리 외에 다른 요인의 영향이 더 클 수 있습니다.
  ⚠️ 표본이 최대 15건인 단순 참고 통계입니다 — 매도가 계산에 자동 반영되지 않습니다.
  ```

## 27. 네이버부동산 지도 바로가기 (경쟁 매물 확인용)

3~8절 실거래가는 "이미 팔린 가격"이고, 11절은 "지금 나온 호가"를 사용자가
직접 복사해서 붙여넣어야 비교할 수 있다. 하지만 매도 시점의 실제 판단은
결국 "지금 이 동네에 경쟁 매물이 얼마에 몇 개나 나와 있는지"를 사용자가
직접 눈으로 보는 것이 가장 정확하다 — 그래서 결과 화면에 5절에서 이미
구해둔 대상 물건 좌표로 네이버부동산 지도를 **그 동네·빌라·매매로 미리
필터링해서** 새 탭에 열 수 있는 링크를 하나 더 보여준다.
`scripts/naver_link.py`의 `naver_land_url()`이 만든다.

- URL 형식: `https://new.land.naver.com/houses?ms={위도},{경도},{줌}&a=VL&b=A1`
  (`a=VL` 빌라/연립다세대, `b=A1` 매매, `ms` 지도 중심 좌표+줌레벨(기본
  17 — 동 단위로 좁혀서 보여줌).
- ⚠️ 이 파라미터는 공개적으로 알려진 네이버부동산 지도 딥링크 규칙을 참고해
  구성한 것으로, 네이버 공식 문서로 검증한 스펙이 아니다 (21절 경기데이터드림
  API와 같은 성격의 "추정" 스펙). 실제로 열었을 때 필터·위치가 예상과
  다르게 뜨면 사용자가 실제 화면을 보여주는 대로 고친다.
- **스크래핑이 아니다** — URL 하나만 만들어서 사용자가 직접 클릭해서 여는
  것뿐이고, 그 화면의 매물 데이터를 읽어오거나 저장하지 않는다. 11절의
  "자동 수집 금지" 원칙(결과를 긁어와 대신 보여주는 것을 금지)과 충돌하지
  않는다.
- 지오코딩이 실패하면(주소 오류 등) 이 링크도 만들 수 없으므로 함께 생략된다
  (5절의 지오코딩 실패 처리와 동일).
- 노출 위치:
  - **웹 버전**: 결과 페이지 맨 위, 주소·분석기간 바로 아래에 큼직한
    버튼형 링크(새 탭)로 보여준다. ⚠️ 한 번은 `<iframe>`으로 지도를 결과
    페이지 안에 바로 띄워봤는데(사용자가 "링크만 말고 지도를 바로 옆에
    띄워달라"고 요청해서), 실제로 배포된 사이트에서 사용자가 확인해준
    결과 **"new.land.naver.com에서 연결을 거부했습니다"** 라며 막혔다 —
    네이버가 `X-Frame-Options`로 자기 사이트를 다른 페이지 안에 삽입하는
    것 자체를 차단해놓은 것으로 확인됐다(추측이 아니라 실측 확인). 그래서
    iframe은 다시 뺐고, 버튼형 링크만 남겼다. **이 문제는 구조적으로
    풀 수 없다** — 네이버 서버가 응답 헤더로 막는 것이라 우리 쪽 코드로는
    우회할 방법이 없다(이런 걸 우회하려는 시도 자체가 11절 스크래핑 금지
    원칙과도 맞지 않는다).
  - **CLI**: 지오코딩 성공 직후(입지 체크보다 먼저) `[참고] 이 지역
    네이버부동산 매물(빌라·매매) 바로 보기: <URL>` 한 줄을 출력한다.
  - **CLI `--html` 리포트**: 제목 바로 아래 subtitle 줄에 같은 링크를
    넣는다.

### 27-1. 웹 버전 CTA 카드 강화 + 카카오맵 위치 미리보기

⚠️ 사용자가 실제로 써보고 "그냥 얇은 링크는 안 눌러볼 것 같다"고 지적한
것을 반영해 두 가지를 바꿨다.

**① 네이버부동산 CTA를 큼직한 버튼형 카드로.** 예전엔 흰 배경에 테두리만
있는 얇은 링크 한 줄이었는데, 이걸 왜 눌러야 하는지(매도 전 경쟁 매물
확인)가 안 보이면 클릭률이 낮을 수밖에 없다. 그래서 아이콘·굵은 제목
("지금 나와 있는 경쟁 매물, 얼마에 몇 개인지 보세요")·부제(왜 확인해야
하는지 한 문장)·명확한 버튼("네이버부동산 매물 보기 ↗")을 갖춘 네이버
그린 그라데이션 카드(`webapp/templates/base.html`의 `.naver-cta`)로
키웠다. 좁은 화면에서는 아이콘을 빼고 버튼을 전체 폭으로 늘려서 계속
누르기 쉽게 유지한다. ⚠️ 그 뒤 사용자가 "이 버튼을 조금 더 크게"라고 해서
한 번 더 키웠다 — 패딩 18→26px, 제목 15.5→19px, 부제 12→13.5px, 아이콘
28→38px, 버튼 13→15px/패딩 14×22px(데스크톱 기준 높이 76→103px).

**② 대상 물건 위치를 보여주는 카카오맵 미리보기.** 26절 문단에서 이미
"iframe은 막혔지만 카카오맵 SDK로 좌표 마커만 표시하는 방법은 대안이
될 수 있다"고 적어둔 것을 실제로 구현했다. 네이버 매물 데이터를 읽어오는
게 아니라, 5절에서 이미 구해둔 대상 물건 좌표에 빨간 마커 하나와 비교거래
반경(기본 400m) 원 하나만 그리는 **완전히 별도의 지도**다 — 네이버
iframe 차단과는 무관하고, 스크래핑도 아니다(좌표 하나 찍는 것뿐).

- `webapp/app.py`가 새 환경변수 `KAKAO_JS_KEY`를 읽어 결과 페이지에
  전달한다. **⚠️ `KAKAO_REST_API_KEY`(서버가 국토부/카카오 API를 호출할
  때 쓰는 비밀키, 6절 원칙대로 절대 노출 금지)와는 완전히 다른 키다.**
  카카오맵 JS SDK는 구조상 방문자 브라우저에서 직접 스크립트 태그로
  불러써야 해서 애초에 HTML/JS에 그대로 노출되도록 설계됐다 — 대신
  카카오 개발자 콘솔에서 "JavaScript 키"를 발급받고, 그 키를 쓸 **도메인**
  (실제 배포 주소, 예: `https://molit-price-estimator.onrender.com`)을
  콘솔에 등록해야만 그 도메인에서 동작한다(등록 안 한 도메인에서는
  카카오가 요청을 거부한다). 그래서 REST 키처럼 "코드에 하드코딩 금지"
  원칙은 똑같이 지키되(항상 환경변수로 주입), 프론트엔드 노출 자체는
  이 키의 정상적인 사용 방식이라는 점이 6절의 다른 키들과 다르다.
- `KAKAO_JS_KEY`를 설정하지 않으면(또는 등록 안 한 도메인에서 카카오가
  요청을 거부하면) 지도 카드 자체가 조용히 생략된다 — 20절/21절/26절과
  같은 원칙으로, 매도가 계산의 필수 조건이 아니다. 지도 로드 스크립트도
  `try/catch`로 감싸서 실패해도 나머지 페이지에는 영향이 없다.
  `webapp/app.py`의 `KAKAO_JS_KEY` 값이 없으면 `result.kakao_js_key`가
  `None`이 되고, `result.html`은 이 값이 있을 때만 지도 블록을 렌더링한다.
- 마커는 대상 물건 좌표, 원은 실제 비교거래 검색에 쓴 반경(`result.
  search_radius_m`, 사용자가 상세 옵션에서 바꾼 값 그대로)을 그린다 —
  "왜 이 반경 안 거래들이 비교 대상인지"를 지도로도 한 번 더 보여주는
  역할이다(5절과 일관됨).
- ⚠️ 지도 확대 레벨(카카오맵 `level`)은 반경(m) 구간별로 대략 맞춘
  근사값이다(300m 이하→레벨4, 600m 이하→레벨5, 1000m 이하→레벨6, 그
  이상→레벨7) — 카카오맵 레벨↔실제 거리 정확한 환산표를 확신할 수
  없어서(21절 경기데이터드림, 27절 네이버 딥링크와 같은 성격의 "추정"
  값), 실제로 반경 원이 화면에 너무 작게/크게 잡히면 사용자가 알려주는
  대로 레벨 구간을 조정한다.
- CLI에는 이 지도 미리보기가 없다 — 브라우저에서만 의미가 있는 기능이라
  웹 버전 전용이다.

### 27-2. 핵심 비교거래별 네이버 검색/지도 링크

"핵심 비교거래" 목록에 단지명만 텍스트로 떠 있으면, 그 건물이 실제로
어떤 모습인지·사진이 있는지·정확히 어디인지 더 알고 싶어도 따로 검색해야
하는 번거로움이 있었다 — 그래서 단지명 옆에 아이콘 두 개를 붙인다.
`scripts/naver_link.py`의 `naver_search_url()`/`naver_land_url()`이 만든다.

- **🗺️ 네이버부동산 지도** — 그 비교거래 행 자체의 좌표(`_lat`/`_lon`,
  5절에서 반경 안 거래를 고를 때 이미 지오코딩해서 구해둔 값)를 지도
  중심으로 `naver_land_url()`을 재사용한다(27절이 대상 물건 자신의 좌표로
  만드는 것과 완전히 같은 함수 — 검증 방식도 같다). 줌 레벨만 19(건물
  하나가 보일 만큼 바짝 확대)로 27절 기본값(17, 동 단위)보다 더 좁게 준다.
  ⚠️ 네이버부동산은 "단지명 텍스트로 검색"하는 딥링크(예:
  `?query=단지명`) 규칙을 공개 문서로 확인할 방법이 없고(new.land.naver.com은
  샌드박스에서 직접 접속·조회도 안 된다 — 27절의 iframe 차단 확인과 같은
  네트워크 제약), 내부 검색 API는 단지별 내부 ID(complexNo)가 있어야
  정확한 단지 페이지로 갈 수 있는데 그 ID를 얻을 방법이 없다. 그래서
  "단지명으로 검색"이 아니라 **"그 좌표를 지도 중심으로 여는"** 이미
  검증된 27절 방식을 재사용하는 쪽을 택했다 — 이름 검색보다는 덜
  직접적이지만, 추측으로 새 URL 스펙을 만드는 것보다 안전하다.
- **🔍 네이버 통합검색** — 기존 기능 그대로. 클릭하면 그 단지명으로
  네이버 통합검색을 새 탭에서 바로 열어준다. 검색어는 **"동 이름 +
  단지명"**(예: "홍은동 경원탑스빌")이다 — 단지명만 검색하면 흔한 이름
  (예: "삼정빌")이 전국 여러 곳에서 겹쳐서 엉뚱한 결과가 섞일 수 있어,
  그 비교거래 행 자체의 `umdNm`(동)을 붙여 좁힌다.
- 27절과 같은 원칙 — **스크래핑이 아니다.** URL 하나만 만들어서 사용자가
  직접 클릭해서 여는 것뿐이고, 검색/지도 결과를 읽어오거나 저장하지
  않는다.
- 노출 위치: 웹 결과 페이지 "핵심 비교거래" 표의 단지명 옆(`result.html`
  `.comp-search-link`, 🗺️가 🔍보다 앞), CLI `--html` 리포트의 비교거래
  표에도 같은 두 아이콘 링크가 붙는다(`scripts/report.py`). CLI 일반
  텍스트 출력에는 둘 다 넣지 않았다 — 터미널에 긴 URL이 줄마다 섞여
  들어가면 오히려 읽기 불편해질 것 같아서, 하이퍼링크를 실제로 렌더링하는
  화면(웹/HTML)에만 단다.
- 🔍 검색 링크는 지오코딩과 무관하게 항상 만들 수 있다(단지명 텍스트만
  있으면 되므로) — 단지명이 없는 거래("(단지명없음)")도 검색어에 그대로
  들어가긴 하지만 무의미한 검색이 될 뿐 오류는 나지 않는다. 🗺️ 지도
  링크는 그 거래의 좌표(`_lat`/`_lon`)가 있어야 만들어진다 — 5절에서
  반경 안 비교거래로 뽑힌 행은 전부 이미 지오코딩돼 있으므로("핵심
  비교거래" 목록 자체가 반경 필터를 통과한 행들이라) 사실상 항상 있다.

## 28. 붙여넣은 매물 유사도 순위 (웹 버전)

27절이 "네이버부동산 화면으로 안내"하는 것까지였다면, 이 절은 한 걸음 더
나가서 "그 화면에서 내 물건과 가장 비슷한 매물이 뭔지"까지 웹 페이지
안에서 바로 보여준다. 사용자가 네이버부동산에서 그 동네·빌라·매매로 검색한
매물 목록 화면을 복사해서 입력 폼의 새 텍스트 영역("네이버부동산 매물 목록
붙여넣기", 상세 옵션 안, 선택 입력)에 붙여넣으면, `scripts/listing_parser.py`가
파싱해서 대상 물건과 면적·층·준공년도가 가장 비슷한 매물 상위 20개를 결과
페이지에 순위로 보여준다.

- **스크래핑이 아니다** — 27절과 같은 원칙. 사용자가 직접 복사해서 붙여넣은
  텍스트만 처리하고, 자동으로 사이트에 접속하지 않는다.
- 11절/18절은 원래 "Claude가 대화 중에 직접 읽고 계산"하는 방식으로
  설계돼 있었다(형식이 사이트마다 달라 정해진 스키마가 없어서). 이 절은
  그중 **웹 버전에서도 자동으로 돌아가야 하는 부분**만 순수 파이썬 정규식
  파서로 최대한 흉내 낸 것이다. 22절 원칙(웹 버전은 AI 호출 없는 순수
  파이썬)을 지키려면 LLM 파싱을 쓸 수 없어서, 채팅에서 Claude가 직접 읽는
  것보다는 인식률이 낮을 수 있다 — 도저히 못 뽑아낸 매물은 조용히 건너뛰고
  몇 건을 걸렀는지만 결과 화면에 알려준다(11절과 같은 원칙).
- 매물 하나당 다음을 정규식으로 추출한다(`listing_parser.parse_listings()`):
  단지명(추정, 가격·면적 패턴이 없는 첫 줄), 가격("3억 5,000"/"3.6억"/
  "32,000만원" 등 혼합 표기 모두 만원 단위로 환산), 전용면적(㎡ 또는 평 →
  ㎡ 환산), 층(분수 표기 "4/5층"도 지원, "반지하"/"지하"는 0층으로 처리),
  방 개수(있으면), 준공년도(있으면, "OOOO년 준공/건축/사용승인" 패턴).
  "전세"/"월세" 키워드가 있고 "매매"가 없으면 11절 원칙대로 제외한다.
- 매물 블록 구분은 먼저 빈 줄 2개 이상 기준으로 나눠보고, 그걸로 1개
  이하로만 나뉘면(사이트가 줄바꿈 없이 붙여서 복사되는 경우) 가격 패턴이
  나오는 줄을 매물 경계로 보고 다시 나눈다.
- 유사도 순위는 5절과 같은 원리를 재사용한다(`rank_similar_listings()`) —
  면적 차이(%)·층 차이·준공년도 차이가 작을수록 위로 온다(가중치가 아니라
  단순 점수 합산이라 5절의 반경 가중치와는 다른 근사치임을 항상 언급).
  최대 20건까지만 보여준다.
- 가격/면적을 못 찾았거나 매매가 아닌 매물은 조용히 걸러지고, 몇 건을
  걸렀는지 결과 화면에 표시한다. 파싱된 매물이 하나도 없으면 "인식 실패,
  다시 붙여넣어 보라"는 안내만 보여주고 나머지 계산은 그대로 진행한다
  (8절 매도가 계산의 필수 조건이 아니다).
- 출력 포맷 (웹 결과 페이지, "핵심 비교거래" 바로 뒤):
  ```
  [비교 매물 유사도 순위] (붙여넣은 네이버부동산 매물 기준)
  붙여넣은 텍스트에서 N건을 읽었고, 그중 대상 물건과 면적·층·연식이 가장
  비슷한 상위 N건입니다 (유사도 높은 순). N건은 가격/면적을 인식하지
  못해 제외했어요.

  - 단지명 면적㎡, N층, X.XX억 (N.N㎡(N.N평) · N층 · YYYY년식)
  ```
  ⚠️ 호가는 실제 체결가가 아니라 매도인 희망가라 실거래가보다 통상 높게
  형성된다는 문구, 형식 인식이 완벽하지 않아 참고용이라는 문구를 항상
  덧붙인다.
- ⚠️ **CLI에는 아직 없다** — CLI는 이미 Claude와의 대화창에서 11절/18절
  방식으로 직접 처리할 수 있어서 별도 스크립트 연결이 급하지 않다. 필요해
  지면 `estimate_price.py`에 `--listings-file` 같은 옵션으로 연결하면 된다.

**붙여넣기 UX 개선(결과 페이지에서 바로 붙여넣고 재계산)** — 원래는
네이버부동산 링크가 결과 페이지에 있고 붙여넣기 칸은 입력 폼 페이지에
있어서, 링크를 열어 매물을 복사한 뒤 "뒤로가기"로 입력 폼을 다시 채워야
하는 불편함이 있었다. 그래서 결과 페이지(`result.html`) 안에 작은 재제출
폼을 하나 더 뒀다:
- 이 폼은 처음 제출 때 쓴 값(주소·면적·층·준공년도·반경 등)을 모두
  숨은 필드(`result.resubmit_fields` — `request.form`을 `listings_text`만
  빼고 그대로 echo)로 담고 있어서, 매물 텍스트만 새로 채워서 제출하면
  `/estimate`가 처음부터 다시 계산하면서 28절/31절 결과까지 포함해서
  보여준다 — 별도 엔드포인트 없이 같은 라우트를 재사용한다.
- **클립보드 자동 붙여넣기**: "📋 클립보드에서 붙여넣기" 버튼을 누르면
  브라우저의 `navigator.clipboard.readText()`로 클립보드 내용을 읽어서
  텍스트 영역에 자동으로 채운다. 이건 **스크래핑이 아니다** — 사용자가
  네이버부동산 화면에서 직접 드래그해서 복사(Ctrl+C)한 내용을, 브라우저
  표준 클립보드 API로 같은 페이지 안에 옮겨주는 것뿐이다. 서버가 개입하지
  않고, 사용자가 명시적으로 버튼을 눌러야만(사용자 제스처 필요 — 브라우저
  보안 정책상 자동으로는 못 읽는다) 동작하며, HTTPS 환경에서만 지원된다.
  클립보드가 비어있거나 권한이 없거나 브라우저가 지원 안 하면 조용히
  안내 문구만 보여주고 수동 Ctrl+V로 유도한다.
- ⚠️ **여기서 "스크래핑"과 "자동 붙여넣기"를 혼동하면 안 된다.** 11절/27절/
  28절이 금지하는 건 서버가 네이버 사이트에 자동으로 접속해서 데이터를
  긁어오는 것이다. 클립보드 읽기는 사용자가 이미 수동으로 복사한 내용을
  브라우저 안에서 옮기는 것뿐이라 그 원칙과 무관하다 — 사용자가 "약관
  위반해도 상관없다"며 실제 스크래핑 자동화를 요청한 적이 있었는데, 그건
  거절했다(공개 배포된 웹 서비스에 넣는 거라 사용자 개인의 리스크
  감수만으로 결정할 문제가 아니고, 네이버가 이미 iframe 차단 등으로
  적극적으로 외부 접근을 막고 있는 걸 확인했기 때문). 이 클립보드 기능은
  그 대안으로 제공하는, 완전히 수동 동작 기반의 UX 개선이다.

**드래그 선택도 생략하는 북마클릿(선택, 접어둔 `<details>` 안)** — 클립보드
버튼도 "네이버 페이지에서 매물 목록을 드래그해서 선택하는" 수고는 못
없애준다. 그래서 그 마지막 수동 단계까지 줄이는 북마클릿을 추가로
제공한다 — 결과 페이지의 링크를 브라우저 즐겨찾기 바에 드래그해서
등록해두면, 다음부터 네이버부동산 페이지에서 그 즐겨찾기를 누르는 것만으로
페이지 텍스트 전체가 클립보드에 자동 복사된다.

- **이것도 스크래핑이 아니다.** 서버가 아니라 사용자 브라우저 안에서,
  사용자가 직접 클릭할 때만 실행되는 JS다 — 이미 사용자가 보고 있는
  페이지의 렌더링된 내용을 읽는 것뿐이라, Ctrl+A로 전체 선택한 뒤
  복사하는 것과 원리상 같다(속도만 빠르게 해주는 것). 다만 대량으로
  자동 순회하며 여러 페이지를 긁는 배치 작업이 아니라 "사용자가 지금
  보고 있는 화면 하나"에만 매번 수동으로 적용되는 거라, 11절이 우려하는
  자동 수집(서버가 백그라운드로 대량 수집)과는 범주가 다르다.
- `document.body.innerText`(페이지에 보이는 전체 텍스트)를 그대로
  복사한다 — 매물 목록만 정확히 골라내는 정교한 셀렉터는 못 썼다.
  **네이버부동산 페이지의 실제 DOM 구조를 확인할 방법이 없기 때문이다**
  (2절과 같은 이유로 샌드박스에서 그 사이트 접속 자체가 막혀 있다). 그래서
  메뉴·광고 같은 잡다한 텍스트도 같이 복사될 수 있는데, `listing_parser.py`
  가 "가격+면적이 함께 있는 블록만" 골라 읽는 관대한 파서라 노이즈가
  섞여도 어느 정도는 걸러진다. ⚠️ 이것도 "추정 스펙"이다(21절 경기데이터
  드림, 27절 네이버 딥링크와 같은 성격) — 사용자가 실제로 써보고 결과를
  알려주면 그에 맞춰 고친다.
- javascript: 링크는 클릭이 아니라 **즐겨찾기 바로 드래그**해야 등록된다
  (브라우저가 주소창/클릭으로 javascript: 실행을 막는 경우가 많다) —
  안내 문구에 이 점을 명시한다.

## 29. 가격 구간별 매도 전략 (초급매가/30일/60일/일반/최고가 테스트)

8절은 다섯 가지 값(보수적 급매가~권장 최초 호가)을 보여주지만 "이 가격에
내놓으면 얼마나 빨리 팔릴 가능성이 있는지"는 알려주지 않는다. 이 절은
`scripts/estimate_price.py`의 `compute_price_tiers()`가 8절과 같은 가중
복제 분포(7절)를 더 세분화한 백분위수(10/30/50/70/92)로 끊어서 5단계
가격 구간에 이름을 붙인다: **초급매가(p10) / 30일 목표가(p30) / 60일
목표가(p50, 현실적 체결가와 거의 같음) / 일반 매도가(p70) / 최고가
테스트(p92)**.

- ⚠️ **"30일/60일"은 실측 판매기간이 아니다.** 국토부 실거래가는 계약
  체결가만 있고 "언제 매물로 등록해서 언제 팔렸는지"(등록일→계약일) 이력이
  없다 — 그래서 이 프로젝트는 실제 판매 소요일수를 아는 방법이 없다. 이
  라벨은 "비교거래 분포 안에서 가격이 낮을수록 상대적으로 빨리 팔릴
  가능성이 높다"는 상식적 가정을 반영한 **참고용 목표 이름**일 뿐이고,
  화면·출력 어디서나 이 사실을 명시한다. 실제 판매 속도와의 상관관계는
  19절 사용자 아이디어 8번(개별 물건 실적 DB — 낙찰가→최초 호가→가격
  변경→계약까지 걸린 일수를 기록)이 쌓이면 그때 실측치로 검증/교정할 수
  있다 — 지금은 그 DB가 없어서 못 한다.
- 30절 유동성 점수(같은 동네가 원래 거래가 활발한지)와 함께 보면 이
  라벨의 현실성을 더 잘 가늠할 수 있다 — 거래가 활발한 동네면 "30일
  목표가"가 실제로도 합리적일 가능성이 높고, 거래가 뜸한 동네면 더 오래
  걸릴 수 있다는 걸 32절 종합 판단이 같이 짚어준다.
- 31절(경쟁매물 가격 포지션)이 계산되면(28절에서 매물을 붙여넣은 경우),
  "일반 매도가" 구간 기준으로 붙여넣은 매물 대비 가격 경쟁력(상위 N%)도
  같은 카드에 한 줄 더 보여준다.

**5단계 각각의 "예상 소진 속도" (매물을 붙여넣은 경우, `speed_label_for_percentile()`)**
— 사용자가 실제 경매 단타 실무에서 쓰는 방식을 그대로 설명해준 걸 반영한
확장이다: "과거 실거래 대비 싼 가격"보다 "지금 이 순간 경쟁하는 매물들
사이에서 내 가격이 어디쯤인지"가 실제 매도 속도를 더 잘 설명한다는
아이디어다. 매물을 붙여넣으면(28절), 5단계 가격 구간 **각각**에 대해
31절 `price_rank_among_listings()`로 "지금 나와 있는 유사면적 매물 대비
몇 % 위치인지"(낮을수록 저렴한 편)를 다시 계산하고, 30절 유동성(반경
500m 최근 3개월 월평균 거래건수)을 더해 퍼센타일을 보정한다(거래 활발
=1 -10p, 거래 뜸함(월 1건 미만)=+10p) — 보정된 퍼센타일을 5단계로 나눠
"빠른 소진 가능성 매우 높음 / 높음 / 정상 매도 구간 / 다소 느릴 수 있음 /
장기화 가능성 큼"으로 표시한다.
- **핵심은 "과거 기준 구간"과 "현재 매물 기준 퍼센타일"이 어긋날 수
  있다는 것**이다 — 예를 들어 29절 기준 "최고가 테스트"(과거 실거래
  p92) 가격인데 지금 매물들 대비로는 오히려 싼 편으로 나온다면, 그
  자체가 "최근 시장이 과거 실거래 때보다 올랐다"는 신호다. 반대로
  "초급매가"인데도 현재 매물 대비 퍼센타일이 높게 나온다면 "이 가격대
  시장 자체가 약해졌을 가능성"을 뜻한다 — 사용자가 직접 든 예시("최근
  실거래 1.5억인데 1.45억 매물이 이미 여러 개 쌓여 있으면 시장이 약해진
  신호")를 그대로 반영한 것이다.
- ⚠️ 29절과 같은 한계가 그대로 적용된다 — 실측 매도소요일 데이터가 없어서
  "몇 주/몇 개월" 같은 구체적 기간은 여전히 말하지 않는다. "가능성"
  표현만 쓴다.
- 웹 결과 페이지의 "가격 구간별 매도 전략" 표에 "현재 매물 대비"(퍼센타일)·
  "예상 소진 속도" 두 열이 추가로 뜬다(매물을 안 붙여넣으면 이 두 열은
  생략).
- 출력 포맷 (8절 산출값 바로 뒤):
  ```
  [가격 구간별 매도 전략] (비교거래 분포 안에서의 위치 기반 참고 라벨 — 실제 매도 소요일수 데이터는 아님)
  - 초급매가: X.XX억
  - 30일 목표가: X.XX억
  - 60일 목표가: X.XX억
  - 일반 매도가: X.XX억
  - 최고가 테스트: X.XX억
  ```
- 웹 버전에서는 CLI와 같은 표를 카드로 보여준다 — 히어로 카드(8절 매도가)와
  같은 행(`hero-row`)에 나란히 놓여서, "핵심 비교거래" 카드와 32절 종합
  판단 카드보다 먼저 보인다(8-1절 그래프는 이 행 아래 전용 카드로 빠졌다).
- ⚠️ **2단 비율은 이 표 쪽이 더 넓다**(`.hero-row.two-col`, `1fr 1.3fr`).
  원래는 히어로가 더 넓었는데(1.35fr 1fr) 사용자가 "가격 구간별 매도 전략이
  더 크게 나왔으면 좋겠고 경매용 매도가 칸은 조금 더 작아져도 된다"고 해서
  뒤집었다 — 실제로 들여다보는 건 구간별 표 쪽이고, 히어로는 큰 숫자 하나라
  좁아도 읽힌다. 표 자체도 행 높이·구간명·가격 글자를 키웠고(`.tier-name`
  15px, `.tier-price` 19px, 강조행 22px), 반대로 히어로는 큰 숫자 40→36px,
  4칸 가격 그리드 값 19→17.5px로 줄였다.

## 30. 거래량·유동성 점수 (반경 300m/500m, 최근 3/6/12개월)

"이 동네·이 면적대가 원래 얼마나 자주 거래되는지"를 참고 수치로 보여준다.
`scripts/estimate_price.py`의 `compute_liquidity()`가 계산한다.

- 5절과 달리 **층·준공년도 하드 필터를 적용하지 않는다** — 여기서는 거래
  빈도 자체를 보는 것이라, 조건을 너무 좁히면(특히 준공년도 ±4년) 표본이
  줄어 유동성 자체를 가늠하기 어려워지기 때문이다. `find_comparables()`를
  `floor=None, build_year=None`으로 불러써서 재사용한다(전용면적 ±허용범위
  조건만 그대로 적용). 반지하 제외 규칙은 그대로 적용된다(5절).
- 반경은 300m·500m 두 단계, 기간은 최근 3·6·12개월 세 단계로 조합해서
  총 6개 숫자를 보여준다.
- 12/13/15절과 같은 원칙으로 시스템 날짜가 아니라 **데이터 안에서 가장
  최근 계약월**을 기준으로 "최근 N개월"을 센다(실거래 신고기한이 최대
  30일이라 이번 달 데이터가 아직 다 들어오지 않았을 수 있어서).
- `find_comparables()`를 재사용하므로 8절 계산 때 이미 지오코딩된 주소는
  캐시(`data/geocode_cache.json`)에 남아 있어, 이 절의 계산은 대부분
  캐시로 빠르게 끝난다(반경만 넓힌 재조회라 새로 지오코딩할 후보가 많지
  않다).
- 출력 포맷 (29절 가격 구간 전략 바로 뒤):
  ```
  [거래량·유동성 점수] (유사면적 기준, 데이터상 최근 계약월 YYYY.MM 기준)
  - 반경 300m: 최근 3개월 N건(월평균 N.N) · 6개월 N건(월평균 N.N) · 12개월 N건(월평균 N.N)
  - 반경 500m: 최근 3개월 N건(월평균 N.N) · 6개월 N건(월평균 N.N) · 12개월 N건(월평균 N.N)
  ```
- 웹 버전에도 같은 표가 있다(29절 카드 바로 뒤).

## 31. 경쟁매물 가격 포지션 (붙여넣은 매물 기준)

28절에서 사용자가 매물 목록을 붙여넣으면, 그 매물들의 평당가(만원/㎡)
분포 안에서 지금 검토 중인 가격이 몇 % 위치(저렴한 쪽부터)인지도 계산한다
— `scripts/listing_parser.py`의 `price_rank_among_listings()`.

- 면적이 다른 매물끼리 원래 가격(억 단위)으로 직접 비교하면 왜곡되므로
  항상 **평당가로 정규화**해서 비교한다.
- 28절과 같은 허용범위(기본 ±15%)로 유사면적 매물만 남긴다. 유사면적
  매물이 3건 미만이면 통계적으로 못 믿을 수준이라 계산을 건너뛴다(조용히
  생략 — 8절 계산을 막지 않는다).
- 기준 가격은 29절의 "일반 매도가"(p70) 구간을 쓴다 — 첫 호가로 가장
  흔히 쓰는 구간이라서다.
- 퍼센타일은 "이 가격보다 싸거나 같은 매물의 비율"이다 — 낮을수록(예:
  상위 20%) 저렴한 편이라 가격 경쟁력이 높다는 뜻이다.
- 웹 버전에서는 29절 "가격 구간별 매도 전략" 카드 안에 한 줄로 붙는다:
  `일반 매도가(X.XX억) 기준 붙여넣은 유사면적 매물 N건 중 가격 경쟁력
  상위 N% (이보다 싼 매물 N개)`. 32절 종합 판단 문단에도 같은 값을 자연어
  문장으로 녹여 넣는다.
- ⚠️ **CLI에는 아직 없다** — 28절과 같은 이유로, CLI는 매물 붙여넣기 입력
  경로 자체가 없다(대화창에서 Claude가 직접 처리). 28절을 CLI에 연결하면
  이 절도 같이 연결한다.

## 32. 종합 판단 (규칙 기반 자동 요약)

숫자만 여러 개 나열하면 "그래서 결론이 뭔데?"를 사용자가 직접 종합해야
한다. 이 절은 시세 신뢰도(7절)·유동성(30절)·경쟁매물 포지션(31절, 있으면)·
가격 추이(15절, CLI에서 있으면)·모델 합의도(7-1절, 있으면)를 묶어서 짧은
한국어 문단을 자동으로 만들어준다 — `scripts/estimate_price.py`의
`build_verdict()`.

- **Claude(LLM)를 호출하지 않는다** — 조건 분기로 미리 써둔 문장을 골라
  이어붙이는 규칙 기반 템플릿이다(22절 원칙: 웹 버전은 AI 호출 없는 순수
  파이썬). 그래서 사람이 직접 쓴 것만큼 섬세한 뉘앙스는 없고, 예외적인
  상황(예: 표본은 많은데 죄다 반지하뿐이라거나)을 다 캐치하지는 못한다 —
  항상 "규칙 기반 자동 요약, 참고용"이라는 표시를 달아서 사람이 직접 쓴
  총평처럼 보이지 않게 한다.
- 조합 로직:
  - 시세 신뢰도 70 이상 → 표본이 충분하다는 문장, 40~69 → 보통 수준(표본
    늘리기 권장), 40 미만 → 참고용으로만 쓰라는 경고.
  - 유동성(30절)이 있으면 반경 500m 최근 3개월 월평균 거래건수로 "활발/
    보통/뜸함"을 판단해서 매도 속도에 대한 문장을 붙인다.
  - 경쟁매물 포지션(31절)이 있으면 퍼센타일에 따라 "저렴한 편"/"중간"/
    "비싼 편, 빠른 매도가 필요하면 초급매가~30일 목표가 검토" 문장을 붙인다.
  - 매도 압력(39절)이 있으면(매물을 붙여넣었고 개월수를 계산할 수 있으면)
    "경쟁 매물 N건은 이 동네 거래 속도로 약 N개월치 물량이라 매도 압력은
    'X' 수준" 문장을 붙인다.
  - 가격 추이(15절, %)가 주어지면(현재 CLI만) ±3% 이상일 때만 상승/하락
    문장을 붙인다(그 이내는 의미 있는 추세로 보지 않는다).
  - 모델 합의도(7-1절, `model_divergence_pct`)가
    `MODEL_DIVERGENCE_MENTION_THRESHOLD_PCT`(8%) 이상이면 "총액 기준
    추정과 ㎡당가 기준 추정이 N% 차이 나 모델 간 의견이 다소 엇갈립니다"
    문장을 붙인다 — 그 미만은 우연한 오차로 보고 언급하지 않는다.
  - 마지막에 항상 "규칙 기반 자동 생성, 참고용, 최종 판단은 직접 확인 후"
    문구를 붙인다.
- 출력 위치: CLI는 8절 산출값 바로 뒤(29절 가격 구간 전략보다 먼저), 웹
  버전은 가격 히어로 카드 바로 뒤(29절 카드보다 먼저) — "숫자를 보기
  전에 결론부터" 배치다.
- 출력 포맷:
  ```
  [종합 판단] (규칙 기반 자동 요약, 참고용)
  <자동 생성된 문단>
  ```

## 33. 테스트 (핵심 계산 함수 회귀 방지)

기능이 32절까지 늘어나면서, 함수 하나를 고치면 다른 절에 영향이 가는지
매번 사람이 머릿속으로 추적해야 하는 위험이 커졌다 — 실제로 29절/30절을
만들다가 `find_comparables()`가 입력 dict를 직접 고쳐서 30절 유동성
계산이 29절 유사도 점수를 덮어쓰는 버그가 있었고, CLI 출력을 우연히
눈으로 보다가 발견했다. 이 절은 그걸 사람이 매번 확인하지 않고 자동으로
잡아내는 안전망이다.

- 위치: `scripts/tests/test_estimate_price.py`(7~8절 가중치·매도가 산출,
  29~32절 확장), `scripts/tests/test_listing_parser.py`(28절/31절 매물
  파싱·유사도 순위·가격 포지션), `scripts/tests/test_price_chart.py`
  (8-1-1절 가격 분포 그래프 확장 — 히스토그램 구간 집계·개월수 계산·반영도
  문장·다이아몬드 좌표 같은 순수 함수 단위 테스트와, 직거래는 다이아몬드로
  렌더링되는지·동일건물/이상치 테두리 색이 붙는지·미세한 시계열 보정은
  무시되는지·강조 캡 밖 점은 클릭 불가능한지 같은 SVG 마크업 구조 확인).
- **pytest 같은 추가 설치가 필요 없다** — 14절/22절의 "외부 라이브러리
  최소화" 원칙을 그대로 따라 파이썬 표준 라이브러리 `unittest`만 쓴다.
- 실행법:
  ```
  python3 -m unittest discover -s scripts/tests -v
  ```
  (Windows PowerShell도 동일 — `python` 대신 `python3`이 안 먹으면 `python`
  으로 바꿔서 실행)
- 커버하는 것: `to_amount_man`/`dedupe`(4절) 파싱, `weight_for_recency`
  (7절 월 단위 최근성 구간 경계값), `similarity_score`(7절 4요소 종합
  유사도 — 동일 스펙 100점, 준공년도 없을 때 비중 재배분, 반경 경계값),
  `SIMILARITY_EMPHASIS_CURVE`(90점이 60점보다 3배 이상 크게 반영되는지),
  `compute_scenarios`(가중 중앙값·신뢰도 범위), `compute_price_tiers`
  (5단계가 항상 오름차순인지), `speed_label_for_percentile`(29절 확장
  구간 경계·유동성 보정 방향), `describe_comparable_similarity`(5절 텍스트
  요약), `find_comparables`의 필터링 규칙(면적 허용범위·반지하 예외·해제
  거래 제외)과 **입력 dict를 직접 고치지 않는지(mutation-safety)** —
  이게 실제로 겪은 버그의 회귀 테스트다. `find_comparables_adaptive`(지정
  반경보다 좁게는 안 내려가는지, 표본 충분하면 확대 안 하는지, 부족하면
  단계적으로 넓히는지 — ⛔ 지금은 꺼져 있어 아무도 안 부르지만, 되살릴 수
  있게 함수와 테스트를 남겨 뒀다), `TestAdaptiveRadiusIsOff`(그 함수를 CLI·웹
  **어디서도 호출하지 않는지**를 소스 검사로 고정 — 주석·독스트링은 세지 않는다), 거래유형 다운웨이트(직거래가 중개거래보다 정확히
  `DEALING_TYPE_WEIGHT` 비율만큼 낮게 반영되는지, 값이 없으면 감점 없는지),
  평당가 이상치 다운웨이트(median±3×MAD 밖 거래가 제외가 아니라
  `PRICE_OUTLIER_WEIGHT`로만 낮아지는지, 표본 5건 미만이면 건너뛰는지),
  `_replication_count`(7절 `WEIGHT_REPLICATION_SCALE` — 가중치가 높을수록
  복제가 실제로 많아지는지, 이상치·직거래 다운웨이트와 동일건물 보너스가
  반올림에 먹히지 않는지, 0에 가까워도 최소 1개는 남는지, 정상 7건 + 고가
  이상치 4건일 때 중앙값이 정상 구간 안에 머무는지 — **전부 실제로 겪은
  버그의 회귀 테스트다**),
  동일건물 보너스(좌표가 `SAME_BUILDING_DISTANCE_M` 이내면 `SAME_BUILDING_
  BONUS`만큼 가중치가 곱해지는지, 멀면 안 붙는지), 7-1절 ㎡당가 모델
  블렌딩(비교거래 면적이 전부 대상 물건과 같으면 두 모델이 일치해
  `model_divergence_pct`가 0인지, 면적이 갈리는 표본일수록 괴리율이 커지고
  신뢰도가 그만큼 낮아지는지), `build_verdict`의 모델 합의도 문장(임계치
  이상일 때만 붙는지), 7-2절 시계열 가격보정(`estimate_monthly_trend_rate`가
  표본 3개월 미만이면 None인지, 상승/하락 추세 부호가 맞게 나오는지,
  `time_correction_factor`가 최근 거래는 보정 없이 그대로인지·외삽이
  12개월에서 멈추는지·배율이 ±15%로 clamp되는지, `find_comparables`가
  `monthly_trend_rate`를 받으면 `_amount_man_adjusted`만 채우고 원본
  `_amount_man`은 그대로 두는지, `_weighted_amounts_sorted`/
  `_weighted_unit_prices_sorted`가 보정값을 우선하고 없으면 원본으로
  폴백하는지), 7-3절 역세권 회귀보정(`compute_distance_premium_correction`이
  표본·거리범위·R²·계수 방향 네 조건 중 하나라도 안 맞으면 None인지, 대상
  물건이 평균보다 가까우면 양의 보정·멀면 음의 보정이 나오는지, 보정폭이
  ±5%로 clamp되는지)도 같이 커버한다. `normalize_dealing_gbn`(5절 — 완전 일치가 아니라 포함 여부로 판정하는지,
  공백·부가 표기가 섞여도 잡는지, 모르는 값은 None인지), 8-1-1절 압축
  범례(직거래가 있으면 건수, 없으면 "0건", 거래유형 정보 자체가 없으면
  그렇게 표시하는지, 없는 기호는 범례에서 빠지는지),
  41절 환금성·경쟁 진단(`build_marketability_report` — 좋은 조건이 높은
  점수를 받는지, 약점이 목록에 잡히는지, 반지하는 항상 warn인지, 승강기가
  고층의 감점을 되돌리는지, 매물을 안 붙여넣으면 경쟁 항목이 unknown인지,
  `info` 항목이 점수에 영향을 주지 않는지, 입력이 없으면 점수가 None인지,
  항목이 강의 우선순위 순서로 나오는지, 순번이 1부터 이어지고 참고 항목은
  번호가 없는지, 일부 항목이 빠져도 순서·번호가 유지되는지),
  층·승강기 항목의 건축물대장 연동(`facts`에 승강기·지상층수·세대수·사용승인일이
  실리는지, 탑층이면 "옥상"을 보라고 말하는지, 탑층이 아니면 "탑층은 아니다"라고
  명시하는지, 건축물대장 조회 실패면 `facts`·`notes` 둘 다 `None`인지),
  43절 체크리스트 라벨(`TestInspectionChecklistLabels` — 엘리베이터가 빠졌는지,
  라벨이 전부 나쁜 상태를 서술하는지, `INSPECTION_CHECKLIST`와
  `INSPECTION_FIELDS`가 어긋나지 않는지), 41절 연식 안내(`TestBuildYearNotes` —
  30년 초과면 누수·인테리어 비용 문구가 붙는지, 준신축이면 안 붙는지),
  38절 상태별 매도가 사다리(`compute_condition_ladder` — 현재 상태부터
  위로만 만드는지, 배율대로 가격이 나오는지, 공사비를 넣으면 순증이
  회수액−공사비인지, 모르는 상태면 빈 리스트인지), 39절 매도 압력이 32절
  종합 판단에 들어가는지(개월수를 못 구하면 문장이 안 붙는지)도 커버한다.
  `listing_parser`는 39절 `sale_pressure`(개월수 = 매물 수 ÷ 월평균
  거래건수인지, 구간 경계, 분모가 0일 때 "판단 보류", 다른 평형은 경쟁
  매물로 안 세는지)와 "3억 5,000"/
  "3.6억"/"32,000만원" 등 가격 혼합 표기 파싱(특히 "3.6억" 뒤 다음 줄
  숫자를 잘못 삼키던 버그의 회귀 테스트 포함), 평→㎡ 환산, 전세/월세
  제외, 유사도 순위, 가격 포지션 퍼센타일 계산.
- ⚠️ **여기 없는 건 아직 안 커버된다는 뜻이다** — 24절 시장 동향 지수 계산,
  25절 인근 동 비교 랭킹, 지오코딩이 필요한 함수들의 네트워크 실패 처리
  등은 아직 테스트가 없다. 새 계산 로직을 추가하거나 기존 로직을 고칠
  때, 특히 여러 절이 같은 함수를 공유하는 부분(예: `find_comparables()`,
  `compute_scenarios()`)을 건드릴 때는 이 폴더에 테스트를 같이 추가하는
  걸 원칙으로 한다.

## 34. 주변 지형 참고 (산/하천 근접 — 실험적)

사용자가 "이 지역의 경사도나 주변 특수성(산이 있다거나 강이 있다거나)도
보여줄 수 있냐"고 요청한 것 중 **지금 바로 되는 부분**만 구현한 절이다.
`scripts/estimate_price.py`의 `compute_terrain_check()`가 대상 물건 좌표
기준 반경 1km 안에서 가장 가까운 "산"과 "하천/강"을 카카오 로컬 키워드
검색으로 찾는다 — 19절 입지체크(지하철역/초등학교/마트)와 같은 API를
재사용하지만, 완전히 별도 절로 뒀다: 19절은 CLI 전용으로 남아있는 반면
이 절은 CLI와 웹 버전(22절) 양쪽에 다 있고, 19절이 못 미더워하지 않아도
될 만큼 신뢰도가 높은 반면 이 절은 아래 이유로 **훨씬 신뢰도가 낮다**.

⚠️ **실험적 기능이다 — 정확도를 장담할 수 없다.** 지하철역·학교·마트는
카카오 로컬에 전용 장소 카테고리가 있어 검색이 정확하지만, "산"·"강"·
"하천"은 자연 지형이라 전용 카테고리 코드가 없다. 그래서 `geocode.
nearby_place()`의 새 `name_suffix` 파라미터로 "장소명이 실제로 그
글자로 끝나는 결과만" 걸러서 쓴다(예: "산" 검색은 이름이 "산"으로
끝나는 것만 인정 — 이렇게 안 하면 "OO부동산"처럼 상호명에 그 글자가
섞인 무관한 업체가 걸린다). 이 필터도 완벽하지 않다는 점(예: "OO부동산"류
상호는 "산"으로 끝나니 여전히 걸릴 수 있다)을 항상 안내한다.

- 산은 "산" 키워드 하나로, 하천/강은 "강"·"천" 두 키워드로 각각 검색해서
  (한국 하천은 "OO강"이나 "OO천"으로 끝나는 이름이 흔해서) 더 가까운 쪽을
  택한다.
- API 호출 자체가 실패해도(키 미설정 등) 전체 계산을 중단하지 않고 이
  섹션만 생략한다 — 8절 매도가 계산의 필수 조건이 아니다(19절/20절/21절과
  같은 원칙).
- CLI는 `--no-location`으로 19절 입지체크와 함께 끌 수 있다(별도 플래그를
  안 만들고 묶었다). 출력 포맷:
  ```
  [주변 지형 참고] (카카오 로컬 키워드 검색, 반경 1km — 실험적, 참고용)
  가장 가까운 산: OO산 (850m)
  가장 가까운 하천/강: OO강 (620m)
  ⚠️ 키워드 검색 기반 참고용 정보라 정확도가 완벽하지 않을 수 있습니다 — 결과가 이상하면 실제 지도로 확인하세요.
  ```
- ⚠️ **웹에는 "주변 지형 참고" 카드가 더 이상 없다** — 19절 입지 체크 카드의
  다섯 번째 갈래 **"🌳 자연"** 으로 합쳐졌다(사용자 요청: "두 개를 합쳐도
  괜찮을 것 같은데? 교통·교육·생활·의료 다음에 자연이라는 파트로"). 둘 다
  카카오 로컬 키워드 검색이라 데이터 성격이 같고, 카드가 둘로 나뉘어 있으면
  같은 성격의 정보를 두 번 찾아 읽어야 했다.
  - `webapp/app.py`는 이제 `terrain` 딕셔너리 대신 **갈래에 그대로 꽂을 수
    있는 항목 목록**(`terrain_places` — `label`/`place`/`scored`/`none_text`)을
    만들어 `location_card["groups"]` 끝에 붙인다. `result["terrain"]` 키는
    없어졌다.
  - 산·강 둘 다 못 찾으면 자연 갈래 자체가 안 붙는다(기존 "카드 생략"과 같은
    동작). 하나만 찾으면 나머지는 "1km 안에 없음"으로 뜬다.
  - 이 절의 **실험적이라는 경고는 그대로 살아 있다** — 갈래 제목 옆 `실험적`
    배지와 카드 아래 안내 한 줄로 나간다. 다른 네 갈래(지하철역·학교 등)는
    카카오 장소 카테고리가 정확해서 같은 경고가 붙지 않는다.
  - 검색 반경은 `TERRAIN_SEARCH_RADIUS_M`(1km)로 **상수화했다**. 입지 체크
    1.5km와 일부러 다르게 둔다 — 45절 판정 때문에 넓힌 게 아니라 순수 참고
    정보라, 너무 넓히면 "1.4km 떨어진 산"처럼 체감과 무관한 결과만 늘어난다.
- CLI는 예전 그대로 `[주변 지형 참고]` 섹션을 따로 찍는다 — 위에서 아래로
  한 번 읽고 끝나는 텍스트 출력이라 "카드가 멀리 떨어져 있다"는 문제 자체가
  없다(20절 건물 정보를 CLI에만 남겨둔 것과 같은 판단).

**경사도(언덕/고저차)는 아직 못 붙였다.** 카카오 로컬 API에는 고도(표고)
데이터가 없다 — 국토정보플랫폼(VWorld)이라는 별도 공공 API에 표고 조회
기능이 있다고 알려져 있지만, Claude가 이 API의 정확한 엔드포인트·파라미터·
응답 형식을 확신할 만큼 알고 있지 못하다(21절 경기데이터드림처럼 "페이지
설명만 보고 추정"할 수 있는 수준이 아니라, 아예 기억이 불확실하다). 그래서
추측으로 잘못된 스펙을 만들어 시간을 낭비하기보다, 사용자가 VWorld
API 키를 발급받고 표고 조회 관련 공식 문서(또는 실제 요청/응답 예시)를
공유해주면 그때 16절 전월세 API처럼 **검증된 스펙**으로 붙이기로 했다 —
아직 미착수 상태다.

## 35. 수리상태별 매도가능가격 참고 배율

사용자가 들은 경매 강의("빌라 = 아파트의 대체재" 편, 요약본은
`data/lecture_notes_villa.md` — 36절·37절도 같은 파일을 본다) 인사이트를
반영했다 —
강사가 "금액이 싸면 팔리긴 팔리는 빌라를 지정하려면 매도가능가격을 디테일
하게 잡아야 한다, 험한집·깔끔한 기본집·올수리는 각각 다른 매도가능가격을
가진다"고 짚었다. `scripts/estimate_price.py`의 `CONDITION_MULTIPLIER`/
`compute_condition_adjustment()`/`print_condition_adjustment()`가 이 배율을
8절/8-2절 산출값에 곱한 **참고값**을 원래 값과 나란히 보여준다.

- ⚠️ **검증된 수치가 아니다.** 국토부 실거래가에는 수리상태 필드 자체가
  없어(7절 `SAME_BUILDING_BONUS`·5절 이상치 다운웨이트 배율과 같은 성격의
  "경험적 참고치") 이 배율을 데이터로 검증할 방법이 없다 — 강의에서 나온
  경험칙을 그대로 상수로 옮긴 것뿐이다.
  ```python
  CONDITION_MULTIPLIER = {"올수리": 1.08, "기본": 1.00, "노후": 0.90}
  ```
  "기본"은 배율 1.0(보정 없음)이라 화면에 따로 표시하지 않는다 — 선택하지
  않은 것과 동일하게 취급한다.
- 8절/8-2절의 여섯 시나리오 값(보수적 급매가/현실적 체결가/경매용 매도가/
  상단 매도가/AI 기준매도가/권장 최초 호가, 웹은 22절 원칙대로 권장 최초
  호가만 제외) 각각에 배율을 곱한 "원래 값 → 보정값"을 나란히 보여준다 —
  18절 구조보정 참고값과 같은 원칙으로, **원래 8절 값 자체는 절대 바꾸지
  않는다**.
- ⚠️ **화면 출력은 38절 "상태별 매도가 3단계"로 대체됐다.** 이 절의 배율
  표(`CONDITION_MULTIPLIER`)는 그대로 쓰이지만, "고른 상태 하나에 배율을
  한 번 곱한 값을 원래 값과 나란히 보여주는" 방식(`compute_condition_
  adjustment()`/`print_condition_adjustment()`)은 CLI·웹 어디서도 더 이상
  호출하지 않는다 — 38절 사다리가 같은 정보를 포함하면서 "손보면 얼마나
  더 받는지"까지 보여주기 때문이다. 함수 자체는 하위호환을 위해
  `estimate_price.py`에 남아있다(24절 KB 지수 계산 로직을 남겨둔 것과 같은
  처리).
- CLI는 `--condition {올수리,기본,노후}`(생략하면 표시 안 함), 웹은 "상세
  옵션" 안 "현재 수리상태(선택)" 드롭다운으로 고른다. 둘 다 선택하지 않으면
  이 섹션 자체가 생략된다(계산에 필수 조건이 아니다).
- 출력 포맷 (CLI: 8-2절 결과 바로 뒤, 웹: 결과 페이지 "참고 정보" 카드):
  ```
  [수리상태 참고 배율] (올수리 (전체 리모델링 완료), +8% 참고용)
  ⚠️ 국토부 실거래가에는 수리상태 정보가 없어 검증된 수치가 아닙니다 — 경매 강의/경험에서 나온 참고 배율일 뿐입니다.
  보수적 급매가: X.XX억 → X.XX억
  현실적 체결가: X.XX억 → X.XX억
  경매용 매도가: X.XX억 → X.XX억
  상단 매도가: X.XX억 → X.XX억
  AI 기준매도가: X.XX억 → X.XX억
  권장 최초 호가: X.XX억 → X.XX억 (CLI만)
  ```
- ⚠️ CLI `--html` 리포트(14절)에는 아직 없다 — `report.py`가 받는 인자가
  이미 많아서, 다른 절이 먼저 쌓아온 "필요해지면 하나씩 추가" 원칙대로
  나중에 붙이면 된다.

## 36. 임장 체크리스트 (팔기 어려운 요소, 참고용)

같은 경매 강의가 짚은 "팔기 어려운 요소 제거" 포인트 — 채광·엘리베이터·
주차·누수·악취·소음·관리상태·경사가 나쁘면 가격을 낮춰도 잘 안 팔린다는
경험칙이다. `scripts/estimate_price.py`의 `INSPECTION_CHECKLIST`/
`print_inspection_checklist()`가 이 여덟 가지를 그대로 보여준다.

- 국토부 실거래가·카카오 API 어디에도 이 정보가 없어 **자동으로는 채울 수
  없다.** API 호출도 없어 비용이 들지 않으므로 CLI·웹 모두 항상 표시한다.
- ⚠️ **항목이 8개에서 7개로 줄었고, 이름도 전부 바뀌었다**(사용자 지적 두 가지):
  1. **"엘리베이터"를 뺐다.** 20절 건축물대장으로 승강기 유무가 이미 자동
     확인되고 41절 층·승강기 항목이 그걸로 판정까지 하는데, 임장에서 또
     체크하게 하면 **한 요소로 두 번 감점된다**. 사용자가 "엘베는 건축물대장
     API로 자동 적용되는 건데 왜 여기 또 있냐"고 정확히 짚었다. 체크리스트
     안내에도 "승강기는 위 층·승강기 항목에서 자동 확인돼 여기서는 빠졌다"고
     한 줄 적는다.
  2. **이름을 "나쁜 상태"로 못박았다.** 예전 라벨("채광"·"주차"·"경사")은
     중립적인 명사라 **좋아서 체크하는 건지 나빠서 체크하는 건지 알 수 없었다**
     — 사용자가 "나쁜 채광 / 주차공간 부족 / 경사도 심함 이런 식이어야
     '이거 나쁜 거니 체크해야지' 하고 체크하지 않겠냐"고 지적했다. 지금은
     라벨 자체가 나쁜 상태를 서술한다:
     **채광 나쁨 · 주차공간 부족 · 누수 흔적 있음 · 악취 심함 · 소음 있음 ·
     건물 관리상태 열악 · 경사 심함**
     회귀 테스트(`TestInspectionChecklistLabels`)가 "라벨마다 나쁨/부족/있음/
     심함/열악 중 하나를 포함하는지", "엘리베이터가 빠져 있는지",
     "`INSPECTION_CHECKLIST`와 `INSPECTION_FIELDS`가 어긋나지 않는지"를 고정한다.
  ⚠️ CLI `--inspection-bad`의 `choices`도 새 라벨이다 — 예전 항목명으로 주면
  argparse가 거부한다.
- ⚠️ **"계산에 전혀 반영하지 않는다"는 더 이상 사실이 아니다** — 43절에서
  사용자가 직접 체크한 결과를 41절 환금성 점수에 반영하도록 바꿨다. 다만
  **매도가(8절)는 여전히 이 체크와 무관하다.**
- 출력 위치: CLI는 20절 건물 정보 바로 뒤(반드시 나오는 항목이라 `--no-*`
  옵션으로 끌 수 없다), 웹은 결과 페이지 "참고 정보" 카드 중 하나로 20절
  건물 정보 카드 근처에 놓인다.
- 출력 포맷:
  ```
  [임장 체크리스트] (참고용 — 매도가 계산에는 반영되지 않습니다)
  아래 요소가 나쁘면 가격을 낮춰도 잘 안 팔릴 수 있다고 합니다 — 임장(현장답사) 때 직접 확인하세요:
  채광 나쁨, 주차공간 부족, 누수 흔적 있음, 악취 심함, 소음 있음, 건물 관리상태 열악, 경사 심함
  ```

## 37. 로딩 중 강의 팁 (웹 전용)

22절 로딩 오버레이는 단계 문구가 끝난 뒤 짧은 팁 한 줄을 3.5초씩 돌려서,
로딩이 오래 걸려도(무료 티어 콜드 스타트 등) 화면이 비어 보이지 않게 한다.
`webapp/templates/base.html`의 `LOADING_TIPS`가 전부다(서버 로직 없음 —
순수 프론트엔드).

- **글자 팁 22개뿐이다.** `{ text }` 객체 하나에 한 줄씩, `TIP_MS_TEXT`(3.5초)
  간격으로 `showTip(i)`가 `setTimeout` 체인으로 순환시킨다.
- ⚠️ **좁은 화면(≤720px)에서는 순환 대신 22개를 한 번에 목록으로 깐다**
  (사용자 요청: "모바일에서는 팁이 밑에 길게 한 번에 나오게, 웹은 그대로").
  작은 화면에서 3.5초마다 한 줄씩 읽는 것보다 쭉 훑어 내리는 편이 낫다는
  판단이다. **넓은 화면 동작은 한 줄도 안 바뀌었다** — 거기선 목록을 아예
  만들지도 않는다(`TIPS_AS_LIST`가 `false`면 `<li>`를 생성하지 않고
  `#loading-tips-wrap`도 `hidden` 그대로).
  - 판정은 `matchMedia("(max-width: 720px)")` — UA 문자열을 보지 않는다.
    데스크톱 창을 좁히면 목록이 뜨는데, 그게 "좁은 화면"의 정의와 맞다.
  - 목록을 깐 경우 단계 문구가 끝나도 팁 순환을 시작하지 않는다(같은 팁을
    위에서 또 돌릴 이유가 없다) — "거의 다 됐어요…"로 바꾸고 멈춘다.
  - ⚠️ 오버레이가 `justify-content: center`였는데 **`flex-start` + 안쪽 박스
    `margin: auto`로 바꿨다.** center로 두면 내용이 화면보다 길어졌을 때
    위쪽이 잘리고 그쪽으로 스크롤이 안 되는 플렉스의 알려진 문제가 있어서다
    (짧을 땐 그대로 가운데 정렬된다).
  - 스피너·상태 문구는 `.load-head`로 묶어 좁은 화면에서만 `position: sticky`
    로 상단에 고정한다 — 팁을 읽으려고 스크롤하면 스피너가 화면 밖으로
    나가 "아직 도는 중인지" 알 수 없어지기 때문이다.
  - 이 기능은 **22절의 fetch 제출(화면을 안 넘기는 방식)이 전제다** — 예전처럼
    화면이 넘어가면 애초에 팁이 보일 시간 자체가 없다.
- 팁 출처는 두 곳이다:
  1. **이 프로젝트의 규칙** — 29절 30일 목표가, 5절 반지하·층 선호, 27절
     경쟁매물 확인, 35절 수리상태, 36절 임장 체크리스트, 지오코딩 때문에
     첫 조회가 느리다는 안내.
  2. **`data/lecture_notes_villa.md`** — 평공쌤 빌라특강(47p)을 사용자가
     직접 텍스트로 압축한 정리본(12KB). ⚠️ **앞으로 강의 내용이 필요하면
     이 파일만 본다** — 원본 PDF 10개는 용량이 커서 읽을 때마다 컨텍스트를
     크게 잡아먹었고, 사용자가 "용량 큰 건 무시하고 이걸로 하라"고 직접
     지정했다. 여기서 뽑은 팁: 빌라=아파트 대체재, "싸게 사는 것보다 팔릴
     가격을 먼저", 팔리는 빌라의 하한선, 개별성이 강해 실거래 한 건으로
     정하면 안 됨, 평당가 맹신 금지, 업거래 왜곡, 하자 6단계(입지·물종·
     물건·법리·절차·시장), "통제 가능한 하자", 수익률↔환금성 트레이드오프,
     레버리지 3:7·낙찰가=투자금 3~3.5배, 취득세 중과배제(수도권 1억/지방
     2억), 명도 3~6개월·대행 약 200만원, 투자금 5천~2억 구간 타겟,
     반복 가능한 시스템.
- ⚠️ **숫자가 들어간 강의 내용은 팁 문구 안에서 "강의 예시"라고 밝힌다** —
  수익률·금리·명도 비용은 보장 수치가 아니고, 세법(취득세 중과배제 기준)은
  시점마다 바뀐다. 정리본 자체도 "실제 수익률을 보장하는 수치가 아님",
  "실제 적용 시 최신 세법 확인 필요"라고 못박아 뒀고, 이 프로젝트가
  24절·29절·35절에서 지켜온 "검증 안 된 수치는 참고용이라고 항상 표시"
  원칙과도 같다.
- ⚠️ **강의 지역분석 표 이미지는 뺐다(사용자 요청).** 한때 `webapp/static/
  tips/`에 표 5장(경기도 규제지역 추천 TOP 10 / 비규제 소·중형 TOP 20 /
  비규제 중대형 TOP 20 / 준공연도별 비교 / 정비구역 vs 비정비구역)을 두고
  글자 팁 사이사이에 9초씩 띄웠는데, 사용자가 "표 이미지는 빼달라"고 해서
  이미지 파일·`<figure>` 마크업·프리로드·확대보기 코드까지 전부 제거했다
  (다시 필요하면 커밋 `ced6aed`에서 꺼내 쓸 수 있다). 결과적으로 **제3자
  강의 자료를 공개 사이트에 띄우는 저작권 부담도 같이 사라졌다** — 글자
  팁은 요약·인용 수준이라 표 이미지를 그대로 올리는 것과는 성격이 다르다.
  ⚠️ `webapp/static/` 폴더 자체는 46절에서 **사용자가 직접 만든 일러스트**를
  넣으면서 되살아났다 — 이 절이 뺀 건 **제3자 강의 표 이미지**이지 폴더가
  아니다. 둘을 헷갈려서 "static은 안 쓴다"고 넘겨짚지 않는다.
- CLI에는 없다 — 로딩 오버레이 자체가 웹 전용이다.

## 38. 상태별 매도가 3단계 (현재 상태 → 기본 정리 → 올수리)

35절은 "지금 이 상태면 배율 얼마"를 한 줄 보여주는 데서 끝났다. 이 절은
같은 배율을 **사다리**로 펼쳐서, 지금 상태에서 손을 볼수록 매도가가 얼마씩
올라가는지, 그리고 그 공사비를 들일 값어치가 있는지까지 한 표로 보여준다.
`scripts/estimate_price.py`의 `compute_condition_ladder()`/
`print_condition_ladder()`가 구현한다.

- 강의(`data/lecture_notes_villa.md` 5절)가 짚은 "험한 집 / 깔끔한 기본집 /
  올수리 집 각각의 현실적인 매도 가능 가격을 따로 잡아라"를 그대로 옮긴
  것이다. 지피티가 정리해준 화면 예시("현재 상태 1.47억 / 기본 정리 후
  1.55억 / 올수리 후 1.60억 / 예상 공사비 450만 → 추가 회수액 +800만")와
  같은 형태다.
- **기준값은 8-2절 경매용 매도가**다 — 이 계산기가 전제하는 "낙찰받아
  되파는" 상황에 가장 가까운 값이라서다. 8절 산출값은 반경 안 실거래를
  섞어서 낸 값이라 **"기본"(깔끔한 보통 상태)을 가정한 것**으로 보고, 각
  단계 가격 = `경매용 매도가 × CONDITION_MULTIPLIER[단계]`로 계산한다.
- **현재 상태부터 위로만** 사다리를 만든다 — 이미 올수리된 집에 "노후로
  되돌리면 얼마"를 보여주는 건 의미가 없어서다(올수리를 고르면 한 줄만
  나온다).
- **공사비를 넣으면 "남는 장사인지"까지 계산한다**: 추가 회수액(현재 대비
  가격 차이) − 예상 공사비 = 순증. CLI는 `--repair-cost-basic`/
  `--repair-cost-full`(만원), 웹은 입력 폼의 **"인테리어" 섹션** 입력칸
  두 개다. 안 넣으면 가격 사다리만 보여주고 "공사비를 넣으면 계산해 준다"고
  안내한다.
- ⚠️ **웹 입력 위치: 상세 옵션 안이 아니라 준공년도 바로 밑 독립 섹션이다.**
  처음엔 수리상태 드롭다운·공사비 두 칸을 "상세 옵션" 접이식 안에 넣었는데,
  사용자가 "생략 칸에 있어야 하는 게 아니라 준공년도 밑에 인테리어 칸으로
  따로 있어야 한다"고 지적해서 밖으로 꺼냈다 — **수리 상태는 매도가가
  실제로 달라지는 요소이지 "고급 설정"이 아니다**(반경·허용범위처럼 건드릴
  일이 드문 값과는 성격이 다르다). 46절 아이콘 타일(`.field`/`.field-icon`,
  🔨 수리상태 · 🧹 기본 정리 공사비 · 🛠️ 올수리 공사비)과 `.form-section`
  헤더("인테리어 — 수리 상태에 따라 팔리는 가격이 달라져요")를 그대로 써서
  기본 정보 입력과 같은 모양으로 맞췄다.
  - 폼 필드 이름(`condition`/`repair_cost_basic`/`repair_cost_full`)은
    그대로라 `webapp/app.py`는 손대지 않았다.
  - ⚠️ **두 공사비 칸은 누적이 아니라 각각 "지금 상태 → 그 단계"까지의
    총액이다.** 사용자가 "기본 정리 공사비랑 올수리 공사비 차익이 뭐냐"고
    물어봐서 드러난 문제 — 라벨이 "기본 정리 공사비 / 올수리 공사비"라
    올수리 칸이 총액인지 추가분인지 안 드러났다. 라벨을 "기본 정리**까지**
    공사비 / 올수리**까지** 공사비"로 바꾸고, "두 칸 모두 지금 상태 기준
    총액이에요 — 더하지 마세요"를 안내에 명시했다.
  - ⚠️ **올수리는 실무에서 드문 선택지다.** 사용자가 "보통 청소·도배·장판
    정도만 하지 전체 리모델링은 당연히 안 한다"고 짚어서, 기본 정리 칸에는
    `보통 여기까지` 배지를, 올수리 칸에는 `드묾` 배지를 달았다(`.field-tag`).
    결과 화면 38절 사다리 설명에도 "보통 기본 정리까지만 하고 팝니다 —
    올수리 줄은 '그렇게까지 하면' 참고용"이라고 적는다. **계산·배율은
    그대로다** — 선택지를 없애지 않고 어느 쪽이 일반적인지만 표시한다.
  - "상세 옵션 자동 펼침" 조건(`adv_filled`)에서도 이 세 값을 뺐다 — 이제
    접이식 밖에 있으므로 이 값이 채워졌다고 상세 옵션을 펼칠 이유가 없다.
  - ⚠️ 덤으로 잡은 것: `<select>`에 스타일 규칙이 아예 없어서 브라우저
    기본 모양 그대로 떴다(옆 입력칸들과 높이·테두리·폭이 따로 놀았다).
    접이식 안에 있을 땐 눈에 덜 띄었는데 밖으로 꺼내니 바로 드러나서,
    `input[type=...]` 규칙에 `select`를 묶고 화살표만 인라인 SVG로 직접
    그려 넣었다(`appearance: none`).
- ⚠️ **35절의 한계가 그대로다 — 검증된 수치가 아니다.** 국토부 실거래가에는
  수리상태 필드 자체가 없어서 배율(노후 0.90 · 기본 1.00 · 올수리 1.08)을
  데이터로 검증할 방법이 없다. 화면·CLI 어디서나 이 문구를 함께 단다.
  공사비 역시 사용자가 직접 넣는 값이지 이 계산기가 추정하는 값이 아니다.
- 출력 위치: CLI는 8-2절 결과 바로 뒤, 웹은 32절 종합 판단 카드 바로 뒤
  (30절 유동성 카드보다 먼저) — 35절 때는 맨 아래 "참고 정보" 카드였는데,
  이건 가격 자체를 다루는 내용이라 위로 올렸다.
- 출력 포맷 (CLI):
  ```
  [상태별 매도가 3단계] (경매용 매도가 기준 — 수리 상태에 따라 매도가능가격이 달라진다는 경매 강의 인사이트 반영)
  ⚠️ 국토부 실거래가에는 수리상태 정보가 없어 검증된 수치가 아닙니다 — 강의/경험에서 나온 참고 배율(노후 0.90 · 기본 1.00 · 올수리 1.08)일 뿐입니다.
  - 현재 상태 (노후 — 수리 필요): X.XX억
  - 기본 정리 후 (청소·도배·장판 등): X.XX억 (현재 대비 +X.XX억) · 예상 공사비 N만원 → 순증 X.XX억 (남는 장사)
  - 올수리 후 (전체 리모델링): X.XX억 (현재 대비 +X.XX억) · 예상 공사비 N만원 → 순증 X.XX억 (손해)
  ```

## 39. 매도 압력 (경쟁매물 ÷ 최근 실거래)

30절 유동성은 "이 동네가 원래 얼마나 자주 거래되는지"(공급 소화 속도)만
보여주고, 31절은 "내 가격이 경쟁 매물 중 몇 % 위치인지"만 본다. 이 절은
둘을 나눠서 **"지금 쌓여 있는 경쟁 매물이 이 동네 거래 속도로 몇 개월치
물량인지"**(부동산에서 흔히 쓰는 재고 소진 개월수, months of supply)를
구한다 — "매물이 없으면 빨리 팔리나?"를 느낌이 아니라 숫자로 바꾼 것이다.
`scripts/listing_parser.py`의 `sale_pressure()`가 구현한다.

- **분자**: 28절에서 붙여넣어 파싱된 매물 중 유사면적(31절과 같은 허용범위,
  기본 ±15%)인 것의 개수. 평형이 다른 매물은 내 경쟁 상대가 아니라서 뺀다.
- **분모**: 30절 유동성의 **반경 500m 최근 3개월 월평균 거래건수**.
  (29절 `speed_label_for_percentile()`이 쓰는 것과 같은 값이라 화면 안에서
  두 숫자가 어긋나지 않는다.)
- 구간(`SALE_PRESSURE_BANDS`, 경험적으로 끊은 참고치 — 검증된 기준이
  아니다): 2개월 미만 "낮음" / 2~4개월 "보통" / 4~8개월 "다소 높음" /
  8개월 이상 "높음".
- 분모가 0이면(반경 안 최근 3개월 거래가 아예 없음) 나눌 수 없으므로
  개월수 없이 "판단 보류"로 돌려주고, "거래 자체가 뜸한 동네라는 신호일 수
  있으니 매도 기간을 넉넉히 잡으라"고 안내한다 — 조용히 숨기지 않는다.
  유사면적 매물이 0건이면 `None`(카드 자체 생략).
- ⚠️ **분자와 분모의 범위가 정확히 같지 않다** — 매물 수는 사용자가 직접
  네이버부동산에서 검색해 붙여넣은 범위의 결과(전수가 아니고 반경 500m와도
  다를 수 있다)이고, 실거래는 반경 500m 기준이다. 그래서 절대값보다
  "쌓여 있나 / 적나"의 **방향만** 참고해야 한다는 문구를 항상 함께 단다.
  28절 파서의 인식률 한계도 그대로 물려받는다.
- 32절 종합 판단 문단에도 한 문장으로 들어간다(`build_verdict()`의
  `sale_pressure` 인자) — 개월수를 계산할 수 있을 때만 붙인다.
- ⚠️ **웹 전용이다** — 28절/31절과 같은 이유로 CLI에는 매물 붙여넣기 입력
  경로 자체가 없다. 28절을 CLI에 연결하면 이 절도 같이 연결하면 된다.
- 출력 위치: 웹 결과 페이지, 38절 카드와 30절 유동성 카드 사이.
- 출력 포맷 (웹):
  ```
  ⚖️ 매도 압력 (경쟁매물 ÷ 최근 실거래) — 높음
  지금 나와 있는 유사면적 경쟁 매물 N건 ÷ 반경 500m 최근 3개월 월평균 실거래 N.N건 = 약 N.N개월치 물량
  → [구간별 해설 문구]
  ⚠️ 분자와 분모의 범위가 정확히 같지 않습니다 — ...
  ```

## 40. 인근 아파트 대비 가격비율

**사용자가 현업 중개사에게 직접 배운 내용을 그대로 옮긴 절이다** — "인근
아파트 시세가 높고 빌라와 차이가 많이 날수록 빌라가 잘 팔린다. 아파트를
사고 싶은데 돈이 모자라 빌라로 내려오는 수요가 두껍고, 빌라 값은 결국
아파트와 **키 맞추기**를 하려는 경향이 있어 상승 여력도 남는다. 그래서 그
차이가 많이 나는 동네를 사라." 강의의 "빌라 = 아파트의 대체재" 명제와 같은
이야기인데, 경고(비싸면 안 팔린다)뿐 아니라 **긍정 신호(갭이 크면 수요가
두껍고 오를 여지도 있다)** 까지 담는다는 점이 다르다.

- 지표: **비율 = 빌라 ㎡당가 ÷ 인근 아파트 ㎡당가** — 총액이 아니라 ㎡당가로
  비교한다(빌라 69㎡ vs 아파트 85㎡처럼 평형이 달라 총액 비교는 왜곡된다).
  **낮을수록 좋다.**
  | 비율 | 판정 |
  |---|---|
  | `APT_GAP_GOOD_MAX`(60%) 이하 | 👍 갭이 넉넉 — 내려오는 수요가 붙기 좋은 구간 |
  | 60~`APT_GAP_OK_MAX`(80%) | ➖ 보통 |
  | 80% 초과 | ⚠️ 굳이 빌라를 고를 이유가 약해짐 |
  ⚠️ 경계값은 검증된 수치가 아니라 위 경험칙을 옮긴 참고값이다.
- **지오코딩을 하지 않는다.** 5절처럼 반경으로 자르려면 아파트 거래마다
  카카오를 한 번씩 더 불러야 해서(7절에서 역거리·엘리베이터를 포기한 것과
  같은 비용 문제) **같은 법정동(`umdNm`)** 으로 근사한다 — "인근 아파트
  시세"는 동 단위로도 충분히 잡힌다.
- **동 표본이 부족하면 바로 인접한 동까지만 넓힌다**(`_nearby_dong_names()`).
  빌라만 빼곡하고 아파트가 거의 없는 구도심 동이 실제로 있는데, 그럴 때
  카드를 통째로 없애는 것보다 옆 동네 시세라도 보여주는 쪽이 낫다.
  - ⚠️ **구 전체로는 넓히지 않는다** — 사용자가 "구 전체로 하지 말고 주변
    바로 인접 동만 포함하라"고 지적했고, 실제로 같은 구라도 반대편 끝
    동네는 시세가 전혀 달라 기준선으로 쓸 수 없다.
  - 동 경계 데이터가 없으므로 **동 중심 좌표 사이 거리**로 근사한다 — 같은
    구의 각 동을 "시도 시군구 동" 주소로 **한 번씩만** 지오코딩해서(한 구에
    보통 10~25개, `data/geocode_cache.json`에 캐시) 대상 물건 좌표에서
    `APT_GAP_NEARBY_RADIUS_M`(2km) 이내인 동만 고른다. 아파트 거래 수백 건을
    통째로 지오코딩하는 것과는 비용이 비교가 안 된다.
  - 폴백 범위는 `sggCd`로 한 번 더 자른다 — **다른 구 아파트는 좌표가
    가까워도 섞이지 않는다**(행정구역이 다르면 시세 형성도 다르다. 회귀
    테스트로 고정).
  - **대상 물건 좌표가 없으면 넓히지 않는다** — 인접 여부를 판단할 수 없는데
    "가깝다"고 우기느니 카드를 생략하는 쪽이 안전하다. 지오코딩에 실패한
    동도 같은 이유로 조용히 건너뛴다.
  - 넓힌 경우 **화면·CLI에 어느 동을 끌어왔는지 이름까지 밝힌다**("같은 동
    표본이 적어 바로 인접한 동(미아동)까지 넓혔습니다") — 5절 적응형 반경이
    넓힌 사실을 반드시 알리는 것과 같은 원칙.
- 면적 허용범위는 8절(±15%)보다 **넓은 ±30%** 다. 빌라와 아파트는 평형 구성
  자체가 달라 좁게 잡으면 표본이 거의 안 남고, 어차피 ㎡당가로 비교하므로
  면적 차이에 덜 민감하다.
- 표본이 `APT_GAP_MIN_SAMPLE`(5건) 미만이면 `None` → 카드 자체를 생략한다.
- 41절 환금성 진단의 **맨 뒤 판정 항목**으로 들어간다 — 원래는 가격 위치
  바로 뒤(2번)였는데, 사용자가 "참고치로 보는 거지 경매 단타용으론 엄청나게
  중한 게 아니다"라고 지적해서 내렸다. 자세한 이유는 41절 표 아래 ⚠️ 참고.
- ⚠️ **단기보다 중·장기 신호다.** 갭이 크다는 건 "지금 당장 빨리 팔린다"가
  아니라 "예산이 모자라 내려오는 수요층이 두껍고 키 맞추기 상승 여력이
  남았다"는 뜻이다 — 항목 문장 끝에 "단기 매도(단타)보다 중·장기 보유에서
  더 크게 작용하는 신호라 참고치로 보세요"를 항상 붙인다.
- **웹에는 별도 카드가 없다** — 41절 진단의 그 항목 안에 접이식
  (`apt_gap_detail()` 매크로)으로 들어가 있다. 예전엔 진단 카드에 한 줄
  요약이 뜨고 **한참 아래에 같은 내용의 카드가 또** 떠서 "이게 왜 밑에 또
  있냐"는 지적을 받았다.
- **막대 라벨은 "OO동 아파트 평균" / "이 빌라 평균"이다** — 예전엔 "홍은동
  아파트"/"이 빌라"라고만 적어서, 아파트 쪽이 실거래 여러 건의 ㎡당가
  **중앙값**이고 빌라 쪽도 8절 산출값이라는 게 안 드러났다(개별 물건 하나의
  값으로 오해할 수 있다).
- ⚠️ **매도가(8절) 계산에는 반영하지 않는다** — 24절 시장동향·26절 역세권과
  같은 원칙으로, 검증된 보정 공식이 없어 판단은 사용자 몫으로 남긴다.

**API 상태**
- ✅ **전부 실측으로 확인됐다.** Endpoint
  `https://apis.data.go.kr/1613000/RTMSDataSvcAptTrade`, 오퍼레이션
  `getRTMSDataSvcAptTrade`, XML 포맷. 인증키는 `MOLIT_SERVICE_KEY`를 그대로
  쓴다(같은 계정·같은 제공기관 1613000 — 아파트 API만 활용신청을 따로 하면
  된다). 사용자가 강북구(11305) 2026-04~07을 실제로 조회해서 205/196/109/
  132건을 받았고 **받은 행이 전부 `normalize_apt_row()`를 통과했다** — 즉
  계산에 필요한 필드가 연립다세대 API와 같은 이름으로 온다.
- 단지명 필드(`NAME_FIELDS`)만은 후보를 여러 개 받아둔다 — 아파트는 `aptNm`이
  표준이지만 연립다세대는 `mhouseNm`이라 규격이 통일돼 있지 않고, 단지명은
  계산에 안 쓰여서 틀려도 "(단지명없음)"으로 뜰 뿐 숫자에는 영향이 없다.
- **필드명이 틀리면 틀린 숫자가 아니라 "0건"으로 드러난다** — `normalize_apt_row()`가
  필수 필드를 못 찾은 행을 버리므로 카드가 아예 안 뜨고, CLI는 실제로 온
  필드 목록을 출력해 준다.
- ⚠️ **전국 데이터를 미리 받아둘 필요가 없다.** 40절은 대상 물건과 같은 동
  (부족하면 같은 구)만 쓰므로, 다른 구 데이터는 한 줄도 쓰이지 않는다.
  - **웹**: `get_apt_rows(lawd_cd, ...)`가 입력 주소의 구만 그때그때 조회하고
    캐시한다 — 방문자가 어느 지역을 넣든 자동으로 된다(사전 다운로드 불필요).
  - **CLI**: `data/raw_apt/`에 미리 받아둔 것만 읽으므로, **대상 물건이 있는
    구 하나만** 받으면 된다. 여러 지역을 분석한다면 그 구들만 추가로 받는다.
- 관련 파일: `scripts/molit_apt_api.py`(조회·`data/raw_apt/` 저장),
  `webapp/data_source.py`의 `get_apt_rows()`(웹 조회·`webapp/cache/apt/`
  캐시, 실패는 삼켜서 카드만 생략), `estimate_price.py`의
  `compute_apt_gap()`/`print_apt_gap()`, CLI `--apt-dir`.

## 41. 환금성·경쟁 진단 (강의 기준 규칙 판정)

8절은 "얼마에 팔리나"만 말해준다. 이 절은 그것과 **별개로 "얼마나 잘
팔리나"** 를 항목별로 진단한다 — 사용자가 "강의 텍스트 내용으로 지금 검색한
빌라가 환금성이 좋은지, 경쟁이 있을지 설명해줄 수 있지 않냐"고 요청해서
붙였다. `scripts/estimate_price.py`의 `build_marketability_report()`/
`print_marketability_report()`가 구현한다.

강의(`data/lecture_notes_villa.md`)의 핵심 주장이 그대로 판정 기준이 된다 —
빌라는 아파트의 대체재라 싸야 팔리지만 **싸다고 다 팔리는 것도 아니고**,
개별성이 강해 실거래 하나로 3~6개월 뒤 매도가를 잡기도 어렵다. 그래서
가격(8절)과 팔림성(이 절)을 화면에서 아예 분리해 보여준다.

- **새로 계산하는 게 없다.** 이미 있는 지표를 강의 기준에 맞춰 등급만
  매긴다 — 30절 유동성 · 39절 매도압력 · 31절 가격 포지션 · 7절 시세
  신뢰도 · 20절 건축물대장(승강기) · 입력받은 층/준공년도. 그래서 추가
  API 호출도, 계산 비용도 없다.
- **항목은 강의가 중요하다고 짚은 순서대로 보여준다**(`MARKETABILITY_ORDER`).
  사용자가 "강의 표본에 나온 것 중 중요한 순서대로 차례대로 해달라"고
  요청해서 정한 순서이고, 계산(점수)에는 전혀 영향이 없다 — 화면 배치와
  번호만 바뀐다. 판정이 매겨진 항목에는 1부터 순번이 붙고, 판정 없는
  참고 항목(연식)은 번호 없이 맨 뒤에 둔다.

  | 순 | 항목 | 👍 좋음 | ➖ 보통 | ⚠️ 주의 | 근거 |
  |---|---|---|---|---|---|
  | 1 | 가격 위치 (31절 퍼센타일) | 상위 30% 이하 | 60% 이하 | 60% 초과 | "빌라는 아파트의 대체재라 싸야 팔린다"(3절), "싸게 사는 것보다 팔릴 가격을 먼저"(17절 6번) — 강의 전체를 관통하는 1순위 명제 |
  | 2 | 층·승강기 | 2~3층, 또는 승강기 있는 4층 이상 | 1층, 승강기 없는 4층, 그 외 | 반지하(0층 이하), 승강기 없는 5층 이상 | "금액이 싸면 팔리긴 팔리는 빌라를 골라라, 팔기 어려운 요소를 제거하라"(5절 1번) — 가격으로도 못 덮는 요소 |
  | 3 | 역세권·학세권 (45절) | 합계 3~4점 | 2점 | 0~1점 | 사용자가 "층·승강기 다음으로 중요한 요소"라고 직접 지정 |
  | 4 | 임장 체크 (43절) | 문제 0개 + "문제 없음" 체크 | 1개 | 2개 이상 | 데이터로 알 수 없어 직접 보고 온 결과만이 근거 |
  | 5 | 연식 | `BUILD_AGE_NEW_MAX`(15년) 이내 | `BUILD_AGE_OK_MAX`(30년) 이내 | 30년 초과 (완전 구축) | "연식별 거래량을 따로 보라"(6절 4번) + 사용자 지정 — 아래 ⚠️ 참고. 오래된 물건이면 `notes`로 임장 포인트·인테리어 비용까지 짚어준다 |
  | 6 | 거래량 (30절, 500m·3개월 월평균) | 3건 이상 | 1건 이상 | 1건 미만 | "필요한 데이터" 목록의 맨 앞(6절 1번) — 가격이 맞아도 거래가 없으면 못 판다 |
  | 7 | 경쟁 매물 (39절 매도압력) | 낮음 | 보통 | 다소 높음·높음 | "실제 매물·실거래·주변 경쟁물건을 함께 확인"(7절) |
  | 8 | 시세 판단 근거 (7절 신뢰도) | 70 이상 | 40 이상 | 40 미만 | "개별성이 강해 실거래 하나로 정하면 안 된다"(4절) |
  | 9 | 인근 아파트 대비 (40절) | 60% 이하 | 80% 이하 | 80% 초과 | 판정 항목 중 **맨 뒤** — 아래 ⚠️ 참고 |

  순서는 **앞쪽 = 물건 자체 속성**(가격·층·입지·임장·연식), **뒤쪽 = 시장
  지표**(거래량·경쟁매물·신뢰도·아파트 대비)로 묶여 있다.

  ⚠️ **연식은 참고 안내(`info`)였다가 실제 감점 항목이 됐다.** 사용자가
  "완전 구축이면 그것도 디버프 요소로 넣어달라"고 해서 바꿨다 — 예전엔
  "2010년 전후로 매수층이 갈린다"는 안내만 하고 번호도 점수도 없었다.
  지금은 30년 초과면 `warn`으로 점수를 깎고 약점 목록에도 잡힌다(실거주
  매수층이 좁아지고 대출·보수비 부담이 겹친다는 이유를 문장에 적는다).
  다만 **"정비구역 기대감이 붙는 자리가 아니면"**이라는 단서를 항상 같이
  단다 — 구축이 무조건 나쁜 게 아니라 재개발 기대가 없는 구축이 불리한
  것이라서다. 경계값 15년/30년은 경험적으로 끊은 참고값이다.
  ⚠️ 이 변경으로 **`info` 판정을 쓰는 항목이 지금은 하나도 없다** — 점수에서
  빠지는 건 `unknown`(정보 미입력)뿐이다. `info` 처리 코드와 "번호를 안
  붙인다"는 규칙은 그대로 남아 있으니 나중에 참고 항목을 추가하면 바로
  쓸 수 있다.

  ⚠️ **인근 아파트 대비는 한때 1번이었다가 맨 뒤로 내렸다.** 사용자가
  "이건 6번처럼 마지막 번호로 내려가야 한다, 참고치로 보는 거지 사실 경매
  단타용으론 엄청나게 중한 게 아니다"라고 지적했다. 갭이 크다는 건 "지금
  당장 빨리 팔린다"가 아니라 "예산이 모자라 내려오는 수요층이 두껍고 키
  맞추기 상승 여력이 남았다"는 뜻이라, **단기 매도(단타)보다 중·장기 보유에서
  더 크게 작용하는 신호**다 — 그 사실을 항목 라벨("인근 아파트 대비 (참고 —
  중·장기 신호)")과 문장 끝에 항상 적는다. 점수 계산 방식은 그대로이고
  (다른 항목과 동등한 한 표), 바뀐 건 화면 순서·번호·문구뿐이다.
  화면 안내에도 "뒷번호로 갈수록 당장의 매도 속도보다 중·장기 참고치에
  가깝다"고 한 줄 덧붙인다.

  (판정 기준값은 전부 강의 경험칙을 옮긴 것이지 통계로 검증한 값이 아니다.)

  ⚠️ **층·승강기 항목은 승강기 유무를 항상 문장으로 말한다.** 사용자가 "항목
  이름이 '층·승강기'인데 왜 승강기 유무는 안 써 있냐"고 지적해서, 20절
  건축물대장에서 받은 `has_elevator`를 판정과 별개로 **항상** 한 문장 덧붙인다
  ("승강기가 있어 매도에 유리해요" / "승강기가 없어 매도에 불리해요" / 조회
  실패면 "건축물대장에서 확인하지 못했어요"). 그리고 승강기가 없을 때 4층과
  5층 이상을 갈라서, **4층은 `ok`**("승강기가 없어도 무난하거나 조금 나쁜
  편")·**5층 이상은 `warn`** 으로 둔다 — 예전엔 4층 이상을 한 묶음으로
  `warn` 처리했는데 사용자가 "4층은 무난하거나 조금 나쁜 편"이라고 직접
  교정해줬다.

  ⚠️ **항목에 `facts`·`notes` 두 필드가 생겼다.** 판정 문장(`text`) 하나로는
  "무엇을 근거로 이렇게 판정했는지"와 "그래서 임장에서 뭘 봐야 하는지"를
  다 담기 어려워서 갈랐다 — 웹은 `facts`를 `mk-facts` 칩 줄로, `notes`를
  `ul.note-lines`로 그리고, CLI는 각각 `[건축물대장] …` 줄과 들여쓴 줄로
  찍는다. 지금 쓰는 곳은 두 군데다:
  | 항목 | `facts` | `notes` |
  |---|---|---|
  | 층·승강기 | 20절 건축물대장 요약 (승강기 · 지상 N층 건물의 M층(탑층) · N세대 · 사용승인 YYYY.MM.DD) | 위반건축물은 이 조회로 못 본다 + 정부24로 직접 확인하라 |
  | 연식 | — | 30년 초과면 임장 포인트(천장·벽 물 자국, 창틀 결로, 녹물, 외벽 균열, 옥상 방수)와 "인테리어 비용을 넉넉히 잡아라"; 15~30년이면 그 한 줄짜리 축약본 |
  값이 없으면(건축물대장 조회 실패, 준신축) `None`이라 화면에 아무것도 안
  붙는다. 다른 항목은 아직 이 필드를 안 쓰므로 템플릿에서 `item.facts`는
  Undefined로 떨어져 조용히 생략된다.

  ⚠️ **탑층은 건축물대장 지상층수로 판정한다** — 5-1절의 "관측된 최고층"
  추정(실거래 기록으로 근사)과 달리, 대상 물건은 이미 20절에서 건축물대장을
  한 번 조회하므로 `grndFlrCnt`(지상층수)를 그대로 쓸 수 있다(`_as_int()`로
  문자열을 정수화 — 건축물대장 필드가 문자열로 오는 경우가 있다).
  `floor >= ground_floors > 1`이면 탑층으로 보고 "누수·단열에 불리할 수
  있으니 임장 때 확인하라"는 문장을 덧붙이며, 그 항목이 `good`이었다면
  `ok`로 한 단계 내린다(탑층이라는 이유만으로 `warn`까지 내리지는 않는다 —
  빌라 탑층은 흔하고 가격으로 충분히 상쇄되는 경우가 많다). `ground_floors`가
  1 이하이거나 조회 실패면 판정하지 않는다.
- **웹 화면은 항목마다 카드 한 장씩이다** — 번호 배지 · 항목명 · 괄호 안
  부제 · 판정 배지(좋음/보통/주의/참고)를 한 줄에 놓고, 그 아래 설명
  한 줄, 맨 아래 근거가 된 강의 문장을 인용 형태로 붙인다. 왼쪽 세로 띠와
  배지 색이 판정에 따라 같이 바뀌어서(좋음 초록 · 보통 회색 · 주의 빨강 ·
  참고 파랑) **훑어만 봐도 어디가 문제인지 보인다.** 점수는 카드 맨 위에
  큰 숫자 + 가로 막대로 보여준다(`.mk-*` 클래스, `base.html`).
  ⚠️ 예전엔 이모지 하나 + 줄글 목록이라 "무엇부터 봐야 하는지"도, "뭐가
  문제인지"도 한눈에 안 들어온다는 지적을 받아 바꾼 것이다.

- **점수** = 판정이 매겨진 항목들의 평균(good 100 · ok 60 · warn 25).
  `info`(지금은 해당 항목 없음)와 `unknown`(매물 미입력 시 경쟁 항목)은 점수에서 뺀다 —
  정보가 없다고 감점하면 안 되기 때문이다. 75 이상 "환금성 좋은 편",
  50 이상 "보통", 그 미만 "주의 — 팔기 어려울 수 있음".
- **약점 목록**을 따로 뽑아 요약 문장에 넣는다: "약점은 거래량 · 경쟁 매물 ·
  층·승강기입니다. 강의 표현대로 '금액이 싸면 팔리긴 팔리는 빌라'가 되려면
  이 항목들을 가격으로 상쇄해야 해요."
- 항목마다 **근거가 된 강의 문장을 한 줄로 같이 보여준다**(`lecture` 필드)
  — 판정만 던지면 "왜 이게 문제인지"가 안 보여서다.
- 매물을 안 붙여넣었으면 경쟁 매물 항목이 `unknown`으로 남고 "붙여넣으면
  계산해 드린다"고 안내한다(31절 가격 위치 항목은 아예 빠진다).
- **관련된 입력·상세는 그 항목 안에 접이식으로 붙인다.** 44절에서 매물
  붙여넣기를 "경쟁 매물" 항목 밑으로 접어 넣은 것과 같은 원칙으로, 지금은
  세 가지가 이 카드 안에 들어와 있다:
  | 항목 | 접었다 펴는 내용 |
  |---|---|
  | 임장 체크 | 43절 체크박스 7개 + "문제 없음" + 재계산 버튼 (`inspection_ui()` 매크로) |
  | 경쟁 매물 | 28절 매물 붙여넣기 폼 + 북마클릿 (`listing_paste_ui()` 매크로) |
  | 인근 아파트 대비 | 40절 막대 비교·근거 설명 (`apt_gap_detail()` 매크로) |
  사용자가 "임장 체크리스트가 환금성·경쟁 진단과 같이 있어야 더 보기 편하지
  않냐", "인근 아파트 대비가 왜 밑에 따로 또 있냐, 접힌 걸 누르면 나오게
  해달라"고 지적해서 **별도 카드 두 개를 없애고** 이 카드 안으로 합쳤다 —
  "이 항목이 왜 이런 판정인지 → 여기서 바로 확인/입력"이 한자리에서 이어진다.
  임장 체크만은 아직 아무것도 입력하지 않았으면 **펼친 채로** 뜬다(예전에
  항상 보이는 카드였던 것을 접어두면 아예 못 찾는다).
- ⚠️ **32절과 같은 성격의 규칙 기반 판정이다** — Claude(LLM)를 부르지
  않는다(22절 원칙). 채광·누수·악취·소음·관리상태·주차처럼 어떤 데이터로도
  알 수 없는 항목은 판정하지 않고 36절 임장 체크리스트로 넘긴다는 점을
  출력에 함께 밝힌다.
- ⚠️ Jinja에서 `result.marketability.items`라고 쓰면 파이썬 dict의
  `.items()` 메서드가 먼저 잡혀 `TypeError`가 난다 — 템플릿에서는 반드시
  `result.marketability['items']`로 접근한다(실제로 이 버그를 겪고 고쳤다).
- 출력 위치: CLI는 32절 종합 판단 바로 뒤, 웹은 32절 종합 판단 카드와
  38절 상태별 매도가 카드 사이.
- 20절 `print_building_info()`가 이제 조회한 `info` dict를 **반환한다** —
  이 절이 승강기 유무를 쓰기 위해서다(실패·결과 없음이면 `None`). 웹
  버전도 `building` dict에 `has_elevator`를 함께 담는다.

## 42. 결과 화면 가독성 — 설명은 문장 단위로 줄을 나눈다

사용자가 "결과 내용에 줄바꿈을 잘 안 한 것 같다, 문장들로 된 설명 같은 거
줄바꿈 좀 제대로 해달라"고 지적해서, 결과 페이지의 안내 문단 규칙을 하나로
정했다. 기능 변경이 아니라 **읽는 방식**에 대한 규칙이다.

- **긴 안내 문단은 한 덩어리 줄글로 두지 않는다.** 문장마다 줄을 나눠
  `ul.note-lines`(`base.html`)로 보여준다 — 각 줄 앞에 옅은 점이 찍혀
  "여러 줄짜리 설명"임이 시각적으로도 드러난다. 적용한 곳: 히어로 카드
  경매용 매도가 설명, 29절 가격 구간 설명·30일 목표가 안내·"현재 매물
  대비" 설명, 8절 핵심 비교거래 설명·비교 기준, 38절 상태별 매도가
  설명·경고, 39절 매도 압력 경고, 41절 환금성 진단 설명·경고, 30절
  유동성 주석, 23절 월세 경고, 36절 임장 체크리스트.
- **자동 생성 문단도 문장 리스트로 받는다.** 32절 `build_verdict_parts()`와
  41절 `build_marketability_report()`의 `summary_lines`가 문장 리스트를
  그대로 돌려주고, 웹은 한 문장에 한 줄씩 렌더링한다. 기존
  `build_verdict()`/`summary`는 그 리스트를 공백으로 이어붙인 문자열을
  그대로 돌려주므로 CLI·기존 호출부는 바뀌지 않는다.
- **`word-break: keep-all`을 body에 건다.** 브라우저 기본값으로 두면 한글이
  어절 한가운데서 끊긴다("팔리 / 긴 팔리는"). 어절 단위로만 넘기고, URL처럼
  긴 통짜 문자열은 `overflow-wrap: break-word`로 넘치지 않게 잡는다.
  `.muted` 행간도 1.6 → 1.75로 올렸다.
- 8-1절 그래프 상단 설명(`price_chart.py`의 `intro`)도 같은 원칙으로 세
  줄로 나눴다.
- ⚠️ **덤으로 잡은 버그**: 열이 많은 표(8절 핵심 비교거래 7열 등)가 좁은
  화면에서 카드 밖으로 삐져나가 **페이지 전체에 가로 스크롤**이 생기고
  있었다(모바일 390px에서 `scrollWidth` 463px 실측). 29절 가격 구간 표만
  `.tier-table-wrap`으로 감싸져 있었고 나머지는 맨몸이었다 — 새
  `.table-scroll`로 핵심 비교거래·38절 사다리·30절 유동성·28절 매물 목록
  표를 모두 감쌌다. 표만 따로 스크롤되고 페이지는 390px 그대로다.

## 42-1. 화면 문구에는 출처(강의)를 드러내지 않는다

⚠️ **이 절은 화면에 나가는 모든 문구에 적용되는 규칙이다.**

사용자가 "강의에서 나온 내용인 티 안 나게, 내가 조언하는 것처럼 해달라"고
요청했다. 이 계산기는 사용자 본인이 다른 사람에게 보여주는 도구라, 화면
문구가 "강의에서 이랬다더라"로 읽히면 본인 조언이 아니라 남의 자료 전달로
보인다.

- **화면·CLI 출력 어디에도 "강의", "평공쌤", "텐엑스", 원본 파일명을 쓰지
  않는다.** 브라우저로 그대로 나가는 `webapp/templates/*.html`의 `<style>`·
  `<script>` 주석도 포함한다(소스 보기로 노출되므로).
- 대신 **단정적인 조언 톤**으로 쓴다:
  - "강의: 빌라는 아파트의 대체재 — 인근 아파트보다 비싸면 잘 안 팔린다"
    → "빌라는 아파트의 대체재라, 인근 아파트보다 비싸지면 잘 안 팔립니다"
  - "강의가 꼽은 대표적인 '팔기 어려운 요소'입니다"
    → "대표적인 '팔기 어려운 요소'예요"
  - "경매 강의·경험에서 나온 참고 배율" → "현장 경험에서 나온 참고 배율"
  - "강의 예시 기준 현금:대출 3:7" → "현금:대출 3:7이 흔한 구조예요 (사례 기준)"
- 41절 항목의 근거 필드는 이름도 `lecture` → **`why`**로 바꿨다 — 키 이름이
  화면에 나가지는 않지만, 나중에 누가 "이건 강의 인용이니까 출처를 달아야지"
  라고 되돌리지 않도록 이름 자체를 중립으로 뒀다.
- ⚠️ **정직성 원칙은 그대로다.** 검증 안 된 수치(배율·구간 경계·사례
  숫자)는 여전히 "검증된 수치가 아닙니다", "사례 기준이라 보장 수치는
  아닙니다"라고 반드시 밝힌다 — 출처 표기를 뺀 것이지 **근거의 성격을
  숨기는 게 아니다**. 24절·29절·35절에서 지켜온 원칙과 같다.
- **이 문서(CLAUDE.md)에는 출처를 그대로 남긴다.** 개발용 내부 문서라
  화면에 나가지 않고, 나중에 "이 기준이 어디서 왔나"를 추적하려면 출처가
  꼭 필요하다 — `data/lecture_notes_villa.md`도 그대로 둔다.

## 43. 임장 체크를 환금성 점수에 반영

36절 임장 체크리스트는 여덟 가지 항목을 **글자로 나열만** 했다 — "계산에
반영되지 않는다"고 못박은 참고 목록이었다. 이 절은 그걸 **입력받아 41절
환금성 점수에 실제로 반영한다.** 채광·누수·악취 같은 항목은 어떤 공개
데이터로도 알 수 없어서(7절에서 방향·주차를 포기한 것과 같은 이유), 사람이
직접 보고 온 결과를 받는 것 말고는 방법이 없다.

- 입력 위치는 **41절 환금성 진단 카드 안의 "임장 체크" 항목**이다(접이식,
  아직 아무것도 입력 안 했으면 펼친 채로 뜬다) — 임장은 결과를 보고 난
  뒤에 가는 것이라 첫 화면 입력 폼이 아니라 결과 페이지에서 바로 체크하고
  재계산하는 흐름이 자연스럽고, 사용자가 "임장 체크리스트가 환금성·경쟁
  진단과 같이 있어야 더 보기 편하지 않냐"고 지적해서 **별도 카드였던 것을
  그 항목 안으로 옮겼다**(44절이 매물 붙여넣기를 "경쟁 매물" 항목으로
  접어 넣은 것과 같은 패턴). 28절 매물 붙여넣기 재제출 폼과
  같은 패턴으로, 처음 제출값을 숨은 필드로 그대로 넘기고 임장 체크만 새로
  받는다(`resubmit_fields_no_inspection` — 임장 필드는 체크박스가 그 자리를
  대신하므로 숨은 값에서 뺀다).
- 항목 ↔ 폼 필드 매핑은 `INSPECTION_FIELDS`(`estimate_price.py`)에 둔다 —
  `insp_light`/`insp_leak`처럼 ASCII 키를 쓰고 라벨은 36절 목록 그대로다.
  ⚠️ 36절에서 엘리베이터를 빼고 라벨을 "나쁜 상태"로 바꾸면서 `insp_elevator`
  필드도 같이 사라졌다 — 예전 폼이 그 값을 보내도 `webapp/app.py`가
  `INSPECTION_FIELDS`를 기준으로 읽으므로 조용히 무시된다.
- **"문제 없음"을 따로 받는다**(`insp_clean`). 체크가 0개인 상태만으로는
  "가봤는데 멀쩡하다"와 "아직 안 가봤다"를 구분할 수 없어서다 — 구분 없이
  0개를 good으로 쳐주면 아무것도 안 한 물건이 점수를 거저 받는다.
- 판정: 문제 0개 + "문제 없음" 체크 → 👍, 1개 → ➖, `INSPECTION_WARN_COUNT`
  (2개) 이상 → ⚠️, 아무 입력 없음 → ❔(점수에서 제외 — 정보가 없다고
  감점하지 않는다는 41절 원칙 그대로). 경계값 1개/2개는 검증된 기준이 아니라
  경험적으로 끊은 값이다.
- 모순 입력("문제 없음"과 개별 항목을 같이 체크)은 **개별 항목을 따른다** —
  더 구체적인 정보이기 때문. 웹·CLI 양쪽에서 같게 처리하고 테스트로 고정했다.
- 41절 항목 순서에서는 **층·승강기 바로 뒤(3번)** 다 — 둘 다 "팔기 어려운
  요소 제거"라는 같은 뿌리에서 나온 항목이라 붙여 뒀다.
- CLI는 `--inspection-bad 누수 경사`(항목명 그대로, `choices`로 오타 방지)와
  `--inspection-clean`.
- ⚠️ **매도가(8절)는 이 체크와 무관하게 그대로다** — 바뀌는 건 41절 환금성
  진단뿐이다. 35절/38절 수리상태 배율이 가격을 건드리는 것과 달리, 이건
  "얼마에 팔리나"가 아니라 "얼마나 잘 팔리나" 쪽 지표라서다. 화면에도 그
  문장을 항상 적는다.

## 44. 결과 화면에 그림 늘리기 (계절성·가격 추이) + 매물 붙여넣기 접기

사용자가 "UI가 공부하거나 책 읽는 것마냥 지루하다, 이미지나 그래프가
부족한 것 같다"고 지적해서 두 가지를 바꿨다.

**① 매물 붙여넣기 칸을 41절 "경쟁 매물" 항목 안으로 접어 넣었다.**
예전엔 결과 페이지 아래쪽에 큰 카드 하나를 통째로 차지하고 있었는데,
매물을 안 붙여넣은 사람에게는 빈 입력칸만 크게 보여서 페이지가 장황해졌다.
이제 41절 진단의 "경쟁 매물" 항목(`item.key == 'competition'`) 바로 밑에
알약 모양 `<summary>`로 접혀 있고, 누르면 펼쳐진다(`.paste-fold`).
- **"이 항목이 왜 정보 없음인지 → 여기서 채우면 된다"가 한자리에서 이어진다**
  — 예전엔 "붙여넣으면 계산해 드려요"라는 안내와 실제 입력칸이 화면상
  멀리 떨어져 있었다.
- 붙여넣기 UI는 Jinja 매크로(`listing_paste_ui()`)로 빼서 한 군데만
  고치면 되게 했다. 분석 결과(유사도 순위 표)는 **읽은 매물이 있을 때만**
  별도 카드로 뜬다 — 빈 카드가 자리를 차지하지 않는다.
- 인식 실패 시에는 접이식이 `open` 상태로 펼쳐진 채 뜬다(다시 붙여넣어야
  하는 상황이라 닫아두면 안 된다).

**② 12절 계절성·15절 가격 추이를 웹에도 그림으로 붙였다.**
둘 다 계산 로직은 이미 CLI에 있었는데 화면에는 CLI 텍스트와 `--html`
리포트에만 나왔다. 웹 결과 페이지에도 카드로 추가했다:
- **가격 추이** — 월별 평균 평당가 꺾은선 + 면적 채움, 가로 눈금 3줄(최저·
  중간·최고)과 x축 라벨 3개(처음·중간·끝). 첫 달 → 마지막 달 변화율도
  문장으로 같이 적는다.
- **계절성** — 12개월 막대(전체 평균=100 지수). 성수기는 초록, 비수기는
  회색, 나머지는 파랑으로 칠해서 훑기만 해도 성수기가 보인다.
- 렌더러는 `scripts/price_chart.py`의 `render_price_trend_svg()`/
  `render_seasonality_bars_html()`다. ⚠️ `report.py`에도 비슷한 렌더러가
  있지만 그쪽은 **그 페이지의 CSS 클래스에 묶여 있어서** 재사용이 안 된다 —
  새 렌더러는 인라인 스타일만 써서 어느 페이지에 넣어도 그대로 그려진다
  (나중에 `report.py`도 이쪽으로 옮길 수 있다).
- **표본이 부족하면 빈 문자열을 돌려주고 카드 자체가 생략된다**(20절·26절과
  같은 "없으면 조용히 생략" 원칙). 동을 못 찾은 주소면 둘 다 안 뜬다.
- ⚠️ **웹은 12절 원문과 기준 기간이 다르다** — 12절은 "연도 필터 없이 전체
  기간"인데 웹은 `get_trade_rows()`가 `year_min` 이후만 들고 온다. 그래서
  화면에 "조회한 기간 기준이라 표본이 적을수록 들쭉날쭉할 수 있다"고
  적는다. CLI는 `data/raw` 전체 기간이라 원문 그대로다.
- 가격 추이 SVG는 8-1절과 같은 이유로 `min-width: 520px` + 가로 스크롤
  래퍼를 쓴다(좁은 화면에서 글자가 뭉개지지 않게).

## 45. 역세권·학세권을 환금성 진단에 (41절 4번 항목)

사용자가 "역세권·초등학교세권인지도 고려해야 한다, 내가 보기엔 **층·승강기
다음으로 중요한 요소**"라고 짚어서 41절 진단에 넣었다. 자리도 요청대로
**층·승강기 바로 다음(4번)** 이다.

- **추가 API 호출이 없다.** 19절 입지 체크(`compute_location_check()`)가 이미
  구해둔 거리를 판정으로만 옮긴다 — 같은 좌표·키워드는
  `data/nearby_place_cache.json`에 캐시돼 있어 같은 동네를 다시 조회하면
  호출조차 안 나간다.
- 점수는 두 시설을 각각 매겨 합산한다(0~4점):

  | | 2점 | 1점 | 0점 |
  |---|---|---|---|
  | 지하철역 | `STATION_NEAR_M`(800m) 이내 | `STATION_OK_M`(1200m) 이내 | 그 밖 / 없음 |
  | 초등학교 | `SCHOOL_NEAR_M`(500m) 이내 | `SCHOOL_OK_M`(1000m) 이내 | 그 밖 / 없음 |

  합계 3~4점 👍 / 2점 ➖ / 0~1점 ⚠️.
  ⚠️ 경계값은 통상 쓰이는 기준(역까지 도보 10분 ≈ 800m, 초등학교 도보
  통학권 ≈ 500m)을 옮긴 참고값이지 데이터로 검증한 수치가 아니다.
- **조회 실패는 감점이 아니다** — 두 키워드 모두 에러면 `unknown`으로 두고
  점수에서 뺀다(41절 "정보가 없다고 감점하지 않는다" 원칙 그대로). 키가
  없거나 카카오가 막혀도 매도가 계산은 그대로 간다.
- 판정만 던지지 않고 **거리 자체도 보여준다** — 결과 페이지 "입지 체크"
  카드(19절)에 역·학교 이름과 미터가 그대로 나오고, "이 거리가 환금성
  진단 판정에 쓰인다"는 문장을 달아 둘이 같은 데이터임을 밝힌다.
- ⚠️ 34절 주변 지형(산·하천)과는 **다른 검색**이다 — 그쪽은 자연 지형이라
  전용 카테고리가 없어 이름 접미사로 걸러내는 실험적 기능이고, 이쪽은
  지하철역·초등학교라 카카오 장소 카테고리가 정확하다(34절 경고 문구가
  이 절에는 붙지 않는 이유).
- ⚠️ **매도가(8절)는 그대로다** — 바뀌는 건 41절 환금성 진단뿐이다.

## 46. 입력 페이지 UI 개편 (히어로 배너 + 아이콘 필드 + CTA)

사용자가 참고 목업 이미지를 보내주며 "요런식으로 처음 유아이 바꾸고 싶은데
어떰?"이라고 물어서, 입력 페이지(`/`, `webapp/templates/index.html`)를 그
구조로 다시 짰다. 28절 매물 붙여넣기·22-1절 탱크옥션 자동 채우기 같은
기능은 하나도 건드리지 않았다 — 순수하게 **보이는 방식만** 바꾼 것이다.

- **히어로 배너(`.home-hero`)**: 예전엔 `<h1>` 한 줄 + 회색 힌트 한 줄이
  전부라 "이게 뭘 해주는 도구인지"가 안 읽혔다. 지금은 연한 그린 그라데이션
  카드 안에 제목(🏠 빌라 매매가 계산기)·한 문장 설명(**얼마에 얼마나 잘
  팔릴지**까지 보여준다는 점을 굵게)·기능 칩 4개(📊 실거래가 기반 · 🧭
  환금성 진단 · 🏢 인근 아파트 대비 · 📈 가격 추이)를 담는다. 칩은 41절·
  40절·15절이 실제로 하는 일을 그대로 옮긴 것이라 과장이 아니다.
- **일러스트는 사용자가 직접 만든 이미지를 그대로 쓴다**(`webapp/static/
  hero-building.png` 히어로 우측 건물 마스코트, `logo-house.png` 제목 옆
  집 아이콘).
  - ⚠️ **처음엔 이모지 워터마크(🏙️)로 대체했다가 사용자 지적을 받고
    고쳤다.** "37절에서 static 폴더를 지웠고, 이미지를 다시 넣으면 용량·
    로딩·라이선스 부담이 따라온다"고 판단했는데, **일러스트는 사용자가
    직접 만든 것이라 라이선스 문제가 애초에 없었다.** 37절이 뺀 건 제3자
    강의 표 이미지였고, 사용자 본인 저작물과는 전혀 다른 얘기다 —
    앞으로 사용자가 만든 자료를 "저작권" 이유로 빼지 않는다.
  - 두 파일 다 목업 이미지에서 해당 영역만 잘라낸 뒤 **배경을 투명하게**
    빼둔 것이다(테두리에서 흘려보내는 플러드필 — 색 전체를 기준으로
    지우면 집 벽·구름 같은 밝은 내부까지 날아간다). 배경이 투명해서
    히어로 그라데이션 위에 그대로 얹힌다.
  - 히어로 일러스트는 `.home-hero::after`의 `background-image`로 오른쪽
    아래에 붙이고, `mask-image: linear-gradient(to right, transparent 0,
    #000 42%)`로 왼쪽 가장자리를 서서히 지워 본문 글줄과 겹쳐도 지저분해
    보이지 않게 했다. 좁은 화면(≤420px)에서는 크기·투명도를 낮춘다.
  - 히어로 그라데이션 오른쪽 끝도 따뜻한 크림색(`#fdf6ea`)에서 일러스트
    톤에 맞는 차가운 민트(`#f8fcfb`)로 바꿨다 — 안 그러면 그림 주변만
    색이 튄다.
  - ⚠️ **원본이 아니라 목업 스크린샷에서 잘라낸 것이라 해상도가 낮다**
    (3~4배 확대해서 저장). 사용자가 원본 파일을 주면 같은 경로에
    덮어쓰기만 하면 된다 — CSS·마크업은 그대로다.
- **아이콘 타일 입력 필드(`.field`/`.field-icon`)**: 필수 네 칸(📍 지번 주소 ·
  📐 전용면적 · 🏢 층 · 📅 준공년도)마다 42px 연녹색 타일에 이모지를 놓고
  라벨+입력을 오른쪽에 붙인다. 라벨만 나열하던 예전 폼보다 한눈에 구분된다.
  전용면적·층은 한 줄에 나란히 놓는다(층은 숫자 한두 자리라 좁아도 된다).
- **섹션 헤더(`.form-section`)**: "기본 정보 입력 — 정확할수록 결과가
  정확해져요"에 왼쪽 그린 액센트 바를 붙여 접이식 섹션(탱크옥션 붙여넣기·
  상세 옵션)과 시각적으로 구분한다.
- **제출 버튼(`.cta-submit`)**: 전체 폭 그린 그라데이션 버튼("🧮 매도가
  계산하기 →"). 27-1절에서 네이버 CTA를 키운 것과 같은 이유 — 작은 버튼은
  "여기를 누르면 된다"는 신호가 약하다.
- **하단 안내(`.home-note`)**: ⏱️ 첫 조회가 느릴 수 있다(22절 지오코딩
  비용), ℹ️ 참고용 추정치라는 면책 두 줄을 테두리 있는 정보 행으로 보여준다.
- ⚠️ **면책 문구가 두 번 나오던 문제를 고쳤다.** `base.html`의 `<footer>`가
  이미 같은 면책을 깔고 있어서, 새 `.home-note`와 화면에 나란히 두 번 떴다
  (실제 스크린샷에서 확인). `base.html`의 footer를 `{% block footer %}`로
  감싸고 `index.html`이 그 블록을 빈 값으로 덮어써서, 입력 페이지에서는
  디자인된 `.home-note`만 남게 했다 — 결과 페이지·로그인 페이지는 기존
  footer를 그대로 쓴다.
- Playwright로 데스크톱(1280px)·모바일(390px) 양쪽을 렌더링해서 가로
  스크롤이 생기지 않는지(`scrollWidth == viewport`), 칩·필드가 좁은 폭에서
  제대로 줄바꿈되는지 스크린샷으로 확인했다. 420px 이하에서는 히어로 제목·
  아이콘 타일을 한 단계 줄이는 `@media` 블록이 따로 있다.
- CLI에는 해당 없다 — 입력 폼 자체가 웹 전용이다.

## 48. 정확도 백테스트 (이미 팔린 거래로 계산기 검증)

여태 이 계산기에는 **"그럴듯하다"는 근거밖에 없었다.** 8-2절 경매용 매도가
공식도, 8-1절 강조 구간도 사용자가 낙찰 후 매도한 사례 몇 건을 눈으로
대조한 게 전부였다. 이 절은 그걸 **숫자로 바꾼다** — 이미 팔린 실거래를
무작위로 뽑아 계산기를 돌리고 실제 체결가와 비교한다.
`scripts/backtest.py`가 구현한다.

```
KAKAO_REST_API_KEY="발급받은_키" python scripts/backtest.py \
    --dir data/raw --gu 강북구 --months 1 --n 30 --csv reports/backtest.csv
```

### ⚠️ 데이터 누출(leakage) — 이 절의 존재 이유

**그냥 최근 거래 주소를 넣고 계산기를 돌리면 결과가 전부 거짓말이 된다.**
5절 비교거래 탐색이 **바로 그 거래 자체를 비교 대상으로 집어오기** 때문이다:

- 거리 0m → 5절 **동일건물 보너스 ×2**
- 계약월이 가장 최근 → 7절 **최근성 가중치 1.6**
- 면적·층·연식 완전 일치 → **유사도 100점**

정답을 보고 정답을 맞히는 셈이다. 그래서 `estimate_as_of()`는 대상 거래의
**계약일보다 앞선 거래만** 계산기에 넘긴다 — 같은 날 계약 건도 뺀다(그날
아침엔 아직 모르는 정보이고, 같은 건물에서 같은 날 여러 호실이 팔리는
분양성 거래가 실제로 있다).
- `scripts/tests/test_backtest.py`의 `TestNoLeakage`가 **"넘겨진 비교군에
  대상 계약일 이후 거래가 한 건도 없다"를 고정한다** — 이 파일의 존재 이유가
  사실상 이것 하나다.
- ⚠️ 테스트에서 지오코딩을 가짜로 바꿀 때는 **`geocode` 모듈의 속성**을
  갈아야 한다. `find_comparables()`는 호출할 때마다 함수 안에서
  `from geocode import geocode`로 새로 가져오므로, `estimate_price`의
  이름만 바꾸면 **실제 카카오 API로 새어 나간다**(실제로 겪었다).

### ⚠️ 비교 기준은 '현실적 체결가'다

무작위로 뽑은 국토부 거래는 **일반 매매**이지 경매 낙찰 후 되판 건이 아니다.
8-2절 경매용 매도가와 대면 구조적으로 낮게 나와서 "계산기가 과소추정한다"는
**잘못된 결론**이 나온다. 그래서 8절 **현실적 체결가(median)**와 비교한다.

### 보는 지표

| 지표 | 왜 보나 |
|---|---|
| MAPE·중앙 절대 오차 | 얼마나 틀리나. 중앙값을 같이 보는 건 이상치 한 건에 안 흔들리려고 |
| **편향(부호 살린 평균 오차)** | 체계적으로 높게/낮게 부르는지. 절대 오차만 보면 "높게 부르는 도구"와 "낮게 부르는 도구"가 구별이 안 된다 |
| ±5%/±10%/±20% 적중률 | 실무에서 쓸 만한 수준인지 |
| **실제가가 p25~p75 안에 든 비율** | 이 도구는 단일 숫자가 아니라 **구간**을 제시하므로, 사실 이게 제일 중요한 지표다 |
| **신뢰도 점수 구간별 오차** | 7절 시세 신뢰도가 **실제로 정확도를 예측하는지** 검증. 신뢰도 높은 쪽이 더 정확하지 않으면 산식을 손봐야 한다 |

### 설계 메모

- **국토부 API를 부르지 않는다** — `data/raw/`에 이미 받아둔 XML만 읽는다
  (2절 샌드박스 제약과 무관하게, 백테스트는 과거 데이터만 있으면 된다).
  카카오 지오코딩은 필요하지만 캐시가 있어 **같은 구를 반복해서 돌리면 호출이
  거의 안 나간다** — `--gu`로 한 구씩 돌리는 걸 권장한다.
- `this_year`/`this_month`에 **대상 거래의 계약 시점**을 넘긴다. 오늘 날짜를
  넘기면 7절 최근성 가중치가 "지금 기준"으로 계산돼서 그 시점 시뮬레이션이
  깨진다.
- 비교거래가 `MIN_COMPARABLES`(3)건 미만이면 그 건은 **건너뛰고 사유를
  집계한다**. 1건짜리 추정까지 통계에 넣으면 도구의 실력이 아니라 데이터
  부족을 재는 꼴이 된다. **건너뛴 비율 자체가 결과다**("이 지역은 애초에
  N%가 추정 불가").
- 대상 선정도 12/13/15/30절과 같은 원칙 — **시스템 날짜가 아니라 데이터
  안에서 가장 최근 계약월**을 기준으로 "최근 N개월"을 센다(신고기한 30일).
  `cdealType`이 "해제"인 거래는 대상에서 뺀다.
- `--seed`로 같은 표본을 재현할 수 있다 — 계산 로직을 고친 뒤 **같은 표본에
  다시 돌려서 좋아졌는지 비교**하는 게 이 도구의 진짜 용도다.
- `--csv`로 누적하면 지역·면적대·연식별 편향까지 볼 수 있다.

### 48-1. 웹 백테스트 페이지 (`/backtest`)

⚠️ **사용자가 "왕초보라 터미널을 못 쓴다"고 해서 만들었다.** CLI 백테스트는
파이썬 설치·저장소 클론·키 설정·명령어 입력이 전부 필요한데, 이 사용자는
그동안 배포된 웹만 써왔다. 이미 키가 들어가 있는 그 사이트에 페이지를 하나
더 붙이는 게 압도적으로 쉬운 길이다.

- **CLI와 같은 함수를 그대로 부른다** — `backtest.pick_targets()` /
  `estimate_as_of()` / `summarize()`. 누출 차단 로직이 **한 곳에만** 있어야
  웹과 CLI 결과가 갈리지 않는다.
- 데이터는 `data/raw`가 아니라 22절 `get_trade_rows(lawd_cd, ...)`로 그때그때
  받는다(캐시 동일) — 그래서 **사전 다운로드가 필요 없다**.
- 지역은 `data/lawd_codes.md` 표를 그대로 드롭다운으로 깐다
  (`_backtest_regions()`, 시도→구 정렬).
- ⚠️ **건수 상한 `BACKTEST_MAX_CASES`(20)는 Render 무료 티어 gunicorn
  타임아웃(120초) 때문이다.** 첫 건은 비교거래 수백 개를 지오코딩해야 해서
  느리지만, 같은 구 안에서는 주소가 겹쳐 캐시가 차므로 두 번째 건부터 훨씬
  빠르다. 화면에도 "처음 돌리는 지역은 1~2분" 이라고 적는다.
- **화면 문구는 전부 초보자 말로 쓴다** — "MAPE", "p25~p75", "편향" 같은
  말을 안 쓰고 "평균 오차", "예상 구간 안에 들어온 비율", "높게/낮게 부르는
  정도"로 바꿨다. 편향은 부호를 보고 **"계산기가 평균적으로 높게/낮게 부르고
  있어요"** 라고 문장으로 풀어준다.
- 신뢰도 구간별 오차표도 같이 보여주고, 신뢰도 높은 쪽이 더 정확하면 ✅,
  아니면 ⚠️로 **판정까지 화면에서 해준다**(7절 점수 산식을 고칠 신호).
- 입력 폼 하단 `.home-note`에 이 페이지로 가는 링크를 달았다.

### 48-2. 첫 실측 결과와 매매가 보정 계수

**첫 백테스트를 사용자가 실제로 돌렸다**(경기도 고양시 덕양구, 최근 1개월,
10건, 2026-09 기준). 결과:

| 지표 | 값 |
|---|---|
| 평균 절대 오차 | 8.2% |
| ±10% 적중 | 70% |
| p25~p75 구간 적중 | 50% |
| 편향 | **+2.6%** (높게 부름) |
| 신뢰도 70점 이상 8건 | 평균 오차 6.8% |
| 신뢰도 40~69점 2건 | 평균 오차 14.0% |

- ✅ **7절 시세 신뢰도가 실제로 정확도를 예측한다**(70점 이상 6.8% vs 40~69점
  14.0%). 이 점수가 장식이 아니라는 게 처음으로 증명됐다.
- ⚠️ **편향 +2.6%는 착시다.** 한 건(`태화빌라5동`, 비교거래 3건뿐)이 -26.1%로
  크게 빗나가 플러스들을 상쇄한 결과다. **그 건을 빼면 나머지 9건 평균은
  +5.8%**. 오차가 고르게 퍼진 게 아니라 **"거의 완벽한 5건(±2.4% 이내)"과
  "크게 튄 5건"으로 갈린다** — 균일 보정으로 풀 문제가 아닐 수 있다는 뜻이다.

**`SALE_CALIBRATION_FACTOR = 0.97`** (7절 옆) — 사용자 요청("3% 정도 낮게")을
반영한 1차 보정치다. 같은 표본에 적용하면 평균 오차 8.2%→7.8%, 편향
+2.6%→-0.5%, ±10% 적중 70%→80%로 개선된다(계산으로 확인).

- **적용 범위**: `compute_scenarios()`·`compute_price_tiers()`에 `calibration`
  인자를 새로 뒀고, **매매 호출부만** 이 상수를 넘긴다(CLI 8절·웹·백테스트).
  - ⚠️ **기본값은 1.0이다** — 16절 전세가 실수로 매매 보정을 받는 걸 막기
    위해서다. 매매 실거래로만 잰 편향이라 전세에 쓰면 근거 없는 보정이 된다.
    `test_jeonse_path_does_not_get_sale_calibration`이 소스 검사로 고정한다.
  - ⚠️ **29절 가격 구간에도 같은 보정을 건다** — 안 그러면 한 화면 안에서
    8절 "현실적 체결가"와 29절 "60일 목표가"가 어긋난다.
  - ⚠️ **신뢰도 점수는 영향받지 않는다** — 세 값에 같은 배율을 곱해 스프레드
    비율이 그대로라서다(테스트로 고정).
  - **백테스트도 같은 보정을 쓴다** — 안 그러면 보정이 먹혔는지 잴 수 없다.
- ⚠️ **표본 10건·한 지역으로 잡은 값이다.** 지역이나 면적대가 바뀌면 편향도
  바뀔 수 있다. 값 하나만 고치면 되므로(1.0이면 보정 없음), 백테스트를 더
  돌린 뒤 조정한다. **다음에 볼 것**:
  - 서울 등 다른 지역에서도 같은 방향으로 높게 부르는지
  - 대형 평형(84㎡, 6억대)에서 유독 +8~10%로 몰렸는데 면적대별 편향인지
  - 비교거래가 3~10건으로 적은 건에서 오차가 큰지(`MIN_COMPARABLES`를 올릴지)

### 48-2-1. 크게 튄 5건의 원인 — 코드로 검증한 결과

48-2절이 "오차가 거의 완벽한 5건과 크게 튄 5건으로 갈린다"고만 적어둔 걸,
사용자가 "원인을 추론할 수 있냐"고 물어서 **가설을 세워 실제로 돌려봤다.**
가설 하나는 **틀린 것으로 판명됐다** — 그래서 여기 같이 적는다(안 적어두면
나중에 또 같은 가설로 돌아온다).

**✅ 확인 ① — 표본 3건이면 한 건이 답을 독점한다.**
`_replication_count()`로 실제 표 수를 찍어봤다:

| 비교거래 | 가중치 | 표 수 | 지분 |
|---|---|---|---|
| 동일건물·최근3개월·유사도95 | 2.744 | 274표 | **80.1%** |
| 일반·6개월전·유사도72 | 0.373 | 37표 | 10.8% |
| 일반·10개월전·유사도68 | 0.314 | 31표 | 9.1% |

5절 동일건물 보너스(2.0) × 7절 최근성(1.6)이 겹치면 한 건이 80%를 먹는다 —
3건짜리 표본의 "가중 중앙값"은 사실상 **그 한 건의 가격**이다.

**✅ 확인 ② — 표본 5건 미만이면 이상치 방어가 아예 안 돈다.**
`PRICE_OUTLIER_MIN_SAMPLE = 5`라, 4건 표본에 +35% 업거래를 섞어 돌려보니
**이상치로 잡힌 건수 0**이었다. 하필 ①과 겹치는 구간이라, 소표본에서는
지배력 큰 한 건이 이상치여도 그대로 통과한다.

**❌ 반증 ③ — "대형 평형 면적 쏠림 → 과대평가"는 사실이 아니다.**
"84㎡는 비교군이 작은 쪽으로 쏠려 ㎡당가 모델이 과대평가한다"는 가설을
세웠는데, 실제로 돌려보니 **7-1절 50:50 블렌딩이 그 편향을 거의 완벽히
상쇄한다**:

| | 계산기 오차 | 모델 괴리율 |
|---|---|---|
| 비교군이 작은 쪽(72~84㎡)으로 쏠림 | −0.8% | **8.7%** |
| 비교군이 대상 평형을 고르게 둘러쌈 | +0.1% | 0.0% |

→ **면적 쏠림은 가격 편향이 아니라 `model_divergence_pct`로 드러난다.**
그래서 `estimate_as_of()` 반환값과 CSV에 `divergence`를 추가했다 — 이 값
없이는 "표본이 대상과 어긋난" 상태를 사후에 구분할 방법이 아예 없다.

**⚠️ 짚어둘 것 ④ — 정답지 자체에 노이즈가 있다.**
`error_pct` 부호는 **양수 = 계산기가 높게 부름**이다. 태화빌라5동 −26.1%는
"계산기가 26% 낮게 불렀다"는 뜻인데, 이건 **그 거래가 유독 비싸게 팔렸을**
가능성(업거래·리모델링 완료·급한 매수자)도 똑같이 설명한다. 무작위로 뽑은
국토부 거래가 곧 "정답"은 아니므로, 그런 건에 계산기를 맞추면 오히려
나빠진다. **편향 보정은 개별 건이 아니라 표본 전체의 방향으로만 판단한다.**

→ **결론: 크게 튄 건의 최소 절반은 산식 문제가 아니라 소표본 문제로 보인다.**
`MIN_COMPARABLES`를 3→5로 올리면 해결될 가능성이 높지만, **10건으로 결정하지
않는다** — 48-3절 전체순회로 "비교거래 건수별 오차" 표를 보고 정한다.

### 48-2-2. 해법 시뮬레이션 — 가설이 또 뒤집혔다

48-2-1절이 "크게 튄 건의 절반은 소표본 문제로 보인다"고 적어둔 걸, 서울
순회 결과를 기다리는 동안 **실제 계산 코드를 태운 몬테카를로로 미리 재봤다**
(공식을 흉내 내지 않고 `find_comparables` + `compute_scenarios`를 그대로
호출했다). 결과는 **48-2-1절의 결론을 반박한다.**

**❌ 반증 — `MIN_COMPARABLES`를 올려도 거의 안 바뀐다.**

| 기준 | 남는 비율 | 평균 오차 | 편향 | ±10% |
|---|---|---|---|---|
| 3건 이상 (현재) | 100% | 8.9% | −1.9% | 63% |
| 5건 이상 | 94% | 8.8% | −2.0% | 63% |
| 7건 이상 | 86% | 8.6% | −2.0% | 64% |
| 10건 이상 | 75% | 8.6% | −2.1% | 64% |

표본을 25% 버려도 평균 오차가 0.3%p밖에 안 준다. **표본 수는 원인이 아니다.**

**✅ 진짜 원인 — 동일건물 보너스(×2.0)가 양날의 칼이다.**

| 경우 | 평균 오차 | 편향 | ±3% 이내 | 15% 초과 |
|---|---|---|---|---|
| 동일건물 없음 (표본 3~4) | 10.5% | −2.5% | 17% | 26% |
| 동일건물 없음 (표본 10~20) | 8.9% | −2.7% | 23% | 18% |
| **동일건물 정상 (표본 3~4)** | **6.2%** | −3.0% | 29% | **5%** |
| 동일건물 정상 (표본 10~20) | 6.9% | −3.3% | 29% | 9% |
| **동일건물 업거래 (표본 3~4)** | **25.8%** | **+25.6%** | 1% | **90%** |
| 동일건물 업거래 (표본 10~20) | 10.4% | +0.6% | 18% | 26% |

→ **48-2절이 관찰한 "거의 완벽한 5건 / 크게 튄 5건"의 쌍봉이 여기서 재현된다.**
같은 건물 직전 거래가 대표성이 있으면 ±3% 안에 못 박고(평균 6.2%), 그게
업거래면 그대로 끌려간다(평균 25.8%, 90%가 15% 초과). 표본 수는 **그 자체가
원인이 아니라 "이상한 동일건물 거래를 희석해 줄 다른 거래가 있느냐"**로만
작동한다(같은 업거래 상황에서 표본이 10~20이면 25.8% → 10.4%).

**❌ 반증 — 지분 상한·보너스 제거도 전체 평균은 못 바꾼다.**
한 건이 전체 표의 N%를 못 넘게 상한을 거는 방식을 25~50%로 바꿔가며
돌려봤는데, 현실적인 섞임(동일건물 45% · 그중 업거래 15%)에서 평균 오차는
8.5% → 8.4%로 사실상 그대로였다. `SAME_BUILDING_BONUS`를 1.0으로 없애면
오히려 8.6%로 **나빠진다** — 잘 맞던 건들까지 같이 잃기 때문이다.

**⚠️ 그래서 지금은 계수를 건드리지 않는다.** 세 가지 후보(최소 표본 상향·
지분 상한·보너스 제거) 전부 전체 정확도를 의미 있게 개선하지 못했다.
근거 없이 계수를 흔들면 48-2절 0.97처럼 **한 표본에 과적합**될 뿐이다.

**■ 오차 하한 — 이 도구가 도달할 수 있는 한계**

빌라는 같은 동네·같은 평형이라도 물건마다 값이 흩어진다(개별성). 중앙값
추정기는 **대상 물건 자신의 흩어짐을 원리적으로 맞힐 수 없다** — 그게 오차의
하한이다:

| 개별성 산포 | 이론적 최소 평균 오차 |
|---|---|
| ±6% | 4.8% |
| ±8% | 6.4% |
| **±10%** | **8.0%** |
| ±12% | 9.6% |
| ±14% | 11.2% |

⚠️ **48-2절 덕양구 실측 MAPE가 8.2%였다.** 빌라 개별성이 ±10% 근처라면
**이 계산기는 이미 하한에 붙어 있다**는 뜻이고, 가중치를 더 만져서 얻을 게
거의 없다는 말이 된다. 다만 산포(σ)를 실측한 적이 없으므로 **가정**이다 —
48-3절 순회 결과의 "신뢰도 70점 이상" 구간 MAPE가 8% 근처에서 더 안 내려가면
그게 하한의 증거다.

**■ 그래서 무엇을 고칠 수 있나**

- **편향(bias)은 고칠 수 있다** — 체계적으로 한쪽으로 쏠리는 건 계수로 잡는다
  (48-2절 0.97이 그것).
- **산포(MAPE)는 못 고친다** — 개별성에서 오는 몫은 어떤 가중치로도 안 준다.
  줄이려면 가중치가 아니라 **새 정보**(수리 상태·향·주차·방 개수)가 필요한데,
  7절에 적은 대로 국토부 실거래에는 그 필드가 아예 없다.
- 따라서 **순회 결과에서 볼 것은 MAPE가 아니라 편향의 방향과 지역별 일관성**이다.

**■ 이 시뮬레이션의 한계**
합성 데이터다. 개별성 σ=10%, 업거래 6%, 동일건물 45% 같은 숫자는 **가정이지
실측이 아니다.** 그래서 같은 질문에 실제 데이터로 답할 수 있게, 건별 결과에
`same_building_n`·`outlier_n`·`top_weight_share`를 추가하고 48-3절 화면에
"같은 건물 거래가 끼면 달라지나" · "한 건이 답을 독점하면 더 틀리나" ·
"최소 비교거래 기준을 올리면?" 세 표를 붙였다 — **순회만 돌리면 위 가설들이
실제 데이터에서 맞는지 바로 확인된다.**

### 48-3. 전체 지역 순회 (`/backtest/one` + 브라우저 루프)

48-2절 보정치 0.97은 **한 구 10건**으로 맞춘 값이라 그대로 믿을 수 없다.
사용자가 "모든 구 자동으로 돌려달라"고 해서 만든 기능 — `/backtest` 화면
맨 위 "🚀 전체 지역 한 번에 돌리기"다.

- ⚠️ **서버에서 for문으로 돌리지 않는다.** Render 무료 티어 gunicorn
  타임아웃이 120초인데 67개 구를 한 요청에 담으면 무조건 끊긴다. 그래서
  **브라우저가 구를 하나씩 순서대로 POST**하고(요청당 한 구), 결과를 쌓아
  전체 통계를 낸다. 구 사이에 300ms를 쉰다(카카오 초당 호출 제한 감안).
- `/backtest/one`은 구 하나를 돌려 **JSON으로 건별 기록을 통째로** 돌려준다.
  ⚠️ 구별 요약만 돌려주면 안 된다 — 전체 평균을 "구별 평균의 평균"으로
  구하면 구마다 건수가 달라 **틀린 값**이 나온다. 브라우저가 건 단위로
  모아서 계산해야 맞다(테스트로 고정).
- **HTML 화면과 JSON이 같은 `_run_region_backtest()`를 쓴다** — 48-1절이
  CLI와 함수를 공유한 것과 같은 이유로, 갈라지면 "화면 숫자와 순회 숫자가
  다른" 문제가 조용히 생긴다. `TestSharedCodePath`가 소스 검사로 고정한다.
- **구 하나가 실패해도 순회가 멈추지 않는다** — `_run_region_backtest()`는
  예외를 던지지 않고 `error` 키에 담아 돌려주고, JSON은 200 + `ok:false`로
  응답한다. 실패한 지역은 이름과 사유를 모아 맨 아래 따로 보여준다.
- 화면에 나오는 것(전부 브라우저에서 계산):
  | 표 | 무엇을 가리려고 |
  |---|---|
  | 한눈에 보기 | 전체 건수·평균 오차·편향·구간 적중 |
  | **비교거래가 적으면 정말 더 틀리나** | 48-2-1절 ①②의 소표본 가설 검증 — `MIN_COMPARABLES`를 올릴지 결정 |
  | 시세 신뢰도가 진짜 의미가 있나 | 7절 점수가 정확도를 예측하는지 (48-2절에서 한 구로만 확인됨) |
  | 평형대별로 다르게 틀리나 | 전역 보정이 맞는지, 평형대별로 나눠야 하는지 |
  | **두 계산 방식이 엇갈릴수록 더 틀리나** | 48-2-1절 ③ — 괴리율이 실제로 부정확의 신호인지 |
  | **같은 건물 거래가 끼면 달라지나** | 48-2-2절이 지목한 진짜 원인 — 동일건물 보너스의 양면성 |
  | **한 건이 답을 독점하면 더 틀리나** | `top_weight_share` 구간별 오차 — 지배력이 오차를 가르는지 |
  | **최소 비교거래 기준을 올리면?** | 3/5/7/10건 기준의 맞바꿈(오차↓ vs 커버리지↓)을 한 표로 |
  | 가장 크게 빗나간 15건 | 비교거래 수·같은건물·최대지분·신뢰도·괴리율을 같이 깔아 공통점을 눈으로 |
  | 지역별 결과 | **지역마다 편향 방향이 제각각이면 전역 보정은 답이 아니다** |
- CSV 내려받기도 붙였다(BOM 포함 — 안 붙이면 엑셀에서 한글이 깨진다).
- **기본 선택은 "전체"가 아니라 첫 시/도다.** 전체(67개 구)는 1시간 넘게
  돌고 API 한도에 걸릴 위험이 있어, 일부러 한 번 더 고르게 만들었다.
  구당 기본 건수도 단건 화면(10)보다 낮은 `BACKTEST_SWEEP_CASES`(6)다 —
  구가 많아 총 표본은 오히려 훨씬 크다.
- ⚠️ **API 한도가 진짜 제약이다.** 국토부는 구당 약 24번(2년치 월별) ×
  67개 구 ≈ 1,600번 — **개발계정 일일 한도(보통 1,000건)를 넘길 수 있다.**
  화면에도 "한도를 넘으면 그 뒤 구들이 줄줄이 실패로 뜨니 다음 날 이어서
  돌리라"고 적어뒀다. 서울 → 경기 순으로 나눠 도는 걸 권한다.
- ⚠️ **검증은 가짜 데이터로만 했다** — 이 환경에서는 국토부·카카오가
  `connect_rejected`(조직 정책)로 막혀 있어 실제 순회를 못 돌린다. 대신
  Flask 테스트 클라이언트 + Playwright(크로미움)로 **39개 구·226건을 끝까지
  순회**시켜 루프·집계·표·CSV·모바일 레이아웃·JS 오류 없음까지 확인했다.

⚠️ **테스트 오염 함정(또 겪었다).** `test_backtest_web.py`가 **단독
실행하면 통과하는데 `discover`에서만 깨졌다.** 원인은 48절에 이미 적어둔
그것 — 대상 물건 주소는 `backtest`가 import해 둔 이름(`bt.geocode`)으로,
비교거래는 `find_comparables`가 매번 새로 가져오는 모듈 속성(`geo.geocode`)
으로 나간다. **둘 다 막아야 하고**, 하나만 막으면 실행 순서에 따라 실제
카카오 API로 새어 나간다.

### 48-4. 첫 대규모 실측이 잡아낸 버그 두 개 (⚠️ 계산기 본체 문제였다)

사용자가 경기도 19개 구 101건을 순회해 CSV를 보내줬다. **정확도를 재려고
만든 도구가 정확도가 아니라 버그 두 개를 잡았다** — 둘 다 백테스트만의
문제가 아니라 **실제 사용자가 쓰는 계산기 본체의 문제**였다.

#### ① 아파트 데이터가 빌라 조회로 새어 들어왔다 (전역 변수 경쟁 조건)

CSV 101건 중 **80건이 빌라가 아니라 아파트였다.** 46층·26억·137㎡짜리가
"연립다세대"로 들어와 있었다(단지명이 전부 `(단지명없음)` — 빌라 API의
`mhouseNm`이 아니라 아파트 API의 `aptNm`으로 와서 이름을 못 찾은 것이다).
지역별로 딱 갈렸다 — 고양~성남 14개 구는 100% 아파트, 수원~안산 5개 구는
100% 빌라.

**원인**: `molit_apt_api.fetch_apt_trade()`가 요청 로직을 재사용하려고
**전역 `molit_rhtrade_api.BASE_URL`을 아파트 엔드포인트로 갈았다가
`finally`로 되돌리는** 방식이었다. 22절 병렬 조회(`FETCH_WORKERS`=8)에서
스레드가 겹치면:
1. 동시에 도는 **빌라 요청이 아파트 엔드포인트로 나가고**,
2. 스레드들의 save/restore가 엇갈려 **전역이 아파트로 고착**된다 — 그러면
   그 gunicorn 워커 프로세스의 **이후 모든 빌라 조회가 아파트를 받아온다.**

**실측 재현**(`urlopen`을 가짜로 갈아 어느 엔드포인트로 갔는지만 기록):

| 재현 | 결과 |
|---|---|
| 빌라·아파트 40건 동시 조회 | 빌라 20건 중 **20건 전부** 아파트 엔드포인트로 샘 |
| 아파트만 40건 병렬 조회 후 전역 상태 | **아파트로 고착됨** |
| 고착된 상태에서 빌라 10건 조회 | **10건 전부** 아파트 |

**고친 방법**: 전역을 건드리지 않고 **엔드포인트를 인자로 넘긴다**
(`fetch_rhtrade(..., base_url=...)` / `fetch_all_pages(..., base_url=...)`).
`test_endpoint_isolation.py`가 위 세 가지를 그대로 회귀 테스트로 고정한다
(옛 코드로 되돌리면 3개가 실패하는 것까지 확인했다).

**같이 넣은 안전장치 두 개** — 이 사고가 오래 안 드러난 이유가 "틀린 데이터가
그럴듯해 보였다"는 것이라서, 다시 생기면 **조용히 틀리지 말고 시끄럽게
실패**하게 했다:
- `data_source.looks_like_apartment_rows()` — 빌라 응답에 `aptNm`이 있으면
  `RuntimeError`. 두 API의 단지명 필드가 달라서 오탐이 없는 판정이다.
- 캐시 폴더를 `trade` → **`trade-v2`**(`TRADE_CACHE_DIR`)로 바꿨다. 서버에
  이미 저장된 **오염된 캐시 파일을 읽지 않고 새로 받게** 하는 가장 단순한
  방법이다(무료 티어는 재배포 때 캐시가 날아가지만 보장은 아니다).

⚠️ **파급**: 40절(인근 아파트 대비)을 계산하는 모든 `/estimate` 요청이
같은 프로세스의 빌라 조회를 오염시킬 수 있었다. 즉 **실사용자가 받아본
매도가 중 일부는 아파트 실거래로 계산된 값이었을 수 있다.** 언제부터인지는
알 수 없다(40절 도입 이후 전 기간).

#### ② 반지하 필터가 한쪽 방향으로만 걸려 있었다

오염을 걷어낸 **진짜 빌라 21건**만 보니 반지하 4건이 유독 튀었다:

| | 표본 | 평균 오차 | 편향 |
|---|---|---|---|
| 반지하(층 ≤ 0) | 4건 | **42.6%** | **+32.6%** |
| 지상층 | 17건 | 11.4% | −5.0% |

최악은 `현대빌라1동` 36.5㎡ 반지하 — **실제 0.95억인데 1.85억으로 추정
(+94.8%)**. 정확히 두 배다. 5절이 "반지하는 지상층의 절반 수준"이라고 적어둔
그 현상이다. 원인과 수정은 5절 반지하 항목 참고.

#### ③ 그래서 `SALE_CALIBRATION_FACTOR`를 0.97 → **1.0으로 되돌렸다**

48-2절이 0.97을 잡은 근거는 "고양시 덕양구 10건에서 +2.6% 높게 불렀다"였다.
**그 10건이 아파트였다.** 이 CSV에서 고양시 덕양구 6건이 전부
`(단지명없음)`인 것으로 확인된다 — 같은 오염이다.

오염을 걷어낸 빌라 표본에서는 방향이 **반대**였다:
- 지상층 빌라 17건 편향 **−5.0%** (계산기가 낮게 부름)
- 0.97 보정을 빼고 환산하면 약 **−2.1%**

즉 0.97은 **이미 낮게 부르고 있는 값을 3% 더 낮추고 있었다.** 되돌리는 게
맞다. 다시 켜려면 위 두 버그를 고친 뒤의 깨끗한 빌라 표본으로 다시 재야 한다.

#### ④ 이 사건에서 배운 것

- **48-2-1절·48-2-2절의 분석은 전부 아파트 데이터 위에서 한 것이라 무효다.**
  "동일건물 보너스가 오차를 가른다"는 결론도 빌라에서 다시 확인해야 한다.
- 합성 데이터 시뮬레이션으로는 이 두 버그를 **하나도 못 잡았다.** 가짜
  데이터는 항상 우리가 의도한 모양으로 나오기 때문이다 — 실측 한 번이
  시뮬레이션 열 번보다 낫다.
- **백테스트의 진짜 가치는 정확도 숫자가 아니라 "데이터가 제정신인지"
  보여주는 것**이었다. 그래서 순회 화면에 단지명·층 분포가 이상한지
  경고하는 장치를 앞으로 더 붙일 만하다.

### 48-5. 화면에 배포 버전 찍기 (⚠️ 실제로 옛 코드로 27분을 돌렸다)

48-4절 수정을 푸시한 직후, 사용자가 64개 구·179건 순회를 **27분** 돌렸는데
결과에 두 버그가 **그대로 남아 있었다** — 분당구 `(단지명없음)` 21층 10.1억
(아파트), `현대빌라1동` 반지하 +94.8%(정확히 직전 실행과 같은 값). 즉 **고치기
전 코드로 돌린 것**인데, 화면만 보고는 그걸 알 방법이 아예 없었다.

- `webapp/app.py`의 `_deploy_version()`이 Render가 배포 때 넣어주는
  `RENDER_GIT_COMMIT` 앞 7자리를 읽고, `context_processor`로 모든 화면
  푸터에 "버전 abc1234"로 찍는다. 환경변수가 없으면(로컬 실행) `local`.
- **결과를 보내줄 때 이 값도 같이 보면 "어느 코드로 나온 숫자인지" 바로
  갈린다.** 백테스트처럼 오래 걸리는 작업은 시작 전에 확인하는 게 맞다.
- ⚠️ **입력 페이지(`/`)에는 한동안 안 보였다.** 46절에서 면책 문구 중복을
  없애려고 `index.html`이 `base.html`의 footer 블록을 **빈 값으로 덮어썼는데**,
  버전 표시도 그 안에 있어서 같이 사라졌다 — **백테스트를 돌리기 직전에
  확인해야 할 값인데 정작 시작 화면에서만 안 보이는** 상태였다(사용자가
  실제로 못 찾아서 드러났다). `index.html`에 `.home-version` 한 줄을 따로
  뒀다.
- ⚠️ **Render 자동 배포가 꺼져 있을 수 있다.** 푸시해도 안 올라가면 배포
  로그의 TRIGGER가 `Manually deployed by you via Dashboard`로 찍힌다 —
  그때는 `Manual Deploy → Deploy latest commit`을 누르거나
  Settings에서 Auto-Deploy를 켜야 한다.
- 사용자는 `git pull`을 직접 칠 필요가 없다 — Render Auto-Deploy가 푸시를
  받아 알아서 올린다. (사용자가 PowerShell에서 `git pull`을 쳤다가
  `fatal: not a git repository`를 본 적이 있다. 저장소를 받아둔 적이 없으니
  당연한 결과이고, 애초에 **할 필요가 없는 일**이다.)

### 48-6. API 일일 한도를 실제로 넘겼다 (경기 39 + 서울 5까지만 성공)

48-3절이 "한도에 걸릴 수 있다"고 경고했던 것이 **그대로 일어났다.** 64개
구를 도는 동안:

| 결과 | 지역 수 |
|---|---|
| 성공 | 40개 구 (경기 35 + 서울 5) |
| 실패 — "오류 N건"(건별 예외) | 19개 구 (전부 서울) |
| 실패 — "통신 실패" | 3개 구 |
| 실패 — "데이터 못 찾음" | 2개 구 (부천시·화성시) |

- 지역 목록이 **시/도 → 구 가나다순**이라 경기 39개가 먼저 다 돌고 서울이
  마지막이었다. 서울은 강남·강동·강북·강서·관악(앞 5개)까지만 성공하고
  **그 뒤 19개가 전부 "오류"** — 카카오 지오코딩 한도가 그 지점에서
  소진됐다는 뜻이다(`geocode()` 예외 → 건별 "오류"로 집계).
- ⚠️ **그래서 "전체"로 한 번에 도는 건 사실상 불가능하다.** 화면 기본값이
  전체가 아닌 이유가 이것이고, 앞으로도 **서울 → (다음 날) 경기**처럼
  나눠 도는 걸 안내한다.
- 부천시·화성시가 "데이터 못 찾음"으로 나온 건 한도 때문인지 LAWD 코드
  문제인지 아직 못 가렸다 — 한도가 리셋된 뒤 그 두 구만 따로 돌려보면 갈린다.

### 48-7. 카카오 실패를 영구 캐시하던 버그 (⚠️ 계산기 본체 문제)

48-6절에서 "한도가 몇이냐"를 파다가 `scripts/geocode.py`에서 찾았다.
**세 번째로 나온 "조용히 틀리는" 버그다.**

```python
except (HTTPError, URLError, json.JSONDecodeError):
    cache[address] = None      # ← 일시적 실패를 영구 저장
    return None
```

카카오 일일 할당량이 소진되면 `HTTPError`가 난다 — 그러면 **그 순간 조회하던
주소가 전부 "지오코딩 불가"로 `data/geocode_cache.json`에 굳어버린다.**
다음 날 할당량이 돌아와도 캐시가 먼저 걸려 **다시 물어보지 않으므로 그
주소들은 영영 비교거래에서 빠진다.**

⚠️ **화면으로는 절대 못 알아챈다** — 그냥 "비교거래가 좀 적네"로 보인다.
48-4절 두 버그와 같은 성격이다(틀린 값이 그럴듯해 보인다).

- **고친 방법**: `HTTPError`/`URLError`/`JSONDecodeError`는 **캐시하지 않고**
  그냥 `None`만 돌려준다 — 다음 호출 때 다시 시도한다. 카카오가 정상
  응답했는데 결과가 0건인 경우(`documents`가 빈 배열)만 캐시한다. 그건
  주소 자체의 문제라 다시 물어봐야 소용없기 때문이다.
- `geocode()`와 `geocode_full()` 둘 다 같은 버그였다. `nearby_place()`는
  원래부터 실패를 캐시하지 않아 멀쩡했다.
- `TestTransientGeocodeFailureIsNotCached`가 고정한다(옛 동작으로 되돌리면
  3개가 실패하는 것까지 확인했다).
- ⚠️ **파급**: 백테스트만이 아니라 **일반 사용자 계산에도 영향이 있었다.**
  누군가 한도 소진 시점에 조회한 지역은 그 뒤로 계속 비교거래가 부족한 채로
  나온다. 서버의 `data/geocode_cache.json`에 이미 굳은 항목은 이 수정으로
  지워지지 않으므로, 의심되면 그 파일을 지우거나 재배포로 초기화해야 한다
  (무료 티어는 재배포 때 날아간다).

**덤 — 순회 화면의 "오류"가 이제 정체를 밝힌다.** 48-6절에서 서울 19개 구가
전부 `오류 6건`으로 죽었는데 **무엇이 터졌는지 알 방법이 전혀 없었다.**
`_run_region_backtest()`가 이제 `오류(KeyError: 'documents')`처럼 예외 종류와
메시지 앞 60자를 사유로 남긴다 — 다음에 같은 일이 나면 화면만 보고 원인을
좁힐 수 있다.

**⚠️ 한도 실측 정정 (운영계정 신청 화면에서 확인)**

사용자가 운영계정 신청 화면을 보여줘서 **아래 "개발계정은 흔히 일 1,000건"
추측이 틀렸다는 게 드러났다.** 실제 화면:

```
신청유형  : 운영계정 | 활용신청
심의여부  : 자동승인          ← 심사 대기 없음
활용기간  : 승인일로부터 24개월
상세기능  : 연립다세대 매매 실거래가 공개 자료 — 일일 트래픽 100000
```

- **운영계정은 일 10만 건이고 자동승인이다** — 신청과 동시에 쓸 수 있다.
  내가 "심사 1~3영업일"이라고 한 것도 이 API에는 해당하지 않는다.
- **API마다 따로 신청해야 한다.** 이 프로젝트가 쓰는 건 4개 —
  `RTMSDataSvcRHTrade`(연립다세대 매매, 핵심) ·
  `RTMSDataSvcAptTrade`(아파트 매매, 40절) ·
  `BldRgstHubService`(건축물대장, 20절) ·
  `RTMSDataSvcRHRent`(연립다세대 전월세, 16절 CLI 전용).
- ⚠️ **그래서 429는 일일 한도가 거의 확실히 아니다.** 59절에서 "429는 초당
  제한일 가능성이 크다"고 정정한 판단이 이 숫자로 뒷받침된다 — 하루 10만
  건을 쓸 수 있는 계정이 수백 번 만에 한도를 넘길 리 없다.
  **남은 용의자는 우리 쪽 호출 속도다**(`FETCH_WORKERS`=8 병렬 + 순회가
  지역을 0.3초 간격으로 이어 도는 구조).
- ⚠️ 100,000은 **그 상세기능 하나**의 값이다. API마다 다를 수 있으니 각
  신청 화면에서 확인한다. "최대 신청 가능 트래픽 이상 입력시 최대
  트래픽으로 자동 세팅"된다고 화면에 적혀 있다.

**API 한도 실측 메모** (2026-09-23 확인, ⚠️ 위 정정을 먼저 읽을 것):
- **카카오 로컬**: 무료 쿼터가 API 종류별 일 10만으로 안내된다
  (developers.kakao.com 쿼터 문서). 실제 남은 양은 개발자 콘솔 → 해당 앱 →
  도구 → 쿼터에서 봐야 한다.
- **공공데이터포털(국토부)**: API·계정마다 달라 일반화할 수 없다 —
  마이페이지 → 오픈API → 개발계정 상세에서 그 API의 일일 트래픽을 본다.
  개발계정은 흔히 일 1,000건이라고 알려져 있으나 **확인 전에는 단정하지 않는다.**
- **이 도구가 실제로 쓰는 호출 수**(2026-09 기준, 캐시가 빈 상태):
  | | 국토부(구당 33개월) | 카카오(구당 대략) |
  |---|---|---|
  | 서울 25개 구 | 825번 | 수천~1만 번대 |
  | 경기 39개 구 | 1,287번 | 〃 |
  | 전체 64개 구 | **2,112번** | 〃 |
  국토부 쪽이 개발계정 한도(1,000)를 **한 번에 넘기는 규모**라, 48-6절에서
  "전체는 못 돈다"고 한 결론이 수치로도 맞는다.

### ⚠️ 아직 안 한 것

- ~~**실측 데이터로 한 번도 안 돌려봤다.**~~ → 48-2절에 첫 실측 결과
  (고양시 덕양구 10건)를 기록했다. 다만 **한 구 10건이 전부라** 48-3절
  전체순회를 돌리기 전까지는 보정치 0.97을 확정으로 보지 않는다.
- 누출을 막았을 때와 안 막았을 때 오차가 실제로 얼마나 벌어지는지도 합성
  데이터로는 못 쟀다(가짜 좌표가 표본을 성글게 만들지 못했다). 진짜 데이터로
  확인할 항목이다.
- 8-2절 경매용 매도가는 **검증할 방법이 없다** — 국토부 실거래에는 "이게
  경매 낙찰 후 되판 건인지" 표시가 없다. 사용자 본인의 낙찰·매도 기록이
  쌓여야 검증 가능하다(29절 실적 DB와 같은 이야기).

## 49. 추정 경고등 (이 값을 믿어도 되는지 화면이 말해준다)

> ⚠️ **세 경고 중 둘이 나중에 꺼졌다** — 최대지분(49-1절)과 같은 건물 1건
> (67절). 아래 실측 표는 **당시 기록**이고, 지금 실제로 뜨는 건 괴리율
> 경고와 50절·50-1절 정비구역 신호뿐이다.

48절 백테스트를 **깨끗한 빌라 데이터 44건**(경기, 48-4절 버그 수정 후)으로
돌려보니, 오차가 고르게 퍼진 게 아니라 **미리 알 수 있는 조건에 몰려
있었다.** 이 절은 그 조건을 결과 화면에 경고로 띄운다.
`scripts/estimate_price.py`의 `compute_estimate_warnings()`.

**실측 44건 — 경고 유무로 가른 성적**

| | 표본 | 평균 오차 | ±10% 적중 | 구간 적중 | 25% 넘게 틀림 |
|---|---|---|---|---|---|
| **경고 없음** | 19건 (43%) | **6.8%** | **79%** | 63% | **5%** |
| 경고 있음 | 25건 (57%) | 17.1% | 44% | 28% | 20% |

크게 틀린 7건 중 **6건이 이 경고에 걸렸다.** (나머지 한 건 `스위트홈103동`
+38.6%는 비교거래 14건·동일건물 3건·지분 25%·괴리율 0%로 **모든 지표가
멀쩡했다** — 그래서 "경고 없음"이 정확을 보장하지는 않는다고 화면에 함께 적는다.)

### 세 가지 경고

| 경고 | 조건 | 실측 근거 |
|---|---|---|
| **두 계산 방식의 답이 갈립니다** | 7-1절 괴리율 ≥ `ESTIMATE_WARN_DIVERGENCE_PCT`(3%) | 1% 미만 8.8% / 1~3% 9.4% / 3~6% 17.2% / 6%↑ **24.5%** — 단조 증가 |
| **비교거래 한 건이 결과를 좌우합니다** | 최대 지분 ≥ `ESTIMATE_WARN_TOP_SHARE_PCT`(45%) | 20~30% 8.9% / 30~45% 17.5% / 45%↑ 15.7%(구간적중 14%) |
| **같은 건물 거래가 딱 한 건뿐입니다** | `_same_building` 개수 == 1 | 0건 14.5% / **1건 18.9%** / 2~3건 11.0% / 4건↑ **3.1%**(±10% 88%) |

⚠️ **세 기준 전부 "이유를 먼저 알고 나서 데이터로 확인한 것"이다** — 숫자만
맞춰 고른 게 아니다. 특히 세 번째는 48-2-2절 몬테카를로가 *먼저* "동일건물
거래가 대표성 없으면 그대로 끌려간다"고 예측했고, 실측에서 **1건일 때가 0건
보다도 나쁘다**는 반직관적 결과까지 그대로 나왔다(견제할 같은 건물 거래 없이
동일건물 보너스 ×2.0을 한 건이 독차지하기 때문).

### ⚠️ 7절 시세 신뢰도 점수는 빌라에서 작동하지 않는다

| 신뢰도 | 표본 | 평균 오차 |
|---|---|---|
| 80점 이상 | 12건 | 10.3% |
| 70~79점 | 15건 | 14.6% |
| 60~69점 | 9건 | 14.3% |
| 60점 미만 | 8건 | 10.6% |

**단조 관계가 아니다** — 60점 미만이 70~79점보다 정확하다. `금강주택가동`은
**신뢰도 89점인데 오차 +55.2%**, `블루스카이빌`은 87점인데 −25.0%였다.

⚠️ 48-2절에서 "신뢰도가 정확도를 예측한다"고 기록했던 근거(덕양구 10건,
70점 이상 6.8% vs 40~69점 14.0%)는 **48-4절에서 드러났듯 아파트 데이터였다.**
진짜 빌라에서는 성립하지 않는다.

- **그래서 신뢰도 점수를 당장 없애지는 않았다** — 표본 44건으로 화면의 주요
  지표를 걷어내는 건 이르고, 경고등이 같은 역할을 더 잘 하기 때문이다.
  200건이 모이면 **점수 산식을 고치거나 화면에서 내린다.**

### 구현 메모

- **새로 계산하는 게 없다.** 이미 구한 값(`model_divergence_pct`,
  `_weight`, `_same_building`)만 읽는다 — API 호출도, 계산 비용도 0이다.
- `top_weight_share()`를 `estimate_price.py`로 옮겨 `backtest.py`가
  가져다 쓴다(중복 구현이 갈리면 화면과 백테스트 숫자가 어긋난다 —
  `TestTopWeightShareShared`가 소스로 고정).
- 출력 위치: 웹은 32절 종합 판단 카드 **바로 위**(숫자를 해석하기 전에
  먼저 보게), CLI는 41절 환금성 진단 바로 앞. **경고가 없으면 아무것도
  안 보인다.**
- 화면에 **두 가지를 항상 함께 적는다**: ① 경고가 떴다고 틀린 값이라는
  뜻은 아니다 ② 경고가 없다고 정확을 보장하지도 않는다(위 `스위트홈` 사례).
- ⚠️ **표본 44건으로 잡은 1차 기준이다.** 임계값 두 개가 상수로 빠져 있어
  더 모은 뒤 조정하면 된다. 다만 48-2절 `SALE_CALIBRATION_FACTOR`와 달리
  **이건 가격을 바꾸지 않는다** — 잘못 잡아도 "확인하라"는 표시가 더/덜
  뜰 뿐이라 과적합의 위험이 훨씬 작다.

### 49-1. 재실측 92건 — 경고 하나가 재현에 실패해서 껐다

49절 세 경고는 **경기 44건 한 표본**으로 잡은 기준이었다. 사용자가 표본을
바꿔 **92건**(경기 83 + 서울 9)을 다시 돌려줬고, 그 결과 **하나는 재현되고
하나는 방향이 뒤집혔다.** 이게 이 절의 존재 이유다 — 한 표본에서 보인 패턴을
확정으로 믿지 않는다(48-2절 `SALE_CALIBRATION_FACTOR` 0.97에서 이미 데인 것).

**전체 성적 (92건)**

| 지표 | 값 |
|---|---|
| 평균 오차(MAPE) | **11.0%** (중앙 7.4%) |
| 편향 | **−3.3%** (계산기가 낮게 부름) |
| ±10% 적중 | 61% |
| p25~p75 구간 적중 | 38% |
| 25% 넘게 틀림 | 11% |

편향 방향이 48-4절 깨끗한 빌라 표본(−5.0%)과 **같다** — 두 표본 연속으로
낮게 부른다. 다만 아직 보정 계수를 켜지 않는다(아래 ⚠️ 참고).

**경고 세 개를 따로 뜯어본 변별력**

| 경고 | 해당 | 아님 | 변별력 | 판정 |
|---|---|---|---|---|
| ① 괴리율 ≥3% | 14.1% (37건) | 9.0% (55건) | **+5.2%p** | ✅ 재현 |
| ② 최대지분 ≥45% | 8.7% (13건) | 11.4% (79건) | **−2.7%p** | ❌ **역방향** |
| ③ 같은건물 딱 1건 | 14.6% (24건) | 9.8% (68건) | **+4.8%p** | ✅ 재현 |

**⛔ ② 최대지분 경고를 껐다.** 92건에서 방향이 뒤집혔을 뿐 아니라, 구간별로
보면 애초에 단조 관계가 아니었다:

| 최대지분 | 44건 표본 | 92건 표본 |
|---|---|---|
| 20% 미만 | — | **14.9%** (가장 나쁨) |
| 20~30% | 8.9% | 6.5% |
| 30~45% | **17.5%** | 12.8% |
| 45% 이상 | 15.7% | 8.7% |

44건 표본에서도 **30~45%가 45% 이상보다 나빴다** — 즉 "지분이 높을수록
부정확"이 아니라 그냥 들쭉날쭉한 것을 임계값 하나로 잘못 끊은 것이다.
`top_weight_share()` 함수는 백테스트 CSV가 계속 쓰므로 남겨 두고,
`test_top_share_never_warns_anymore`가 **되살아나지 않는지** 고정한다.

**✅ ③ 같은건물 1건은 표본을 바꿔도 순서가 그대로 나왔다**

| 같은 건물 거래 | 44건 | 92건 |
|---|---|---|
| 0건 | 14.5% | 11.7% |
| **1건** | **18.9%** | **14.6%** |
| 2~3건 | 11.0% | 8.4% |
| 4건 이상 | 3.1% | 7.8% |

"1건이 0건보다 나쁘다"는 **반직관적 순서가 두 표본 모두에서** 나왔다 —
재현된 몇 안 되는 신호다.

⚠️ **다만 51절 수정(`SAME_BUILDING_MIN_COUNT = 2`)의 효과는 아직 못 쟀다.**
이 순회가 그 수정 이후 버전에서 돌았는지 확인되지 않았다. 데이터로 역추적을
시도했지만(같은 n 구간에서 sb=0 vs sb=1 최대지분 비교) 표본이 작아 갈리지
않았다 — 비교거래 6~9건 구간에서는 sb=1이 21%p 높아(보너스 켜짐 쪽) 보이고,
10건 이상에서는 차이가 없다. **다음 순회는 푸터 버전을 먼저 확인하고 돌린다**
(48-5절이 그래서 있다).

**경고등 전체 효과 (② 제거 후)**

| | 표본 | 평균 오차 | ±10% 적중 |
|---|---|---|---|
| 경고 없음 | 40건 (43%) | **7.8%** | **78%** |
| 경고 있음 | 52건 (57%) | 13.5% | 48% |

② 를 뺐는데도 갈라내는 힘이 거의 안 줄었다(7.6→7.8% / 13.1→13.5%),
경고율만 62% → 57%로 내려갔다 — **쓸모없는 경고를 뺀 게 맞다는 증거다.**

**⚠️ p25~p75 구간 적중 38%는 산식 버그가 아니다**

"구간이 너무 좁은 것 아니냐"를 의심해 실제 코드로 재봤다:

| 표본의 실제 산포 σ | 계산기 p25~p75 폭 | 정규분포 이론 IQR(1.35σ) |
|---|---|---|
| 8% | 10.6~11.3% | 10.8% |
| 12% | 15.3~15.8% | 16.2% |

**폭은 이론값과 거의 일치한다** — 가중 복제가 분포를 뭉개지도 않는다.
38%가 50%에 못 미치는 건 구간이 좁아서가 아니라 **중앙값 추정 자체에 오차가
있어서**다(MAPE 11%가 이론 하한 8%보다 큰 것과 같은 이야기, 48-2-2절).
그래서 **아무것도 고치지 않았다.** ⚠️ 대신 화면이 이 구간을 "여기서 팔린다"는
범위처럼 읽히게 두면 안 된다 — 실제로는 열에 넷만 들어온다.

**⚠️ 7절 시세 신뢰도는 이번에도 시원찮다**

| 신뢰도 | 표본 | 평균 오차 |
|---|---|---|
| 80점 이상 | 31건 | **8.4%** |
| 70~79 | 28건 | 12.8% |
| 60~69 | 13건 | 11.9% |
| 60점 미만 | 20건 | 12.0% |

49절(44건)에서는 아예 무작위였는데, 이번엔 **80점 이상만** 유의하게 낫고
그 아래로는 여전히 구별이 안 된다. 점수를 유지하되 **경고등이 주된 신호**라는
판단은 그대로다.

**다음에 볼 것**
- 작은 평형이 더 틀린다: 85㎡ 이상 3.9% · 60~85㎡ 6.7% · 40~60㎡ 13.0% ·
  40㎡ 미만 12.2%. 면적대별 보정이 필요한지, 아니면 소형이 원래 개별성이
  큰 건지 가려야 한다.
- 편향 −3.3%가 세 번째 표본에서도 같은 방향이면 그때 계수를 켠다.
- 반지하는 이번 표본에 1건뿐이라(오차 −2.2%) 5절 대칭 수정의 효과를
  못 쟀다.

### 49-2. 예측구간 — 신뢰도 점수를 대체한다

> ⚠️ **위험요인이 셋에서 둘로 줄었다**(67절 — '같은 건물 1건' 폐기)이고,
> **구간 폭도 ±10/18/25 → ±20/30/35로 넓어졌다**(66절). 아래 표의 숫자는
> 당시 기록이다 — 지금 값은 `PREDICTION_INTERVAL_PCT`를 본다.

49절·49-1절에서 **7절 시세 신뢰도 점수가 빌라에서 정확도를 예측하지 못한다**는
게 두 번 확인됐다(80점 이상만 약간 낫고 그 아래는 무작위, 89점인데 +55% 틀린
사례). 점수를 고치는 대신 **백테스트 잔차를 그대로 구간으로 돌려준다** —
"78점"이 뭔지 해석할 필요 없이 `80% 확률로 1.44~1.76억`이라고 바로 읽힌다.
`scripts/estimate_price.py`의 `count_prediction_risks()`/
`compute_prediction_interval()`.

⚠️ **기준은 8절 현실적 체결가(median)다** — 백테스트가 실제 체결가와 비교한
값이 그것이라서다. 히어로의 경매용 매도가(8-2절)에 그대로 붙이면 근거 없는
숫자가 된다(화면에도 "현실적 체결가 X.XX억 기준"이라고 명시).

**위험요인 세 가지를 세기만 한다**(새 가중치를 만들지 않는다):

| 요인 | 조건 | 92건 부트스트랩 95% 신뢰구간 | 증거 강도 |
|---|---|---|---|
| 소형 | 전용 < `PREDICTION_RISK_SMALL_AREA_SQM`(50㎡) | +8.8%p [+4.3, +13.9] | ✅ **튼튼** |
| 모델 괴리율 | ≥ `PREDICTION_RISK_DIVERGENCE_PCT`(3%) | +5.2%p [+0.2, +10.7] | ✅ 간신히 |
| 같은 건물 1건 | `_same_building` 개수 == 1 | +2.9%p [−4.2, +10.4] | ❌ **단독으론 무의미** |

⚠️ **세 번째는 단독으로 유의하지 않다.** 그런데 셋을 **함께 셀 때** 구간이
단조로 갈라져서 세 개를 다 쓴다 — 다만 하나하나를 독립된 근거로 내세우지 않는다.
⚠️ **51-2절에서 더 밀려났다** — 51-1절로 판별을 정확하게 고친 뒤 A/B로 재봐도
방향이 안 나왔다. 세 번째는 이제 **보조 지표일 뿐**이고, 실측 근거가 있는 신호는
소형·괴리율 둘이다.

**개수 → 구간 폭** (`PREDICTION_INTERVAL_PCT`):

| 위험요인 | 표본 | 실측 80% 분위수 | **채택값** |
|---|---|---|---|
| 0개 | 29건 | ±9.3% | **±10%** |
| 1개 | 37건 | ±17.4% | **±18%** |
| 2개 이상 | 26건 | ±25.0% | **±25%** |

⚠️ **실측값보다 넓게 잡았다** — 칸당 26~37건뿐이라 꼬리가 과소평가되기 쉽다.
`test_table_is_wider_than_the_measured_quantiles`가 이 관계를 고정한다.
위험 3개짜리는 실측 표본이 8건뿐이라 2개 칸에 합친다(`tier = min(risks, 2)`).

**화면 문구에 항상 두 가지를 적는다**: ① "비슷한 조건의 과거 실거래 N건 중
80%가 이 범위 안에서 팔렸다" ② "통계 모형으로 만든 신뢰구간이 아니라 이
계산기를 과거 거래에 돌려본 **실측 오차 분포**다."

**화면 배치**: 웹은 히어로 카드 가격 그리드 바로 아래 전용 블록(`.pred-band`),
히어로 배지의 `시세 신뢰도 N/100`은 **`추정 안정성 <라벨>`로 교체**했다.
CLI는 8절 산출값 바로 뒤에 `[예측구간]` 섹션.

⚠️ **신뢰도 점수 자체는 계속 계산한다** — 32절 종합 판단과 41절 "시세 판단
근거" 항목이 아직 쓴다. 화면의 **주된 신호**만 구간으로 바꿨다. 41절에서도
뺄지는 표본이 더 쌓인 뒤 따로 정한다.

#### ⚠️ 49-1절의 "두 표본에서 재현됐다"는 근거는 무효였다

49-1절이 "같은 건물 1건이 나쁜 게 44건·92건 두 표본에서 재현됐다"고 적어뒀는데,
**같은 물건을 두 번 센 것이었다.** 웹 순회는 시드가 고정이라 같은 지역을 다시
돌리면 **같은 물건이 다시 뽑힌다** — 실측으로 확인했다(경기 재순회 68건이
겹쳤고 추정값이 **100% 한 자릿수까지 동일**했다).

**덤으로 이게 다른 질문 하나를 풀었다**: 그 재순회는 배포 버전 `3485f6d`로
확실히 돌았는데 92건과 결과가 같았다 → **92건도 이미 51절 수정
(`SAME_BUILDING_MIN_COUNT = 2`)이 들어간 코드였다.** 즉 14.6%는 **수정 후**
숫자이고, 보너스를 껐는데도 1건이 여전히 0건보다 나쁘다.
⚠️ 그래서 51절이 적어둔 "1건 줄이 18.9%에서 내려가야 한다"는 검증은
**이미 끝난 것이고, 결과는 '안 풀렸다'** 이다.

**이 시드 고정에는 쓸모도 있다** — 같은 지역을 다시 돌리면 **같은 표본에 다른
코드**를 태우는 셈이라, 코드 변경 효과를 순수하게 잴 수 있는 A/B 비교가 된다.
반대로 **새 표본이 필요하면 다른 시/도를 돌려야 한다.**

## 50. 정비구역(재개발) 신호 — 탐지만 하고 가격은 안 바꾼다

48절 백테스트에서 **크게 틀린 건의 원인을 건별로 뜯어본 결과**, 오차의 절반이
"그 물건이 애초에 동네 평균이 아닌 물건"에서 나왔다. 그중 가장 극단적인 게
정비구역 빌라다.

| 실측 사례 | 면적 | 실제가 | 실제 ㎡당가 | 동네 중앙값 | 배수 | 오차 |
|---|---|---|---|---|---|---|
| 광명 `한보주택(49-46)` 1991년식 | 34㎡ | **6.5억** | 1,912만 | 680만 | **2.81배** | −48.5% |

정비구역 빌라는 **건물이 아니라 대지지분(입주권) 값으로 팔린다.** 그래서
전용면적 대비 가격이 동네 시세의 두세 배까지 뛰는데, **국토부 실거래가에는
정비구역 여부도 대지지분도 필드가 없다** — 계산기가 원리적으로 못 맞힌다.

### ⛔ 프리미엄을 가격에 더하지 않는다 (핵심 결정)

사용자가 "재개발 대지지분 프리미엄은 어떻게 넣지?"라고 물었고, **안 넣는 쪽이
맞다**고 판단했다:

- **구역 경계 한 블록 차이로 0이 된다.** 같은 동·같은 평형인데 길 하나 건너면
  프리미엄이 통째로 사라진다 — 동 단위로 근사하는 이 프로젝트 방식(40절)으로는
  구분할 수가 없다.
- **단계별로 0~수억까지 갈린다.** 추진위 → 조합설립 → 사업시행인가 → 관리처분
  순으로 프리미엄이 계단식으로 뛴다. 어느 단계인지 모르면 배수를 못 정한다.
- **틀리면 방향이 한쪽이다.** 잘못 붙이면 **전 물건이 과대평가**되고, 그건 곧
  입찰가 과다 → 손실이다. 24절 시장동향·26절 역세권·40절 아파트 대비를 전부
  "계산에 자동 반영하지 않는다"로 둔 것과 같은 원칙이다.

**대신 "있을 수 있으니 확인하라"고 알린다** — 49절 경고등의 네 번째 항목.

### 탐지 방법 (추가 API 호출 0)

`scripts/estimate_price.py`의 `detect_redevelopment_signal()`.

- **지오코딩을 하지 않는다** — 40절과 같은 이유로 같은 법정동(`umdNm`)으로
  근사한다. 이미 받아둔 `rows`만 읽으므로 비용이 0이다.
- 조건 세 가지를 **모두** 만족해야 신호로 본다:
  | 조건 | 상수 | 왜 |
  |---|---|---|
  | 지은 지 25년 이상 | `REDEV_AGE_MIN` | **신축이 비싼 건 정상이다** — 재개발 기대는 구축에 붙는다 |
  | ㎡당가가 기준선의 1.8배 이상 | `REDEV_PRICE_RATIO` | 실측 사례가 2.81배였다 |
  | 그런 거래가 2건 이상 | `REDEV_MIN_COUNT` | 한 건은 업거래·특수관계일 수 있다 |
- ⚠️ **기준선은 구 전체 ㎡당가 중앙값이다.** 동 중앙값을 쓰면 그 동이 통째로
  정비구역일 때 기준선까지 같이 올라가 **신호가 사라진다.**
- 기준선 표본이 `REDEV_MIN_BASELINE_SAMPLE`(20건) 미만이면 판단하지 않는다.
- 화면 문구는 근거를 그대로 보여준다: "○○동에 지은 지 오래됐는데 시세의
  2.8배까지 거래된 빌라가 3건 있어요 (예: 한보주택 34㎡ 3.06억, 1991년식)".
- ⚠️ `test_price_is_never_changed_by_the_signal`이 **입력 행을 건드리지
  않는지**, `test_warning_does_not_touch_the_scenarios`가 **8절 산출값이
  그대로인지**를 고정한다. (소스 검사로 하면 `to_amount_man` 같은 함수
  이름에 `_amount_man`이 걸려 오탐이 난다 — 실제로 겪어서 행동 검사로 바꿨다.)

### 더 정확히 하려면 (아직 안 함)

| 방법 | 필요한 것 | 상태 |
|---|---|---|
| **대지지분율** | 건축물대장 표제부의 `platArea`(대지면적). 대지지분 ≈ 대지면적 × (전용면적 ÷ 연면적), 이게 전용면적 대비 크면 재건축 가치가 크다 | ⚠️ **미착수** — 20절이 문서로 검증한 필드 목록에 `platArea`가 없다. 21절/27절처럼 **필드명을 지어내지 않는다.** 사용자가 실제 응답을 보여주면 그때 붙인다. 이미 부르는 API라 **호출은 안 늘어난다** |
| **정비구역 경계 데이터** | 서울 정비사업 정보(클린업시스템 등), 경기도 정비사업 데이터 | ⚠️ 미착수 — 24절 REB 스냅샷처럼 사용자가 파일을 받아와야 한다. 구역 안/밖을 정확히 가를 수 있어 **가장 확실한 방법** |
| **개별공시지가** | 국토부 개별공시지가 API | ⚠️ 미착수 — 대지지분 × 공시지가로 토지가치를 직접 계산할 수 있지만, 스펙을 확인한 적이 없다 |

이 중 **정비구역 경계 데이터가 붙으면 그때는 가격 반영을 다시 검토할 수
있다** — "구역 안"이라고 확신할 수 있으면 위 세 가지 반대 이유 중 첫 번째가
사라지기 때문이다. 그전까지는 경고만 한다.

### 50-1. 정비구역을 **직접** 확인하는 API (엔드포인트 확인됨)

사용자가 "정비구역 체크하는 API 없어?"라고 물어서 찾아봤다. **있다.**

**⭐ 브이월드 NED 토지이용계획속성 조회 (`getLandUseAttr`)**

```
https://api.vworld.kr/ned/data/getLandUseAttr?key=<VWORLD_KEY>&pnu=<PNU 19자리>&format=json
```

- **전국**이 대상이고, 입력이 **PNU(법정동코드+본번+부번)** 라
  20절 건축물대장에 넘기는 값(`geocode_full()`의 `b_code`/`main_address_no`/
  `sub_address_no`)을 **그대로 재사용**하면 된다 — 주소 검색을 새로 안 한다.
- 응답이 그 필지에 지정된 **지역지구를 전부** 돌려준다
  (`prposAreaDstrcCode` 코드 + `prposAreaDstrcCodeNm` 이름). `UQA…`로 시작하는
  코드가 용도지역(제1종일반주거 등)이고, **정비구역·재정비촉진지구는 그 밖의
  개별법 지역지구로 함께 실린다** — 우리가 읽는 건 이름 쪽이라 코드 접두사를
  가리지 않는다.

⚠️ **키가 MOLIT이 아니라 브이월드 키다.** 이 API는 공공데이터포털이 아니라
국가공간정보포털(NED)이 브이월드를 통해 서비스한다 — `vworld.kr`에서 무료
발급받아 **`VWORLD_API_KEY`** 환경변수로 넣는다(6절 원칙 그대로, 값은 git에
안 올린다). 키가 없으면 **호출조차 안 하고** 조용히 생략된다.

⚠️ **다음 두 개는 이게 아니다 — 사용자가 후보로 가져왔던 것들이다:**

| 후보 | 실제 내용 | 왜 아닌가 |
|---|---|---|
| 국토교통부_토지이용규제**정보**서비스 (data.go.kr 15058410) | `arLandUseInfoService` — 지역지구 코드를 주면 그 안에서 **뭘 지을 수 있는지(행위제한)** | 입력이 `areaCd`/`ucodeList`라 **"이 필지가 어느 구역인가"를 못 묻는다** |
| 국토교통부_토지이용규제**법령정보**서비스 (15057174) | 규제 **법령·조례 조항 텍스트** | 같은 이유 |
| VWorld `LT_C_LHBLPN` (토지이용계획도, LH공사) | **택지개발촉진법**상 신도시 획지 용도(주택건설용지/공공시설용지) | 근거 법부터 다르다 (우리는 도시및주거환경정비법) |
| VWorld `LT_C_UQ111~114` | 도시/관리/농림/자연환경보전 **지역** 4종 | 용도지역일 뿐 정비구역이 아니다 |
| data.go.kr 파일데이터 "토지이용계획정보" | 내용은 맞지만 **전국 필지 CSV** | 용량이 커서 무료 티어에 못 올린다 |

**서울 한정 대안**(아직 미착수): 서울 열린데이터광장 재개발·재건축 정비사업
현황(OA-2253) — 구역 목록 자체를 받을 수 있다. 전국을 커버하는 위 API가
동작하면 굳이 필요 없다.

**구현** — `scripts/land_use.py`. **검증 상태가 항목마다 다르니 섞어서
믿지 않는다:**

| 항목 | 상태 |
|---|---|
| PNU 구성 (법정동코드 10 + 필지구분 1 + 본번 4 + 부번 4 = 19자리) | ✅ 공개된 실제 PNU `1126010200100830008`과 자릿수·순서가 일치함을 테스트로 고정 |
| 엔드포인트·파라미터 (`key`/`pnu`/`format=json`) | ⚠️ **실제로 이 API를 호출해 동작하는 공개 구현체의 소스**로 확인 — 공식 기술문서는 못 봄 |
| 응답 필드명 (`prposAreaDstrcCodeNm` 등) | ⚠️ 같은 출처 |
| **실제 호출로 응답을 받아본 적** | ❌ **없음** — 이 환경은 `vworld.kr`·`data.go.kr` 접속이 막혀 있다 |

- ⚠️ **MOLIT 계열과 파라미터 이름이 다르다** — MOLIT은 `serviceKey`/`type=json`,
  브이월드는 `key`/`format=json`이다. 헷갈려 바꿔 쓰면 **조용히 전부 실패**하므로
  `TestRequestShape`가 요청 URL 모양을 고정한다.
- ⚠️ **PNU의 필지구분은 일반=1 / 산=2**인데, 20절 건축물대장의 `platGbCd`는
  **0=대지 / 1=산**이다 — 같은 `is_mountain`에서 다른 숫자가 나가는 게 정상이다.
  헷갈리기 쉬워 테스트로 고정해 뒀다.
- ⚠️ 응답을 감싸는 구조(`{"landUses": {"field": [...]}}` 등)를 실측 못 했으므로
  `parse_zone_names()`는 **어떤 깊이에 있든 아는 필드명을 가진 dict를 전부
  훑는다** — 구조를 추측하지 않는 방법이다.
- **URL·필드명이 틀려도 틀린 값이 아니라 "못 찾음"으로 드러난다**(40절과 같은
  설계). 못 찾으면 경고가 안 뜰 뿐 매도가 계산은 그대로 간다.
- `LAND_USE_API_URL` 환경변수로 엔드포인트를 **덮어쓸 수 있다** — 실제 응답을
  보고 다른 주소로 바꿔야 할 때 코드를 안 고치기 위해서다. `VWORLD_DOMAIN`을
  설정하면 `domain` 파라미터도 같이 보낸다(브이월드 키는 도메인 등록 방식을
  쓰는 경우가 있다).

**판정 쪽**(네트워크를 안 탄다) — `is_redevelopment_zone(zone_names)`:

- 키워드(`REDEV_ZONE_KEYWORDS`): 정비구역 · 재정비촉진 · 재개발 · 재건축 ·
  도시환경정비 · 주거환경개선 · 가로주택정비 · 소규모주택정비
- ⚠️ **"지구단위계획구역"은 일부러 뺐다** — 전국에 널려 있어 넣으면 거의
  모든 물건에 경고가 떠서 경고 자체가 무의미해진다.
- **직접 확인된 구역이 있으면 50절의 간접 추정 경고는 안 띄운다** — 근거가
  훨씬 강한 쪽만 보여준다(테스트로 고정).
- 가격은 여전히 안 바꾼다. 다만 구역 안이라고 **확신**할 수 있게 되므로,
  50절에 적어둔 "반대 이유 세 개" 중 첫 번째(구역 경계를 모른다)가 사라진다
  — 그때 가격 반영을 다시 검토할 수 있다.

**사용자가 해줘야 하는 것**: `vworld.kr` 가입 → 오픈API 인증키 발급 → Render
대시보드 Environment에 `VWORLD_API_KEY` 추가. 그게 끝이고, 코드 수정은 없다.
실제 응답을 한 번 보여주면 위 ⚠️ 두 줄(엔드포인트·필드명)을 ✅로 올릴 수 있다.

## 51. 동일건물 보너스는 2건 이상일 때만 (실측으로 잡은 결함)

48절 백테스트가 **방법 자체의 결함**을 하나 잡아냈다.

| 같은 건물 거래 | 표본 | 평균 오차 | 중앙 오차 |
|---|---|---|---|
| 0건 | 14건 | 14.5% | 9.2% |
| **1건** | 11건 | **18.9%** | 10.2% |
| 2~3건 | 11건 | 11.0% | 8.7% |
| 4건 이상 | 8건 | **3.1%** | **1.5%** |

**1건일 때가 0건보다 나쁘다.** 정보를 더 줬는데 결과가 나빠지는 건 데이터
문제가 아니라 **방법의 결함**이다.

**원인**: 5절 동일건물 보너스(×2.0)를 그 한 건이 견제 없이 독차지한다.
실측으로도 확인된다 — 같은 건물 1건인 케이스의 평균 최대지분이 31.5%이고
지분 40%를 넘는 게 4건인 반면, **4건 이상인 케이스는 지분 40% 초과가 0건**
이다(서로 나눠 가져서 한 건이 튀지 못한다).

**고친 방법**: `SAME_BUILDING_MIN_COUNT = 2` — **같은 건물 거래가 2건 이상일
때만** 보너스를 준다.

- **원칙**: 보너스의 목적은 "이 건물이 실제로 얼마에 팔렸는지"를 반영하는
  것인데, **한 건은 '건물 시세'가 아니라 '그 한 호실이 받은 값'** 이다.
  두 건 이상이어야 서로 평균돼 "건물 시세"라고 부를 수 있다.
- **버리는 게 아니다.** 보너스를 안 줘도 그 거래는 거리 0m라 7절 유사도의
  거리 점수를 이미 만점으로 받는다 — **이중으로 세지 않는** 것뿐이다.
- `_same_building` 플래그는 그대로 둔다 — 8-1-1절 그래프 테두리와 49절
  경고등이 쓴다.
- 구현상 보너스를 **루프 안에서 바로 곱하지 않고** 건수를 다 세고 난 뒤
  한꺼번에 적용한다(`find_comparables()` 뒷부분).
- 49절 경고 문구도 같이 고쳤다 — "가중치를 2배로 받는데"가 더 이상 사실이
  아니므로 "한 건만으로는 건물 시세로 보기 어려워 보너스를 주지 않았다"로 바꿨다.

⚠️ **이건 48-2절 `SALE_CALIBRATION_FACTOR`(0.97)와 성격이 다르다.**
0.97은 **편향 숫자 하나에 맞춘 곡선 맞추기**였지만, 이건
① 48-2-2절 몬테카를로가 **실측을 보기 전에** "동일건물 거래가 대표성 없으면
그대로 끌려간다"고 예측했고, ② 실측에서 "1건이 0건보다 나쁘다"는 **반직관적
예측까지 그대로** 나왔으며, ③ 지분 데이터로 **메커니즘이 직접 확인**됐다.
숫자를 맞추려고 고른 값이 아니다.

⚠️ **그래도 효과는 아직 실측 전이다.** 다음 백테스트에서 볼 것:
**"같은 건물 거래가 끼면 달라지나" 표의 1건 줄** — 18.9%에서 내려가야 맞다.
안 내려가면 이 수정은 되돌린다. 4건 이상(3.1%)이 나빠지지 않는지도 같이 본다.

### 51-1. 같은 건물 판별을 좌표 20m → 지번으로

51절이 "같은 건물 1건일 때가 0건보다 나쁘다"를 보너스 독점으로 설명했는데,
**보너스를 껐는데도 1건이 여전히 제일 나빴다**(49-2절에서 확인). 그래서 남은
설명은 하나다 — **판별 자체가 틀렸다.**

`SAME_BUILDING_DISTANCE_M`(20m)은 빌라 밀집지에서 **바로 옆 동을 같은 건물로
잡는다.** 그러면 "같은 건물이 딱 한 건"은 사실 *남의 건물 한 건이 동일건물
보너스를 독차지하는* 상태다. 설계 결함이 맞다.

**고친 방법** — `동 이름 + 지번`으로 판별한다(`building_identity()`):

| | 값 |
|---|---|
| 실거래 행 | `umdNm` + `jibun` |
| 대상 물건 | 카카오 지오코딩이 이미 준 `main_no`/`sub_no`/`is_mountain` (`geocode_full()`) |

**추가 API 호출이 0이다** — 20절 건축물대장에 넘기는 값을 그대로 재사용한다.

- `normalize_jibun()`이 표기 흔들림을 흡수한다: `'123-4'` / `'0123-0004'` /
  `'123'` → 전부 같은 키. 5절이 애초에 지번 비교를 포기했던 이유가 이거였는데,
  숫자만 뽑아 정수로 만들면 안정적으로 잡힌다.
- ⚠️ **`'산 123-4'`와 `'123-4'`는 다른 필지다** — 같은 건물로 보면 안 된다
  (테스트로 고정).
- ⚠️ **동 이름을 반드시 함께 넣는다** — 지번 번호는 동마다 반복된다.
- **대상 물건의 지번을 못 만들면 `None`이라 예전 20m 폴백으로 돌아간다** —
  회귀가 없다(`test_distance_fallback_marks_everything`).

**연결한 곳**: CLI 8절 · 웹 `/estimate` · 백테스트(대상이 실거래 행이라 지번이
그대로 있다).
⚠️ **16절 전세·23-1절 전환율 경로에는 아직 안 넣었다** — 거기는 폴백(20m)이
그대로 돈다. 매매만큼 근거가 쌓이지 않아 미뤘고, 넣으려면 같은 인자를 한 단계
더 내려보내면 된다.

⚠️ **이게 오차를 줄인다는 보장은 없다.** 49-2절 부트스트랩에서 "같은 건물 1건"
신호 자체가 단독으로는 유의하지 않았다(+2.9%p, 95% CI [−4.2, +10.4]).
**판별을 정확하게 만드는 것이 목적**이지 정확도 개선을 주장하는 변경이 아니다.
효과는 다음 백테스트에서 본다 — 웹 순회는 시드가 고정이라(49-2절) **같은 68건에
새 코드를 태우는 A/B 비교**가 된다.

### 51-2. A/B 실측 — 판별은 고쳐졌고, 오차는 안 줄었다

같은 경기 지역을 새 코드로 다시 돌려 **49건이 정확히 겹쳤다**(49-2절 시드 고정
덕에 성립하는 통제 비교). 결과를 있는 그대로 적는다.

**✅ 판별은 의도대로 바뀌었다** — 동일건물 판정 합계 131건 → **120건**,
7개 물건에서 줄었고 **늘어난 건 0건**이다. 지번이 다른 옆 동을 20m 규칙이 잘못
세고 있었다는 51-1절 진단이 데이터로 확인됐다(예: `몬드빌라라동` 4→1,
`광일빌라` 3→1).

**❌ 오차는 줄지 않았다 — 가중치가 실제로 움직인 3건이 전부 나빠졌다.**
7건을 갈라야 제대로 읽힌다:

| | 건수 | 왜 | 평균 절대오차 |
|---|---|---|---|
| sb 숫자만 바뀜 | 4건 | 51절 `SAME_BUILDING_MIN_COUNT=2` 때문에 **애초에 보너스가 안 걸려 있던** 건이라 가중치가 그대로 | 변화 없음 |
| **가중치가 바뀜** | **3건** | 잘못 잡던 옆 건물의 보너스를 뺀 것 | **8.0% → 13.4%** |

세 건 전부(`광일빌라` −8.7→−10.4, `몬드빌라라동` −11.9→−15.0,
`대신빌라가동나동` +3.3→+14.7) 나빠졌다. ⚠️ **n=3이라 통계가 아니다** — "나빠졌다"고
단정할 수도 없지만 "좋아졌다"고는 더더욱 못 한다.

#### ⚠️ 내가 잡았던 판정 기준이 틀렸다

51절이 "다음 순회에서 **같은 건물 1건 줄이 내려가는지** 보면 된다"고 적어뒀는데,
그 줄은 실제로 16.0% → **13.3%로 내려갔다.** 그런데 **이건 개선이 아니다** —
판별이 바뀌면서 다른 물건들이 그 칸으로 들어오고 나갔을 뿐, **같은 물건이 좋아진
게 아니다**(짝지어 보면 위 표대로 나빠졌다). 그룹 평균은 구성 변화에 오염된다.

→ **앞으로 코드 변경 효과는 그룹 평균이 아니라 짝지은 비교(paired)로만 판정한다.**

#### 그래서 어떻게 했나

- **51-1절 지번 판별은 유지한다.** 옆 건물을 같은 건물로 세는 건 그냥 틀린 것이고,
  고치는 게 맞다. 51-1절이 애초에 "정확도 개선을 주장하는 변경이 아니다"라고
  적어둔 그대로다.
- **"같은 건물 거래가 오차를 가른다"는 가설은 폐기한다.** 49-2절 부트스트랩에서
  이미 단독 유의성이 없었고(+2.9%p, 95% CI [−4.2, +10.4]), 판별을 정확하게
  고친 뒤에도 방향이 안 나왔다. 49-2절 위험요인 세 개 중 **소형·괴리율 둘만
  실측 근거가 있는 신호**이고, 같은건물 1건은 셋을 함께 셀 때의 보조 지표로만 남는다.

**덤 — 2번(7-4절 가중 분위수)의 순수 효과가 측정됐다.** 판별이 안 바뀐 42건에서
9.6% → **9.8%**(+0.2%p). 7-4절이 "정확도를 바꾸려는 변경이 아니라 버그가 생길 수
없는 구조로 바꾸는 것"이라고 적어둔 예측과 일치한다.

**전체 52건 성적**: MAPE **10.3%**(중앙 6.9%) · 편향 **−2.2%** · ±10% 적중 60% ·
구간 적중 48% · 25% 초과 10%.
⚠️ 49건이 92건 표본과 겹치므로 **독립 표본이 아니다** — "편향이 세 번째로 음수"가
아니라 "같은 표본을 다시 잰 것"이다.

**✅ 49-2절 예측구간 캘리브레이션은 잘 맞았다**:

| 위험요인 | 표본 | 실측 80% 분위 | 표에 넣은 값 |
|---|---|---|---|
| 0개 | 21건 | ±7.7% | ±10% (여유 있음) |
| 1개 | 14건 | ±18.4% | ±18% (살짝 빠듯) |
| 2개+ | 17건 | ±25.3% | ±25% (살짝 빠듯) |

⚠️ 이 표본은 구간 폭을 **정한 데 쓰인 표본과 겹치므로** 진짜 검증이 아니다 —
서울처럼 **겹치지 않는 지역**을 돌려야 독립 확인이 된다.

## 52. 500 Internal Server Error — 응답이 XML이 아닐 때

사용자의 친구가 경기도 김포시 사우동 물건을 조회하다가 **Flask 기본
`Internal Server Error` 흰 화면**을 받았다. 500은 처리되지 않은 예외라,
이 프로젝트가 여태 지켜온 "실패해도 안내로 바꿔서 보여준다"는 원칙이
한 군데 뚫려 있었다는 뜻이다.

**원인**: `molit_rhtrade_api._parse_response()`의 `ET.fromstring(raw)`.
일일 트래픽 한도를 넘기거나 점검 중이면 data.go.kr은 **HTTP 200에 HTML(또는
JSON) 오류 페이지를 실어 보낸다.** 그러면 `ET.ParseError`가 나는데 —

- 재시도 루프는 `HTTPError`/`URLError`/`RuntimeError`만 잡는다 → 안 잡힘
- `webapp/app.py`의 호출부도 `RuntimeError`만 잡았다 → 안 잡힘
- → 예외가 Flask까지 올라가 **500**

48-6절에서 이미 "한도를 실제로 넘겼다"고 기록해 둔 상황이라 충분히 나올 수
있는 경로였는데, 그때는 백테스트 화면(`오류 N건`)으로만 드러나서 일반
`/estimate` 경로가 같은 이유로 500이 된다는 걸 놓쳤다.

**고친 것 세 가지**

| | 내용 |
|---|---|
| ① 깨진 응답 | `ET.ParseError`를 `RuntimeError`로 바꾼다. **받은 내용 앞 200자를 메시지에 담는다** — 로그를 못 보는 사용자가 화면 문구만 전해줘도 원인이 좁혀진다 |
| ② 잘 닫힌 HTML | ⚠️ `<html>…</html>`은 **XML로도 파싱된다** — 예외 없이 `items` 0건이 되어 "데이터를 찾지 못했습니다"로 **조용히 틀린다**(48-4절이 지적한 바로 그 실패 방식). 루트 태그가 `html`/`body`/`error`면 오류로 처리한다 |
| ③ 마지막 안전망 | `@app.errorhandler(Exception)` — 어떤 예외가 나도 입력 폼 + 안내 문구를 렌더링하고, 예외 종류·메시지를 화면에 적는다. `HTTPException`(404 등)은 그대로 통과시킨다 |

⚠️ **①②만으로 끝내지 않은 이유**: 이번 원인은 특정했지만 500을 내는 경로는
또 생길 수 있다. ③은 "원인이 뭐든 방문자는 읽을 수 있는 문구를 본다"를
보장하는 장치다 — 다만 ③에만 기대면 진짜 버그가 안내 문구 뒤에 숨으므로,
개별 호출부는 계속 제대로 막는다(`app.logger.exception`으로 로그에는 스택을
그대로 남긴다).

⚠️ **이게 그 친구가 본 500의 원인이라고 단정하지는 않는다** — 서버 로그를
확인하지 못했다. 증상이 정확히 일치하고 재현도 되는 유력한 경로이며,
**다음에 또 나면 화면이 스스로 원인을 말해준다**는 게 이 수정의 핵심이다.

`scripts/tests/test_error_pages.py`가 고정한다 — 깨진 HTML·잘 닫힌 HTML·
JSON 세 모양이 전부 `RuntimeError`가 되는지, 정상 XML은 그대로 파싱되는지,
예상 못 한 예외에서 Flask 기본 문구(`The server encountered an internal
error`)가 **안 나오는지**, 404는 여전히 404인지.

## 53. 깨진 캐시 파일이 서비스를 영구히 멈춰 세웠다

52절 수정을 배포한 뒤 순회를 돌렸더니 **19개 지역이 전부
`오류(JSONDecodeError)`** 로 죽었다. 48-7절에서 넣어둔 "예외 종류를 사유에
적는다"가 처음으로 값을 했다 — 예전이라면 그냥 `오류 6건`이었을 것이다.

⚠️ **카카오 한도가 아니었다.** `geocode()`·`geocode_full()`·`nearby_place()`는
셋 다 이미 `json.JSONDecodeError`를 잡고 있다(48-7절). 새는 곳은 **캐시 파일을
읽는 쪽**이었다.

**원인** — 이 프로젝트의 모든 JSON 캐시가 같은 두 줄이었다:

```python
with open(path, "w", encoding="utf-8") as f:   # ← 여는 순간 파일이 비워진다
    json.dump(cache, f, ...)                   # ← 여기서 죽으면 반쪽이 남는다

with open(path) as f:
    return json.load(f)                        # ← 반쪽을 만나면 JSONDecodeError
```

`"w"`는 **여는 즉시 기존 내용을 날린다.** 그래서 쓰는 도중 프로세스가 죽거나
(Render 무료 티어의 gunicorn 워커 타임아웃·재시작 — 순회처럼 오래 도는 작업에서
실제로 일어난다) 워커 둘이 동시에 쓰면 파일이 깨진 채 남고, **그 뒤로는 읽을
때마다 영영 같은 예외**가 난다. 한 번 깨지면 저절로 낫지 않는다.

⚠️ `threading.Lock`은 **한 프로세스 안에서만** 듣는다 — gunicorn 워커가 여러 개면
아무 소용이 없다. 락을 걸어뒀다고 안심할 문제가 아니었다.

**고친 것** — `scripts/json_cache.py` 하나로 모아 6군데를 갈아끼웠다
(`geocode.py` 캐시 2쌍 + `webapp/data_source.py` 빌라·아파트 캐시):

| | 내용 |
|---|---|
| `read_json()` | 깨진 파일은 **없는 것으로 친다**. 캐시를 잃으면 느려질 뿐이지만, 못 읽는 캐시는 기능을 통째로 멈춘다. 깨진 파일은 `.corrupt`로 **치워서 다음엔 새로 만들어지게** 한다 — 그대로 두면 다음 호출도 똑같이 죽는다 |
| `write_json()` | 임시 파일에 다 쓰고 `os.replace()`로 **원자적 교체**. 죽더라도 **이전 파일이 멀쩡히 남는다**(반쪽짜리가 보이는 순간 자체가 없다). 프로세스가 여러 개여도 성립한다 |

⚠️ **고치다가 진짜 버그를 하나 심을 뻔했다.** `read_json(path, default=None)`에서
`None`을 "기본값 미지정"으로 해석해 `{}`로 바꿨는데, `data_source`는
`cached is not None`으로 **"캐시 없음"과 "캐시가 빈 목록"을 가른다** — 그대로
뒀으면 캐시가 없는데도 "조회 결과 0건"으로 읽혀 **국토부 호출을 건너뛰었다.**
전용 센티널(`_MISSING`)로 고쳤고 `TestMissingIsNotTheSameAsEmpty`가 고정한다.

`scripts/tests/test_json_cache.py`가 고정한다 — 쓰다 만 파일·빈 파일이 `{}`로
읽히는지, 깨진 파일이 `.corrupt`로 치워지는지, **쓰다가 실패해도 이전 내용이
그대로인지**, 임시 파일이 안 남는지, `default=None`이 살아남는지.

**남아 있는 것**: 서버의 `data/geocode_cache.json`이 **이미 깨져 있다면** 이
수정이 배포되는 순간 자동으로 치워지고 새로 쌓인다 — 손댈 필요 없다. 다만
**캐시가 비어 새로 채워지므로 그 지역 첫 조회는 다시 느리다**(카카오 호출이
한 번씩 나간다).

## 54. 전수 점검 — 같은 *유형*의 버그를 찾아서 한꺼번에 막았다

52절(XML 아닌 응답)·53절(깨진 캐시)을 고친 뒤 **"같은 종류가 또 있나"를
기준으로** 전 코드를 훑었다. 개별 증상이 아니라 **실패 유형**으로 찾는
게 핵심이다 — 52절은 `molit_rhtrade_api`만 고쳤는데 전월세 API에 똑같은
코드가 그대로 남아 있었다.

### ⚠️ 읽기 타임아웃은 `URLError`가 아니다 (진짜 버그)

```python
socket.timeout is TimeoutError        # True
issubclass(TimeoutError, URLError)    # False  ← 여기
issubclass(TimeoutError, OSError)     # True
```

그래서 `except (HTTPError, URLError)`로 잡던 재시도 루프가 **읽기 타임아웃을
재시도조차 못 하고** 예외를 그대로 올려보냈다. 네트워크 실패 중 가장 흔한
게 타임아웃인데 하필 그것만 못 잡고 있었던 것이다.

**48-3절 순회 화면이 증거였다** — `경기도 용인시 처인구(조회 실패: The read
operation timed out)`가 그대로 찍혔다. 48-7절에서 "예외 종류를 사유에 적는다"로
바꿔둔 게 여기서 값을 했다.

- 두 MOLIT API 재시도 루프를 **`except OSError`** 로 바꿨다(HTTPError·URLError·
  TimeoutError를 전부 덮는다).
- 카카오(`geocode`·`geocode_full`·`nearby_place`)·건축물대장·경기데이터드림·
  브이월드는 **`except (OSError, ValueError)`** — `ValueError`가
  `JSONDecodeError`와 **`UnicodeDecodeError`** 를 함께 덮는다. ⚠️
  `UnicodeDecodeError`는 `JSONDecodeError`가 **아니다**(둘 다 `ValueError`일
  뿐) — 오류 페이지가 UTF-8이 아니면 기존 코드로는 못 잡았다.

### 52절을 전월세 API에도 (쌍둥이 코드)

`molit_rhrent_api._parse_response()`에 `ET.ParseError` 방어가 없었다. 16절이
아직 웹에 없어 지금은 500이 안 나지만, **웹에 붙이는 순간 같은 사고가
재발한다.** 매매 API와 똑같이 막았다.

### 0/빈 입력에 말이 되는 예외를 둔다

퍼징으로 나온 것들 — **전부 호출부가 이미 막고 있어 지금 터지지는 않는다.**
그래도 가드를 두는 이유는, 가드가 없으면 **다음에 부르는 사람이 똑같이 밟고**
`ZeroDivisionError`만 보고는 원인을 모르기 때문이다.

| 자리 | 예전 | 지금 |
|---|---|---|
| `find_comparables(area=0)` | `ZeroDivisionError` | `ValueError("전용면적은 0보다 커야 합니다 (받은 값: 0)")` |
| `compute_scenarios([])` | 나눗셈에서 터짐 | `ValueError("비교거래가 비어 있어…")` |
| `compute_price_tiers([])` | 〃 | 〃 |
| CLI `--area 0` | argparse가 그냥 통과 | `ap.error()`로 거절 |

웹은 `area <= 0`을 폼 단계에서 이미 막고 있어 **영향 없다**(확인함).

### 훑어보고 **문제 없다고 확인한** 것

- **전역 변수를 함수 안에서 바꾸는 곳**(48-4절 아파트 오염 유형): `lawd_lookup`의
  읽기 전용 메모이제이션 하나뿐 — 두 스레드가 같이 읽어도 표를 두 번 만들 뿐
  깨지지 않는다.
- **가변 기본인자**(`def f(x=[])`): 없음.
- **남은 `open(…, "w")`**: CLI 전용 출력(CSV·HTML 리포트·`data/raw` XML)뿐이라
  서비스를 멈추지 않는다 — 53절 대상은 **읽어서 다시 쓰는 캐시**였다.
- **웹 전 경로 25가지**(정상 계산·매물 붙여넣기·임장 체크·수리상태·반지하·
  탑층·면적 0/음수/문자·주소 빈칸·반경 0/거대·미래 연도·역세권 옵션·쓰레기
  텍스트·잘못된 메서드·없는 경로): **500이 하나도 없다.** 잘못된 입력은 전부
  안내 문구로 떨어진다.

⚠️ **"버그가 없다"는 뜻이 아니다.** 확인한 건 *이번에 찾은 유형*과 *퍼징이
닿은 범위*다. 여전히 테스트가 없는 곳은 33절이 적어둔 그대로다 — 24절 시장
동향 지수, 25절 인근 동 랭킹, 그리고 **실제 API 응답을 한 번도 못 본 것들**
(21절 경기데이터드림, 27절 네이버 딥링크, 50-1절 브이월드)이다. 그건 실측
없이는 확인할 방법이 없다.

## 55. 테스트가 없던 5개 모듈 — 붙이자마자 버그 둘이 나왔다

33절이 "여기 없는 건 아직 안 커버된다는 뜻"이라고 적어둔 목록을 실제로
메웠다. `market_index`(24절) · `rank_areas`(25절) · `naver_link`(27절) ·
`broker_lookup`(21절) · `report`(14절) — 전부 테스트가 하나도 없었다.

**테스트를 쓰는 과정에서 진짜 버그 두 개가 나왔다.** 둘 다 24절 지역
매칭인데, **화면에 그럴듯한 숫자가 뜨는** 종류라 눈으로는 절대 못 잡는다.

### ⚠️ 버그 ① 서울 아닌 "중구"가 서울 생활권으로 잡혔다

`seoul_zone_from_address()`가 **구 이름만** 보고 판정했다. 그래서:

| 주소 | 예전 판정 | 화면에 뜨던 것 |
|---|---|---|
| 부산광역시 **중구** | 도심권 | **서울 도심권** 매매수급동향지수 |
| 대구·인천·대전·울산 **중구** | 도심권 | 〃 |
| 부산광역시 **강서구** | 서남권 | **서울 서남권** 지수 |

`webapp/app.py`는 권역 판정을 **먼저** 보므로, 부산 중구 물건에 "서울
도심권"이라는 라벨과 함께 **남의 동네 지수**가 그대로 떴다. 48-4절 아파트
오염과 같은 "조용히 틀리는" 유형이다.

**고친 것**: 맨 앞 시/도 토큰이 서울이 아니면 `None`. 시/도 토큰이 아예 없는
주소("강북구 수유동 468")는 예전처럼 구 이름으로 판정한다 — 서울 전용 구
이름이 대부분이라 그쪽이 더 쓸모 있다.

### ⚠️ 버그 ② 짧은 주소 표기면 24절 카드가 통째로 사라졌다

`region_from_address()`는 `SIDO_ALIAS`의 **긴 표기**("서울특별시")만 부분
문자열로 찾았다. 사용자가 흔하게 치는 `경기 김포시 사우동 1309`는 `None`이
되어 **시장동향 카드가 조용히 생략**됐다(에러도 안 난다).

**고친 것**: 새 `sido_token()`이 맨 앞 토큰을 표준 축약명으로 맞춘다. 긴
표기·짧은 표기 둘 다 받는다.

⚠️ **맨 앞 토큰만 보는 게 핵심이다** — 부분 문자열로 찾으면 `경기도 광주시`가
`광주광역시`로 잡힌다. 테스트로 고정했다.

### ⚠️ 덤 — 테스트끼리 오염시키고 있었다 (두 방향 모두)

48절이 적어둔 함정에 **쓰면서 또 빠졌고**, 반대 방향도 하나 찾았다:

| 방향 | 내용 |
|---|---|
| 나가는 오염 | 새 `broker_lookup` 테스트가 `geocode.geocode`를 갈아끼우고 **안 되돌려서**, 뒤에 도는 5개가 깨졌다 → `setUp`/`tearDown`으로 복원 |
| 들어오는 오염 | `test_endpoint_isolation`이 **단독 실행하면 깨졌다** — `test_backtest_web`이 import 시점에 `os.environ.setdefault("KAKAO_REST_API_KEY", …)`로 키를 심고 알파벳순으로 먼저 돌아서 **가려져 있었다** → 자기 `setUp`에서 직접 넣고 되돌리게 고침 |

⚠️ **그래서 이제 "전체 실행 OK"만 보지 않는다.** 16개 테스트 파일을
**하나씩 단독으로도** 돌려서 전부 통과하는 것까지 확인한다 — 순서에 기대는
테스트는 안전망이 아니라 **안전망인 척하는 것**이다.

```
# 전체
python3 -m unittest discover -s scripts/tests
# 순서 의존 확인 (파일마다 단독)
for f in scripts/tests/test_*.py; do python3 -m unittest "scripts.tests.$(basename $f .py)"; done
```

### 새로 커버한 것

| 모듈 | 주요 항목 |
|---|---|
| `market_index`(24절) | 서울 25개 구 전부 권역 매칭, 다른 광역시 오탐 차단, 긴·짧은 표기, 경기도 광주 vs 광주광역시, 추세 계산(표본 부족·상승·하락·최신월·지역 섞임), 랭킹(내림차순·최신 시점만·중간집계 제외) |
| `rank_areas`(25·13·17절) | 최신 계약월 기준 창(연말 넘김 포함), 해제 거래·면적 0·금액 파싱 실패 제외, `MIN_SAMPLE` 미만 탈락, 거래량 정렬, 순위 1부터, 평형대 경계값 |
| `naver_link`(27·27-2절) | 빌라·매매 필터 포함, **위경도 뒤바뀜 방지**, 줌 오버라이드, 한글·특수문자 퍼센트 인코딩 |
| `broker_lookup`(21절) | 같은 동+영업중만, 공백 포함 비교, 반경 밖 제외, 거리순 정렬, `MAX_CANDIDATES` 상한과 잘림 표시, 지오코딩 실패는 그 건만 건너뜀, cp949 CSV |
| `report`(14절) | 선택 섹션 없이도 렌더, 억 단위 포맷, 비교거래 표와 27-2절 링크, 빈 계절성·추이 |

**458개 통과.**

⚠️ **여전히 못 본 것**: 실제 응답을 한 번도 확인 못 한 API들(21절 경기데이터
드림, 27절 네이버 딥링크, 50-1절 브이월드). 테스트는 **우리가 가정한 형식**을
고정할 뿐이라, 실제 응답이 다르면 테스트는 통과하면서 화면만 비어 있다 —
이건 실측 말고는 방법이 없다.

## 56. 부천시가 계속 "데이터 못 찾음" — 낡은 LAWD_CD

48-6절이 "부천시·화성시가 데이터 못 찾음으로 나온 건 한도 때문인지 LAWD
코드 문제인지 아직 못 가렸다"고 남겨둔 것을 **순회 CSV로 갈랐다.**

**한도가 아니다.** 지역 목록은 가나다순인데, 부천시 바로 앞뒤인 **동두천시와
성남시 분당구가 같은 순회에서 멀쩡히 성공**했다. 즉 API가 정상일 때 부천시만
혼자 0건이었다 — `data/lawd_codes.md`의 **41190이 낡은 것**이다.

⚠️ **화성시는 아직 못 가른다** — 가나다순 맨 끝이라 한도 소진 시점과 겹친다.

### ⚠️ 그런데 **일반 계산(`/estimate`)은 부천시도 멀쩡했다**

두 경로가 LAWD_CD를 구하는 방법이 다르기 때문이다:

| 경로 | LAWD_CD를 어디서 | 행정구역 개편에 |
|---|---|---|
| `/estimate` (22절) | 사용자가 친 주소 → **카카오 b_code 앞 5자리** | ✅ 자동으로 따라간다 |
| `/backtest` 순회 (48-3절) | **`data/lawd_codes.md` 표** | ❌ 사람이 고쳐야 한다 |

그래서 **표가 낡아도 실사용자는 아무 문제가 없었고**, 순회만 막혔다.

### 고친 방법 — 코드를 추측하지 않고 **카카오에 물어본다**

2절/10절은 "LAWD_CD를 기억으로 추측하지 않는다"고 못박고 있다. 그래서 새
코드를 적어 넣는 대신, `/estimate`가 원래 쓰던 방법을 순회에도 붙였다
(`_lawd_from_kakao()`):

1. 표의 코드로 받아봤는데 **33개월이 전부 0건**이면,
2. `lawd_lookup`으로 그 코드의 지역명("경기도 부천시")을 되찾아
3. `geocode_full()`에 물어 **지금 맞는 b_code 앞 5자리**를 받고,
4. 다르면 그 코드로 **다시 받는다.** 성공하면 화면에 "표 코드(41190)로는
   0건이라 카카오가 알려준 코드(4119X)로 다시 받았다"고 밝힌다.

- 잘 되는 지역에서는 **카카오를 아예 안 부른다**(0건일 때만 탄다) — 테스트로 고정.
- 이 경로가 실패해도 조용히 넘어간다. 순회 전체가 멈추면 안 된다.
- 되살리기도 실패하면 오류 문구가 `코드 41190으로 33개월 내내 0건이었어요 —
  행정구역이 바뀌어 코드가 낡았을 수 있습니다`로 **원인을 짚어준다.**
  예전 "데이터 못 찾음"만 보고는 한도 문제와 구분이 안 됐다.

⚠️ **표는 아직 안 고쳤다.** 카카오가 실제로 어떤 코드를 주는지 확인한 뒤에
고친다 — 그게 이 프로젝트가 21절·27절·50-1절에서 지켜온 "실측 전에는
확정하지 않는다"는 원칙이다. 다음 순회 화면이 그 값을 알려준다.

`scripts/tests/test_lawd_recovery.py`가 고정한다 — 지역명으로 물어보는지,
0건일 때만 재시도하는지, 잘 되면 안 부르는지, 카카오가 죽어도 순회가 안
멈추는지, 오류 문구가 코드가 낡았다고 말하는지.

## 57. 로딩이 느린 진짜 이유 — **쓸수록 느려지는 캐시**

22절이 "느린 건 전부 네트워크 대기"라고 정리해 뒀는데, 실측해 보니 **틀렸다.**
코드 쪽에 쓸수록 나빠지는 구조가 하나 있었다.

### ⚠️ ① 지오코딩 캐시를 매번 파일에서 통째로 읽고 있었다

`geocode()`는 **호출할 때마다** 캐시 파일 전체를 읽었다 — **캐시 히트여도**.
그리고 미스면 전체를 다시 썼다(53절에서 `fsync`까지 붙여 더 느려졌다).
게다가 그게 전부 `_cache_lock` 안에서 벌어져서 **22절 병렬 8워커가 줄을 섰다**
— 병렬로 만들어 둔 의미가 없었다.

실측 (지오코딩 200회 기준, 순수 디스크 시간):

| 캐시 규모 | 히트 200회 | 미스 200회 |
|---|---|---|
| 2,000건 | 0.24초 | 1.70초 |
| 10,000건 | 1.28초 | 7.07초 |
| **30,000건** | **4.53초** | **23.43초** |

**사이트를 쓸수록 캐시가 커지고, 캐시가 커질수록 느려진다.** 순회를 몇 번
돌리면 수만 건은 금방이라 사용자가 체감한 게 이것일 가능성이 크다.

**고친 방법**: 파일은 **프로세스당 한 번만** 읽고 메모리에 들고 있는다. 쓰기는
모아서 가끔 한다(`CACHE_FLUSH_EVERY`=50건 / `CACHE_FLUSH_SECONDS`=5초,
종료 시 `atexit`으로 마저 내려씀). 캐시를 잃어봐야 느려질 뿐이라(53절과 같은
판단) 안전한 맞바꿈이다.

| 캐시 30,000건 | 전 | 후 |
|---|---|---|
| 히트 200회 | 4.53초 | **0.03초** (150배) |
| 미스 200회 | 23.43초 | **0.24초** (98배) |

- 메모리는 **경로별로** 들고 있어서 `CACHE_PATH`를 바꾸면 그 파일을 새로 읽는다
  (테스트가 경로를 갈아끼우는 걸 깨지 않게).
- 53절 보장(깨진 캐시는 없는 것으로 친다)은 그대로 유지된다 — 테스트로 고정.

### ⚡ ② 40절 아파트 조회가 빌라 조회와 **줄줄이** 돌고 있었다

`/estimate`는 빌라 실거래를 받고(국토부 ~24개월) → 비교거래를 지오코딩하고
(카카오 수십 번) → **그제서야** 아파트 실거래를 받았다(국토부 또 ~24개월).
셋 중 아파트 조회는 **앞의 둘과 아무 의존이 없다** — `lawd_cd`와 `year_min`만
있으면 되고, 그 둘은 맨 처음에 이미 정해진다.

빌라 조회 직후에 백그라운드로 던져두고 필요할 때 받도록 했다. 22절이 정해둔
"서로 의존이 없는 호출은 전부 병렬로" 방침 그대로다.

**A/B 실측**(호출당 0.35초 가짜 지연, 3회 중 최소):

| | 시간 |
|---|---|
| 미리받기 OFF (예전) | 5.35초 |
| 미리받기 ON | **4.30초** (−20%) |

- `PREFETCH_APT` 스위치로 두 동작을 모두 테스트한다 — 미리받기가 실패해도
  **아래에서 직접 부르는 폴백**이 살아 있고, **두 번 부르지 않는지**도 고정했다.

### 그래서 실제로 얼마나 빨라졌나 (요청 전체 A/B 실측)

부품별 배수가 아니라 **사용자가 기다리는 시간**으로 쟀다. 옛 코드(호출마다
파일 전체 읽기 + 아파트 순차)와 새 코드를 같은 조건에 나란히 돌린 값이다.
가정: 카카오 0.15초/회 · 국토부 0.6초/회 · 24개월 · 8워커.

**새 지역 첫 조회** (국토부·카카오를 다 기다려야 하는 최악)

| 지오코딩 캐시 규모 | 옛 코드 | 새 코드 | |
|---|---|---|---|
| 비었을 때 | 5.43초 | 4.38초 | −19% |
| 1만 건 쌓인 뒤 | 8.43초 | 4.37초 | −48% |
| 3만 건 쌓인 뒤 | **16.64초** | **4.37초** | **−74%** |

**같은 구 재방문** (국토부 월별 캐시까지 찬, 제일 흔한 상황)

| 지오코딩 캐시 규모 | 옛 코드 | 새 코드 | |
|---|---|---|---|
| 5천 건 | 2.11초 | 0.78초 | −63% |
| 2만 건 | **6.07초** | **0.78초** | **−87%** |

⚠️ **숫자보다 중요한 건 모양이다** — 새 코드는 캐시가 5천이든 3만이든
**시간이 똑같다(평평하다).** 옛 코드는 **쓸수록 느려졌다.** 고친 건 상수가
아니라 기울기다.

⚠️ 네트워크 지연은 가정값이라 절대 시간은 환경마다 다르다. **두 열의 차이**가
이 변경의 효과다.

### 순수 계산은 애초에 문제가 아니었다

실거래 3,000행을 넣고 프로파일해도 **38ms**다. 즉 남은 대기는 전부 네트워크다.

### ⚠️ 그래도 제일 느린 건 여전히 **Render 무료 티어 콜드 스타트**다

방문자가 없으면 서버가 잠들고, 다음 접속 때 깨어나느라 **첫 요청이 수십 초**
걸린다. **이건 코드로 못 고친다** — 유료 플랜으로 올리거나, 외부에서 주기적으로
깨우는 핑을 걸어야 한다.

요청 한 번에 나가는 외부 호출(새 지역, 캐시가 빈 최악):

| 구간 | 호출 수 |
|---|---|
| 국토부 (빌라 + 아파트) | 약 24개월 × 2 |
| 카카오 지오코딩 | 서로 다른 주소 약 80개 |
| 카카오 장소검색(19·34절) | 13개 |

⚠️ 지오코딩은 **같은 주소를 400번 부르지만 서로 다른 건 80개**다(8·30·40절이
같은 행을 여러 번 훑는다). 예전엔 그 320번의 중복이 **전부 파일 읽기**였고,
지금은 메모리 조회라 사실상 공짜다 — ①이 그래서 컸다.

## 60. 연결 재사용 — 호출마다 악수를 다시 하고 있었다

57절이 "남은 대기는 전부 네트워크"라고 정리했는데, **그 네트워크 안에서도
낭비가 있었다.** `urllib.request.urlopen()`은 호출마다 연결을 새로 맺는다 —
HTTPS 연결 하나를 맺으려면 요청을 보내기도 전에 왕복이 2~3번 필요하다
(TCP 악수 1번 + TLS 악수 1~2번). 매도가 계산 한 건에 국토부·카카오를 합쳐
**150번 넘게** 부르는데, 그때마다 이 악수를 처음부터 다시 쳤다.

⚠️ **거리가 멀수록 손해가 커진다.** 서버와 API가 같은 나라면 왕복이 짧아
티가 안 나지만, 지금은 서버가 미국(오리건)이고 데이터는 전부 한국이라
**악수 비용이 실제 데이터를 받는 시간보다 크다.**

**실측(로컬 서버로 거리를 흉내 냄 — 편도 왕복 150ms 가정, 160회 호출, 동시 8개)**

| | 시간 |
|---|---|
| 매번 새 연결 (예전) | 12.1초 |
| 연결 재사용 (지금) | **4.5초** |
| | **7.6초 단축 (63%)** |

⚠️ **시뮬레이션이다** — 이 환경에서 한국까지 실제로 못 닿는다. 왕복 150ms는
추정치이고, 실제 단축폭은 배포해 봐야 안다. 다만 "새 연결마다 왕복 3번이
더 든다"는 건 프로토콜 구조라 방향은 확실하다.

**구현** — `scripts/http_pool.py`가 `urlopen` 드롭인을 제공하고, 8개 호출
지점의 import 한 줄만 바꿨다(`from urllib.request import Request, urlopen`
→ `Request`만 남기고 `from http_pool import urlopen`).

- **연결은 스레드마다 따로 들고 있는다**(`threading.local`). ⚠️ 22절 병렬
  조회가 워커 8개로 도는데 `http.client`는 스레드 안전하지 않다 — 연결
  하나를 같이 쓰면 **응답이 섞인다.** 48-4절 엔드포인트 오염과 같은 종류의
  사고라 테스트로 고정했다(스레드 8개가 24번 호출 → 연결 정확히 8개).
- **서버가 쉬는 연결을 끊는 건 흔한 일이다** — 한 번은 조용히 다시 맺고
  재시도한다. 사용자에게 오류로 새면 안 된다.
- **본문을 미리 다 읽어서 들고 있는다** — 다 읽어야 그 연결을 재사용할 수
  있다. 이 프로젝트의 응답은 한 달치 XML이 100KB 안팎이라 문제없다.
- ⚠️ **4xx/5xx는 `urllib.error.HTTPError`로 그대로 올린다** — 59절이
  `e.code == 429`로 초당 제한을 가려내고 52절이 본문을 읽어 안내를 만든다.
  예외 종류가 바뀌면 그 처리들이 **조용히 안 듣는다.** 테스트로 고정.
- **`HTTP_POOL=0`이면 예전 `urlopen`으로 즉시 되돌아간다** — 문제가 생겼을
  때 코드를 되돌리지 않고 환경변수 하나로 끌 수 있어야 한다.

⚠️ **테스트는 가짜가 아니라 진짜 로컬 HTTP 서버로 한다** — 이 모듈의 존재
이유가 "연결이 진짜 재사용되는가" 하나뿐이라, 흉내로 확인하면 아무것도
검증하지 못한다. `test_http_pool.py`가 예전 방식(5회 호출 = 연결 5개)과
새 방식(연결 1개)을 **나란히** 확인한다.
  - ⚠️ 연결 수를 `id(self.connection)`으로 세면 안 된다 — 소켓 객체가
    수거되면 파이썬이 같은 메모리 주소를 재활용해서 서로 다른 연결이 하나로
    합쳐 세어진다(**실제로 5개가 2개로 나왔다**). 연결마다 핸들러가 한 번씩
    만들어지므로 그 횟수를 센다.

### 60-1. 구간별 소요시간을 화면에 찍는다

⚠️ **"로딩이 느리다"를 추측으로 고치지 않기 위한 장치다.** 서버를 옮길지
말지 같은 되돌리기 비싼 결정을(URL이 죽는다) 느낌으로 내리면 안 된다.

결과 페이지 맨 아래에 `⏱ 이번 조회 N초 (주소→좌표 N초 · 실거래 조회 N초 ·
비교거래 찾기 N초)`를 찍는다. 이 숫자로 원인이 갈린다:

| 큰 구간 | 원인 | 답 |
|---|---|---|
| 실거래 조회 | 데이터 서버까지의 거리, 또는 빈 캐시 | 리전 / 캐시 보존 |
| 비교거래 찾기 | 좌표 변환 호출 수 | 캐시 적중률 |
| 셋 다 작은데 체감이 느림 | 콜드 스타트 | 깨우기 핑 |

- `_PhaseTimer`(`webapp/app.py`)는 **감싸기만 한다** — 결과를 그대로
  돌려주고 예외도 그대로 올려보낸다.
- 0.05초 미만 구간은 숨기고, 계측 안 한 시간은 **"나머지"로 드러낸다** —
  합이 전체보다 한참 작은 줄이 찍히면 그것대로 오해를 부른다.
- ⚠️ 이 계측이 조용히 틀리면 **엉뚱한 결론(예: 리전 이전)으로 이어지므로**
  집계 규칙을 `test_phase_timer.py`로 고정했다.

**참고로 CPU는 범인이 아니다**(실측): 한 달치 XML 94KB × 24개월 파싱 56ms,
캐시 쓰기 101ms, **캐시 읽기 15ms.** Render 무료(0.1 CPU)로 10배 잡아도
캐시가 찬 조회의 처리 비용은 0.1초다.

## 61. 콜드 스타트 깨우기용 `/healthz`

무료 호스팅은 15분간 방문이 없으면 서버를 재우고, 다음 접속 때 깨어나느라
**첫 요청이 30~60초** 걸린다. 외부에서 주기적으로 찔러 주면 사라지는데,
찌를 경로가 필요하다.

- `/`는 입력 폼 전체를 렌더링해서(17.8ms) 깨우기용으로는 낭비다.
  `/healthz`는 템플릿·API·캐시를 일절 안 건드려 **2.4ms**로 끝나고,
  배포 버전을 같이 돌려줘서 **핑 로그만 봐도 어느 코드가 떠 있는지** 안다
  (48-5절 버전 표시와 같은 목적).
- ⚠️ **`/robots.txt`를 찌르면 안 된다** — 잠든 동안 호스팅 쪽이 그 경로를
  가로채 자기가 응답해서 **요청이 서버까지 오지 않고, 따라서 안 깨어난다.**
  널리 퍼진 함정이다.
- ⚠️ **무료 티어는 월 750시간인데 31일 = 744시간**이라, 깨워 두면 한 달치를
  거의 다 쓴다. 무료 서비스가 이것 하나면 딱 맞고, 하나라도 더 만들면
  둘 다 정지된다.
- ⚠️ **재배포하면 깨워 둔 게 무의미해진다** — 서버가 재시작되고 캐시
  (`webapp/cache/`, `data/geocode_cache.json`)도 같이 날아간다. 고치느라
  푸시한 직후의 첫 조회는 **캐시가 0인 최악의 상태**라, 그때 재보면
  "핑을 걸었는데도 느리다"는 결론이 나온다. 속도를 잴 때는 배포 직후를
  피한다.

## 62. 슈퍼베이스를 캐시 보관소로 (재배포해도 안 죽는 캐시)

⚠️ **먼저 못박아 둘 것: 슈퍼베이스가 이 서비스를 돌리는 게 아니다.**
슈퍼베이스는 데이터베이스(BaaS)이지 서버 호스팅이 아니고, Edge Functions는
**Deno/TypeScript 전용이라 파이썬이 아예 안 돌아간다**(공식 입장도 "파이썬이
필요하면 다른 데에 호스팅하고 슈퍼베이스는 DB로 쓰라"이다). `estimate_price.py`
하나만 5천 줄이 넘는 이 프로젝트를 옮기는 건 이식이 아니라 재작성이다.

**그래서 슈퍼베이스가 실제로 푸는 문제는 따로 있다** — 61절이 짚은 그것:
무료 호스팅은 **재배포·재시작 때마다 파일이 날아간다.** 국토부 월별 캐시
(`webapp/cache/`)와 좌표 캐시(`data/geocode_cache.json`) 수만 건이 통째로
사라져서, **57절이 잡아둔 속도 개선이 배포할 때마다 초기화됐다.** 배포 직후
첫 조회가 유난히 느린 게 이것 때문이다.

### 설계 — 앞이 아니라 **뒤**에 둔다

```
파일/메모리 캐시  →  (없으면) 슈퍼베이스  →  (없으면) 원래 API
```

- ⚠️ **57절 메모리 캐시를 대체하지 않는다.** 좌표 캐시를 메모리로 올려
  150배 빠르게 만든 걸 되돌리면 안 된다 — 슈퍼베이스는 **파일이 비었을 때
  복구해 주고, 새로 생긴 항목을 올려두는** 역할만 한다.
  `test_existing_file_is_never_overridden_by_remote`가 고정한다.
- **어느 단계가 실패하든 조용히 다음으로 넘어간다.** 슈퍼베이스가 죽어도,
  키가 없어도, 무료 플랜이 7일 무활동으로 잠들어 있어도 **매도가 계산은
  그대로 된다**(20·21·26·50-1절 원칙 그대로).
- **키가 없으면 호출이 한 번도 안 나간다** — 안 쓰는 사람에게 이 기능은
  아예 존재하지 않아야 한다(`test_no_keys_means_no_network_at_all`).
  URL만 있고 키가 없는 절반 설정도 꺼진 것으로 친다.
- **올린 키를 기억해 차이분만 보낸다** — 매번 전체를 올리면 수만 건을
  반복해서 보내게 된다.
- ⚠️ **되살린 데이터도 `_checked()`를 통과시킨다.** 48-4절 아파트 오염이
  DB에 저장돼 있었다면 복구할 때 그냥 믿는 순간 **오염이 영구히 남는다**
  (`TestRestoredDataIsStillChecked`).

### 외부 라이브러리를 쓰지 않는다

14·22절 원칙대로 `psycopg2`를 넣지 않고, 슈퍼베이스가 기본 제공하는
REST(PostgREST)를 표준 라이브러리로 부른다 — **60절 연결 재사용도 그대로
얹힌다.** `requirements.txt`는 여전히 flask·gunicorn 둘뿐이다.

- ⚠️ 쓰기는 반드시 `on_conflict=ns,k` + `Prefer: resolution=merge-duplicates`
  로 한다. 빠지면 같은 달을 받을 때마다 행이 쌓여 **무료 500MB를 금방 먹는다.**
- ⚠️ 읽기는 **페이지를 나눠 받는다.** PostgREST는 한 번에 돌려주는 행 수에
  상한이 있어서, 한 방에 다 받은 줄 알고 넘어가면 **캐시가 조용히 일부만
  복구된다.**
- 헤더는 `apikey`와 `Authorization: Bearer` **둘 다** 보낸다 — 하나만 보내면 401.

### 설정 (사용자가 할 일)

1. supabase.com에서 프로젝트 생성 — ⚠️ **리전은 서버와 같은 쪽으로.**
   서버가 미국인데 DB를 서울에 두면 **캐시를 읽을 때마다 태평양을 왕복해서
   지금보다 느려진다.** DB는 사용자 옆이 아니라 **서버 옆**에 있어야 한다.
2. SQL Editor에서 한 번 실행:
   ```sql
   create table if not exists cache_entries (
     ns text not null, k text not null, v jsonb not null,
     updated_at timestamptz not null default now(),
     primary key (ns, k)
   );
   alter table cache_entries enable row level security;
   -- 정책을 만들지 않으면 service_role 키로만 읽고 쓸 수 있다(원하는 상태다).
   ```
3. 호스팅 대시보드 환경변수에 `SUPABASE_URL`, `SUPABASE_SERVICE_KEY` 추가.

⚠️ **`SUPABASE_SERVICE_KEY`는 서버 전용이다** — 27-1절 `KAKAO_JS_KEY`(프론트
노출이 정상 사용법)와 **성격이 전혀 다르다.** 이 키가 새면 DB가 통째로
열린다. 템플릿에 이 이름이 등장하지 않는지 테스트로 고정해 뒀다.

### 한계

- **무료 플랜은 7일 무활동이면 잠든다**(깨는 데 약 30초). 매일 쓰면 문제없고,
  잠들어 있어도 캐시 복구만 건너뛴다.
- 무료 용량 500MB. 한 달치 빌라 XML이 구당 100KB 안팎이라 실제로 조회되는
  지역만 쌓이면 충분하지만, 전국을 여러 번 순회하면 좁아질 수 있다.
- **콜드 스타트는 해결하지 못한다** — 그건 61절 깨우기 핑, 또는 유료 플랜의 몫이다.

### ⚠️ 이게 유일한 선택지는 아니다

같은 문제(캐시 보존)를 호스팅의 **영구 디스크**로도 푼다. 그쪽은 **코드
수정이 0줄**이고 콜드 스타트까지 같이 해결되는 대신 유료다. 슈퍼베이스는
공짜인 대신 DB 연결·지연·7일 정지라는 새 실패 지점이 생긴다 — 그래서 위
설계가 전부 "실패해도 조용히 넘어간다"로 되어 있다.

## 63. 어디로든 옮길 수 있게 짐 싸두기

서버를 한국 가까이로 옮기자는 판단이 나왔는데, **Render에는 서울 리전이
없다**(오리건·오하이오·버지니아·프랑크푸르트·싱가포르 5곳). 한국에 진짜
서버를 두려면 다른 호스팅으로 가야 하고, 그러면 이 프로젝트가 Render에
얼마나 묶여 있는지가 문제가 된다.

**확인해 보니 묶인 건 거의 없었다** — 의존성은 `flask`·`gunicorn` 둘뿐이고
(14·22·62절이 지켜온 "외부 라이브러리 최소화" 덕이다), Render 전용 코드는
버전 표시 한 줄뿐이었다. 그 하나를 풀고 이사 준비를 해뒀다.

### ① 버전 표시를 호스팅 중립으로

⚠️ **호스팅마다 커밋 해시를 넣어주는 환경변수 이름이 다르다.** 한 곳만 보면
**서버를 옮기는 순간 버전 표시가 조용히 사라지는데**, 하필 그때가 "지금 어느
코드로 도는지"를 제일 알아야 하는 시점이다(48-5절에서 실제로 옛 코드로
27분을 돌렸다 — 그래서 이 표시를 만든 것이다).

`_VERSION_ENVS`를 순서대로 훑는다: `APP_VERSION`(직접 지정, 최후의 수단) →
`RENDER_GIT_COMMIT` → `RAILWAY_GIT_COMMIT_SHA` → `VERCEL_GIT_COMMIT_SHA` →
`FLY_IMAGE_REF` → `HEROKU_SLUG_COMMIT` → `SOURCE_VERSION` → `GIT_COMMIT` →
`COMMIT_SHA`. 빈 문자열은 건너뛴다. 어디서도 못 찾으면 `local`.

### ② `Dockerfile` + `Procfile`

⚠️ `render.yaml`만 있으면 옮길 때마다 빌드·실행 커맨드를 손으로 다시 맞춰야
하고, 그 과정에서 **`--timeout 120`이 빠지면 조용히 500이 난다**(22절에서
실제로 겪었다 — 처음 조회하는 지역은 30초를 넘긴다). 컨테이너에 박아두면
그 위험이 없다.

- **워커는 1개로 못박았다** — 캐시를 프로세스 메모리에 들고 있어서(57절)
  워커를 늘리면 각자 다른 캐시를 갖게 되고 적중률이 떨어진다. 늘리려면
  62절 슈퍼베이스 같은 공용 저장소를 먼저 붙인다.
- `.dockerignore`로 `data/raw*`·`webapp/cache`·`.git`을 뺀다(이미지가
  쓸데없이 커진다).

### 갈 수 있는 곳

| | 리전 | 배포 방식 | 관리 |
|---|---|---|---|
| **Render 싱가포르** | 싱가포르 | 지금과 동일 | 없음 |
| **클라우드타입** | **서울** | 깃허브 연동 (Render와 비슷) | 없음 |
| **AWS Lightsail 서울** | **서울** | Docker 직접 실행 | ⚠️ 직접 |

⚠️ **어디로 가든 URL이 바뀐다.** 그래서 **커스텀 도메인을 먼저 사는 것**이
이사보다 순서상 앞이다 — 내 주소를 달아 두면 그 뒤로는 호스팅을 몇 번
옮기든 링크가 안 죽는다.

⚠️ **DB(62절)는 사용자 옆이 아니라 서버 옆에 둔다.** 서버가 오리건인데 DB만
서울에 두면 한 요청에 24번 읽는 캐시가 매번 태평양을 왕복해서 **0.07초가
3.6초가 된다** — 캐시를 쓰는 이유가 통째로 뒤집힌다. 서버를 옮길 때 DB도
같이 옮긴다(캐시라서 잃어도 손해가 없다, 다시 쌓이면 그만이다).

## 64. 1층 가격대 보정 — 실측 편향을 설명한 첫 메커니즘

백테스트 74건을 면적·층·연식으로 갈라 보니, **제일 선명한 건 소형이 아니라
층**이었다.

| 층 | 표본 | MAPE | **편향** |
|---|---|---|---|
| 1층 | 15건 | 13.0% | **+7.0%** (높게 부름) |
| 2~3층 | 44건 | 10.6% | **−2.7%** (낮게 부름) |

**양쪽으로 갈린다** — 이건 "1층과 위층이 섞이고 있다"는 신호다.

### 원인은 층 점수 한 줄이었다

```python
floor_score = 100.0 if diff <= 1 else max(20.0, 100 - (diff - 1) * 20)
```

7절 층 점수는 **층 번호 차이만** 본다. `diff <= 1`이라 **1층과 2층이 만점으로
같게** 취급됐다 — 빌라에서 1층은 사생활·보안·채광 때문에 값이 따로 노는데
계산기가 그걸 전혀 몰랐다. 그래서 1층 물건은 위층 거래로 과대평가되고,
위층 물건은 1층 거래가 섞여 과소평가된다.

⚠️ **5절에 사용자가 적어둔 도메인 지식이 이미 이걸 말하고 있었다**
("층 선호순위 3층>2층>4층>5층>{1층,6층}") — 화면에서 "너무 복잡하다"고
걷어냈을 뿐, **계산에는 애초에 들어간 적이 없었다.**

### 검증 순서 (48-2절 0.97 과적합의 반성)

1. **교란 통제** — 1층 그룹이 구축에 조금 몰려 있었지만(평균 1998.6년 vs
   2005.8년), 면적·연식을 함께 넣은 회귀에서도 **1층 효과 +8.2%p**가 남았다.
2. **부트스트랩** — 1층 단독은 95%CI [−0.9, +16.6]로 0을 포함하지만,
   **1층 − 2~3층 차이는 +9.7%p [+0.4, +19.9]로 0을 넘지 않는다.**
3. **⭐ 몬테카를로가 실측을 재현했다** — "1층이 9% 싸다"만 넣고 실제 코드를
   돌리니 1층 편향 **+7.4%**, 3층 편향 **−1.4%**가 나왔다(실측 +7.0% / −2.7%).
   **가설이 관찰된 편향을 그대로 설명한다.**
4. **해법 비교** — 가중치만 손대는 건 거의 소용없었다:
   | 방법 | 1층 편향 |
   |---|---|
   | 지금 | +7.36% |
   | 유사도 페널티 −40 | +6.72% |
   | 유사도 페널티 −100(최대) | +5.50% |
   | **가격 보정** | **+0.33%** |
   1층 비교거래가 애초에 적어서, 가중치를 아무리 낮춰도 끌어내릴 표본이 없다.

### ⚠️ 계수는 실측 점추정치(9%)가 아니라 **7%**다

신뢰구간이 넓어(진짜 값이 2%일 수도 18%일 수도) **"맞았을 때 얼마나 좋아지나"가
아니라 "틀렸을 때 얼마나 나빠지나"로 골랐다** — 1층 대상 편향의 민감도:

| 적용 계수 | 최악 편향 | 평균 편향 |
|---|---|---|
| 없음(지금) | 13.9% | 5.5% |
| 3% | 10.9% | 4.1% |
| 5% | 7.9% | 3.6% |
| **7% ← 채택** | **6.3%** | **3.3%** |
| 9%(점추정치) | 6.4% | 3.5% |

**진짜 할인이 아예 없더라도 7% 보정 쪽이 지금보다 편향이 작다.**
실제 코드로 A/B 하면:

| 진짜 할인 | 보정 끔 | 보정 켬 |
|---|---|---|
| 9% (점추정치) | MAPE 9.84% · 편향 +3.22% | **8.67% · +1.14%** |
| **없음 (최악의 오적용)** | 8.56% · +0.53% | 8.89% · −1.36% (**+0.33%p만 손해**) |
| 15% (과소보정) | 12.18% · +5.73% | **9.72% · +3.08%** |

`FIRST_FLOOR_PRICE_RATIO = 0.93`. 얻는 게 크고 잃는 게 작다.

### 구현

`first_floor_price_factor()` — 대상이 1층이면 위층 거래를 `×0.93`,
대상이 위층이면 1층 거래를 `÷0.93`. **양방향 대칭이다**(5절 반지하가 한쪽만
걸려 있어 반지하 매도가가 두 배로 부푼 사고가 있었다 — `test_the_two_
directions_are_exact_inverses`가 고정).

- ⚠️ **원래 `_amount_man`(실제 체결가)은 절대 안 건드린다.** 7-2절이 만든
  `_amount_man_adjusted`를 재사용해서 계산에만 보정값을 쓴다 — "핵심 비교거래"
  목록에는 **국토부에 신고된 금액 그대로** 뜬다. 이게 이 절의 첫 번째 계약이고
  테스트 두 개가 고정한다.
- ⚠️ 7-2절이 다시 켜져도 **덮어쓰지 않고 곱한다** — 한쪽이 조용히 사라지면 안 된다.
- **반지하(층 0 이하)에는 적용하지 않는다** — 5절이 이미 양방향 하드 제외한다.
- **층을 모르면 적용하지 않는다**(5절 원칙 그대로).
- `_weighted_amounts_sorted()`/`_weighted_unit_prices_sorted()`가 보정값을
  우선 쓰므로 **8절 총액 모델과 7-1절 ㎡당가 모델 둘 다 자동 반영**된다.

### 보정이 걸리면 반드시 알린다

목록의 금액은 실제 체결가 그대로라, 안 알리면 **"표에 뜬 가격이랑 매도가가 왜
안 맞지"**로 읽힌다(7-2절이 안내 문구를 단 것과 같은 이유). 비교 기준 설명에
한 줄 붙는다 — "이 물건은 1층이라, 위층 실거래는 1층 시세대로 약 7% 낮춰서
계산했습니다(아래 목록의 금액은 신고된 실제 체결가 그대로입니다)."

### ⚠️ 아직 안 한 것 / 하지 않기로 한 것

- **4층 편향 −10.4%(n=12)** 도 보였지만 **손대지 않았다** — 표본이 작고
  메커니즘이 1층만큼 명확하지 않다. n=12에 맞추면 그게 과적합이다.
- **소형(<50㎡)은 편향이 아니라 산포였다**(표준편차 18.7% vs 14.3%,
  편향은 −4.6% vs −0.3%). 48-2-2절이 적어둔 대로 **산포는 가중치로 못 고친다**
  — 49-2절 예측구간이 이미 소형을 위험요인으로 세어 구간을 넓히고 있다.
- **효과는 다음 백테스트에서 짝지은 비교로 확인한다**(51-2절 교훈 — 그룹
  평균은 구성 변화에 오염된다). 볼 것: **1층 대상 물건들의 개별 오차**가
  +7% 근처에서 0 쪽으로 움직였는지.

## 58. HTTP 429 — 한도를 넘기고도 계속 긁고 있었다

> ⚠️ **이 절의 핵심 판단("429 = 일일 한도")은 59절에서 뒤집혔다.** 아래 ①의
> "즉시 멈춘다"는 지금 코드와 다르다 — 429는 물러섰다 재시도하고, 일일
> 한도는 `resultCode 22`로 따로 잡는다. ②(순회 중단)는 22에만 적용된다.

사용자 화면에 이게 떴다:

> 국토부 API 조회 중 문제가 발생했습니다: **API 호출 3회 실패: HTTP Error 429: Too Many Requests**

52절 안내 문구가 제 역할을 해서 500 대신 읽을 수 있는 말이 나온 건 맞는데,
**내용 자체가 두 가지 낭비를 드러냈다.**

### ⚠️ ① 429를 3번 재시도하고 있었다

429는 **일일 한도 초과**다. 하루가 지나야 풀리므로 **재시도로는 절대 안 된다.**
그런데 재시도 루프가 `OSError`로 싸잡아 잡아서 1.5초·3초 쉬며 두 번 더 긁었다
— **한 번 실패할 때마다 한도를 3배로 깎았다.** 순회처럼 수백 번 도는 작업에서
이 낭비가 그대로 곱해진다.

이제 429는 **즉시 멈춘다.** 503 같은 일시적 오류는 예전처럼 재시도한다(테스트로 구분 고정).
문구도 `"국토부 API 일일 조회 한도를 넘겼습니다(429). 재시도로는 풀리지 않고
하루가 지나야 복구됩니다 — 내일 다시 시도해 주세요."`로 바꿨다.

### ⚠️ ② 한도가 바닥났는데 순회가 남은 지역을 계속 두들겼다

48-3절 순회는 "구 하나가 실패해도 멈추지 않는다"가 설계였다. 그건 **한 지역의
데이터 문제**에는 맞지만 **계정 전체의 한도 문제**에는 정반대다 — 이미 바닥난
한도를 30개 지역 × 24개월 = 수백 번 더 긁고 **전부 실패로 끝난다.** 그러고도
다음 날 몫까지 미리 까먹는다.

`/backtest/one`이 이제 `quota_exhausted` 플래그를 함께 돌려주고, 브라우저
루프가 그걸 보면 **즉시 멈춘다.** 그때까지 모은 결과는 화면에 그대로 남고
"내일 이어서 돌리라"고 안내한다.

⚠️ **다른 실패는 예전처럼 멈추지 않는다** — "이 지역 데이터 없음"으로 순회
전체가 죽으면 안 된다(테스트로 두 경우를 갈라 고정).

### ⚠️ 왜 한도가 자꾸 바닥나는가 — 구조적인 이유

| 작업 | 국토부 호출 수 |
|---|---|
| `/estimate` 새 지역 1건 | 약 24개월 × 2(빌라+아파트) = **48번** |
| 순회 1개 지역 | 약 24번 (아파트는 안 쓴다) |
| 순회 경기 39개 구 | 약 **940번** |

개발계정 일일 한도가 흔히 1,000건이라고 알려져 있으니 **순회 한 번이 하루치를
거의 다 쓴다.** 48-6절에서 이미 겪었고 48-7절에 호출 수 표까지 적어뒀는데,
**그때는 "나눠 돌려라"는 안내만 하고 코드가 한도를 아끼게 만들지는 않았다.**

⚠️ **재배포가 캐시를 지운다는 게 겹악재다.** 월별 캐시(`webapp/cache/`)가
있으면 같은 구를 다시 돌 때 국토부를 안 부르는데, Render 무료 티어는 **배포할
때마다 캐시가 날아간다.** 오늘처럼 여러 번 푸시한 날은 순회를 돌릴 때마다
처음부터 다시 받는 셈이다 — 고치는 작업 자체가 한도를 태운다.

### 이번 CSV(run6, 74건)에서 확인한 것

- **run5와 겹치는 49건의 추정값이 한 자리까지 완전히 동일하다.**
  ⚠️ 이건 "옛 코드로 돌았다"는 뜻이 **아니다** — 52~57절은 오류 처리·캐시·
  속도만 건드렸고 **계산식을 하나도 안 바꿨다.** 같은 시드·같은 표본이면
  같은 값이 나오는 게 정상이다(49-2절 시드 고정).
- 부천시·화성시는 이번에도 0건이다. 56절 되살리기가 동작했는지는 **이
  CSV로는 알 수 없다** — 성공한 지역만 실리기 때문이다. 화면의
  "돌리지 못한 지역" 문구를 봐야 한다.
- 전체 74건: MAPE 11.2%(중앙 7.8%) · 편향 −1.8% · ±10% 적중 57% · 구간 적중 43%.
  49-1절 92건(11.0% / −3.3%)과 사실상 같다 — 겹치는 표본이라 새 정보는 아니다.

## 59. ⚠️ 58절을 뒤집었다 — 429는 일일 한도가 **아니다**

58절에서 "HTTP 429 = 일일 한도 초과"라고 단정하고 **즉시 포기 + 순회 중단**
으로 고쳤는데, **근거 없는 추측이었다.** 사용자가 "한도를 돈 내서라도 올리고
싶다"고 해서 다시 파다가 드러났다.

**공식 스펙(사용자가 올려준 기술문서로 검증한 `ERROR_MESSAGES`)상 일일 한도는
`resultCode 22`다** — HTTP 200에 XML로 온다:

```
"22": "일일 요청 한도 초과"
```

즉 **HTTP 429는 국토부 API가 문서화한 한도 신호가 아니다.** 게이트웨이
계층의 **초당·분당 호출 제한(throttling)**일 가능성이 크고, 그건 **기다리면
풀린다.** 그런데 58절은 그걸 "하루 지나야 됨"으로 단정해서:

- 잠깐 몰린 것뿐인데 **즉시 실패**로 끝냈고,
- 순회를 **통째로 멈춰** 버렸다.

⚠️ 22절 `FETCH_WORKERS`=8로 월별 조회를 병렬로 던지는 데다 순회가 지역을
300ms 간격으로 이어 돌린다 — **초당 제한에 걸리기 딱 좋은 모양**이다. 원인이
우리 쪽 호출 속도일 가능성이 충분한데 "한도 소진"으로 단정한 것이다.

### 고친 것

| 신호 | 전(58절) | 후(59절) |
|---|---|---|
| **HTTP 429** | 즉시 포기 · 순회 중단 | **4→8→16초 물러섰다 재시도** · 순회 계속 |
| **resultCode 22** | (구분 안 함) | **즉시 포기** · 순회 중단 · "내일 다시" |

- 429가 한 번 나고 다음 시도에 풀리면 **성공으로 끝난다**(58절은 이걸 실패로
  만들었다) — 테스트로 고정.
- 503 같은 일시 오류는 예전처럼 1.5초 간격 재시도 그대로.

### ⚠️ 그래서 아직 모르는 것

**그 429가 실제로 무엇인지 확인하지 못했다.** 응답 본문·헤더(`Retry-After`
등)를 못 봤기 때문이다. 지금 설계는 **둘 중 무엇이든 안전한 쪽**이다 —
초당 제한이면 물러섰다 성공하고, 진짜 한도면 3번 더 쓰고 실패한다(58절
이전과 같음). 실제 응답을 한 번 보면 확정할 수 있다.

### 한도를 "돈 내서" 올릴 수 있나 — **없다**

공공데이터포털 국토부 실거래가 API는 **무료 공공데이터이고 유료 요금제가
없다.** 결제로 한도를 사는 경로 자체가 존재하지 않는다. 올리는 방법은:

1. **개발계정 → 운영계정 전환 신청**(무료, 심사) — 표준 경로.
2. 그래도 모자라면 **제공기관에 한도 상향 요청**.

⚠️ Render 유료 플랜은 **국토부 한도와 아무 상관이 없다.** 콜드 스타트와
캐시 유지에만 도움이 되고, 캐시가 유지되면 **호출이 줄어들어 간접적으로**
한도를 아끼는 효과는 있다.

## 47. 글자 대비·크기 (흐려서 안 읽히는 문제)

사용자가 "전체적으로 전부 다 글씨가 흐린 거 존나 안 읽힌다, 나도 안 읽히는데
어른들은 더 안 읽히겠지?"라고 지적해서 색 토큰과 본문 크기를 한 번에 올렸다.
기능 변경은 없고 42절(문장 단위 줄바꿈)과 같은 **읽는 방식** 규칙이다.

- **원인은 색 토큰이었다.** 흰 배경(#fff) 대비 명암비를 재보면:
  | 토큰 | 전 | 명암비 | 후 | 명암비 |
  |---|---|---|---|---|
  | `--muted` | `#7c8288` | 약 4.3:1 (AA 4.5:1 미달) | `#555b61` | 약 6.9:1 |
  | `--muted-2` | `#a4a9ae` | 약 2.5:1 (한참 미달) | `#6e757b` | 약 4.8:1 |
  `--muted-2`는 `.mk-sub`(항목 부제)·`footer`·`.section-divider`처럼 **설명
  문구 전반**에 쓰이고 있어서, 이 하나가 화면 전체를 흐리게 만들고 있었다.
- **크기도 같이 키웠다** — 색만 진하게 하면 작은 글자는 여전히 안 읽힌다.
  `.muted`/`ul.note-lines li` 13→14px, `.hint` 12.5→13.5px, `.mk-lecture`
  11.5→13px(색도 `--muted-2`→`--muted`), `.mk-sub` 12→13px, `.mk-title`
  14.5→15.5px, `h3` 15→16.5px, `table` 13.5→14.5px, `th` 12→13px,
  `footer` 11.5→13px, `.hero-range` 13.5→14.5px, `.price-item .label`
  12→13px.
- ⚠️ **새 문구를 쓸 때 `--muted-2`를 "거의 안 보이는 회색"으로 쓰지 않는다.**
  두 토큰의 차이는 이제 "보조 정보"와 "본문" 정도이지, "읽지 말라는 글씨"가
  아니다. 정말 배경처럼 깔고 싶은 요소(접이식 화살표 등)는 토큰 대신 그
  자리에서 직접 색을 준다.
- Playwright로 데스크톱(1280px)·모바일(390px) 결과 페이지를 렌더링해서
  가로 스크롤이 안 생기는지(`scrollWidth == viewport`), 글자가 커지면서
  카드·표·접이식 레이아웃이 깨지지 않는지 스크린샷으로 확인했다.

## 65. 전수 점검 2차 — 64절이 새는 데와 조용히 틀리던 세 가지

64절(1층 보정)을 넣은 직후 "같은 *유형*으로 또 있나"를 기준으로 훑었다
(54절과 같은 방식 — 개별 증상이 아니라 실패 유형으로 찾는다). 네 개가 나왔고
**셋은 화면으로는 못 알아챌 종류**다.

### ⚠️ ① 64절 계수가 전세·전환율 계산에 새고 있었다 (내가 방금 심은 버그)

`first_floor_price_factor()`를 `find_comparables()` 안에서 **무조건** 곱하고
있었다. 그런데 `find_comparables()`는 매매만 쓰는 게 아니다 — 16절 예상 전세가와
23-1절 실측 전환율도 `floor`를 넘겨 같은 함수를 부른다. 즉 **매매 실거래로만
잰 −7%가 전세 계산에 그대로 걸렸다.**

48-2절이 `SALE_CALIBRATION_FACTOR` 기본값을 1.0으로 두고 "16절 전세가 실수로
매매 보정을 받는 걸 막기 위해서"라고 적어놨는데, **그 교훈을 적어놓고도 바로
다음 절에서 똑같이 밟았다.**

- **고친 방법**: `find_comparables(first_floor_ratio=...)`를 새로 받고
  **기본값 1.0(꺼짐)**. 매매 호출부 세 군데(CLI 8절 · 웹 `/estimate` ·
  백테스트)만 `FIRST_FLOOR_PRICE_RATIO`를 명시적으로 넘긴다.
- `test_default_is_off` · `test_the_jeonse_call_sites_do_not_pass_it`(소스 검사)가 고정한다.
- ⚠️ **전세에도 1층 할인이 있을 것 같다는 짐작으로 켜지 않는다** — 전세는
  아직 백테스트로 재본 적이 없다. 재고 나서 켠다.

### ⚠️ ② 리다이렉트를 안 따라가서 빈 본문을 받고 있었다 (60절 연결 재사용의 구멍)

`urllib.request.urlopen()`은 3xx를 **자동으로 따라간다.** `http.client`는 **안
따라간다** — 리다이렉트 응답 본문(보통 빈 문자열)을 그대로 돌려준다.
60절에서 연결 재사용으로 바꾸면서 이걸 빠뜨렸다.

서버가 경로를 옮기거나 http→https로 올려보내는 순간:
- 국토부 → XML 대신 빈 본문 → "데이터를 찾지 못했습니다"
- 카카오 → JSON 대신 빈 본문 → `ValueError` → 48-7절 처리로 **조용히 None**

즉 **전 지역이 한꺼번에 먹통이 되는데 원인은 화면 어디에도 안 나온다.**
실측 확인: 옛 동작에서 리다이렉트 테스트 8개가 전부 `b'' != b'hello /new'`로
실패한다.

- 이제 urllib과 같은 규칙으로 따라간다 — 상대 경로 해석, 체인, 최대 5회,
  루프는 예외, **301/302/303 + POST는 GET으로 바뀌고 307/308은 메서드 유지.**
- ⚠️ **호스트가 바뀌면 `Authorization`/`apikey`/`Cookie`를 떼고 보낸다** —
  카카오 REST 키·슈퍼베이스 서비스 키가 엉뚱한 서버로 새면 안 된다(6절).
- `test_http_pool.py`가 **진짜 로컬 HTTP 서버**로 8가지를 확인한다.

### ⚠️ ③ CLI로 준공년도를 주면 환금성 진단에서 죽었다

```
TypeError: unsupported operand type(s) for -: 'int' and 'str'
```

`build_marketability_report()`가 `this_year - build_year`를 바로 했는데,
`build_year`가 **호출부마다 타입이 다르다** — 웹은 `int(build_year)`로 바꿔
넘기지만 CLI `--build-year`는 문자열 그대로다(5절 하드 필터가 문자열 비교를
쓰기 때문에 그게 자연스러웠다).

⚠️ **웹만 멀쩡해서 아무도 몰랐다.** CLI로 `--build-year`를 준 모든 실행이 32절
종합 판단 직후 통째로 멈췄다. 테스트가 `int`만 넣어보고 있어서 못 잡았다.

- 호출부가 정규화해 주기를 기대하지 말고 **함수 안에서 `_as_int()`로 맞춘다**
  (숫자로 못 읽으면 그 항목만 조용히 빠진다 — 41절 `unknown` 원칙 그대로).
- `test_a_string_build_year_does_not_crash`가 고정한다.

### ⚠️ ④ 슈퍼베이스 업로드가 `_cache_lock`을 쥔 채 네트워크를 탔다

62절 `_push_new_entries()`가 `_save_cache()` → 즉 **락 안에서** HTTP를 쳤다.
슈퍼베이스를 켜는 순간 **22절 병렬 8워커가 왕복마다 줄을 선다** — 57절이
없앤 직렬화가 그대로 되살아난다. 무료 플랜이 7일 정지에서 깨어나는
중(약 30초)이면 지오코딩 전체가 그만큼 멈춘다. **캐시를 남기려다 계산을
느리게 만들면 본말전도다.**

- 올릴 것만 락 안에서 복사해 두고 **백그라운드 스레드**로 보낸다. 실패하면
  '올림' 표시를 되돌려 다음 기회에 다시 시도한다.
- 프로세스 종료 직전(`flush_caches`)만 `blocking=True` — 그때는 데몬 스레드가
  영영 안 돌 수 있다.
- ⚠️ `_mem_load()`의 복구(`get_all`)는 **일부러 락 안에 뒀다** — 프로세스당
  한 번뿐이고, 여기서 직렬화하는 게 오히려 맞다(8워커가 같은 복구를 8번
  하지 않게).

### 덤 — 64절 안내 문구를 CLI에도 달았다

웹에만 있었다. 7-2절 시계열 보정이 **CLI·웹 양쪽에** 안내를 단 것과 같은
이유다 — 목록의 금액은 신고된 실제 체결가 그대로라, 안 알리면 "표에 뜬
가격이랑 매도가가 왜 안 맞지"로 읽힌다.

### ⚠️ 내가 또 틀렸던 것 (기록해 둔다)

- **가짜(fake)가 또 틀렸다.** 스모크 테스트의 가짜 `get_building_info()`에
  `elevator_count`를 안 넣어서 전 경로가 500이 났다. 앱이 아니라 **내 가짜가
  틀린 것**이었다(직전에도 `nearby_place`의 `distance_m`으로 같은 실수를 했다).
  → **가짜를 쓸 때는 실제 함수의 반환 dict를 먼저 읽는다.**
- **덤으로 드러난 것**: 52절 `@app.errorhandler(Exception)` 안전망이 실제로
  동작해서, 그 500들이 전부 읽을 수 있는 한국어 안내로 나갔다. 그래서 내
  스모크가 "OK"로 통과해버렸다 — **status 200까지 확인하게 조였다.**
- **검증 정규식이 틀려서 멀쩡한 코드를 버그로 의심했다.** 1층 안내의 두 방향
  문구가 "1층 시세대로"/"**위층** 시세대로"로 다른데 하나만 찾고 있었다.
  코드는 처음부터 정상이었다.

**546개 통과, 24개 테스트 파일 단독 실행도 전부 통과**(55절 순서 의존 방지),
웹 19가지 입력에 500 없음, CLI 한 바퀴 완주.

## 66. 독립 표본(서울 53건)이 49-2절 예측구간을 깨뜨렸다

51-2절이 "구간 폭을 **정한 데 쓰인 표본과 겹치니** 진짜 검증이 아니다 —
서울처럼 겹치지 않는 지역을 돌려야 독립 확인이 된다"고 적어뒀다.
그 순회를 돌렸고, **떨어졌다.**

**전체 성적 (서울 53건, 경기와 비교)**

| | 경기 74건 | **서울 53건** |
|---|---|---|
| MAPE | 11.2% | **17.3%** |
| 중앙 절대오차 | 7.8% | **15.3%** |
| 편향 | −1.8% | **−5.2%** |
| ±10% 적중 | 57% | **42%** |
| 25% 넘게 틀림 | 11% | **25%** |

비교거래 수는 거의 같다(중앙값 10건 vs 9건) — **표본이 얇아서가 아니라
서울 빌라가 원래 더 어렵다.**

### ⚠️ ① 예측구간이 약속을 못 지켰다 → 폭을 넓혔다

화면은 "80%가 이 범위 안에서 팔렸다"고 말한다. 실제로는:

| 위험요인 | 표본 | 예전 폭 | **실제 적중** |
|---|---|---|---|
| 0개 | 8건 | ±10% | **50%** |
| 1개 | 24건 | ±18% | **58%** |
| 2개+ | 21건 | ±25% | **76%** |

**좁아서 틀리는 쪽이 넓어서 싱거운 쪽보다 훨씬 해롭다** — 입찰가를 그만큼
잘못 쓰게 된다. `PREDICTION_INTERVAL_PCT`를 **±20/30/35%** 로 올렸다.

| | 서울 적중 | 경기 적중 |
|---|---|---|
| 예전 ±10/18/25 | 50/58/76% (전체 64%) | 82% |
| **지금 ±20/30/35** | **75/79/90% (전체 83%)** | **93%** |

⚠️ **위험0개 칸은 서울 표본이 8건뿐이라 폭을 거기 맞추지 않았다.** 그 8건의
오차가 1·5·7·9·16·17·26·30%인데, 80% 분위(26%)가 **한 건에 통째로
좌우된다.** 게다가 ±20과 ±26 사이에는 데이터가 **한 건도 없어서** 그 사이
어디로 잡든 적중률이 똑같다 — 거기 맞추는 건 48-2절 0.97과 같은 과적합이다.
`test_table_is_wider_than_the_measured_quantiles`가 **표본 15건 미만인 칸은
기준에서 빼는** 규칙까지 같이 고정한다.

### ⚠️ ② 위험요인 세 개가 **하나도 재현되지 않았다**

49-2절이 고른 세 요인을 서울에서 따로 뜯어보면:

| 요인 | 해당 | 아님 | 차이 | 95% CI | 판정 |
|---|---|---|---|---|---|
| 소형(<50㎡) | 18.8% (28) | 15.7% (25) | +3.1%p | [−3.9, +10.1] | ❌ |
| 괴리율 ≥3% | 16.2% (31) | 18.9% (22) | **−2.7%p** | [−10.1, +4.4] | ❌ **역방향** |
| 같은건물 1건 | 14.4% (11) | 18.1% (42) | **−3.8%p** | [−11.2, +5.5] | ❌ **역방향** |

⚠️ 49-2절이 "✅ **튼튼**"이라고 적었던 소형조차 이번 표본에서는 유의하지 않고,
**경기 74건 CSV로 다시 재도 +3.1%p [−2.6, +9.4]로 유의하지 않다**(그 판정은
지금 없는 92건 파일에서 나온 것이다).

**화면에 있는 어떤 신호도 오차를 예측하지 못한다** — |오차|와의 상관계수:
괴리율 −0.12 · 시세 신뢰도 −0.21 · 비교거래 수 −0.04 · 최대지분 −0.13 ·
동일건물 수 +0.16 · 면적 −0.10. 전부 0에 가깝다.

**그래도 등급 구조 자체는 안 지웠다** — 경기 92건에서는 ±9.3/17.4/25.0으로
깨끗하게 단조였고, 서울 칸당 표본이 8~24건이라 **양쪽 다 확정할 만큼 크지
않다.** 대신 폭을 "둘 중 넓은 쪽"으로 맞춰서, 등급이 틀려도 **구간이 거짓말은
안 하게** 해뒀다. 등급이 계속 재현에 실패하면 그때 평평하게 만든다.

### ⚠️ ③ 64절 1층 보정 — **판정 보류. 내 잘못이다**

서울 1층 편향은 **−11.3%** 로, 경기(+7.0%)와 **방향이 반대**로 나왔다.
그런데 이건 착시였다 — 서울 1층 그룹에 정비구역 고가 물건이 몰려 있다:

| | 통제 전 1층 편향 | **가격대(㎡당가) 통제 후** | n |
|---|---|---|---|
| 경기 | +7.0% | **+10.8%p** (남는다) | 15 |
| 서울 | −11.3% | **−1.9%p** (사라진다) | 9 |

부트스트랩으로도 서울 1층−2~3층 차이는 −11.1%p [−25.2, +7.0]으로 **0을
포함한다.** 즉 서울 데이터는 1층 효과에 대해 **아무 말도 하지 않는다.**

⚠️ **그런데 정작 중요한 걸 못 정했다 — 이 순회가 64절 이전 코드인지 이후인지
알 수 없다.**
- **이후라면**: 보정이 +7%를 걷어내고 −1.9%p만 남긴 것이니 **의도대로 먹혔다.**
- **이전이라면**: 서울의 진짜 1층 효과가 ~0이라는 뜻이고, 경기와 **모순**이다.

두 해석이 정반대인데 **CSV에 그 정보가 없어서 가를 수가 없다.** 48-5절에서
화면 푸터에 버전을 찍어뒀는데도, **내려받은 파일만 보고는 알 수 없었다.**

### 그래서 건별 기록에 배포 버전을 박았다

`/backtest/one` JSON과 CSV 내보내기에 **`version` 열**을 넣었다. 순회는 수십 분
걸려서 "다시 돌려보자"가 싸지 않다 — 결과에 붙여 두는 게 맞다.
`TestVersionIsStampedOnEveryCase`가 고정한다.

### ⚠️ 가격대가 높을수록 낮게 부른다 (축소, shrinkage)

| 물건 ㎡당가 / 표본 중앙값 | 표본 | MAPE | 편향 |
|---|---|---|---|
| 0.8배 미만 (싼 물건) | 16건 | 19.3% | **+5.6%** (높게 부름) |
| 0.8~1.3배 (보통) | 19건 | 14.7% | −3.0% |
| 1.3~2.0배 | 10건 | 14.3% | −13.4% |
| 2.0배 이상 (비싼 물건) | 8건 | 23.5% | **−21.5%** (낮게 부름) |

⚠️ **이 표만으로 결론 내면 안 된다** — `오차`와 `㎡당가`가 둘 다 `실제가`를
품고 있어서 **지표 자체가 음의 상관을 만들어낸다.** 그래서 제대로 검정했다:

> `log(실제 ㎡당가) = a + b · log(추정 ㎡당가)`. **b > 1이면 추정이 가운데로
> 눌려 있다**(축소). 추정값의 잡음은 b를 **아래로** 끌어내리므로, b>1이
> 나오면 그건 강한 증거다.

| | b | 95% CI | |
|---|---|---|---|
| 경기 74건 | **1.09** | [1.04, 1.14] | ✅ 축소 확인 |
| 서울 53건 | 1.08 | [0.97, 1.20] | — 유의하지 않음 |

**동네 중앙값 추정기는 원리적으로 축소된다** — 48-2-2절이 "산포는 가중치로
못 고친다"고 적어둔 것과 같은 이야기다. 대상 물건 자신이 동네 평균에서
얼마나 벗어났는지는 **그게 바로 우리가 맞히려는 값**이라 미리 알 수 없다.
⛔ **그래서 역보정(b로 나눠 펼치기)은 하지 않는다** — 맞을 때 조금 좋아지고
틀릴 때 크게 나빠지는, 50절이 프리미엄을 안 넣기로 한 것과 같은 구조다.

### 다음에 볼 것

- **버전 열이 붙은 첫 순회** — 64절 1층 보정의 판정을 그때 끝낸다.
- 서울이 경기보다 6%p 더 틀리는 이유. 비교거래 수는 같으니 다른 데 있다
  (정비구역 밀도? 가격 산포?). 50-1절 브이월드 키가 붙으면 정비구역을
  **직접** 갈라서 확인할 수 있다.
- 위험등급이 또 재현에 실패하면 **평평한 단일 구간으로 바꾼다.**

## 67. 독립 표본이 판정한 것 — 64절은 통과, 위험요인 하나는 폐기

66절이 "**버전 열이 붙은 첫 순회**에서 64절 1층 보정의 판정을 끝낸다"고
남겨뒀다. 그 순회가 왔다 — 경기 65건, 전부 `version=1049fc9`.
드디어 **어느 코드로 나온 숫자인지 알고** 분석할 수 있었다.

### ✅ 64절 1층 보정 — 통과

웹 순회는 시드가 고정이라(49-2절) 같은 지역을 다시 돌리면 **같은 물건이 다시
뽑힌다.** 그래서 직전 74건 CSV와 **62건이 겹쳤고**, 51-2절이 못박은 대로
그룹 평균이 아니라 **짝지은 비교(paired)** 로 판정했다.

| 1층 물건 (13건) | 전 | 후 | 변화 |
|---|---|---|---|
| 라피네101동,102동 | +2.8% | −2.6% | −5.4%p |
| 라이프맨션다동 | +23.1% | +15.1% | −8.0%p |
| 승원빌라(나동) | −0.8% | −7.8% | −6.9%p |
| 오룡스카이빌라(399-3) | +18.4% | +10.1% | −8.3%p |
| 금산다세대주택 | +55.2% | +42.8% | −12.4%p |
| **13건 전체 편향** | **+7.3%** | **+3.0%** | **−4.3%p** |

**13건 중 12건이 설계한 방향으로 움직였다.** 그리고 대조군인 2층 이상 48건은
편향 −4.1% → −3.3%로 **반대 방향(+0.8%p)** 으로 갔다 — 양방향 대칭 설계가
의도대로 작동했다는 증거다(한쪽만 걸려 있어 사고가 났던 5절 반지하와 대비).

⚠️ **남은 +3.0%를 더 깎지 않는다.** 보정은 **층이 서로 다른 비교거래에만**
걸린다 — 표본의 약 60%다. 7% × 0.6 ≈ 4.2%p이고 실제로 −4.3%p가 움직였으니,
**계수는 이미 설계대로 다 일한 것**이다. 여기서 `FIRST_FLOOR_PRICE_RATIO`를
올리면 1층 비교거래가 애초에 적은 건들을 과하게 끌어내린다.

### ✅ 66절 예측구간 — 약속을 지켰다

| | 예전 폭 | **새 폭** |
|---|---|---|
| 위험0개 (n=25) | ±10% → 72% | ±20% → **92%** |
| 위험1개 (n=21) | ±18% → 81% | ±30% → **95%** |
| 위험2개 (n=19) | ±25% → 74% | ±35% → **84%** |
| **전체 65건** | **75%** | **91%** |

설계 목표가 80%인데 경기는 91%로 다소 헐겁고 서울은 83%다. **이 비대칭은
서울을 덮기 위해 치르는 값**이라 그대로 둔다 — 좁아서 틀리는 쪽이 넓어서
싱거운 쪽보다 훨씬 해롭다(입찰가를 그만큼 잘못 쓰게 된다).

전체 65건: MAPE 11.0% · 중앙 7.8% · 편향 −2.1% · ±10% 57% · 25% 초과 11%.

### ⛔ "같은 건물 거래가 딱 한 건" 위험요인을 폐기했다

**49-1절 최대지분 경고와 완전히 같은 경로를 밟았다.**

| 표본 | 차이 | 95% CI | |
|---|---|---|---|
| 경기 65건 | +5.2%p | — | ❌ |
| 서울 53건 | **−3.8%p** | — | ❌ **역방향** |
| **합계 118건** | **+1.1%p** | **[−3.8, +6.8]** | ❌ |

⚠️ **"두 표본에서 재현됐다"던 근거가 애초에 없었다.** 49-2절이 이미 적어둔
대로 44건과 92건은 **같은 물건을 두 번 센 것**이다(시드 고정). 진짜 독립
표본인 서울에서는 방향이 뒤집혔고, 합쳐도 0을 못 넘는다.

**빼도 갈라내는 힘이 그대로다**(합계 +4.2%p → +4.2%p). 오히려:
- 경고율이 **59% → 47%** 로 내려간다 — **둘에 한 번 뜨는 경고는 경고가 아니다.**
- 등급이 더 잘 갈린다(경기 갈라내는 폭 +9.5%p → **+10.8%p**).
- 합계 등급도 단조 그대로(9.4 / 15.9 / 16.8%).
- 폭 ±20/30/35를 그대로 씌워도 경기 89% · 서울 83% · 합계 86%.

`_same_building` 플래그 자체는 **그대로 둔다** — 5절 동일건물 보너스와
8-1-1절 그래프 테두리가 계속 쓴다. 뺀 건 **경고와 위험요인 집계 두 군데**다.

### 남은 두 요인의 증거 강도 (118건)

| 요인 | 경기 | 서울 | 합계 | |
|---|---|---|---|---|
| 소형(<50㎡) | +5.5%p | +3.1%p | **+5.2%p [+0.6, +10.0]** | ✅ 합쳐야 유의 |
| 괴리율 ≥3% | +7.8%p ✅ | −2.7%p | +4.2%p [−0.3, +8.9] | ⚠️ 지역마다 갈림 |

⚠️ **둘 다 지역 하나만으로는 유의하지 않다.** 49-2절이 소형을 "✅ 튼튼"이라고
적은 근거는 지금 없는 92건 파일에서 나온 것이고, **지금 있는 경기 CSV로 다시
재면 유의하지 않다.** 그래도 남기는 이유는 ① 방향이 두 지역 모두 양수이고
② 합치면 0을 넘으며 ③ 등급 구조가 경기·합계에서 단조이기 때문이다 —
**하나하나를 독립된 근거로 내세우지 않는다**는 49-2절 원칙 그대로다.

66절이 남긴 "위험등급이 또 재현에 실패하면 평평한 단일 구간으로 바꾼다"는
기준은 **아직 유효하다.** 다음 독립 표본에서 합계 단조가 깨지면 그때 등급을
없애고 폭 하나로 간다.

### ❌ 4층 보정은 기각했다

64절이 "4층 편향 −10.4%(n=12)도 보였지만 손대지 않았다 — n=12에 맞추면 그게
과적합이다"라고 적어둔 것을 **제대로 검정해서 기각한다.**

| | 편향 | 부트스트랩 95% CI | 가격·면적·연식 통제 후 |
|---|---|---|---|
| 경기 | −9.8%p | [−21.1, +0.0] | −4.4%p |
| 서울 | −6.1%p | [−19.6, +7.4] | −7.7%p |
| **합계** | −8.2%p | **[−16.7, +0.3]** | — |

세 표본 모두 **0을 포함한다.** 결정적으로 **인접 층의 반작용이 없다** —
2층 +0.4 · 3층 −2.3 · 5층 이상 −3.5. 64절이 1층 보정을 정당화한 근거는
"1층과 위층이 **서로 섞여** 양쪽으로 편향이 갈린다"는 메커니즘이었는데,
4층에는 그 대칭 구조가 없다. **메커니즘 없는 숫자는 보정하지 않는다.**

### 버전 열이 제 몫을 했다

66절에서 붙인 `version` 열이 없었다면 64절 판정을 이번에도 못 했다 —
"이 CSV가 보정 전인가 후인가"를 가를 방법이 CSV 안에 없었기 때문이다.
순회는 수십 분 걸려서 "다시 돌려보자"가 싸지 않다. **오래 걸리는 작업의
결과에는 그걸 만든 코드의 신원을 같이 박아 둔다.**

## 68. 서울이 유독 틀리던 이유 — 오르는 시장을 계산에 안 넣고 있었다

사용자가 **"서울 자체가 빌라 가격이 오르고 있는데 그걸 적용을 안 해서 그럴지도
모르겠다"**고 짚었다. 맞았다. 세 갈래 증거가 전부 같은 곳을 가리킨다.

### ① 지수가 이미 그렇게 말하고 있었다

리포의 한국부동산원 연립다세대 매매수급동향지수(24절, 100 초과 = 상승 압력):

| | 2025-11 | 2026-08 |
|---|---|---|
| **서울** | 104.6 | **111.5** ↗ |
| 경기 | 95.8 | 98.0 |

### ② 실제 코드로 재보니 크기까지 맞는다

`find_comparables` + `compute_scenarios`를 그대로 태워, 가격이 오르는 동네에서
과거 거래로 중앙값을 낼 때의 편향을 측정했다:

| 진짜 월 상승률 | 연 환산 | 계산기 편향 |
|---|---|---|
| 0.0% | 0% | −0.0% |
| 0.3% | 3.7% | −2.7% |
| **0.68%** | **8.5%** | **−5.8%** |
| 1.0% | 12.7% | −8.2% |

**역산**: 경기 실측 편향 −2.1% → 월 +0.28%, 서울 −5.2% → **월 +0.68%**.

### ③ 끌 때의 근거가 틀렸다

7-2절을 끌 때 적어둔 이유가 "**7절 최근성 가중치(1.6/1.0/0.4)가 이미 하고
있다**"였는데, 위 −5.8%는 **그 가중치가 켜진 상태에서 나온 값**이다.
줄여주긴 하지만 없애진 못한다.

⚠️ 이것만으로 서울 편향 −5.2%가 **전부** 추세 때문이라고 단정하지는 않는다.
다만 방향·크기·지역 차이가 전부 맞고 수급지수가 독립적으로 같은 얘기를 하므로,
**주된 원인일 가능성이 크다**고 본다. 서울이 경기보다 MAPE가 +5.7%p 높은 것 중
**절반 정도**가 이걸로 설명된다(6.1% → 3.5%).

### 그래서 다시 켰다 — 다만 추정기를 세 군데 고치고서

예전 추정기를 그대로 켰다면 **보합 동네를 망칠 뻔했다.** 실측으로 하나씩 막았다.

**(가) 첫점-끝점 → 최소제곱 회귀**
예전엔 시계열의 첫 달과 마지막 달 **두 점만** 썼다. 그 두 달이 우연히 튀면
추세 전체가 흔들린다 — 추정 편차 ±0.25%p/월. 모든 점을 쓰는 회귀로 바꾸니
**±0.13%p/월로 절반**이 됐다. `log(평당가)`에 회귀해야 기울기가 곧 월 복리율이다.

**(나) 월별 평균 → 중앙값 (추정기만)**
회귀로 바꾼 뒤에도 **업거래 한 건이 그 달 평균을 통째로 끌고 가 추세 부호가
뒤집혔다**(테스트가 실제로 잡았다). `_price_trend_monthly_series()`에
`aggregate` 인자를 새로 두고 추정기만 `statistics.median`을 넘긴다 —
⚠️ **15절 화면은 익숙한 평균 그대로다.** 가격을 실제로 움직이는 쪽만
보수적인 집계를 쓴다.

**(다) ⭐ 유의성 게이트 (`TREND_RATE_MIN_T` = 1.5)**
가장 중요한 안전장치다. 회귀를 쓰면 **기울기의 표준오차가 공짜로 나오는데**,
그걸 안 보면 **가격이 안 움직이는 동네에서 잡음을 추세로 착각해 멀쩡한
매도가를 흔든다**(실측: 거래가 드문 동네 보합장 MAPE 3.2% → 3.8%).
`t = |기울기| / 표준오차`가 1.5 미만이면 `None`을 돌려 보정 자체를 건너뛴다.

추가로 `TREND_RATE_SANITY_MAX`(월 ±2% = 연 ±27%)로 추정치 자체를 clamp한다.

### 게이트 적용 후 — 손해는 거의 없고 이득은 그대로

업거래 6%를 섞은 조건에서 실제 코드로 측정 (MAPE):

| 진짜 월상승 | | 꺼짐 | 켜짐 | 보정 적용률 |
|---|---|---|---|---|
| 0.00% (보합) | 얇은 동네 | 3.2% | 3.5% | **17%** |
| | 보통 동네 | 3.6% | 3.7% | **14%** |
| 0.30% | 얇은 동네 | 3.8% | 4.0% | 34% |
| | 보통 동네 | 3.9% | **3.8%** | 66% |
| **0.68% (서울)** | 얇은 동네 | 5.7% | **4.1%** | 83% |
| | 보통 동네 | 5.8% | **3.7%** | 99% |
| 1.00% | 보통 동네 | 8.0% | **3.9%** | 100% |

**보합장에서는 14~17%만 적용돼 손해가 +0.1~0.3%p로 그치고, 실제 상승장에서는
83~100% 적용돼 −1.6~−4.1%p를 번다.** 추정 자체에 편향도 없다(보합에서
+0.00±0.29%).

### ⚠️ 한계 — 합성 데이터다

추세가 매달 일정하다고 가정했고, 개별성 σ=10%·업거래 6%는 **가정이지 실측이
아니다.** 실제 시장은 구간마다 다르게 움직인다. **진짜 검증은 다음 백테스트에서
짝지은 비교로 한다**(51-2절 교훈 — 그룹 평균은 구성 변화에 오염된다).
**볼 것: 서울 편향 −5.2%가 0 쪽으로 움직였는지.**

### 사용자가 보내준 추가 자료 두 개

| 파일 | 내용 | 쓸모 |
|---|---|---|
| 월별 지역 지수 (2025.10~2026.07) | 전국/수도권/지방/서울/인천/경기/5대광역시/4대광역시/7개도 | ⭐ **서울 92.5 → 99.9 = 월 +0.86%** — 내가 역산한 +0.68%와 근접. 경기는 100.7 → 99.6(월 −0.12%)로 **서울만 혼자 오른다** |
| 매입자 연령대별 거래량 (2019~2025, 구 단위) | 동(호)수·면적, 20대이하~70대이상 | 연 단위라 추세에는 못 쓴다. 41절 환금성(매수층 두께)에 쓸 여지는 있다 |

⚠️ **첫 번째 파일이 정확히 무슨 지수인지 확인하지 못했다** — 업로드 과정에서
파일명이 깨졌다. 값의 범위(100 근처)와 지역 구분이 한국부동산원 R-ONE
전국주택가격동향조사 형식과 같지만, **21절·27절·50-1절 원칙대로 추측해서
확정하지 않는다.** 매매가격지수가 맞다면 24절처럼 스냅샷으로 넣어 **동네
표본이 얇을 때의 폴백 추세**로 쓸 수 있다(지금은 동 단위 실거래로만 추정하므로
거래가 드문 동네에서 게이트에 막혀 보정을 못 건다).

## 69. 매입자 연령대 — "이 동네에서 집을 사는 사람이 누구인가"

68절에서 사용자가 보내준 두 번째 자료(매입자 연령대별 거래량, 2019~2025,
구 단위 전국)를 결과 화면에 붙였다. 30~40대는 실거주 목적으로 사는 주력
매수층이라, 그 비중이 높으면 **팔 때 사줄 사람이 두껍다**는 신호로 읽힌다 —
41절 환금성 진단이 재려는 것과 같은 성격의 정보다.

`scripts/buyer_age.py` · `data/buyer_age_by_region.csv`.

### ⛔ 점수에도 매도가에도 반영하지 않는다

⚠️ **주택유형이 구분돼 있지 않다.** 원본에 아파트/연립다세대/단독을 가르는
열이 **아예 없어서 주택 전체 합계**다. 건수로는 아파트가 우세하니 연령
구성도 아파트 쪽으로 끌린다 — 24절이 "빌라 계산기에 아파트 지표는 안
맞다"며 KB 지수를 화면에서 뺀 것과 같은 문제다. 그때는 통째로 뺐고,
이번엔 **빼는 대신 한계를 화면에 적고 참고 정보로만** 둔다(연령 구성은
지역 성격을 꽤 잘 드러내는데, 이걸 대체할 빌라 전용 자료가 없다).

⚠️ **연 단위 스냅샷이다** — 24절 REB 지수처럼 자동 갱신되지 않는다.

그래서 24절·26절·40절과 같은 원칙으로 **8절 매도가에도 41절 환금성 점수에도
넣지 않는다.** `TestItNeverTouchesThePrice`가 `build_marketability_report()`
**함수 본문**에 이 모듈이 들어오지 않는지 소스로 고정한다(⚠️ 파일 전체를
검사하면 CLI 출력부의 정상적인 import가 오탐으로 걸린다 — 실제로 그렇게
썼다가 테스트가 깨졌다).

### 보여주는 것

- **30~40대 비중**(대표 숫자) + 전국 평균과의 차이
- 6구간 구성비 — 연령은 **순서가 있는 구간**이라 한 가지 색의 연→진 램프로
  그린다(무지개색을 쓰지 않는다). 어떤 색이 어느 구간인지는 **아래 범례가
  글자로** 말해주므로 색만으로 식별하게 두지 않는다.
- 같은 시/도 안에서의 순위 — 숫자 하나만 던지면 높은지 낮은지 모른다
  (24절이 지역간 순위를 같이 보여주는 것과 같은 이유).
- 2019 → 2025 매입 거래량 변화

⚠️ **비중은 '기타'(연령 미상)를 뺀 값으로 낸다.** 지역마다 미상 비율이
달라서 합계로 나누면 **미상이 많은 동네의 비중이 통째로 낮아 보인다.**
`MIN_TOTAL`(200건)은 합계와 '기타 제외 합계' **양쪽에** 건다.

**변별력은 있다**(붙이기 전에 실측): 서울 25개 구가 41.8~66.1%로 갈리고,
전국 표준편차 11.3%p · 범위 15.8~69.8%. 어디를 넣어도 비슷한 숫자가 뜨는
지표였다면 붙이지 않았다.

### 지역 매칭 — 55절 버그 ①을 다시 밟지 않으려고

`region_for_address()`가 지키는 규칙 네 가지:

1. **반드시 시/도로 먼저 좁힌다.** 55절 버그 ①이 정확히 이것이었다 — 구
   이름만 보고 판정해서 **부산 중구가 서울 도심권 지수를 받았다.** 시/도를
   못 정하면 시군구 매칭 자체를 하지 않는다.
2. **`SIDO_ALIAS = {"경기도": "경기"}`** — 원본 표기가 `sido_token()`의
   표준 축약명과 딱 한 글자 어긋난다(24절 `VILLA_SIDO_ALIAS`와 같은 문제).
   나머지 14개는 그대로 맞는 것을 확인했고, `TheRealFileLoads`가 **실제
   파일이 바뀌어 조용히 매칭이 끊기는 것**까지 잡는다.
3. **다른 매치에 통째로 들어가는 이름은 버린다** — '강북구'가 걸렸는데
   '북구'까지 같이 잡히면 안 된다.
4. **남은 것 중 주소에서 더 뒤에 나온 것을 쓴다.** 한국 주소는 넓은 단위 →
   좁은 단위 순이라 뒤쪽이 더 좁은 단위다. 원본이 `성남시`와 `분당구`를
   **둘 다** 갖고 있는데 이름 길이가 같아서, 길이 정렬로는 성남시가
   가로챈다(실제로 그렇게 나왔다).

### ⛔ 광주광역시·전라남도는 지원하지 않는다

원본이 둘을 **`전남광주`라는 한 라벨로 묶어** 내려보낸다 — 그 시/도 합계
행은 전부 0이고, 하위에 `(구)광주`·`(구)전남`과 실제 시군구가 섞여 있는데
**어느 시군구가 광주고 어느 게 전남인지 원본이 말해주지 않는다.** 24절이
경기 7권역·부산 3권역을 "구성 시군구를 신뢰할 수 있게 확인하지 못해"
빼둔 것과 같은 판단이다. 지금은 두 지역 주소에서 **카드가 조용히
생략된다** — 틀린 값을 주는 것보다 안 보여주는 쪽이 안전하다.

### 위치·비용

- 웹은 결과 페이지 "참고 정보" 섹션, 24절 시장 동향 카드 바로 뒤.
  CLI는 24절 출력 바로 뒤(`--no-buyer-age`로 끌 수 있다).
- **API 호출이 0이다** — CSV 한 번 읽는 게 전부이고, 읽은 표는 프로세스에
  캐시된다. 실패해도 카드만 생략한다(20·21·26·50-1절 원칙 그대로).

## 70. ✅ 68절 검증 통과 + 얇은 표본은 높게 부른다

순회 세 번(경기 47 + 서울 38 = 고유 85건, 버전 `ca3f28f`·`f2d709a`)을 받아
**66·67절이 남긴 숙제를 끝냈다.** 짝지은 비교(51-2절 교훈 — 그룹 평균은 구성
변화에 오염된다)로 판정했고, 이전 순회 CSV가 그대로 남아 있어 **같은 물건에
새 코드를 태운 통제 비교**가 성립했다.

### ✅ 68절 시계열 보정 — 서울에서 확실히 먹혔다

| | 편향 | 절대오차 |
|---|---|---|
| **서울 짝지은 36건** | −2.6% → **+0.7%** (+3.30%p) | 16.3% → **13.9%** (−2.42%p) |
| 95% 신뢰구간 | **[+1.74, +4.78] ✅** | **[−4.07, −0.78] ✅** |
| 그중 값이 실제로 바뀐 22건 | −8.2% → −2.8% | 14.8% → **10.8%** |
| | **22건 중 18건이 좋아졌다** | |

- **경기**는 편향 +1.57%p [+0.32, +2.94] ✅ 이지만 절대오차는 −0.51%p
  [−1.95, +0.81]로 유의하지 않다. **예측한 그대로다** — 68절이 적어둔 대로
  경기는 월 −0.12%(보합)라 유의성 게이트가 대부분 막고, 서울만 월 +0.86%로
  혼자 오른다. 보합 지역에서 안 건드리는 게 설계 의도였다.
- 지역 전체 성적: 서울 MAPE 17.3% → **14.2%**, 편향 −5.2% → **+0.9%**.
  경기 11.0% → **9.5%**, 편향 −2.1% → **+0.3%**.
- **66절이 정한 예측구간 ±20/30/35는 약속을 지켰다** — 서울 84% · 경기 94% ·
  전체 89%(목표 80%). 67절이 "서울을 덮기 위해 치르는 값"이라고 적은
  비대칭이 그대로 유지된다.

⚠️ **경기 쪽 비교 기준(`1049fc9`)은 64절 이후라 이 차이는 순수하게 68절이다.**
서울 쪽 기준 CSV는 버전 열이 없던 시절 것이라(66절이 그래서 버전 열을 만들었다)
64절이 섞였을 가능성이 남는다 — 다만 경기에서 같은 방향·같은 구조가 나오므로
주된 원인은 68절로 본다.

### ⚠️ 순회 중간에 서버가 재배포됐다

받은 CSV 한 개에 `b6fcfd8`(68절 이전) 24건과 `ca3f28f`(이후) 18건이 **섞여
있었다.** 66절에서 버전 열을 안 붙였으면 이 표본은 통째로 못 썼다 —
**오래 걸리는 작업의 결과에는 그걸 만든 코드의 신원을 같이 박아 둔다**는
67절 결론이 바로 다음 순회에서 값을 했다. 덤으로 같은 물건이 두 버전으로
나온 12건이 그대로 A/B 쌍이 됐다.

### ⭐ 새로 찾은 것 — 비교거래가 얇으면 **높게** 부른다

| 비교거래 | 표본 | 편향 | MAPE |
|---|---|---|---|
| 3~5건 | 24건 | **+8.8%** | 16.6% |
| 6~9건 | 36건 | −3.2% | 7.9% |
| 10~19건 | 37건 | −2.6% | 14.5% |
| 20건+ | 28건 | −7.5% | 13.5% |

**증거**(합친 고유 125건):
- 비교거래 수 ↔ 편향 스피어만 **ρ=−0.223**, 무작위 순열검정 **p=0.011**.
- 임계값을 3~12 **어디로 끊어도 전부 같은 방향**이고 5~9 구간이 유의하다.
- 서울(+14.2%p ✅)·경기(+13.3%p ✅)·이전 코드(+14.9%p ✅)·이번 코드
  (+14.2%p ✅) **네 갈래 전부에서 재현**된다.
- ㎡당가·면적을 통제한 회귀에서도 **+9.0%p**가 남는다(66절 축소만으로는
  설명 안 된다).

⚠️ **내가 중간에 한 번 틀렸다.** "n=3은 +1.6%인데 n=4~5만 나쁘니 임계값
쇼핑이다"라고 판정했는데, **10개 경계가 전부 같은 방향이고 유의 구간이
연속**이라 그 판정이 틀렸다(n=3은 9건뿐이라 −33·+40 두 건에 평균이
끌린 것이다). 49-1절 최대지분 경고는 **구간별로 방향이 뒤집혔던** 것이고,
이건 다르다.

**해석**: 얇은 표본 = 그 동네에서 비교 대상을 찾기 어려운 물건 = 대개
동네 평균보다 싼 물건인데, 추정기는 동네 평균 쪽으로 끌어올린다. 66절
축소(shrinkage)와 같은 계열이다.

### ⛔ 그래서 가격은 안 바꾼다 — 경고만 단다

- **66절이 축소를 역보정하지 않기로 한 것과 같은 이유**다(맞을 때 조금
  좋아지고 틀릴 때 크게 나빠진다).
- **관계가 계단이 아니라 완만하다** — 임계값 하나로 곱하는 보정은 애초에
  모양이 안 맞는다. 48-2절 `SALE_CALIBRATION_FACTOR` 0.97이 한 표본에
  과적합됐던 것과 같은 실수가 된다.
- 50절 "탐지만 하고 가격은 안 바꾼다"와 같은 처리다.

**49절 경고등에 여섯 번째 항목**(`ESTIMATE_WARN_THIN_SAMPLE` = 6, 비교거래
5건 이하)을 넣었다. ⚠️ **기존 괴리율 경고와 성격이 다르고, 그게 둘 다 두는
이유다**:

| | 크기(\|오차\|)를 가르나 | 방향(편향)을 아나 |
|---|---|---|
| 괴리율 ≥3% | ✅ +4.7%p [+0.6, +9.3] | ❌ +0.2% [−5.3, +5.8] |
| **비교거래 ≤5건** | ❌ +4.7%p [−1.5, +11.5] | ✅ **+8.8% [+0.7, +17.2]** |

괴리율은 "얼마나 틀릴지", 이건 "어느 쪽으로 틀릴지"를 말해준다 —
**입찰가를 쓰는 사람에게는 "높게 잡혀 있다"가 그 자체로 쓸 정보다.**
그래서 문구도 한쪽을 가리키게 쓰고("이 값을 상한으로 보세요"), 테스트가
그 방향성까지 고정한다.

⚠️ **둘을 합치면 경고율이 44% → 53%로 올라간다.** 67절이 "둘에 한 번 뜨는
경고는 경고가 아니다"라며 같은건물 경고를 뺐던 기준에 걸리는 수치다 —
그때는 **아무 정보도 추가하지 않는** 경고였고(갈라내는 폭 +4.2%p 그대로),
이건 **다른 종류의 정보**를 더한다는 점이 다르다. 다음 독립 표본에서
방향성(+8.8%)이 재현되지 않으면 그때 뺀다.

### 남은 것

- 화면 문구의 "실측 44건" 숫자를 **125건 기준으로 갱신**했다(CLI·웹 양쪽).
- **1층 편향이 −2.7%(n=14)로 뒤집혔다** — 67절에서 +3.0%였다. 표본이 작고
  방향이 왔다갔다 하므로 `FIRST_FLOOR_PRICE_RATIO`는 **건드리지 않는다.**
- 반지하 4건 편향 +5.5%(최악 +27.5%, 비교거래 4건) — 얇은 표본 문제와
  겹친다. 5절 대칭 수정의 효과를 재려면 표본이 더 필요하다.
- **가장 크게 빗나간 것은 여전히 극단 물건이다**: 광진구 `도시엔` 13.4㎡
  5.5억(㎡당 4,104만원) −50.0%, 고양시 덕양구 `금강주택가동` +56.0%
  (67절에서도 +55.2%로 같은 물건이 같은 크기로 틀렸다 — 시드 고정 때문에
  계속 뽑힌다). 이런 건 동네 중앙값 추정기가 원리적으로 못 맞힌다.

### 70-1. ⚠️ 시드가 박혀 있어 **새 표본을 만들 방법이 아예 없었다**

70절을 푸시한 직후 84건짜리 순회를 또 받았는데, **처음 보는 물건이 9건뿐**
이었다(나머지 75건은 이미 분석한 물건이 그대로 다시 나왔다).

원인은 `webapp/app.py`에 **`seed=42`가 박혀 있던 것**이다. 49-2절이 이미
"웹 순회는 시드가 고정이라 같은 지역을 다시 돌리면 같은 물건이 다시 뽑힌다"고
적어뒀고, 그걸 **A/B 비교의 장점으로만** 봤다(실제로 64절·68절 검증이 그 덕에
됐다). 그런데 뒤집어 보면 **같은 지역에서는 새 표본을 영영 못 만든다**는
뜻이기도 했다 — 67절과 70절이 둘 다 "다음 독립 표본에서 확인한다"로 끝나 놓고,
정작 그 독립 표본을 만들 수단이 도구에 없었던 것이다.

**고친 것**: 순회·단건 화면 양쪽에 **표본 번호(시드)** 입력을 넣었다
(`BACKTEST_DEFAULT_SEED` = 42, `_backtest_seed()`가 파싱).

- **번호가 같으면 예전 그대로** — 코드 변경 효과를 재는 짝지은 비교(51-2절)가
  계속 된다. 기본값을 42로 둔 이유다.
- **번호를 바꾸면 같은 지역에서 다른 물건**이 뽑힌다 — 이제 이미 돌려본 지역도
  독립 표본으로 재검증할 수 있다.
- ⚠️ **건별 결과와 CSV에 `seed` 열을 같이 싣는다.** 66절 `version` 열과 똑같은
  이유다 — 이 값이 없으면 두 CSV가 독립 표본인지 같은 표본을 다시 잰 것인지
  **사후에 가릴 방법이 없다.** 실제로 그것 때문에 84건을 통째로 못 썼다.
- 숫자가 아닌 값이 오면 조용히 기본값으로 돌아간다 — 시드 하나 때문에 순회
  전체가 멈추면 안 된다.

⚠️ CLI는 원래부터 `--seed`가 있었다. **웹만 막혀 있었고, 사용자는 웹만 쓴다**
(48-1절 — "왕초보라 터미널을 못 쓴다"). 같은 기능이 한쪽에만 있으면 없는 것과
같다.

**이번 84건에서 확인한 것**(독립 표본이 아니므로 새 증거는 아니다):
- 전체 MAPE 11.5% · 편향 +1.2%(서울 15.7%/+1.8% · 경기 9.7%/+1.0%) —
  68절 이후 편향이 0 근처에 머무는 것이 한 번 더 보인다.
- 70절 얇은 표본: 3~5건 11건 편향 **+12.3%** [+0.7, +25.2] vs 6건 이상
  −0.5%. 같은 물건이 대부분이라 **재현이 아니라 같은 값을 다시 읽은 것**이다.
- 합친 고유 표본은 125 → **134건**(+9).

**다음 순회는 표본 번호를 바꿔서 돌린다** — 그래야 70절 경고가 진짜로
재현되는지 판정할 수 있다.

### 70-2. 정비구역 조회가 **왜** 안 되는지 알려주는 진단 페이지

사용자가 `VWORLD_API_KEY`를 실제로 발급받아 넣으면서 드러난 구멍이다.
`get_land_use_zones()`는 **다섯 가지 실패를 전부 `None` 하나로 뭉갰다** —
엔드포인트 없음 · 키 없음 · PNU 실패 · 호출 실패 · 필드명 불일치. 그리고
`None`이면 카드가 안 뜰 뿐이라, 화면에서 **"키가 안 먹는 것"과 "이 땅이
정비구역이 아닌 것"을 구분할 방법이 아예 없었다** — 48-4절이 제일 비싸게
배운 "조용히 틀리는" 패턴 그대로다.

⚠️ **50-1절이 "응답 필드명을 실측으로 확인 못 했다"고 남겨둔 것과 겹쳐서
특히 위험하다.** 필드명이 하나만 달라도 전 물건이 영원히 "정비구역 아님"으로
보이는데, 그게 정상 동작과 구별이 안 된다.

**`probe_land_use()`** (`scripts/land_use.py`) — 같은 조회를 하되
`{"status", "detail", "zones", "pnu", "sample"}`을 돌려준다.
`get_land_use_zones()`는 이제 이 함수의 얇은 껍데기라 **기존 호출부는
그대로**다(테스트로 고정).

| status | 뜻 |
|---|---|
| `no_key` | 서버에 `VWORLD_API_KEY`가 없다 (대시보드에 넣고 재배포 안 됨) |
| `bad_pnu` | 지오코딩 결과로 PNU 19자리를 못 만들었다 |
| `call_failed` | 호출 자체가 실패 — 예외 종류와 메시지를 담는다 |
| `unreadable` | **응답은 받았는데 아는 필드명이 없다** ← 50-1절이 걱정한 그것 |
| `ok` | 지역지구 목록을 읽었다 |

- ⚠️ **`sample`(응답 앞부분 1,200자)을 같이 돌려준다** — 이거 한 번 돌려보면
  50-1절의 ⚠️ 두 줄(엔드포인트·필드명)이 **실측으로 확정된다.**
- ⚠️ **키는 절대 안 싣는다.** 응답이 요청을 되비추는 경우를 대비해
  `sample`에서 키 문자열을 `***`로 지운다(6절). 테스트로 고정.

**화면**: `/land-use-check` — 지번 주소를 넣으면 위 다섯 상태 중 하나를
한국어로 알려주고, 성공하면 지역지구 목록과 정비구역 판정까지 보여준다.
접이식으로 실제 응답 앞부분도 볼 수 있다. ⚠️ **이 페이지가 500을 내면
본말전도라** 모든 예외를 잡아 화면 문구로 바꾼다(테스트로 고정).

**`/healthz`**에 `optional_keys`를 더했다 — `VWORLD_API_KEY`·`KAKAO_JS_KEY`·
`SUPABASE_URL`·`SUPABASE_SERVICE_KEY`가 서버에 **도착했는지 여부만**(값은
절대 안 싣는다) 알려준다. 대시보드에 넣었는데 재배포가 안 돼서 반영이 안 된
경우를 여기서 가린다.

⚠️ **내가 또 위치를 확인 안 하고 썼다** — `is_redevelopment_zone`을
`land_use`에서 import했는데 실제로는 `estimate_price`에 있다. 65절이
"가짜를 쓸 때는 실제 함수의 반환 dict를 먼저 읽는다"고 적어둔 것과 같은
부류다. **다행히 진단 페이지의 try/except가 잡아서 500 대신 화면에
`ImportError: cannot import name ...`을 찍어줬다** — 이 절이 만들려던 바로
그 동작이 자기 자신의 버그를 먼저 잡은 셈이다.

### 70-3. 끝 슬래시 하나에 막혔다 — 404·405가 영어 기본 화면으로 나가고 있었다

70-2절 진단 페이지를 안내했더니 사용자가 **`Method Not Allowed`**(405)와
**`Not Found`**(404) 영어 화면을 받고 멈췄다. 원인은 브라우저가 주소 끝에
자동으로 붙인 **슬래시 하나**였다 — `/healthz/`는 Flask 기본값
(`strict_slashes=True`)에서 `/healthz`와 **다른 주소**다.

- `/healthz`·`/land-use-check`·`/backtest`·`/backtest/one`·`/estimate`에
  `strict_slashes=False`를 달았다. 끝 슬래시가 붙든 말든 같은 페이지로 간다.
- **404·405 화면을 한국어 안내로 바꿨다.** 52절이 `@app.errorhandler(Exception)`
  에서 `HTTPException`을 일부러 통과시키는데, 그게 곧 "브라우저 기본 영어
  문구를 그대로 보여준다"는 뜻이었다 — 그 화면만 보고는 **오타인지, 배포가
  안 된 건지, 서버가 죽은 건지** 알 수가 없다. 이제 입력 폼과 함께
  "주소에 오타가 없는지, 끝에 슬래시가 붙지 않았는지 확인해 주세요"라고 적는다.

⚠️ **이건 22절·48-1절이 지켜온 원칙을 한 군데 빠뜨린 것이다** — "왕초보가
쓴다"는 전제로 화면 문구를 전부 쉬운 말로 써놓고, 정작 **길을 잘못 들었을
때의 화면**은 손대지 않았다. 새 경로를 추가할 때마다 같은 걸 확인한다.

**덤 — `/healthz`가 제 역할을 했다.** 이 소동 중에 사용자가 열어본 값이
`{"version":"f941116","optional_keys":{"VWORLD_API_KEY":true,...}}`였다.
**배포도 됐고 키도 도착했다는 게 한 번에 확인돼서**, "배포가 안 된 건가"를
더 파지 않고 바로 슬래시로 좁힐 수 있었다(70-2절이 이걸 노리고 만든 것이다).
같이 드러난 것: `KAKAO_JS_KEY`가 없어 27-1절 카카오맵 미리보기가 조용히
생략되고 있고, `SUPABASE_*`가 없어 62절 캐시 보존도 꺼져 있다.

## 71. 전수 점검 3차 — 고친 건 에러 화면이고 원인은 그대로였다

70-3절에서 "405·404를 한국어 안내로 바꿨다"고 적고 끝냈는데, **그건 증상만
예쁘게 만든 것이었다.** 사용자가 맞은 405의 원인은 따로 있었고 그대로
살아 있었다. 54절·65절과 같은 방식(증상이 아니라 **실패 유형**으로 훑기)으로
다시 돌았고, 일곱 개가 나왔다.

### ⚠️ ① 페이지의 **모든 폼**을 무조건 POST로 바꿔 보내고 있었다 (진짜 원인)

`base.html`의 제출 핸들러는 `document.querySelectorAll("form")`으로 페이지의
모든 폼을 가로채는데, `submitWithoutLeaving()`이 **`method: "POST"`를 박아
두고** 있었다. 그래서 `method="get"`인 70-2절 진단 폼이 서버가 받을 수 없는
메서드로 나가 **405**가 떴다.

⚠️ **이건 "그 페이지 하나가 잘못됐다"가 아니라 구조적인 문제다** — 앞으로
만드는 모든 GET 폼이 같은 사고를 낸다. 22절이 fetch 제출을 넣을 때 그 시점에
POST 폼밖에 없어서 드러나지 않았을 뿐이다.

- **고친 방법**: `form.getAttribute("method")`를 읽어 그대로 쓴다. GET 폼은
  값을 쿼리스트링으로 붙여 GET으로 보낸다 — **22절 연출(화면을 안 넘기고
  팁을 돌리는 것)은 그대로 유지**된다.
- **덤**: `/land-use-check`가 이제 GET·POST 둘 다 받는다. 진단 페이지가
  프론트 버그 하나에 같이 죽으면 본말전도다.
- `TestFormsKeepTheirOwnMethod`가 **템플릿의 모든 폼을 훑어**, 각 폼이 선언한
  method를 그 라우트가 실제로 받는지 확인한다 — 새 폼을 추가해도 자동으로 걸린다.
- ✅ **진짜 크로미움으로 확인했다**(60절 원칙 — 이 수정의 존재 이유가 "어느
  메서드로 나가는가" 하나뿐이라 흉내로는 아무것도 검증되지 않는다): 진단
  폼은 `GET /land-use-check?address=…`, 계산 폼은 `POST /estimate` 정확히
  1회, 양쪽 다 화면이 갈아끼워지고 JS 오류 없음.

### ⚠️ ② 표본 번호가 단일 지역 실행에 안 넘어가고 있었다

70-1절에서 시드를 화면에서 바꿀 수 있게 만들었는데, `/backtest` **단일 지역**
실행이 `_run_region_backtest()`에 그 값을 안 넘기고 있었다 — **입력칸을 아무리
바꿔도 항상 42로 돌았다.**

⚠️ **테스트가 소스 검사(`seed=seed`)뿐이라 못 잡았다.** 순회 경로 한 곳만
보고 통과해버린 것이다. 지금은 `TestBothRunPathsActuallyUseIt`이 **두 경로를
실제로 호출해서** 시드가 도달하는지 확인한다.

### ⚠️ ③ 오류 문구로 브이월드 키가 샐 수 있었다

70-2절 진단 화면의 `detail`은 **사용자가 캡처해서 그대로 보낸다.** 예외 문구에
요청 URL이 섞여 오면 키가 통째로 노출되는데(저장소가 공개라 한 번 새면 끝이다),
`sample`만 지우고 `detail`은 안 지우고 있었다.

지금 쓰는 예외들은 실제로 URL을 안 담는 것을 확인했지만 **그대로 두지 않았다** —
6절 원칙은 "안 새는 걸 확인했다"가 아니라 **"샐 수 없게 해둔다"**이다.

### ⚠️ ④ 깨진 CSV 한 줄이 매도가 계산 전체를 500으로 만들 수 있었다

69절 `load_buyer_age()`가 `row["sido"].strip()`을 바로 했다. 짧은 줄이면
`csv.DictReader`가 빠진 칸을 `None`으로 채우는데, 거기서 나는
`AttributeError`가 호출부(웹·CLI)의 `except (OSError, ValueError, KeyError,
ImportError)` 목록에 **없어서** 그대로 뚫고 올라간다 — **참고용 카드 하나
때문에 매도가 계산 전체가 죽는다.** 53절이 캐시에서 배운 것과 같은 실패다.

### ⚠️ ⑤ 입력 폼의 라벨이 어떤 입력칸과도 연결돼 있지 않았다

`<label>`에 `for=`가 없고 입력칸에도 `id=`가 없었다 — **라벨을 눌러도 커서가
안 간다.** 46절이 아이콘 타일로 다시 짤 때 빠진 것으로 보이고, 화면만 봐서는
티가 안 난다. 47절이 "어른들은 더 안 읽힌다"고 글자를 키운 것과 같은 결의
문제로, 모바일에서 탭 영역이 라벨만큼 통째로 줄어든다.

필수 4칸을 포함해 14개 칸에 `for`/`id`를 짝지었다(`f_<name>` 규칙).
⚠️ **`name=`은 그대로 둔다** — 22-1절 탱크옥션 자동 채우기가 `input[name=…]`로
찾기 때문이다(브라우저로 자동 채우기가 그대로 도는 것까지 확인했다).

### ⚠️ ⑥⑦ CLI 도움말이 **자기가 거부하는 예시**를 안내했다

36절에서 임장 항목 이름을 "나쁜 상태"로 바꾸면서 `choices`는 고쳤는데
`help`의 예시는 옛 이름(`--inspection-bad 누수 경사`)으로 남아 있었다 —
안내대로 치면 argparse가 그 자리에서 거부한다. 항목 수도 엘리베이터를 뺀 뒤
7개인데 "8가지"로 남아 있었다.

- 값에 공백이 있어 **따옴표가 필요하다는 것**도 같이 적는다.
- 개수는 `len(INSPECTION_CHECKLIST)`로 뽑는다 — 다시 박으면 또 어긋난다.
- ⚠️ **36절이 "choices도 새 라벨이라 예전 항목명으로 주면 거부한다"고 적어
  놓고도 help를 안 고쳤다.** 문서에 적어두는 것과 코드를 고치는 건 다른 일이다.

### 이번에 배운 것

- **증상을 고쳤다고 원인을 고친 게 아니다.** 70-3절은 405 화면을 한국어로
  바꿨을 뿐이고, 사용자가 다시 시도하면 똑같이 막혔다. 에러 문구를 손볼
  때는 **"그 에러가 왜 났는지"를 먼저 재현**한다.
- **소스 검사는 경로 하나만 보고 통과한다.** ②가 그랬다 — 같은 값을 쓰는
  경로가 둘이면 **둘 다 실제로 호출해서** 확인한다.
- **내 설명 주석이 내 소스 검사를 깨뜨린다**(69절에 이어 두 번째). 소스를
  검사할 때는 주석을 먼저 걷어낸다.

628개 통과, 27개 테스트 파일 단독 실행도 전부 통과(55절), 웹 24경로 스모크
500 없음, CLI 8가지 조합 완주, 크로미움 폼 제출 확인.

### 71-1. 브이월드가 응답 없이 연결을 끊는다 — 원인을 가르는 장치

70-2절 진단 페이지를 사용자가 실제로 돌렸다(서대문구 홍은동 265-218).
**70-2·71절 수정은 확인됐다** — 폼이 정상으로 넘어가고 PNU도 19자리로 잘
만들어졌다(`1141011800102650218`). 그런데 그다음이 막혔다:

```
❌ API 호출이 실패했습니다
RemoteDisconnected: Remote end closed connection without response
```

**상대 서버가 HTTP 응답을 하나도 주지 않고 연결을 끊었다.** 요청 형식이
틀렸다면 400이든 401이든 **답이라도 돌려준다** — 아무것도 안 준 것은 그보다
앞 단계에서 막혔다는 뜻이다.

⚠️ **60절 연결 재사용은 범인이 아니다** — `http_pool`은 이미 실패하면 **새
연결로 한 번 더** 시도한다(끊긴 keep-alive를 조용히 넘기려고 넣어둔 것). 즉
새 연결 두 번이 똑같이 끊긴 것이다.

**그래도 그걸 화면만 보고 단정할 수 없어서 장치를 넣었다** — `_fetch_raw()`가
연결 단계에서 끊기면 **연결 재사용을 아예 끈 원래 `urllib.urlopen`으로 한 번
더** 보낸다.

| 화면에 뜨는 것 | 뜻 |
|---|---|
| 정상 + "연결을 재사용하지 않고 새로 맺으니 받아졌습니다" | 우리 쪽 풀링 문제 → `HTTP_POOL=0` 또는 이 엔드포인트만 예외 처리 |
| 여전히 `RemoteDisconnected` | **상대 서버가 우리를 막는 것** — 코드로 못 고친다 |

- ⚠️ **4xx·5xx는 다시 안 보낸다** — 상대가 답을 준 것이라 재시도해도 같은
  답이 온다. 테스트로 고정(`test_a_real_server_answer_is_not_retried`).
- `_why_it_failed()`가 예외 종류를 **사람 말 한 줄**로 덧붙인다 — 48-7절이
  "예외 종류를 사유에 적는다"로 원인을 좁힌 것과 같은 계열인데, 여기서는
  한 걸음 더 나가 **그래서 뭘 해야 하는지**까지 적는다.
  ⚠️ 화면은 마크다운을 렌더링하지 않으므로 `**`를 쓰지 않는다(테스트로 고정).

**⚠️ 남은 유력한 원인 두 가지** (둘 다 코드로는 못 고친다):

1. **키가 아직 승인 전이다.** 브이월드는 발급 직후 바로 안 되는 경우가 있다
   — `vworld.kr` 마이페이지에서 그 인증키의 **상태(승인/신청중)** 와 **활용
   API 목록에 NED 토지이용계획속성이 들어 있는지**를 봐야 한다.
2. **해외 리전 차단.** 서버가 오리건(미국)에 있다(63절). 국내 공공 API 중
   해외 IP를 막는 곳이 있는데, 그럴 때 정확히 이 증상(응답 없이 연결 종료)이
   난다. ⚠️ 국토부·카카오는 같은 서버에서 잘 되므로 **전부 그런 건 아니다** —
   브이월드가 그런지는 확인 전이다.
   - 이게 맞다면 63절이 적어둔 **서울 리전 이전**이 해결책이 되고, 그건
     이미 다른 이유(속도)로도 검토 중인 선택지다.

⚠️ **어느 쪽이든 매도가 계산은 그대로 간다** — 50-1절 설계대로 이 조회는
실패해도 조용히 생략될 뿐이다(진단 페이지에서만 시끄럽게 보여준다).

632개 통과 · 28개 파일 단독 통과 · 웹 24경로 스모크 500 없음.

## 72. 반복 점검 — 버그 → 속도 → 정확도를 네 바퀴

사용자 요청: **"버그 확실히 다 잡아, 여러 번 돌리는 한이 있더라도. 그 다음에
속도 빠르게, 정확도 더 높이고, 이렇게 계속 반복해 세네 번."** 한 바퀴를
**버그 → 속도 → 정확도** 순으로 잡고 네 번 돈다.

### 72-1. 1바퀴 · 버그

54·65·71절이 예외 처리·전역 오염·경로 불일치를 훑었으니, 이번엔 **아직 안 본
실패 유형**으로 들어갔다 — ① 사용자가 친 글자가 화면 코드로 들어가는 자리,
② 입력이 길어질 때의 계산량, ③ 외부 응답이 예상과 다를 때.

#### ⚠️ ① 붙여넣기 파서가 입력 길이의 **제곱**으로 느려졌다 (제일 값이 큼)

숫자가 길게 이어진 한 줄을 붙여넣으면:

| 입력 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 2,000자 | 209ms | 2.5ms |
| 8,000자 | 3,486ms | 9.7ms |
| **20,000자** | **20,000ms** | **25ms** |

원인은 `_AREA = (\d+(?:\.\d+)?)\s*㎡` 처럼 **끝이 열린 숫자 반복**이다. 엔진이
시작 위치마다 숫자를 끝까지 먹었다가 `㎡`를 못 찾고 되돌아오길 반복한다 —
위치 n개 × 되돌아오기 n번 = O(n²).

⚠️ **공개 사이트에서 이건 워커 하나를 묶는 수단이 된다.** 누가 일부러 하지
않아도 표가 통째로 복사된 텍스트 한 번이면 걸리고, 워커가 1개라(63절) 그동안
다른 방문자는 전부 기다린다.

**고친 방법**: 모든 숫자 반복에 상한(가격 5자리·면적 6자리·층 3자리·방 2자리).
현실의 매물은 전부 들어오므로 **잃는 것이 없다.** `test_it_grows_linearly_
not_quadratically`가 "길이를 4배로 늘렸을 때 8배 안쪽"을 고정하고,
소스 검사로 `\d+`가 다시 들어오는 것도 막는다.

#### ⚠️ ② `3억 2012년 준공`이 **3억 2,012만원**으로 읽혔다 (값 오류)

억 뒤의 **아무 숫자나** 만원 단위 나머지로 삼켰다. 최대 2천만원까지 틀린다.

| 입력 | 전 | 후 |
|---|---|---|
| `3억 2012년 준공` | 32,012만 | **30,000만** |
| `3.6억 18평` | 36,018만 | **36,000만** |
| `3억 59.9㎡` | 30,059만 | **30,000만** |
| `3억 5,000` | 35,000만 | 35,000만 (그대로) |

지금은 **콤마가 찍혀 있거나(`5,000`) 바로 뒤에 "만"이 오는 경우만** 나머지로
인정한다 — 실제 매물 표기는 둘 중 하나다. ⚠️ 이건 화면 오타가 아니라 **31절
가격 포지션·39절 매도압력을 통째로 흔드는 값 오류**다.

#### ⚠️ ③ 단지명을 두 번 이스케이프했다

`price_chart.py`가 344줄에서 `html.escape`한 값을 툴팁·상세·aria 라벨에서 **한
번 더** 감쌌다. 속성값은 HTML 파서가 한 겹만 풀어 주므로, `A&D빌라`가 화면에
**`A&amp;D빌라`로 글자 그대로** 보인다.

#### ⚠️ ④ CLI HTML 리포트는 이스케이프를 **아예 안 했다**

`_comparables_table_html()`이 단지명·거리·날짜를, `render_report()`가 주소·동을
날것으로 넣었다. 웹은 Jinja가 자동으로 막아 주는데 **이 경로만 맨몸**이었다 —
단지명에 `&`나 `<`가 있으면 표가 깨진다.

#### ⚠️ ⑤ 성공 코드를 `"000"` 하나만 인정했다

`if result_code != "000": raise` — 그런데 **`"00"`은 오류 목록에도 없다.** 이
계열 API에서 흔한 성공 표기라, 국토부가 그렇게 바꾸는 순간 **정상 응답이 통째로
오류가 되어 사이트 전체가 죽고**, 화면에는 `[00] NORMAL SERVICE - 알 수 없는
오류`라고만 뜬다. `SUCCESS_CODES = {"000","00","0","0000"}`로 바꾸고, **매매·
전월세가 같은 목록을 쓰도록** 묶었다(52절이 `ET.ParseError` 방어를 매매에만
넣었다가 전월세에 그대로 남겨둔 것과 같은 실수를 막으려고 `assertIs`로 고정).

#### ⚠️ ⑥ 테스트가 **실제 브이월드로 나가고 있었다**

71-1절 `_fetch_raw()`에 폴백 경로를 만들면서, 테스트가 `urlopen`만 가짜로 바꾸고
**`urlopen_no_pool`은 안 막았다** — 풀링이 실패하면 폴백이 진짜 네트워크로
나간다(프록시 로그에 8번 찍혔다). 48절·55절이 못박은 그 함정을 **또** 밟았다.
이제 폴백도 같이 막고, 막은 쪽은 `OSError`로 돌려 "둘 다 실패" 상황은 그대로
재현한다.

#### 확인만 하고 **손대지 않은** 것

- **`<script>` 안 `{{ }}`**: 좌표·숫자·`tojson`뿐이라 안전.
- **CSV 내보내기**: 쉼표·따옴표·개행을 정상적으로 인용한다.
- **Jinja `.items` 함정(41절)**: `villa_market_trend.index`·`b.count`는
  **dict 메서드가 아니라** 안전하다 — 추측하지 않고 실제로 렌더링해서 값이
  숫자로 찍히는 것까지 확인했다.
- **국토부 행이 망가져 올 때**: `(child.text or "")`로 이미 막혀 있어 `None`이
  나올 수 없다. `None`을 뺀 현실적 입력 1,500~3,000회 무작위 퍼징에서
  `find_comparables`·`compute_scenarios`·`build_dong_stats`·`pick_targets`
  전부 통과.
- **68절 추세 회귀·49-2절 예측구간·70절 경고**: 빈 목록·1건·전부 같은 값·
  NaN·0 면적·거대 면적에서 전부 통과.

⚠️ **내가 이번에도 틀린 것**: 퍼징 실패 11건이 전부 **내가 시그니처를 틀린 것**
이었고, 새 테스트 3개가 **그 파일의 import 방식을 안 보고 써서** 깨졌다.
65절에 "가짜를 쓸 때는 실제 반환 dict를 먼저 읽는다"고 적어놓고 같은 부류를
반복했다 — **남의 파일에 뭘 쓰기 전에 그 파일부터 읽는다.**

### 72-2. 1바퀴 · 속도 — 5.57초 → 3.17초 (−43%)

⚠️ **추측으로 고치지 않았다.** 60-1절이 만들어 둔 구간 계측을 더 잘게 쪼개서
**어디가 느린지 먼저 보고** 손댔다. 쪼개 보니 예상과 달랐다:

| 구간 | 고치기 전 |
|---|---|
| 주소→좌표 | 0.15초 |
| 건물대장 | 0.60초 |
| 입지 체크 | 0.31초 |
| 주변 지형 | 0.45초 |
| 실거래 조회 | 1.81초 |
| 비교거래 찾기 | 0.31초 |
| **아파트 조회** | **1.81초** ← 전체의 33% |

#### ① 국토부 조회를 **앞으로 당겼다** (−32%)

건축물대장·19절 입지 체크·34절 주변 지형은 **좌표만 있으면 되는 일**인데,
예전엔 그 셋이 다 끝나야 실거래 조회가 시작됐다 — 네트워크 대기가 그대로
더해졌다. 이제 좌표가 나오는 즉시 빌라·아파트 조회를 던지고, 셋이 도는 동안
내려온다.

⚠️ **국토부 동시 호출 수는 안 늘렸다.** 빌라·아파트가 각자 워커 8개를 쓰면
순간 16개가 나가 59절이 지목한 **429(초당 제한)** 위험이 커진다. `data_source.
_MOLIT_SLOTS`(BoundedSemaphore) 하나를 **전체가 공유**해서, 겹쳐 돌면서도 순간
호출 수는 예전 그대로다 — 겹치기의 이득만 가져오고 위험은 안 늘린다.
`test_never_more_than_fetch_workers_at_once`가 스레드 3개를 동시에 돌려
실제 최대 동시 실행 수를 세서 고정한다.

#### ② 40절 아파트는 **최근 12개월만** 받는다 (−13%, 호출 9회 절약)

빌라는 8·12·15·44절이 전부 같은 `rows`를 쓰므로 21개월이 필요하지만, 아파트는
**"인근 아파트 ㎡당가 중앙값" 하나만** 쓴다. 아파트는 거래가 워낙 많아(실측
사례: 강북구 한 달 100~200건) 12개월이면 동 단위 표본이 넘친다.

- ⚠️ **12개월(만 1년)로 끊은 건 계절성 때문이다** — 8개월로 더 줄이면 1.2초를
  더 벌지만 특정 계절에 치우친 중앙값이 나온다. 속도보다 이쪽이 중요하다.
- **48-6절 한도 문제에도 도움**이 된다 — 요청당 국토부 호출이 34회로 줄고,
  순회 한 바퀴의 총 호출량도 그만큼 준다.

#### 결과 (가짜 네트워크 지연으로 A/B, 2회 중 최소)

| | 시간 | |
|---|---|---|
| 고치기 전(직렬 · 아파트 21개월) | 5.57초 | |
| 아파트 12개월만 | 4.85초 | −12.9% |
| 겹치기만 | 3.77초 | −32.3% |
| **둘 다(지금)** | **3.17초** | **−43.1%** |

#### ⚠️ 벤치마크를 두 번 잘못 만들었다 — 기록해 둔다

1. **캐시를 우회했다.** `geocode.geocode`를 통째로 가짜로 바꿔서 400번이 전부
   진짜로 잠들었다(실제로는 캐시가 막아 30번만 나간다). 그 상태로 재면
   "지오코딩이 제일 느리다"는 **틀린 결론**이 나온다.
2. **월 범위 로직을 우회했다.** `get_apt_rows`를 통째로 바꿔서 아파트를 12개월로
   줄여도 벤치마크 숫자가 안 변했다.

→ **가짜는 네트워크 계층에만 씌운다.** 캐시·월 범위·병렬·슬롯 같은 로직은
전부 진짜로 돌아야 그 측정에 의미가 있다. 60-1절이 "로딩이 느리다를 추측으로
고치지 않기 위한 장치"라고 적어둔 것의 연장이다.

#### 안전장치

- `PREFETCH_ROWS`/`PREFETCH_APT` 플래그로 **코드를 되돌리지 않고 끌 수 있다**
  (60절 `HTTP_POOL=0`과 같은 원칙).
- `test_estimate_prefetch.py`가 **네 가지 조합(켬/끔 × 빌라/아파트)이 전부
  같은 숫자를 내는지** 화면의 "N.NN억"을 전부 모아 비교한다 — 순서만 바꿨는데
  숫자가 달라지면 어딘가 공유 상태가 있다는 뜻이고, 48-4절이 정확히 그런
  사고였다.
- 구간 계측이 **7개로 늘었다** — 화면 맨 아래 한 줄만 캡처해 보내주면 이제
  건물대장·입지·지형·아파트까지 따로 보인다.

#### 남은 것

- **콜드 스타트(수십 초)가 여전히 제일 크다** — 61절 깨우기 핑이나 유료 플랜의
  몫이지 코드로 못 고친다.
- 남은 3.17초의 대부분은 여전히 국토부 34회다. 더 줄이려면 워커를 늘려야 하는데
  429 위험이 있어 손대지 않았다.

### 72-3. 1바퀴 · 정확도

새 백테스트 CSV가 없는 상태라, **"숫자를 맞춰 계수를 흔드는 일"은 하지
않았다**(48-2절 0.97의 교훈). 대신 두 가지를 했다 — ① 값이 실제로 틀리던 곳을
고치고, ② 바꿀까 싶던 것을 **실제 코드로 검증해서 말렸다.**

#### ① 값이 틀리던 곳 (72-1절 ②에서 이미 고침)

`3억 2012년 준공`이 **3억 2,012만원**으로 읽히던 것 — 최대 2천만원 오차다.
이건 화면 오타가 아니라 **31절 가격 포지션·39절 매도압력의 입력값**이라,
붙여넣은 매물 한 건이 2천만원 비싸게 잡히면 그 두 판정이 통째로 흔들린다.

#### ② 반지하 하드 제외 — **바꾸려다 실측으로 말렸다**

5절은 반지하와 지상층을 서로 비교 대상에서 통째로 뺀다. 그러면 표본이 아주
얇아져 70절("얇으면 높게 부른다")에 정면으로 걸릴 것처럼 보였다 — 실제로
70절 표본에서 반지하 4건 편향이 +5.5%였다. 그래서 64절 1층처럼 **"섞고
배율로 보정"** 하는 쪽을 검토했다.

실제 코드를 태운 몬테카를로 결과는 **정반대**였다:

| 동네 거래수 | 반지하 비율 | 비교거래 | 평균오차 | 편향 | 25% 초과 |
|---|---|---|---|---|---|
| 40 | 5% | 2.4 | 6.7% | **−0.0%** | 0.6% |
| 40 | 10% | 4.3 | 5.1% | −0.3% | 0.0% |
| 80 | 10% | 8.3 | 3.8% | +0.2% | 0.0% |
| 80 | 20% | 16.1 | 2.5% | −0.4% | 0.0% |

**비교거래 2.4건에서도 편향이 없다.** 같은 모집단(반지하)끼리만 비교하니
당연한 결과인데, 숫자로 확인하기 전에는 알 수 없었다.

⛔ **그래서 안 바꿨다.** 섞는 쪽으로 가면 배율(약 50%)이 조금만 틀려도 크게
흔들린다 — 64절 1층 보정이 7%라 "틀려도 손해가 작다"는 이유로 채택된 것과
**위험 구조가 정반대**다. 70절이 관찰한 반지하 +5.5%(n=4)는 섞임 문제가
아니라 얇은 표본의 잡음으로 본다.

`TestBasementExclusionIsSound`가 이 판단을 고정한다 — 나중에 누가 다시
섞으려 하면 여기서 걸린다.

#### ③ 계산 불변식을 무작위 입력으로 검증 (`test_invariants.py`)

단위 테스트는 특정 입력 하나를 보지만, 이 파일은 **무작위 동네 200개**에
대해 성질이 유지되는지 본다 — 여기가 깨지면 **화면은 멀쩡한데 숫자가 틀린다**:

| 불변식 | 왜 |
|---|---|
| p25 ≤ 중앙값 ≤ p75 | 역전되면 "급매가가 상단보다 비싸다"가 화면에 뜬다 |
| 시세 신뢰도 10~100 | 7절이 clamp한다고 적어 둔 것 |
| 모델 괴리율 ≥ 0 | 절댓값이라 음수가 나오면 산식이 깨진 것 |
| 5단계 가격 구간이 오름차순 | 29절의 전제 |
| **60일 목표가 == 현실적 체결가** | 7-4절이 맞춰 둔 것 — 한 화면에 나란히 뜬다 |
| 예측구간이 중앙값을 감싼다 | 49-2절 |
| 얇은 표본 경고 ↔ 실제 비교거래 수 | 70절 — 어긋나면 화면이 거짓말을 한다 |

⚠️ **여기서 한 번 오진했다.** "60일 목표가 != 현실적 체결가"가 22번 걸렸는데,
차이가 전부 **5만원 이하**였다 — 구간 값만 10만원 단위로 반올림되기 때문이고
7-4절 주장은 그대로 성립한다. 반올림을 오류로 세지 않도록 허용오차를 뒀다.

#### 확인하고 **안 고친** 것

- **15절 구 단위 폴백은 이미 있다** — 68절이 "거래가 드문 동네에서 보정을 못
  건다"고 남겨둔 게 표본 범위 문제인 줄 알았는데, `_price_trend_monthly_series`
  가 이미 동 12건 미만이면 구 전체로 넓힌다. 막는 건 **유의성 게이트**이고,
  그건 68절이 의도한 동작이다(보합 동네를 안 흔들려고).
- **66절 축소(shrinkage) 역보정**: 여전히 안 한다. 대상 물건이 동네 평균에서
  얼마나 벗어났는지가 **바로 우리가 맞히려는 값**이라, 그걸 알아야 펼 수 있다.

### 72-4. 2바퀴 · 버그

1바퀴에서 **내가 동시성을 늘렸으니**(72-2절) 거기부터 봤다.

#### 확인만 하고 넘어간 것 — 동시성·캐시 격리

- **동시 요청 36건**(3파 × 12): 오류 0건, **같은 입력은 항상 같은 결과**.
  전역 슬롯·미리받기·메모리 캐시가 서로를 망치지 않는다.
- **지역 간 캐시 격리**: 11305와 11110이 안 섞이고, 캐시 경로가 구별·유형별로
  갈려 있으며(`trade-v2/<구>` vs `apt/<구>`), 빌라 조회에 아파트 응답이 오면
  48-4절 안전장치가 `RuntimeError`로 **시끄럽게** 실패한다.
- 미리받기와 결과 대기 사이에 **조기 반환이 없다** — 던져 둔 작업이 버려지지 않는다.

#### ⚠️ ① `--build-year-tolerance 0`에서 터졌다 (웹도 같은 칸이 있다)

```
TypeError: unsupported operand type(s) for *: 'NoneType' and 'float'
```

허용범위 0은 **"같은 연식만 보겠다"는 정상적인 입력**이다. 그런데 준공년도
점수만 `None`이 되고 **가중합 분기는 그대로 준공년도 항목을 넣고 있었다**
(`have_build_year`로 갈랐는데 점수가 None인 경우를 안 봤다). 웹 상세 옵션에도
같은 칸이 있어 방문자가 0을 넣으면 그대로 오류 화면을 본다.

지금은 5절 하드 필터를 통과했다면 같은 해라는 점을 이용해 **만점 100**을 준다.
분기 조건도 `build_year_score is not None`으로 바꿨다 — 점수가 없으면 아예 안
넣는다.

#### ⚠️ ② 53절 "깨진 캐시는 없는 것으로 친다"에 구멍이 있었다

파이썬 3.11부터 **아주 긴 숫자 문자열**은 `json.loads`에서
`ValueError: Exceeds the limit (4300 digits)`로 죽는데, 그건
`JSONDecodeError`가 **아니라서** 잡히지 않고 그대로 올라갔다. 깨진 캐시 파일에
숫자 쓰레기가 남으면 그 지역 조회가 계속 죽는다 — 53절이 없애려던 바로 그 상황이다.

`except (ValueError, OSError)`로 넓혔다. `JSONDecodeError`·`UnicodeDecodeError`가
둘 다 `ValueError` 하위라 **전보다 좁아지지 않는다.**

#### ⚠️ ③ 중개업소 한 건이 CLI 실행 전체를 멈출 수 있었다

`find_nearby_brokers_seoul()`이 `geocode()` 예외를 안 잡았고, 호출부에도 방어가
없었다. 21절은 **참고 정보**인데 여기서 터지면 뒤에 오는 섹션(건물 정보·시장
동향·매도가 산출)이 통째로 날아간다. 이제 사무소 한 건이 실패하면 **그 건만**
건너뛰고, 호출부도 20·26·50-1절과 같은 방어를 갖는다.

#### CLI 24가지 조합 완주

층 −1/0/99 · 준공 1900/2099 · 반경 1/99999 · 허용범위 0.1/500/0/200 ·
기준연도 2000/2099 · 공사비 0/99999999 · 낙찰가 1 · 취득세율 13% ·
전환율 0.1/30 · 임장 체크 · 모든 섹션 끄기 · 역세권+HTML 리포트.
**위 ①이 여기서 나왔다.**

### 72-5. 2바퀴 · 속도 — 재방문 0.63초 → 0.04초 (16배)

1바퀴에서 첫 조회를 5.57→3.17초로 줄이고 나니, **남은 임계 경로가 국토부
호출 수 자체**였다(34회 ÷ 8슬롯 × 0.6초 ≈ 2.5초). 워커를 늘리면 59절이
지목한 429 위험이 커져서 손대지 않았다. 대신 **가장 흔한 경우인 재방문**을
쟀더니 전혀 다른 그림이 나왔다.

#### ⛔ 먼저 실패한 시도 — "아파트 슬롯을 좁히면 빌라가 먼저 끝난다"

빌라는 받자마자 쓰이고(비교거래 찾기) 아파트는 **화면 맨 끝에서야** 쓰이니,
아파트를 3칸으로 묶으면 급한 쪽이 먼저 끝날 거라고 봤다. **실측은 정반대였다**:

| 아파트 슬롯 | 전체 시간 |
|---|---|
| 8칸(제한 없음) | **3.17초** |
| 3칸 | 3.78초 |
| 2칸 | 4.98초 |

아파트가 **꼬리**라서 조일수록 페이지 전체가 늦어진다 — 빌라는 이미 충분히
빨리 끝나고 있었다. 되돌렸고, 되살리지 않도록 이유를 여기 적어 둔다.

#### ⚠️ 재방문 요청의 **95%가 건축물대장 한 번**이었다

캐시가 다 찬 재방문이 0.63초인데 그중 **0.60초가 건축물대장**이었다 — 그리고
그건 **캐시가 아예 없었다.** 승강기 수·지상층수·세대수·사용승인일은 한 번
지어지면 사실상 안 바뀌는 값인데 매번 물어보고 있었다.

- `data/building_cache.json`에 캐시한다(53절 원자적 교체 · 57절 메모리 캐시).
- ⚠️ **증축·용도변경이 있을 수 있어 무기한이 아니라 `CACHE_TTL_DAYS`(30일)**.
- ⚠️ **네트워크·형식 실패는 캐시하지 않는다** — 48-7절에서 일시적 실패를
  영구 저장했다가 그 지역이 영영 안 뜨던 사고가 있었다. 응답을 **정상적으로
  읽은** 결과만 캐시한다(건축물대장에 없는 건물이면 그 사실도 캐시한다).

#### ⚠️ 이번 달도 **짧게는** 캐시한다

22절이 "이번 달은 신고가 계속 들어오므로 캐시하지 않는다"고 정한 건 맞지만,
**실거래 신고기한이 30일**이라 몇 분 사이에 바뀔 일이 사실상 없다. 그 한 달
때문에 재방문이 매번 국토부를 기다렸다.

`CURRENT_MONTH_TTL_SEC = 600`(10분), `<구>/<이번달>.current.json`에 시각과 함께
저장한다. 완료된 달의 영구 캐시와 **파일 이름이 달라** 섞이지 않는다.
48-4절 `_checked()` 검사는 되살린 값에도 그대로 걸린다.

#### 결과

| | 첫 조회 | 재방문 |
|---|---|---|
| 1바퀴 시작 | 5.57초 | 0.63초 |
| 1바퀴 끝 | 3.17초 | 0.63초 |
| **2바퀴 끝** | **3.17초** | **0.04초** |

재방문은 **외부 호출이 0회**다 — 속도만이 아니라 48-6절 한도 문제에도 그대로
도움이 된다.

⚠️ 기존 테스트 하나가 "이번 달은 매번 다시 받는다"를 전제로 하고 있어 같이
고쳤다 — **의도한 계약 변경**이고, TTL이 지난 뒤의 동작은
`TestCurrentMonthHasAShortTtl`이 따로 본다(만료 후 재조회 · 완료된 달은 영구
캐시 유지 · 깨진 파일은 53절대로 무시 · 아파트도 같은 처리).
